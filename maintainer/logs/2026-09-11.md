# Maintainer Session — 2026-09-11

## WAKE — the feedback reached the research core in three places, and one of them runs through SPINE

Required reading done in full: `../../Synchronism/SPINE.md`, `../../Synchronism/PREDICTIONS.md`.

Proposal written **before any site edit**:
`../../Synchronism/Research/proposals/instruments_sentence_floor_identity_and_refutation_scopes_20260911.md`.
Seven items, all routed to dp. **No bucket moved; executed count stays 6; Bucket 0 stays 0.** SPINE and
PREDICTIONS were not edited.

### What reached the research core

1. **SPINE's bolded sentence contradicts Bucket 2.** *"Untested for lack of means, not refuted on the
   merits"* had been compressed on the landing page and `/fundamentals` to *"lack of instruments, not
   refutation"*, directly above a scoreboard with six refutations. The researcher persona was right.
   SPINE's own paragraph draws the correct line (borrowed data can refute; it cannot confirm), but the
   bolded sentence erases it, and that is the sentence people quote. The site now carries both clauses:
   refuted where testable, ontology untested. The SPINE wording gates on dp.
2. **The galaxy floor *is* the dark-energy sector's C today.** Session 100's closure forces
   C_DE(ρ̄_m,0) = Ω_m identically, at every γ. That reconciles a live ledger contradiction: PREDICTIONS says
   the floor is "genuinely derived from cosmology", the site says 1/Ω_m is "nowhere derived". The accurate
   statement is that the floor is *identified with* a calibration constant. Taken literally, the floor
   evolves, B_max(z=2) ≈ 1.09 at every γ ∈ [0.3, 2], and published high-z dark-matter fractions can
   check that cheaply. That would be the first specific number on SPINE's door #3, gated on two knees
   3.9×10⁶ apart and an undefined smoothing scale. Not registered; topic seeded.
3. **Bell B1: the missing primitive has a price.** Toner & Bacon 2003 (one hidden bit per trial, singlet
   correlations exactly, no observable signaling) is the minimal instance of the "non-relabelable
   conditional setting-dependence" SPINE says the ontology would have to derive. The site had counted
   three constructions as refuting the substrate class. SPINE's own question ("purely local") was always
   correctly scoped; the site's class wording was not.

### The session's own result

`scripts/floor_is_cosmic_C_and_w_sign.py` (+ `_output.txt`, mirrored to `Synchronism/simulations/`). It
verifies four claims the researcher persona derived by hand, **before** any went on the site:

| Claim | Check | Result |
|---|---|---|
| C_DE(ρ̄_m,0) = Ω_m for every γ | closure at γ = 0.3, 0.487, 0.5, 1, 2 | exact to printed precision |
| sign(1+w) = sign(1−2γ) at every z | numeric continuity eq., not the closed form | **0 violations**, 11 γ × 51 z; max \|num − closed\| 1.4×10⁻⁶ |
| γ = 0.487: w₀ ≈ −0.993 | same | w₀ = −0.9923, wₐ = +0.0148 (runs along +diagonal) |
| floor binds on the past light cone? (persona's Q8) | C_DE(z) for z ≥ 0 | never; C(z=0.01) = 0.3214 > 0.315 |
| B_max(z=2) ≈ 1.09 | table | 1.085 at γ = 0.487; 1.00–1.20 over γ ∈ [0.3, 2] |

TEST-26 is now exact for the substituted sector and says so on `/test-catalog`.

### Two persona charges that were wrong, and pointed at real defects anyway

- **"The 3–17× RG comparison used the algebraic shortcut — a straw RG."** False on two counts. The numbers
  come from `l2_sparc_core.py`, an axisymmetric finite-volume solve with the refraction term, validated
  against Hankel transforms. RG at its **own published parameters** was also run with it (2026-08-28): χ²/N
  188–1252 against MOND's 21. **But the site said neither**, and `/for-researchers` artifact 1 did scope
  the no-go to "algebraic coupling — the class this framework belongs to" while naming the field-equation
  form as the completion. Both are now stated. What stays open is a SPARC *refit* of RG (plus a 2.3×
  log-base ambiguity) and the striction force.
- **"Honest Assessment says run as registered; Tier 1 says never run."** Both are true, of different
  registrations: Session 177, run as registered, and TEST-03 on ALFALFA, never run. Chasing this turned up
  a test-ID collision nobody had flagged: **PREDICTIONS calls the environment run "TEST-08"; on the site
  TEST-08 is the Freeman-law card.** Routed.

This matches the standing memory: check the quoted fact first, then fix the passage that made the
misreading possible.

---

## Feedback Reviewed

- **Visitor log 2026-09-11** (4 personas, 356 lines, 14 priority groups), the primary input.
  - *Casual enthusiast:* Coherence Explorer contradicts itself on which way γ goes (a math error on the first
    recommended tool); "Refutations executed: 6" read as a boxer's record; the `/core-idea` opener reads as a
    victory lap; no glossary entry for dark matter or kpc.
  - *Technical writer:* "Tier" carries two unrelated schemes; the Galaxy Plotter card describes an older
    tool; the 6-vs-5 roots sum; label families beyond the two-family contract.
  - *Graduate physics student:* all hand-checked arithmetic correct. fσ₈ "triggered" on one page and
    underpowered on three; no page writes the coupling or the floor; TEST-10 "structural, not tuned" is
    wrong *and* undersells (B = 13.7 beats both conventions).
  - *Leading-edge researcher:* the no-go is scoped wider than computed; RG uncredited where its equation
    appears; the Bell class claim; A2ACW "exactly at chance"; GC-fork cutoffs; the EFE-null class test; the
    floor identity; the analytic sign(1+w).
- **Forum** (`../forum/`): nothing new since the last session.
- **Explorer 2026-09-10** (TEST-02 identically null): its P0s landed 09-10. No new finding since.

**Cross-persona pattern, which the tech writer named:** *one object, several surfaces, drift.* The fσ₈
wording retired on `/cosmology-predictions` yesterday "at every point of use" was still live on
`/galaxy-rotation` this morning. That is why this session built a lint instead of adding a caveat.

---

## Changes Made (Site) — build passes (exit 0), lint clean

**Headline consistency with the ledger (HIGH)**
- `page.tsx`, `fundamentals/page.tsx`: "lack of instruments, not refutation" replaced with the two-clause
  reading, consistent with SPINE's paragraph and PREDICTIONS Bucket 2.
- `page.tsx`: one-line plain captions on the scoreboard labels (prospective / refutations executed /
  post-hoc retrodictions).
- `core-idea/page.tsx`: the opener now reads "One function, proposed for every scale… where it was tested,
  and where it broke."
- `layout.tsx`: site title changed to the question voice. **The meta description claimed the framework "maps
  density to coherence across 80 orders of magnitude"**, which PREDICTIONS Bucket 2 lists as refuted
  (S633). Replaced.
- `scale-invariance/page.tsx`: badge `speculative / Framework Claim` → `failed / Refuted as a Unification`,
  with the saturation arithmetic (C 0.1→0.9 spans 1.3 decades at γ = 2, 1.9 at γ = 0.49, computed here).
  The page had presented a Bucket-2 refutation as open speculation.
- `scale-navigator/page.tsx`, `research-philosophy/page.tsx` ("same γ across 80 orders" is false on the
  site's own numbers), `navigation.ts` desc: the same correction at each point of use.

**Math and adjudication (HIGH)**
- `coherence-explorer/page.tsx`: the low-γ sentence was **mathematically wrong** ("high coherence at almost
  all densities"; at γ = 0.01, C(10¹⁰ρ_crit) = 0.23 and C = 0.5 needs ~7×10²³). Rewritten. The "collective /
  correlated" label no longer collides with "C near 1". The γ = 0.49 preset is now labelled as the
  acceleration-keyed fit, illustrative on a density axis.
- `galaxy-rotation/page.tsx`: **removed the stale "Kill criterion (fσ₈ > 0.46) triggered"**, retired
  site-wide 2026-07-14 and missed yesterday. Also removed the stale "TEST-03 tripped its kill criterion";
  "three live readings" became two (MOND Unification was corrected 08-04); "Möbius transform of density"
  now says acceleration ratio; "see TEST-08" pointed at the wrong card.
- `coherence-function/page.tsx`: **new "How C enters dynamics" box** (`#how-c-enters-dynamics`). It gives the
  floored form written out, both couplings (algebraic g_bar/C; the field equation, which reduces to it only
  in spherical symmetry, up to 5.9× apart in discs), both keyings, RG credit, and which refutation used
  which. The grad persona could not reconstruct two of the six refutations from the equation pages; now
  they can. γ is relabelled as half the Hill exponent (n = 2γ).
- `mond-unification/page.tsx`: **RG credited where the equation appears** (twice); disc and striction
  scope limits added; badge "13% Error" → "6–13% Below Milgrom's a₀, H₀-Dependent".
- `tier-1-existing/page.tsx`: TEST-10 "structural, not tuned" retired and replaced by the convention-robust
  lead (B = 13.7 > 3.17 and > 6.4), with the M/L caveat under 6.4 kept. **TEST-03 reconciliation:** S177
  run-as-registered vs ALFALFA TEST-03 never run, the undecided band 0.09 ≤ r² < 0.20, and the TEST-08
  collision. TEST-07 "DEMOTED TO TIER-2" → "exploratory, not yet falsifiable (readiness, not cost tier)".
- `honest-assessment/page.tsx`: the environment row renamed off the colliding "TEST-08" with the bracket
  band stated; the Bell class claim scoped with Toner–Bacon; **GC-fork disclosure** (cutoffs set at
  execution against MOND+EFE; mass profiles Newtonian-fitted, not refit per law; what survives, and that
  "marginal" is not a pass).
- `for-researchers/page.tsx`: **how the 3–17× was computed** (L2 disc solve) and the unrefitted RG-proper
  numbers, with what stays open; the artifact-1 no-go scoped to the algebraic reading.
- `parameter-derivations/page.tsx`: method note on the 3–17× at its second point of use.
- `dark-matter/page.tsx`: "69% of galaxies exceed" now carries its convention caveat and leads with 13.7 vs
  3.17; links to the floored form.
- `test-catalog/page.tsx`: TEST-26 sign theorem, verified.

**Over-demotion (MEDIUM) — audited as hard as the claims**
- `a2acw/page.tsx`: "carries no information — performs exactly at chance" → "degenerate operating point on
  controls selected to trip prior art; specificity unmeasured until contemporaneous controls run". The page
  had said both things in consecutive sentences.
- `two-reframes/page.tsx`: Bell scope. Three constructions; hidden-communication substrates untested;
  signaling in the global-clock run was observable (checked in `04_global_clock_chsh.py`).
- `measurement-without-observers/page.tsx`: badge `untested` → `speculative` ("interpretive overlay; no
  differing prediction"). "Untested" implied a differing prediction exists.

**Terminology and findability**
- `lib/terms.ts`: **Tier entry rewritten**; it said "There is no Tier 3" while the roadmap has four cost
  tiers. New entries: Dark matter, kpc, OOM, Model Explainer, Artifact Lesson. The tanh "shifted" gloss
  now says why C stays in [0, 1].
- `interactive-tools/page.tsx`: the Galaxy Plotter card rewritten from the tool's own text. The old card
  described a curve that overlapped MOND, while the tool shows it sitting on Newton. Tag
  `reparametrization` → `core` (Model Explainer, explicitly not a verdict).
- `first-encounter/page.tsx`: the Two Reframes warning now names the real hurdle (Bell/CHSH, Kochen–Specker).
- `lib/ledger.ts`: TEST-08 comment corrected.

**New tool: `maintainer/tools/site_lint.py`.** Fourteen retired wordings, each with its retirement date
and reason. A line is exempt if it is visibly a correction note. **Positive control run:** against the
pre-session `HEAD` it fires 13 times, on exactly the defects fixed above; against the working tree it
fires 0 times. Known gap, found by the control: the exemption regex skipped the HA "TEST-08 / TEST-03s"
row because that line also contains "this row read". **Add a rule whenever a session retires a wording.**

---

## Back-Annotations (Research Repo)

- `Research/proposals/instruments_sentence_floor_identity_and_refutation_scopes_20260911.md`, new, seven items:
  (1) SPINE sentence rewording; (2) floor ≡ C_DE today, evolving-floor candidate with two gates, not
  registered; (3) TEST-26 sign theorem into the registration text; (4) RG comparison, charge refuted but
  scope narrowed, refit and striction open; (5) B1 criterion "observable signaling" plus Toner–Bacon as the
  priced primitive; (6) the TEST-08 ID collision; (7) GC-fork disclosures for the P611.2 note.
- `simulations/floor_is_cosmic_C_and_w_sign.py` + `_output.txt`, the verification, runnable.

**Direction check.** Items 1 and 2 cut against the framework. Items 4, 5, 7 and the A2ACW rewording narrow
over-refutations. Item 3 hardens an expected no-go. Both directions, recorded as such.

---

## Topics Seeded for Explorer

- **`floor-meaning-evolving-ceiling-high-z-dm-fraction-check.md`** (HIGH). Does published f_DM(<R_e) at
  z ≈ 1–2.5 exceed 1 − C_DE(z)? A refute-or-survive check on existing data. Guards: read the primary layer
  for the floor's stated meaning first; two knees; enclosed vs v²-ratio definitions and IMF; a pre-stated
  refutation rule in both directions.
- **`refracted-gravity-sparc-refit-under-l2-plus-striction.md`** (MEDIUM). Refit RG's three parameters on
  SPARC with the existing L2 solver, fix the log base, then add striction. Decides whether a class-level
  no-go can be stated, or only "this framework placed the knee wrong".
- **`efe-null-as-a-class-test-for-linear-permittivity-gravity.md`** (MEDIUM). EFE = 0 is not-evaluable *for
  this framework* (baseline miss 38–92×), but RG's baseline is far closer. Re-apply the baseline gate per
  class member before any EFE comparison; read the existing Crater II and EFE-discriminator work first.
- **`toner-bacon-bit-on-the-one-grid.md`** (LOW, but it is SPINE's one test). Can the framework's own update
  rules *generate* a conditional hidden bit, or must it be inserted, which amounts to Bohm by fiat? Guard:
  hard-coding the Toner–Bacon protocol answers nothing.

---

## Open Items (Deferred)

- **The badge-vocabulary / label-legend refactor (fifth consecutive session). Decision, as SESSION_FOCUS
  asked for, not a re-derivation: not doing the `/validation-taxonomy` page split.** What three personas
  actually hit in five sessions was *drift* between surfaces, not the legend's location, and a page split
  does not stop drift. What does is a single source plus a check. `lib/ledger.ts` already holds the counts,
  and `site_lint.py` now holds retired wordings. The next increment is to render verdict *strings*
  (TEST-04a status, environment status, root counts) from `ledger.ts`, not to move definitions. The tech
  writer's specific TEST-01 "$0 / 6 weeks in the badge slot" is a one-line fix I did not reach.
- Separate the **Test Roadmap banner** "Untested — 24 Proposed Experiments" from the badge word; fix the Tier 2
  header range ($5K is outside "$50K–$200K").
- **Chemistry Correlation Explorer statistics** (N, p, curation rule for 23 of 1,703, Z-polynomial null
  beside each r, non-causal tags). Medium, self-contained, needs the data file.
- /why-synchronism beginner step density (fold condensed-matter specifics); /phase-boundary-visualizer
  Ginzburg-criterion sentence and title; consciousness-demo SD label; edit-log notes in reader text;
  breadcrumb hubs; TEST-09 unrounded slopes (0.40 vs "0.41"); the ~800-word γ Calculator caveats.
- `/for-researchers` relabel of LIV and unidentifiability as "known obstructions that apply" vs "results of
  this program". The researcher is right that they inflate the citable list; not done today to keep the
  diff reviewable.
- Carried: glossary hover for survey acronyms; the Coherence Explorer range cannot show ρ/ρ_crit ≈ 4×10⁻⁵;
  tool state not in URLs; the GC exclusion window still has no TEST- number.

---

## So What?

**Does this advance discovery, or just document state?** Three things that were not known this morning.

1. **The floor has a meaning, and the meaning makes a prediction.** Two ledger documents disagreed about
   whether 1/Ω_m was derived; the dark-energy closure says exactly what it is. Taking that literally gives
   door #3, which SPINE says has no number, its first one: high-z discs capped near Newtonian, checkable
   against published kinematics this week. It will probably die. If it does, it dies as the last physical
   reading of the floor, which settles the question of what the floor is.
2. **The TEST-26 no-go is a theorem now, not a class argument,** and the floor cannot rescue it.
3. **The missing Bell primitive has a price: one bit per trial.** "The ontology would have to derive a
   conditional setting-dependence" was a sentence. It is now a construction problem with a known minimal
   solution and a clean question: can the grid generate it, or only borrow it?

The uncomfortable part, stated plainly: **half of today's site fixes were the site under-claiming its own
rigor** (the RG method unstated, TEST-10 underselling itself, the environment run looking contradictory
when it wasn't), and **the other half was stale over-claiming** ("80 orders of magnitude" in the page
metadata, a refuted unification badged speculative, a retired kill criterion still live). Both are the
same failure: a claim edited at one surface and not the others. Nobody reads 105 files. A lint with a
positive control does, and today's control shows it would have caught thirteen of today's defects before
the visitor did.
