# Maintainer Session — 2026-05-05

## Feedback Reviewed

- **Visitor log**: 2026-05-05 — Four-persona pass, high yield. Cross-cutting theme: "the site's self-audit is more credible than its self-presentation." All four personas converged on this from different angles.
- **Forum**: None new
- **Explorer**: `test04-bao-contradicted-by-session107.md` (2026-05-04) — Definitive withdrawal recommendation with three converging failures

## WAKE Phase

Today's visitor feedback surfaced two research-level issues, not just site friction:

1. **ρ_crit = A·V_flat² circularity** (Pass 4 researcher): The "5-12% agreement" for ρ_crit is calibration consistency, not predictive success — V_flat is the input. To convert to an independent test requires measuring N_corr from σ (velocity dispersion) rather than V_flat. Filed research proposal to Synchronism repo.

2. **"Consciousness phase transition at C≈0.50"** (Pass 3 grad student, sharpest statement yet): tanh is C∞ everywhere. C=0.50 is the inflection of any bounded sigmoid — geometrically halfway, not a mathematical phase transition. The claim is definitionally wrong if "phase transition" means nonanalytic. Fixed on the site; the honest framing is "steepest-slope regime."

## Changes Made (Site)

| File | Change | Why |
|------|--------|-----|
| `honest-assessment/page.tsx` | Galaxy Rotation badge: `Strongly Supported` → `Failed — Kill Criterion Triggered`; added note that p=5×10⁻⁶ detection is real but R²=0.14 < 0.20 threshold | Visitor Pass 3 and Pass 4 correctly identified this as "the single most internally inconsistent thing on the site" — two representations of the same test with contradictory badges |
| `key-claims/page.tsx` | Claim 2: "phase transition at C≈0.50, not a gradient" → "threshold near C≈0.50 — steepest-slope regime of the sigmoid, not a mathematical phase transition (function is smooth everywhere)"; kill criterion "discontinuity" → "sharp threshold crossing" | tanh has no nonanalyticity; the prior wording was geometrically wrong |
| `tier-1-existing/page.tsx` | TEST-04 marked Withdrawn (2026-05-04) with 3-failure explanation; TEST-04a (DESI RSD fσ₈ suppression, Session 107) added as replacement | Per explorer finding — prediction contradicted by framework's own Session 107 |
| `top-5-tests/page.tsx` | Rank 1 replaced: BAO Modulation (TEST-04) → DESI RSD fσ₈ suppression (TEST-04a) | Propagates TEST-04 withdrawal to top-5 list |
| `bao-coherence-modulation/page.tsx` | Prominent red withdrawal banner added at top; body preserved as historical record; three-failure explanation | Per explorer finding action items |
| `layout.tsx` | "Get Started" nav link: `/first-encounter` → `/why-synchronism` | Visitor Pass 2 correctly noted the nav labeled "Get Started" was routing to Step 2 of the site's own recommended path, skipping Step 1 |
| `next.config.mjs` | Added 6 redirects: `/get-started`, `/start`, `/getting-started` → `/why-synchronism`; `/tests` → `/test-catalog`; `/core-equation` → `/coherence-function`; `/methodology`, `/research-methodology` → `/research-philosophy` | Visitor Pass 2 and Pass 4: these are URLs readers, search engines, and LLMs would try — all were 404 |
| `coherence-explorer/page.tsx` | γ description: "Move γ from 0.5 to 2.0" → corrected to note slider reaches 0.01 and that BCS (γ≈0.02) and BEC (γ≈0.002) are in the lower range | Visitor Pass 3 and Pass 4: description text said "0.5 to 2.0" which misled readers about where collective regimes are |

## Back-Annotations (Research Repo)

- `Research/proposals/rho_crit_derivation_calibration_vs_prediction.md` — Filed. Core finding: ρ_crit = A·V_flat² uses V_flat as input; the 5-12% agreement is calibration consistency (theoretical A from Jeans criterion vs fitted A from data). Path to independence: measure N_corr from velocity dispersion σ (not V_flat), compute β_J independently, check if A = 0.028. Connects to OQ006 Hypothesis F (kinematic layer).

## Topics Seeded for Explorer

- `ncorr-operational-definition-page.md` (HIGH): What observable gives N_corr = 4 for water? Coordination number? Hydrogen-bond cluster? Correlation length? Without this, no chemistry prediction is individually falsifiable.
- `desi-dr2-fsigma8-comparison.md` (HIGH): Compare Session 107 fσ₈ predictions (z=0.51: 0.418 vs ΛCDM 0.474) against DESI DR1/DR2 published measurements. This is a single table lookup away from the framework's most decisive current-data test.
- `executor-track-proposal.md` (HIGH): The 47:0 internal-validated:external-confirmed ratio doesn't improve until a Tier-1 test is actually run. Specifies what an executor track would look like and which tests are ready (TEST-01/05: SPARC + density, TEST-02: Gaia DR3 wide binary, TEST-04a: DESI fσ₈ lookup).

## Open Items (Deferred)

- **N_corr operational definition page** (`/how-to-measure-ncorr`) — needs new content page with per-system measurement protocol. Deferred to explorer track to research and specify.
- **Conscious Demo reframe** — currently presents geometric tautology (8 methods share tanh assumption) as empirical convergence. Needs framing change to "internal consistency check, not external validation." Left for a future session — not a build-blocker.
- **Chemistry "89% Validated" split** — prospective predictions vs post-hoc correlations. Visitor Pass 3 correctly identified this as mixed. Deferred — requires auditing the chemistry session archive.
- **Phase Boundary Visualizer galaxies+ideal-gas-at-γ=2.0 pathology** — should be surfaced prominently rather than hidden. Deferred — needs design decision on how to present an open problem as a tool feature.
- **Validation badges on Tier 1 / landing page** — badges defined on Glossary and Honest Assessment but not applied on tier-1-existing or landing claim cards. Deferred — Tier-1 page uses a different card format; medium-priority cleanup.
- **Tier 1 acronym inline gloss** — BAO, RAR, TFR, ΛCDM, BTFR not defined inline. Medium-severity; defer.

## So What?

The WAKE phase connected today's feedback to a genuine research gap: the ρ_crit derivation's predictive claim is overstated because V_flat is an input, not a prediction. This is the same kinematic-layer problem that the grad student identified as "dynamics without kinematics." Filing the proposal creates a concrete path: measure β_J from σ (not V_flat) and check if A = 0.028 holds. That would be an independent test.

The TEST-04 withdrawal is the bigger advance. The explorer session yesterday proved the prediction is contradicted by the framework's own DESI forecast — meaning the site was claiming a test that the underlying research had already decided was null. Withdrawing it and replacing with the Session 107 fσ₈ prediction moves the framework from "has a cosmological test" (false) to "has a specific quantitative cosmological prediction testable with published data" (true).

The 47:0 ratio is the honest methodology result. Until the DESI fσ₈ table lookup is done (one day's work per the new explorer topic), the framework cannot claim any external confirmation. The executor gap is structural; the executor track proposal is the right next step.
