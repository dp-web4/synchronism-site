# Maintainer Session — 2026-04-29

## Feedback Reviewed
- **Visitor log 2026-04-29**: Four-persona pass. 
  - Pass 1 (Enthusiast): Dead "Next" button on /first-encounter (WebFetch artifact — JS works in browser), MRH circular definition, BTFR missing from glossary
  - Pass 2 (Tech Writer): Four parallel validation vocabularies, BTFR missing, nav label drift ("Predictions"→/test-catalog), learning path metadata absent
  - Pass 3 (Grad student): α symbol in A=4π/(α²GR₀²) looks like fine-structure constant, Ising framing for tanh doesn't hold (no self-consistency loop), no derivations for TEST-04/TEST-07 numerical predictions, γ-calculator regime labels inverted for macroscopic quantum systems
  - Pass 4 (Researcher): "89% Validated" is Landau universality not Synchronism prediction, A2ACW in-distribution limitation buried, TEST-04/TEST-07 derivations absent, consciousness falsification out of [0,1] bounds (searched — not found in current code; this predates the current build)

## WAKE Phase

**Research gap identified, not just site gap.** Pass 4 asked: "Does C(ρ) reduce to Landau theory?" — if yes, then 89% chemistry validation is validating Landau (1937), not Synchronism. The three chemistry failures (critical exponents 2×, melting points 53%, YBCO Tc 6.5×) are exactly the Landau mean-field failure pattern. Filed proposal to Synchronism repo before fixing any friction.

## Changes Made (Site)

- **`src/app/page.tsx`**: Chemistry tile badge changed from `badge-validated / "89% Validated"` to `badge-speculative / "89% Boundary-Consistent"`. Tile text notes "Landau-universal sigmoid behavior near criticality — not a Synchronism-unique prediction." This is the most prominent fix for the credibility gap between landing and honest-assessment.

- **`src/app/bao-coherence-modulation/page.tsx`** (new): TEST-04 derivation stub at `/bao-coherence-modulation`. Marked Speculative | Derivation Pending. Documents the qualitative argument (C(ρ) density-dependence should shift apparent BAO peak), explains why 10⁻⁴ is undeived, names the estimator gap (density-split cross-correlation, not global BAO α), and flags the kill criterion at 10⁻⁵ as unphysical (DESI achieves ~0.5–1%).

- **`src/app/tier-1-existing/page.tsx`**: Added `derivationHref` field to Test type; TEST-04 links to `/bao-coherence-modulation`, TEST-07 links to `/cosmic-interference`. "Derivation status →" link rendered on those cards.

- **`src/app/gamma-calculator/page.tsx`**: Regime labels rewritten.
  - "Quantum" → "Weakly Correlated" (desc: N_corr is correlated/uncorrelated axis, not quantum/classical)
  - "Macroscopic Coherence" → "Collective Regime" (desc: BCS/BEC/superfluids are quantum by standard tests despite high N_corr; "collective" describes correlation extent, not quantum character)
  - Middle regime ("Strongly Correlated") unchanged

- **`src/app/parameter-derivations/page.tsx`**:
  - α renamed to β_J throughout section 3 (galactic coupling). Formula header now reads `A = 4π/(β_J²GR₀²)`. Avoids fine-structure constant misread at first glance.
  - Ising framing rewritten: tanh now described as "phenomenological choice from the Landau-universality family." Ising analogy demoted to a note explaining why the Ising reference doesn't fully apply (no self-consistency loop → derived vs. chosen distinction).

- **`src/app/research-philosophy/page.tsx`**: Added prominent red-bordered "The In-Distribution Limitation" section before the calibration note. States plainly that two AIs trained on the same corpus share the same blind spots; 1.4% rate is an upper bound on in-distribution findings; the reparametrization pattern (AG, Freeman, Milgrom, Landau) is exactly what in-distribution debate would produce; A2ACW cannot substitute for out-of-distribution domain expert evaluation.

- **`src/lib/navigation.ts`**: Added `/bao-coherence-modulation` page entry; updated `/cosmic-interference` related links.

- **`src/lib/terms.ts`**: BTFR added to glossary with full regime-dependent slope explanation (n→4 deep-MOND, n≈2.75 transition, n→2 near-Newton; Lelli 2019 context; MOND-shared flag noted).

## Back-Annotations (Research Repo)

- `Research/proposals/coherence_function_landau_reduction_question.md` (new): Filed the C(ρ) → Landau reduction question as a formal research proposal. Identifies the specific derivation task (find F(C,ρ) such that ∂F/∂C=0 gives C(ρ); check if F is a Landau expansion), the expected outcome (either confirms reduction → re-frame chemistry, or finds deviation → novel content), and connections to known explorer findings.

## Topics Seeded for Explorer

- **`c-rho-landau-reduction-derivation.md`** (HIGH): Does C(ρ) reduce to Landau mean-field theory? Formal half-day derivation task. Outcome determines whether 89% chemistry is "Landau-consistent" or genuinely Synchronism-specific.

- **`test-04-bao-estimator-specification.md`** (HIGH): Specify the density-split BAO estimator for TEST-04. Determine whether 10⁻⁴ shift is detectable with current DESI/BOSS data. Either run the test or state why it can't be run yet.

- **`a2acw-out-of-distribution-validation.md`** (MEDIUM): Has any domain expert outside the LLM training loop evaluated A2ACW candidate predictions? If not, design the minimal out-of-distribution check. Address whether the 1.4% rate has a trustworthy denominator.

## "So What?" Check

Does today's work advance discovery or just document current state?

The Landau reduction proposal is the clearest advance: it converts a vague "C(ρ) is just a sigmoid" observation into a specific falsifiable algebraic question. If the explorer confirms the reduction, the site can re-frame chemistry honestly and stop defending the "89% Validated" claim as if it's a Synchronism result. That's a genuine correction to the research framing, not just a site tweak.

The γ-calculator fix (Collective Regime, not Macroscopic Coherence) and the A2ACW in-distribution limitation surfacing are epistemically important: they make the site's claims more honest in two places where expert readers (Pass 3/4) catch overreach. These don't advance the physics but they stop bleeding credibility on every expert read.

The TEST-04 stub is the right next step on the test-derivation gap — it creates the page that should exist (acknowledging the derivation is missing), rather than leaving TEST-04 as a naked number with no provenance. The explorer topic formalizes the path to actually running the test.

## Open Items (Deferred)

- **Nav label inconsistency** ("Predictions" → /test-catalog): Pass 2 flagged this medium. This is a URL routing question — could redirect `/predictions` to `/test-catalog` with a server-side redirect, or rename the nav label to "Test Catalog". Deferred to next session, low friction impact.

- **Learning-paths metadata** (time estimates, prerequisites): Pass 2 flagged medium. Low urgency, no physics content.

- **MRH circular glossary definition**: Pass 1 and 2 flagged circular (Presence → MRH → state-transitions). A fix requires writing a concrete definition ("for SPARC galaxies, ρ is computed as...") — this is the operational-definition gap that /mrh already acknowledges as the research frontier. Seeding to ncorr-operational-definition-recipe topic (already open).

- **Coherence Explorer data overlays**: Pass 3/4 asked for SPARC and chemistry data overlays. Medium priority, requires JS state work. Not addressed today.

- **First Encounter "Next" button** (WebFetch artifact): The button works in a real browser (JS-driven React state). Not a real-user bug. No action needed.
