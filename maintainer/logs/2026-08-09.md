# Maintainer Session — 2026-08-09

## WAKE

Working tree was dirty at start (`AGENTS.md`, `CLAUDE.md`, an untracked log). Per the
orphaned-inscription rule I audited before working: the diff was a GitNexus symbol-count bump
(5643→5956) and the log file held one line, this session's own start banner. **Not** a crashed
predecessor. Proceeded.

Then the standing rule — *check unactioned explorer findings before the visitor log*. It fired, hard.

The 2026-08-08 explorer sessions (`3048978`, `2ca6d48`) landed **after** the 2026-08-08 maintainer
run (`8720eba`), so their five action items had never been picked up. The headline item: the archive
**has** a field equation (`manuscripts/Appendix_D_...md` §D.2/D.3/D.5, committed 2025-12-01), and the
site had been asserting since 2026-07-23 that none exists.

Today's visitor log reached the same P0 from the opposite direction and neither had the other's half:

- **Explorer** had the archive document, the L1/L2/L3 decomposition, the 0.57–1.42 dex fork
  amplitude, and the a-priori kill of L1 — but no idea the site *contradicted itself*.
- **Visitor Pass 4** found the contradiction (TEST-11 computes a solar-system quadrupole **in
  QUMOND**; that is impossible without dynamics) and Pass 3 supplied the general argument (AQUAL is
  42 years old, substituting C for μ is mechanical) — but proposed a fix that was weaker than the
  truth. Their recommended wording was "cite Bekenstein & Milgrom 1984." The sharper fact is that the
  framework's *own archive* already had one, and the site invented a different one on 2026-08-04
  without looking.

Convergence from two directions on an item that had sat unactioned for one day made this the spine of
the session. **No new research proposal was needed for it** — the explorer already wrote one. A
proposal *was* written for the second finding (below).

## What I changed

Commit `ac23705`, pushed. Build clean.

### 1. "There is no field equation" — false, on 6 pages

Swept wider than the explorer's list of 3 and found it in 8 places across 6 files:
`/honest-assessment` (×4, including the page's self-described "single most important sentence"),
`/mond-unification`, `/galaxy-plotter`, `/test-catalog`, `/research-philosophy`, `/tier-1-existing`.

Replaced with the narrower and still-damaging claim: the field equation is **postulated, not
derived**; the archive's own version (L1: `∇²Φ = 4πGρ/C`) is eliminated with no data at all (as ρ→0,
`ρ/C → ρ_crit/γ`, a uniform vacuum source floor ⇒ 10¹⁷–10¹⁸ M☉ inside 100 kpc, for every γ and every
ρ_crit); the site's tests all use L2 ≡ L3; and **writing the surviving one down changes none of the
refutations**, because they are driven by the shape of C and the B ≤ 3.17 ceiling.

Two cosmology variants ("no field equation *sources a growth suppression*") were false in a subtler
way and got their own correction: §D.3's effective Einstein equations *are* cosmology-capable and
have simply never been solved for the growth history. **The gap is an unrun calculation, not a
missing object** — which makes it a task rather than an excuse. That reframing is the part I think
matters most; "no dynamics exist" was functioning as a reason not to try.

Per the explorer's guardrail, the refutation count was **not** bumped. A reading was eliminated;
nothing was refuted.

### 2. "At γ ≈ 1 the coherence function has maximum curvature" — false, on 2 pages

Visitor Pass 3's headline. I verified numerically before touching anything:

- `dC/dx|₀ = γ` exactly (matched to 6 digits) — monotone, no interior maximum at any finite γ.
- Log-density reading doesn't rescue it: `max_x dC/d ln x` rises monotonically and **saturates** at
  ≈0.446. γ=1 sits at 72% of the ceiling — an unremarkable point on a saturation curve.
- `d²C/dx² < 0` for all x ≥ 0: C is concave everywhere, so there's no inflection point to sit at.

**No feature of C(ρ) singles out γ ≈ 1.** This was the stated physical rationale for the entire
chemistry sector — 1,703 phenomena, 89% boundary-consistent. Retracted on `/gamma-boundary` with the
full derivation shown.

Sweeping found a **second instance the visitor never saw** — `/phase-transitions`, same sentence,
same claim, `N_corr ≈ 4`. Corrected too. `/consciousness-demo` has had the *correct* derivative fact
on it the whole time, one click away. Someone fixed one page and didn't sweep. That is the site's
most frequent failure mode and it caught me a second time in the same session.

### 3. Ledger

- **"four executed refutations" was live in two places** while both pages' headers said six — the
  pre-recount number, stale on `/honest-assessment` *and* the landing page. Every persona hit this.
- Added a **3-class table** with the inclusion rule, which had never been written down anywhere:
  4 framework-specific (testing *two* asserted constants plus one real prediction), 1 inherited from
  MOND, 1 theorem. Total stays 6 — dropping Bell and Cassini on a technicality would re-commit the
  2026-07-30 scope-word error in the opposite direction, and that error is why the count is 6.
- **TEST-11 prior art.** Desmond, Hees & Famaey 2024 (MNRAS 530, 1781; 8.7σ) now cited **on the row
  that carries the count**. The row already cited Blanchet & Novak 2011 and Hees 2016, and four
  *other* pages already mention Desmond — the caveat had reached everywhere except the page that
  counts. Recorded the two consequences: it is **non-discriminating** (MOND uses the same IF family,
  and `/galaxy-plotter` draws its MOND curve with the very ν this test excludes), and the +17.95σ is
  ~2× inflated because it fixes what Desmond et al. marginalize.
- Landing page LIV range 11–28 → **16–28**, matching the only computation that derives it. The "11"
  had nothing behind it.

## Seeded for the explorer

**Escalated `four-galaxy-formalisms-one-ledger.md`** (rewrote the lead, didn't append). Visitor Pass 4
*rediscovered* this open 2026-07-30 topic — but drew an inference the topic stops short of:

> Δχ² = −0.84 (ties MOND) is only attainable with `x = g_bar/a₀`. `max C = 0.001` follows from
> `x = ρ/ρ_crit`. These cannot both be the same function.

**If the free-γ RAR fit ran in acceleration space, then ΔBIC = +184 refutes a model the framework
does not hold, and γ = 0.489 is not the γ in C(ρ).** That would make the site's most-cited galaxy
number a wrong-model error in the same class as TEST-04a's wrong-observable error — and refutation #1
would need re-scoping. The mirror is equally sharp: the density-space compander is the framework's
only structurally novel commitment and has **never been BIC-compared to anything**. One of the two is
true. It's decidable by reading the fit's objective function — a one-session task, ten days open.

**New topic**: `gamma-boundary-rationale-is-void-what-holds-up-the-chemistry-sector.md`. I deleted the
chemistry sector's mechanism today; the 89% clustering is now bare. Three hypotheses (fit artifact /
template bias with unknown N_eff / a real reason nobody has found), and it should be worked jointly
with the already-open `chemistry-gamma-assignment-fork`, since they're one investigation.

## Back-annotated

`Synchronism/Research/proposals/gamma_boundary_maximum_curvature_is_false_20260809.md` (pushed,
`4f212a91`).

I applied the 2026-08-08 invariant — *any sentence asserting an object does not exist should cite the
grep that failed to find it* — **to my own proposal**, and it changed the conclusion. My draft assumed
the error came from the corpus (the documented drift direction). The grep found two hits and
**neither is the claim**: one is Planck-scale spacetime curvature, one is a procedure on an
empirically averaged curve. So this error is site-originated or lives in the chemistry session logs —
drift running the *other way*. Rewrote that section.

The grep also turned up an adjacent flag I recorded honestly rather than overstating:
`Coupling_Coherence_Experiment.md` accepts a tanh fit when `p* = argmax|d²C/dp²|` lands near
`p_crit` — but for the analytic form `|C''|` peaks at the origin, so `p* → 0` regardless and the test
could never pass. It's written against a *measured* curve, so this only bites if that curve inherits
the concavity. Flagged, not adjudicated.

Proposed a second invariant to pair with the first: **any sentence asserting a function has an
extremum should cite the derivative.**

## So what?

Two false statements were load-bearing on this site for months. Both were checkable in under a minute
— one grep, one derivative. Both were caught by readers, not by the people writing them. And both had
a *correct* version already sitting on an adjacent page or in an adjacent repo.

The pattern I'd name from today: **the site's errors are not in its hard physics.** Pass 3 recomputed
the Hill identity, the MOND ν-function, the ΔBIC bookkeeping, the DDO 154 coherence value, and the a₀
arithmetic — all correct. What fails is the *connective tissue*: counts left stale after a recount,
a rationale nobody differentiated, an existence claim nobody grepped, a citation that reached four
pages but not the fifth. Propagation, not computation.

The thing I'd flag to dp as genuinely unresolved, and the reason I escalated rather than closed:
**the site may be over-refuting its own headline result.** Three independent instances now
(ΔBIC effective-N, the a₀ 8σ dropping a 12×-larger systematic, TEST-11's 2× against the published
figure), and the galaxy-formalism question could make it four by putting refutation #1 on the wrong
model entirely. A framework that inflates its kill statistics is making the same class of error as
one that inflates its confirmations. The direction is more sympathetic; the epistemics aren't better.
That is now the single most consequential open question on the site, and it is answerable by reading
one script.
