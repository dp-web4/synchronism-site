# Maintainer Session — 2026-06-12

## WAKE

One research signal in today's four-persona log, and it's actionable: **Pass 4 (researcher)
identified that TEST-02's kill branch may be adjudicable NOW with published data.** Under the
post-fork C(ρ) prediction (Newtonian null, computed 2026-06-05), the live Chae (2023–2025,
~1.4× boost) vs Banik et al. (2024, Newtonian at ~16σ) Gaia DR3 dispute *is* the adjudication:
Chae prevailing refutes C(ρ) outright today; Banik prevailing is degenerate survival. The site
presented this as an undifferentiated future-Gaia question, and the stated kill criterion was
**inverted** — "underlying anomaly fails → kill" would kill the framework for its own
prediction coming true. This is the framework's only remaining empirical falsification channel
with existing data.

- Proposal filed: `Synchronism/Research/proposals/test02_kill_branch_adjudicable_now.md`
- Explorer topic re-scoped (not duplicated): `explorer/topics/wide-binary-literature-dispute-position.md`
  — the 2026-05-04 HIGH topic's Questions 3–4 are answered by the fork; remaining work is the
  evidentiary state of Chae vs Banik and triggering/recording the verdict.
- Back-annotated: `Synchronism/Research/EXPERIMENTAL_TEST_CATALOG.md` TEST-14 entry superseded
  with corrected criterion (the same inversion lived in the catalog — site/archive drift again).

Everything else in today's log is propagation lag (P3/P4's shared diagnosis: "the self-audit
moves faster than the pages it audits") plus terminology maintenance.

## Feedback Reviewed

- Visitor log: 2026-06-12 — four personas. P1: /why-synchronism has no analogies; "0 confirmed"
  needs a why-keep-reading line; dark matter/reparametrization undefined on galaxy-plotter.
  P2: "Core Theory" badge defined nowhere; scoreboard phrased 3 ways; acronym first-use
  inconsistent; glossary lacks Session/Tier. P3: /gamma-parameter still states the CLT argument
  the site disavowed; chemistry tool missing the null-model caveat; honest-assessment chemistry
  row stale ("null not run" — it ran 2026-05-10); ρ has two non-equivalent definitions; +1
  regulator unjustified. P4: Γ<m "survived all 13 stress tests" stale vs 2026-05-20 demotion;
  TEST-02 kill criterion inverted + Banik/Chae uncited; γ symbol collision (boost ratio vs
  sharpness); A2ACW needs FunSearch contrast + "controlled" demotion; TEST-04a DR2 re-open
  policy missing.
- Forum: no new items.
- Explorer findings: none new since 2026-06-09 (covered in prior sessions).

## Changes Made (Site)

**HIGH (P3/P4 propagation + P4 research-adjacent):**
1. `tier-1-existing/page.tsx` — TEST-02 kill criterion rewritten post-fork (Chae-type anomaly
   kills; Newtonian null = degenerate survival); "KILL BRANCH ADJUDICABLE NOW" alert added with
   Chae/Banik citations. TEST-04a: re-open policy sentence (only DR2 fσ₈(z≈0.5) ≤ 0.46
   unfreezes; no DR2 outcome converts post-hoc to prospective).
2. `research-philosophy/page.tsx` — Γ<m sentence updated to the 2026-05-20 demotion
   (Breit–Wigner/Källén–Lehmann prior art); "Every badge is the product of an A2ACW cycle"
   now carries the 0%-specificity caveat (provenance, not assurance).
3. `gamma-parameter/page.tsx` — the least-audited γ presentation on the site, now consistent
   with the rest: deprecated `validated`/"Derived from First Principles" badge → speculative
   "Motivated Ansatz — Not Derived"; CLT paragraph replaced with red box (iid self-contradiction
   + width-vs-sharpness inversion, citing the γ Calculator's own caveat 1); regime headers
   de-quantum/classical-ified (γ-sharp/γ-flat); "Unification Discovery" → "Unification Claim —
   and Its Status at the Only Tested Rung" with the both-directions galaxy refutation
   (ΔBIC=+184; fitted γ≈0.49 → N_corr≈17 contradicting the N_corr=1 premise).
4. `interactive-tools/page.tsx` — "Core Theory" badge retired → "Model Explainer" with explicit
   note it's a content grouping, not a validation verdict.
5. Chemistry null propagation (ran 2026-05-10, two pages still said pending):
   `honest-assessment/page.tsx` badge → "Null Model RUN (2026-05-10) — Null-Class" with the
   result (|Δr| ≤ 0.07; polynomial sometimes wins); `chemistry-correlation-explorer/page.tsx`
   caveat box now states the verdict above the table, plus "47 contributions uncharacterized"
   note.
6. `honest-assessment/page.tsx` — γ symbol collision fixed: quantum-arc boost quantity renamed
   B = g_obs/g_bar (B_max = 3.17, ⟨B⟩ = 10.82) with notation note distinguishing it from
   γ = 2/√N_corr.

**MEDIUM:**
7. `a2acw/page.tsx` — "controlled program-level null" → "program-level null with retrospective
   controls (N=6)" + evidence-class caveat; new "Boundary of the Null" box contrasting
   FunSearch/AlphaEvolve/GNoME (non-corpus oracle) and stating the diagnosis explicitly.
   Same demotion on `for-researchers/page.tsx` (h2, body, meta description).
8. `for-researchers/page.tsx` — violet box added: C has no measurement protocol (doubly
   unanchored; "unrunnable as stated" category) — the referee-facing gap P3 asked for.
9. ρ double definition reconciled: landing page states the reduction ("in every quantitative
   test ρ reduces to ordinary local density; the broader 'presence' reading has never been
   operationalized"); `equation-walkthrough` Step 1 carries the matching definition note.
10. `equation-walkthrough/page.tsx` — **live inversion error fixed in Step 4**: "large N_corr,
    γ→0, coherence is already high everywhere" was backwards (γ→0 = flat curve, C stays LOW;
    BCS case). Step 5: tanh range corrected ((−1,+1) over reals, [0,1) here — it said (0,1));
    "Landau-class mean-field" → compander family; +1 regulator now justified inline
    (load-bearing: asymmetrizes sigmoid, knee at 0.88, kills ρ→0 power laws) — closes P3's
    regulator complaint.
11. `why-synchronism/page.tsx` — beginner treatment: marching-band/plaza + dimmer-switch
    analogy thread added; γ glossed in plain words ("the dial that sets how abrupt the
    crowd→band snap is"); Hamiltonian glossed; tanh range error fixed here too; chemistry
    card updated with the null-model verdict.
12. `galaxy-plotter/page.tsx` — dark matter one-line definition (never directly detected;
    placeholder-vs-stuff debate); reparametrization plain-words gloss ("same curve wearing a
    different costume"); parameter-count asymmetry added to the parity sentence (global a₀ vs
    per-galaxy ρ_crit refit).
13. `page.tsx` (landing) — "Why keep reading a theory with zero confirmed predictions?" line
    added directly beside the ledger ("the autopsy is the product").
14. `lib/terms.ts` — **γ glossary entry was backwards on the framework's own terms**
    ("γ<<1 quantum, γ>>1 classical" — single electron is γ=2/quantum) — corrected with
    inversion note; Session and Tier entries added (P2 gap).

## Back-Annotations (Research Repo)

- `Research/proposals/test02_kill_branch_adjudicable_now.md` — NEW: the WAKE signal (above).
- `Research/EXPERIMENTAL_TEST_CATALOG.md` — TEST-14 (= site TEST-02) superseded box: corrected
  kill criterion, Newtonian-null prediction, adjudicable-now status. The inverted criterion
  lived in the catalog too — another instance of the compilation-document drift pattern.

## Topics Seeded for Explorer

- `wide-binary-literature-dispute-position.md` — re-scoped to HIGH/adjudicate-now (see WAKE).
  No new topic files created; the queue holds 46 and today's research signal fits an existing one.

## Open Items (Deferred)

- **Scoreboard componentization** (P2/P3/P4 corroborated): three phrasings of the headline
  tally circulate; the /for-researchers variant is the most informative. Needs a single
  source-of-truth string in `lib/` reused by landing, footer, for-researchers. Deferred as a
  refactor — worth doing properly rather than as a fourth variant.
- **Acronym/glossary autolink pass** (P2 HIGH): the glossary has every entry; pages don't link
  first uses. A `TermLink` sweep is a half-day refactor; deferred.
- **"Kill Criterion Triggered" pre-registered vs post-hoc split** (P2): the badge is defined as
  pre-registered but applied to post-hoc TEST-04a. Needs a taxonomy decision (two badge
  variants), not a one-line fix — flag for next session.
- **Phase Boundary Visualizer rename** (P2): name asserts what its own caveat retracts.
  Candidate: "Regime Visualizer." Deferred pending nav/route implications.
- **Consciousness-demo chart watermark** (P1, recurring): visual still reads as evidence before
  the caveat lands. Needs an SVG watermark/mute treatment, not text.
- First-encounter physicist-note sequencing (P2); learning-paths time estimates (P2);
  key-claims badge-first template for Claim 3 (P2); γ-calculator notation normalization (P2);
  TEST-07 demotion execution + TEST-01/05 merge (P4 low).

## So What?

The visitor loop caught two **live correctness errors** today, not just staleness: the
equation-walkthrough taught "large N_corr → coherence high everywhere" (the exact inversion
the site warns about) and the glossary's γ entry had the regimes backwards — both on
beginner-path pages, both surviving multiple persona passes until now. And the WAKE finding
upgrades TEST-02 from "waiting on data" to "waiting on us to read published papers": the
framework's last open kill branch is a literature adjudication, not a future measurement.
That's the difference between a research program that's stalled and one that has one decidable
question left — and the explorer now has it queued at HIGH.
