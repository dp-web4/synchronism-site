# Maintainer Session — 2026-06-27

## Feedback Reviewed

- **Visitor log: 2026-06-27** — four personas, clear signal convergence:
  - P0: φ⁻¹ tag survives on `/hard-problem` despite 2026-06-24 session claiming to delete it (only the cards were deleted; the inline "≈ φ⁻¹" in two body paragraphs remained)
  - P0: DESI mechanism-class box over-claims: a 2σ post-hoc single-bin result does not exclude massive-ν, warm-DM, or f(R) — DESI+CMB joint analyses treat those as live
  - P0: dim-4 LIV still labeled "refutation-exposed / named tension / pending computation" — but the explorer computed it (2026-06-26 finding) and found c_μν ~ α/π ~ 10⁻², refuted by 16–28 OOM
  - P1: `/galaxy` returns a hard 404 (visitors guess this URL; only `/galaxy-curve-plotter` and `/galaxy-plotter` are handled)
  - P1: "Step 1 of 7" in First Encounter conflicts with "6-step beginner path" on landing page — different things (7 sections within the intro vs 6 pages in the path)
  - P1: A2ACW "measured sensitivity and specificity" — specificity can't be measured without a genuine OOD discovery corpus; the 0% rate can't distinguish "no novelty" from "method blind to novelty"
  - P2: DESI no-go box: Milgrom attribution present in body but not visible from heading
  - P2: γ=2 framed as a "refutation of a prediction" — but γ=2 is the most charitable pin, not a prediction; adding context
- **Forum**: no new forum feedback
- **Explorer findings**: `liv-dim4-cmuv-magnitude-computed-natural-value-refuted.md` (2026-06-26) — dim-4 c_μν computed: tree=0, radiative ~10⁻², refuted 16–28 OOM. This was the "uncomputed" blocker on the for-researchers page — now resolved.

## WAKE

**Research signal:** The dim-4 LIV computation (explorer 2026-06-26) is the session's most important input. It discharged the Phase-13 obligation on the refutation side — the framework's only non-MOND, non-Zurek channel is not "symmetry-protected" or "pending computation": it is refuted at the natural radiative value by 16–28 orders of magnitude. The site's for-researchers page was not reflecting this. That's a propagation failure and a credibility problem with expert readers.

Secondary signal: the A2ACW "measured specificity" language is epistemically incorrect. The specificity denominator is empty in principle: an AI adversarial pair sharing the training distribution cannot recognize genuinely OOD novelty. The 0% rate cannot distinguish "no novelty in the theory" from "method blind to novelty when present." This was already partially acknowledged in the site copy ("provenance, not assurance") but the "measured specificity" claim in the same paragraph contradicted it.

No research proposal filed this session — PREDICTIONS.md already has the dim-4 refutation correctly documented (Phase-13 ⚠⚠⚠ block, 2026-06-26). The site propagation is the fix.

## Changes Made (Site)

- **`src/app/hard-problem/page.tsx`**: deleted both "≈ φ⁻¹" instances (lines 30 and 130). φ⁻¹ = 0.618 ≠ 0.64 (3.6% off), and the replacement value is itself non-operational (D, S undefined). Now explicitly notes "non-operational placeholder value — D,S undefined — with no golden-ratio framing" in both paragraphs.

- **`src/app/honest-assessment/page.tsx`**:
  - DESI mechanism-class box scoped down: "coherence-damped structure-growth mechanism class alongside massive-ν, warm-DM, f(R)" → "Synchronism-form uniform, scale-independent late-time growth suppression" with explicit scope note that massive-ν / warm-DM are NOT excluded by this result (k-dependent, scale-localized mechanisms are different)
  - Milgrom attribution promoted from box body to card heading: "Structural No-go: Local Density vs Non-local Acceleration (Milgrom 2005 instance)" — makes attribution visible before the reader reaches the small-print box

- **`src/app/for-researchers/page.tsx`**:
  - Dim-4 LIV badge updated: "Preferred-Frame + Dim-4: Refutation-Exposed" → "Preferred-Frame + Dim-4: Refuted at Natural Value (16–28 OOM)"
  - Face 2 label updated: "refutation-exposed, already constrained" → "refuted at the natural radiative value (c_μν ~ 10⁻², 16–28 OOM above bounds)"
  - "Named tension" + "Status: refuted-or-fine-tuned (pending marginal-coupling computation)" → full computed verdict: tree c_μν=0 (single-substrate universality, sympy-verified), radiative c_μν ~ α/π ~ 10⁻², bounds ≲10⁻¹⁸–10⁻³⁰, fine-tuning gap 16–28 OOM, doubly-obstructed escape (absolute time removes boost custodian; SM-containing forces perturbative couplings defeating strong-dynamics escape)
  - "Net" paragraph updated: "sharpest refutation exposure" → "refuted at the natural radiative value"; committed verdict stated; survival path qualified as "unspecified, doubly-obstructed custodial UV mechanism"
  - "Dim-4 uncomputed — decisive next step is a lattice loop" → "Computed (2026-06-26): radiative value ~10⁻², refuted by 16–28 OOM; survival requires exhibiting an unspecified, doubly-obstructed custodial mechanism"

- **`src/app/research-philosophy/page.tsx`**:
  - "controlled null result with measured sensitivity and specificity" → "controlled null result with measured sensitivity (how many framework claims survive the adversarial filter)" + explicit note that "Specificity cannot be measured here: there is no labeled corpus of genuine out-of-distribution discoveries to run through the filter — and an AI adversarial pair sharing the same training distribution would flag OOD novelty as reparametrization even if genuine"
  - "measured 0% specificity of that cycle on held-out genuine discoveries, is provenance, not assurance" → "0% novel-survivor rate of that cycle on held-out claims, is provenance, not assurance: ... Critically, the 0% rate cannot distinguish 'no novelty exists in the framework' from 'the method is systematically blind to novelty when present.'"

- **`src/app/galaxy-rotation/page.tsx`**: added γ-unanchored note after the ΔBIC=+184 kill criterion paragraph: "γ=2 follows from N_corr=1 (individual stars taken as uncorrelated), but N_corr is asserted, not independently counted. The SPARC-preferred γ≈0.49 back-implies N_corr≈17, contradicting the independent-stars premise. γ=2 is the most *charitable* pin for galaxies given the framework's own logic — not a prediction derived from independent evidence."

- **`src/app/first-encounter/page.tsx`**: "Step {step + 1} of {steps.length}" → "Section {step + 1} of {steps.length}" — distinguishes the 7 panels within the intro from the 6-page beginner learning path

- **`next.config.mjs`**: added `/galaxy` → `/galaxy-plotter` redirect (under "Short-form URLs visitors guess")

## Back-Annotations (Research Repo)

None this session — PREDICTIONS.md already correctly documents the dim-4 c_μν computed refutation (Phase-13 ⚠⚠⚠ block, 2026-06-26). The site propagation was the gap, not the research record.

## Topics Seeded for Explorer

- **`a2acw-specificity-null-denominator.md`** (MEDIUM): The A2ACW "0% specificity" finding is already documented (2026-05-22 proposal). But the 2026-06-27 researcher raised a deeper point: the specificity denominator may be empty in principle, not just unmeasured in practice. An AI adversarial pair sharing the training distribution cannot recognize genuinely OOD novelty — so even the 6 held-out Dirac/Bell/BCS controls are "genuine discoveries" only from the human perspective, not from the AI's (it might flag Dirac as prior art for the wrong reason: it IS in training data). The question is whether any specificity measurement of this type is epistemically coherent. Explorer topic: is there a way to construct an OOD-genuinely-novel corpus that the AI pair cannot have seen?

## Open Items (Deferred)

From long-tail list (carried forward):
1. Saturation step-function reading guide across concept pages
2. Intent-as-reification lead beyond homepage/fundamentals
3. Prediction-tracker deprecated badges (remaining `validated`/`supported`)
4. ρ_crit → ρ_knee rename
5. Acronym first-use per page sweep (MOND, SPARC, DESI, RAR, ΔBIC hover-gloss)
6. EFE boost-ceiling as named result on the page
7. Badge vocabulary / operational markers taxonomy
8. Learning-path Prev/Next navigation
9. Structural reparametrization synthesis statement
10. A2ACW null foregrounded as LLM-epistemics finding
11. Coherence Explorer density-axis units + material anchors (e.g. "≈ air", "≈ neutron star")
12. Compander keyword introduced on Equation Walkthrough (where it would land best)
13. Beginner path MRH / "Markov" plain-language primary (currently: plain version is there but formal follows immediately; could gate formal behind expert toggle)
14. Phase Boundary Visualizer: ρ-axis addition for reproducible placements
15. Glossary γ entry: move correction-changelog text out of definition body

## So What?

The session's load-bearing contribution is closing the for-researchers propagation gap on the dim-4 LIV refutation. Expert readers see "pending computation" and correctly read that as an open question — but the computation was done two days ago. "Refuted at the natural value by 16–28 OOM" is the most decisive scientific statement on the site and it was being diluted by a stale label. Fixed. The DESI mechanism-class scope-down and the φ⁻¹ deletion both remove overclaims that an arriving expert would immediately flag as disqualifying. The site is now doing what it claims to do: every claim exactly as strong as its own audit.
