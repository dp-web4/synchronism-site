# Explorer Session — 2026-07-23

## WAKE Phase (answered before working)

1. **What assumptions am I inheriting?** That the topic queue reflects true priorities; that the
   locality no-go is the program's one novel artifact; that persona reports about the site are
   accurate (memory says they aren't always — verified the /for-researchers claims against source
   before auditing them). Also inherited: the A2ACW lesson that the prior on "our quantification
   is novel" should be LOW — which made "nobody has looked" itself the red flag.
2. **What if the frame is wrong?** The frame assumes citable novelty matters. But the audit is
   frame-robust: either branch of the pre-fixed verdict rule moves the program (demotion saves dp
   wasted preprint effort; survival buys the referee line). The deeper frame risk — that the walk
   is judgment-heavy and a persona-style skim would rubber-stamp it — was handled by reading
   primaries (pdftotext on arXiv/ADS PDFs), not abstracts.
3. **Highest-information investigation right now?** This one: a binary verdict gating a human
   decision (dp's preprint call), executable today without NotebookLM, and blocking another
   queued topic (`locality-nogo-standalone-writeup`). Chosen over the also-HIGH Cassini Q₂
   squeeze because the squeeze adds a *second* closure route to an already-closed family, while
   the audit protects the program's *only* novelty claim from its known top failure mode
   (prior-art rediscovery).
4. **What would falsify the current posture?** Finding a published quantified instance (demotes
   the artifact to re-derivation) — or finding the no-go isn't well-posed against the
   surface-density literature. Neither happened; what was found instead is a required credit
   line (the 1983 argument template) that a referee would have caught.

**Dirty-tree audit at WAKE** (orphaned-inscription rule): AGENTS.md/CLAUDE.md diffs are
supervisor GitNexus count bumps (left untouched per primer); the stray .log is this session's
own launch stamp. No crashed predecessor.

## Topic(s) Explored

- `milgrom-surface-density-prior-art-audit` (HIGH, gates preprint): executed the registered
  prior-art walk over 13 sources. Verdict Branch 2 — no prior quantification of volumetric
  density failing as the MOND organizing variable exists anywhere in the walked corpus;
  every published discrimination is acceleration vs {radius, frequency, size, surface density}.
  Archived to done/.

## Key Findings

- `locality-nogo-milgrom-prior-art-audit-executed.md`: Branch 2 fires; citability strengthens;
  required credit line = Milgrom 1983b §III / Sanders 1986 argument template (length variable);
  two /for-researchers citation glosses imprecise (0510117 temporal-vs-spatial non-locality;
  Lelli surface-vs-volume); Stiskalek & Desmond 2023 is the nearest-miss modern study (feature
  sweep with no ρ(r) feature). Maintainer P1 wording included.
- Research repo: `explorations/2026-07-23-locality-nogo-prior-art-audit.md` (per-paper table,
  honest limitations) + dated gate-cleared note appended to
  `stable_fixed_point_preprint_strategy.md`.

## NotebookLM Usage

- Sources added: none — auth still expired (operator re-login on CBP pending since 07-22).
- Queries run: not used. Workaround: curl + pdftotext on arXiv PDFs and ADS scans (1983a has an
  OCR layer; 1983b does not — flagged as the audit's residual hole), full-HTML pulls of
  Scholarpedia and Living Reviews.

## Rabbit Holes (Worth Revisiting)

- Milgrom 1983b §III primary read (image-only scan; needs a human or OCR pass — content
  currently vouched by three agreeing secondary witnesses).
- The F&M 2012 footnote noting a theory can carry "a characteristic matter density ρ₀ as an
  additional order parameter" — theory-construction remark; the referenced Eq. 3.1 lineage
  (likely Bruneton–Esposito-Farèse-adjacent) was not chased.
- Chameleon/symmetron screening literature: needed only if the writeup claims priority for the
  *diagnosis* (switch/force conflation), not just the quantification.

## Self-Seeded Questions

- **Executable follow-up**: add ρ(r) (e.g., midplane density from Σ/2h with a standard
  scale-height relation) as a feature in the Stiskalek & Desmond 2023 framework and measure its
  importance against g_bar on SPARC — converts "never tested" into a number, and gives the
  1.7 dex offset an external, framework-independent counterpart. Candidate execution topic.
- Should the site's escape taxonomy cite the 1983 template explicitly as the *method's* lineage
  ("this is Milgrom's own exclusion argument, pointed at a variable he never needed to
  exclude")? Proposed wording already does this.

## So What?

Advance, not documentation: the program's single claimed citable artifact now carries an
executed prior-art audit with a pre-registered two-way verdict — the exact artifact a referee
would demand and the A2ACW failure mode (novelty claims dying on unwalked prior art) says we
must never skip. The strengthening branch fired, and the walk's by-product (the 1983 argument
template) makes the claim *more* defensible, not less. The writeup topic is unblocked and the
preprint gate is cleared on this axis.
