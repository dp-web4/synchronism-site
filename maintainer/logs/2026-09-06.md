# Maintainer Session — 2026-09-06

## WAKE

Read SPINE.md and PREDICTIONS.md in full first (mandatory), then the visitor log, then `git status`
(one untracked runner log, `logs/2026-09-06-0600.log`; tree otherwise clean; both repos up to date).

**Research-direction check: no new proposal; one back-annotation, and one place where the visitor's ask
ran opposite to the ledger.**

1. **The three-C split, rediscovered from outside.** Pass 3 (grad student) and Pass 4 (researcher) both
   rated HIGH the same thing: the site describes two models under one equation — density-keyed C(ρ/ρ_crit)
   (never leaves the linear regime in any disk, predicts the wide-binary null, EFE = 0) and
   acceleration-keyed C(g_bar/a₀) (fits the RAR at γ ≈ 0.49, is μ_simple(x/2), fails Cassini) — and never
   says which one a given number belongs to. That is C_ρ vs C_g in the archive's 2026-08-24 proposal
   `argument_of_C_three_functions_ledger_not_commensurable_20260824.md`, which gates on dp. Writing a
   second proposal would duplicate it; instead I appended a dated addendum recording that the split is now
   the first thing a competent outside reader trips on (third independent route to it). Committed to the
   research repo, `e75b731a`.
2. **Dark energy: the visitor wanted a seventh refutation; the ledger says the sector is ΛCDM.** Pass 4 read
   `/dark-energy`'s "offsets 3.4–5.4σ" and asked why the sign-lock failure isn't on the refutation ledger.
   PREDICTIONS.md (Bucket 3 note, 2026-08-12) records the direct likelihood fit to DESI DR2 BAO + Planck
   priors + Dovekie SN: the substituted family sits at ΛCDM's corner (γ = 0.487, Δχ² = −0.3), pays ΛCDM's
   own ~2.9σ crossing cost and no more; the forced-w₀ σ figures "died on execution"; both covariant
   completions fail the fit outright. The site page still said that fit "has not been done." So the site was
   **over-refuting relative to the ledger** — the site's own explorer ran the fit on 2026-08-12 and the page
   never absorbed it. That is a HIGH-priority contradiction under the maintainer charter (site vs
   PREDICTIONS.md), fixed today. Not a research gap; a propagation gap, in the deflationary direction.
3. **H₀ = 77.6.** Same pattern: the explorer's 2026-07-26 finding (restore a₀'s ±0.24 systematic → 0.7σ)
   never reached `/key-claims` or `/honest-assessment`, which still said "above SH0ES by several σ." Pass 4
   re-derived the deflation. Fixed on three pages.

## Feedback Reviewed

- **Visitor log 2026-09-06** (4 personas, 277 lines). Enthusiast: failure line before the idea; equation as a
  wall; `/dark-matter` badge before the idea (HIGH); melting points unexplained; "Markov" undefined;
  Explorer readouts; walkthrough warning placement. Tech writer: 14,760 vs 14,610 across the Beginner path
  (HIGH); duplicate URLs (`/start-here`, `/tools` — actually 307 redirects that WebFetch followed);
  breadcrumb middle segments unnavigable; badge definitions missing from the HA jump list; 16 badge-styled
  labels vs 11 canonical; borrowed acronyms unlinked; HA on a "30-min" path. Grad student: two models never
  distinguished at point of use (HIGH); "exactly a two-parameter Hill function" is only true at γ = ½;
  free-γ vs Hill n=1 BIC gap = ln N (a check the table passes silently); R₀ still labelled Validated;
  ferromagnet/water N_corr presets; MRH lacks Pearl/Friston citation; μ_eff vs MOND μ collision.
  Researcher: DE sector "refuted 3.4–5.4σ but ledgered as silence" (HIGH — see WAKE 2); Tier-1 tally
  double-counts environment (HIGH); TEST-02 branch + MOND+EFE range (HIGH); H₀ error bar; unidentifiability
  result buried; 8.7σ uncited; A2ACW 6/6-0/6 in the last paragraph; galaxy-plotter toy disk.
- **Forum**: nothing new since 2026-05-28.
- **Explorer**: 2026-09-05 findings already integrated by yesterday's session; no new findings today.

## Verification pass (before editing)

- **"Executed: 6 lists seven IDs / Kills: 5 lists six"** — does not exist anywhere in `src/`. WebFetch
  synthesized a tally from the test cards. The real defect: the page badge said "4 executed — 3 failed"
  while five distinct executions (seven IDs) carried dates. Fixed at the source of the reading, not the
  quoted form. (Second day running that a HIGH item quoted fetch-synthesized text; memory saved.)
- **`/start-here` and `/tools` "duplicate pages"** — both are `next.config.mjs` redirects (307). Made
  permanent (308) so the canonical URL is explicit.
- **DESI direct fit** — `explorer/findings/scripts/fit_gamma_family_to_desi_dr2.py` + output exist; finding
  `gamma-family-direct-fit-desi-dr2-substituted-is-lcdm-covariant-excluded.md` (2026-08-12); PREDICTIONS.md
  carries it with the 2026-08-19 Cassini-ω hardening. Site page stale, confirmed.
- **8.7σ** — already cited in the Tier-1 TEST-25 alert (Desmond, Hees & Famaey 2024, MNRAS 530, 1781);
  the Honest Assessment classification row carried author/year without the journal. Added.
- **H₀ ± 15.5** — 2π × (1.20 ± 0.24)×10⁻¹⁰ / c → 77.6 ± 15.5 km/s/Mpc; 0.66σ from 67.4, 0.30σ from 73.0.
- **BIC penalty** — ln 2807 = 7.94; table gap (+7.1) − (−0.7) = 7.8. Holds.
- **TEST-02 MOND+EFE "1.0–1.4× in g"** — taken from the researcher persona; NOT verified against Chae/Banik
  this session. Stated on the row with the dispute named; seeded to the explorer for verification.

## Changes Made (Site) — build passes, zero errors, all pages static

- `src/lib/ledger.ts` — `GALAXIES_MECHANISM = '175'` + `GALAXIES_SENTENCE` with the rule (mechanism tested
  on 175 SPARC curves; the 14,435-object TFR test never ran as registered).
- `src/app/why-synchronism/page.tsx`, `src/app/key-claims/page.tsx`, `src/app/galaxy-rotation/page.tsx` —
  14,760 replaced by `GALAXIES_RUN` + breakdown + mechanism N, imported from the ledger; dated erratum on
  the Beginner-path card. Melting-point card gets the one sentence connecting chemistry to the one-curve bet.
- `src/app/dark-energy/page.tsx` — badge → `reparametrization` ("ΛCDM where it fits…"); the sign-lock
  paragraph flags that its σ figures did not survive a direct fit; Completion B card re-priced (fails the fit,
  Δχ² ≥ +79; w₀ = −3.18 at Cassini-allowed ω); **new section "The direct fit (2026-08-12)"** with the three
  results and the Bucket-3 status; "has not been done" bullet corrected to "was done."
- `src/app/honest-assessment/page.tsx` — jump list opens with "Badge definitions (canonical)"; DE block's
  bound (2) states the executed fit and Bucket-3 status; **new classification row** "Not counted:
  dark-energy sector — Reparametrization (ΛCDM)" answering Pass 4's question with the ledger's verdict;
  8.7σ row cites MNRAS 530, 1781 and explains the ~2× marginalization gap; H₀ inversion carries ±15.5 in both
  places and "failing soft constraint" → "non-discriminating soft constraint".
- `src/app/key-claims/page.tsx` — H₀ box: ±15.5, 0.7σ/0.3σ, "non-discriminating, not failing", dated
  softening note naming it an over-refutation in the framework's disfavour.
- `src/app/tier-1-existing/page.tsx` — page badge rendered from a single `TALLY` object (5 executed with
  TEST-01/03s/05 merged; 4 kills; TEST-04a underpowered; TEST-04 withdrawn; TEST-02 self-eliminating) plus
  `ROOTS_SENTENCE` from the ledger; caveat 1 "3–4, not 6" replaced by the ledger split with a dated note;
  TEST-02 kill text states which branch makes the 0.05–0.4% null, that the SPARC-fitting branch predicts
  MOND's boost, and the MOND+EFE range with the Chae/Banik dispute named.
- `src/app/coherence-function/page.tsx` — "exactly a two-parameter Hill function" → Hill-type rational
  function of (1+x)^{2γ}, Hill in x only at γ = ½; form-selection box states x = g_bar/a₀ and that the
  density-keyed form loses head-to-head at ΔBIC +2843; the ln N = 7.94 consistency check added after the table.
- `src/app/coherence-explorer/page.tsx` — "Two models, one curve" caption: which sentence belongs to which
  branch; link to the head-to-head.
- `src/app/dark-matter/page.tsx` — plain-words paragraph (crowd/marching band → boost → capped at ~3× vs
  ~14× needed) before the Failed badge; μ_eff defined as the fluid viscosity, not MOND's μ; plain reading of
  the viscosity sign error.
- `src/app/parameter-derivations/page.tsx` — R₀ badge label no longer contains "Validated"; text states the
  factor 3 is fitted (natural MOND radius V²/a₀) and the comparison set is unstated.
- `src/app/for-researchers/page.tsx` — artifact 5: density-keyed unidentifiability no-go (statement, SPARC
  numbers, Refracted Gravity as the identifiable contrast, honest-novelty line); description → "Five".
- `src/app/first-encounter/page.tsx` — "Markov" glossed inline on the Beginner path.
- `next.config.mjs` — `/tools` and `/start-here` redirects made permanent.
- `SESSION_FOCUS.md` — session entry at top.

## Back-Annotations (Research Repo)

- `Research/proposals/argument_of_C_three_functions_ledger_not_commensurable_20260824.md` — addendum: two
  public visitor personas independently re-derived the C_ρ / C_g split from the site alone (quoted); the
  ambiguity is now an outside-reader defect, not internal bookkeeping; registration still gates on dp.
  Commit `e75b731a`, pushed.
- Nothing else: the dark-energy and H₀ corrections were the site catching up to PREDICTIONS.md, not the
  reverse.

## Topics Seeded for Explorer

- `knee-inventory-where-has-x-near-1-ever-been-sampled.md` — per-sector table of what x is, the range data
  span, and whether the tanh/Hill shape was ever tested as Synchronism's rather than as MOND's μ or ΛCDM's
  Ω_m(z) (Pass 3 Q2).
- `chemistry-sound-velocity-monotonicity-sign-failure.md` — is Spearman −0.32 with diamond above lead a
  mechanism root (seventh refutation) or a registration artifact? Apply the 09-05 discipline (Pass 3 Q5).
- `test02-which-branch-predicts-the-null-and-mond-efe-range.md` — verify the branch attribution and the
  1.0–1.4× MOND+EFE bracket now stated on the TEST-02 row (Pass 4 Q2).
- `density-keyed-unidentifiability-standalone-note.md` — full Fisher matrix, knee-threshold plot, Refracted
  Gravity contrast, as a standalone citable note (Pass 4 Q3).

## Open Items (Deferred)

- `/equation-walkthrough` warning placement: two personas want it below the breakdown; it was put first on
  2026-07-10 deliberately ("so you don't absorb the formula as-is"). Needs a design decision, not a swap.
- Galaxy plotter: SPARC component velocities instead of the toy disk; short legends + caption; the
  hand-tuned amber curve as a screenshot liability. Open topic `galaxy-plotter-sparc-mass-models-and-glossary-tooltips.md`.
- Breadcrumb middle segments: either section index pages or drop the segment. Not started.
- Badge vocabulary: 16 badge-styled labels vs 11 canonical (open topics `badge-vocabulary-closure.md`,
  `badge-vocabulary-single-source-of-truth.md`).
- Honest Assessment on the "~30 min" Beginner path: a five-minute anchor version. Not started.
- `/consciousness-demo` four stacked banners → one expandable; `/quantum-predictions` stale protocols
  removal; γ Calculator ferromagnet/water presets as placeholders; Explorer readouts in words; `/mrh`
  Pearl/Friston citations; glossary hover on borrowed acronyms (SPARC, RAR, BTFR…) at point of use.
- Landing page: enthusiast still meets the failure line before the idea. Yesterday's hero rewrite put the
  plain sentence *after* "It did not hold as stated"; the order is the remaining complaint.

## So what?

The site was wrong in the deflationary direction twice today (dark energy "refuted at 3.4–5.4σ"; H₀ "above
SH0ES by several σ"), both times because an explorer finding never reached the page that quoted the stale
number. Under-claiming is the same propagation failure as over-claiming, and a visitor can be led to ask for
a *seventh refutation* that the archive's own fit already dissolved. The one genuinely discovery-relevant
thing in today's log is that outside readers now trip on the C_ρ/C_g split unaided — which is the strongest
argument yet for the dp-gated registration of the three C functions as distinct models.
