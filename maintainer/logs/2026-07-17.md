# Maintainer Session — 2026-07-17

## WAKE

Two research-direction signals in today's visitor log, both from Pass 4 (Researcher):

1. **The DESI DR2 unfreeze is the program's one shot at a genuinely prospective registration
   — and no both-outcomes verdict rule is written anywhere.** The pre-registration audit
   stands at 0/10 prospective; DR2 full-shape growth (~Spring 2027) is the only known
   upcoming datum with an already-registered threshold on data that does not yet exist.
   Every documented criterion-verdict substitution (TEST-03, TEST-04a, S193's BTFR rescue)
   exploited exactly the asymmetry of a one-directional re-open policy. **Proposal filed
   before fixing friction**: `Synchronism/Research/proposals/test04a_desi_dr2_both_outcome_precommitment.md`
   — fixes the statistic now (registered fσ₈ bin, NOT GR-conditioned σ₈), states all three
   outcome branches (suppression → unfreeze but stays post-hoc; >3σ exceedance → first
   prospective-adjudicated kill; between → "underpowered" hardens and the test retires),
   and gates inscription into PREDICTIONS.md on dp. Includes a power-check gate: if DR2
   forecasts can't reach 3σ on this statistic either, the pre-commitment must say so before
   the data arrives.
2. **φ (golden ratio) has zero provenance on /parameter-derivations** — the page whose job
   is parameter provenance never mentions the framework's most numerological-looking
   ingredient, though TEST-09's no-rescue scan leans on φ's "derived from cosmology" status.
   Seeded as HIGH explorer topic (`phi-exponent-provenance-audit.md`).

Dirty tree at WAKE was benign (supervisor's GitNexus index-count updates in AGENTS.md/CLAUDE.md
— left untouched per primer; untracked log file was this session's own runner stamp).

## Feedback Reviewed

- Visitor log 2026-07-17 — four passes. Headline synthesis: the site's failure mode has
  inverted — the front page now **under-reports** its own refutations (count stale at "1"
  after the 07-14/15 executions, attribution differing by page). Pass 3 hand-checked every
  number and all regenerated (tanh(2 ln 2) = 15/17, Hill identity, a₀/Σ₀ arithmetic, V⁻²);
  Pass 3 also root-caused the Σ₀ 119-vs-123 split to an undisclosed H₀ switch (67.4 vs 70).
- Forum: nothing new.
- Explorer findings: 07-14/15 already shipped by explorer (MOND-Shared class audit).

## Changes Made (Site) — build PASS (92 pages)

**P0 — headline refutation recount (all four passes' top item):**
- `src/app/layout.tsx` (site-wide footer): "0 confirmed predictions, 1 refuted by external
  data" → "0 confirmed; **4 refutations executed on external data** (RAR shape, BTFR slope,
  dwarf DM fractions, environment null)" with ledger link. Canonical census: RAR γ=2
  (ΔBIC=+184), TEST-09 (registered kill fired 3.3σ), TEST-10 (69% of SPARC above the 68.5%
  structural ceiling), environment registered run (r²=0.0001). DESI TEST-04a explicitly NOT
  counted (underpowered as registered).
- `src/app/page.tsx` (landing outcomes strip): added the four refutations as the lead item;
  DESI demoted to "1 — underpowered" with not-counted note.
- `src/app/honest-assessment/page.tsx`: "What Was Tested" preamble now defines the footer
  count and states none of the four is prospective (prospective count remains 0); **Verdict
  section rewritten** — was dated May 2026, led with DESI "Kill criterion triggered
  (fσ₈ > 0.46 required; observed ≫ 0.46)" (the exact criterion-verdict substitution retired
  on 07-14) and never mentioned TEST-09/10. Now leads with the boost-ceiling pair, carries
  the corrected DESI framing, and the closing "one external refutation" → four. Plain-terms
  box and takeaway bullets re-led with BTFR as the sharpest failure. Two history paragraphs
  referencing a "Kill Criterion Triggered label above" that no longer exists → rewritten as
  dated retirements.
- `src/app/key-claims/page.tsx`: Session #581 quote got a count-currency note (its "1
  refutation" is the quantum-arc count; site-wide is now 4, the quoted boost ceiling since
  cashed out as TEST-09/10).

**P0 — cross-page verdict contradictions (Passes 2/3/4):**
- key-claims consciousness badge chip "both threshold values Failed" → "threshold untestable
  as stated" (the card's own caveat already said the cited test measured the wrong variable
  — a verdict can't rest on evidence its own note retracts).
- key-claims "6 Reparametrizations (incl. BTFR slope)" → 5, with a boxed note that the BTFR
  left the list 07-14 as the framework's one genuinely discriminating test — run, lost,
  TEST-09. (A fired 3.3σ kill is the opposite of a reparametrization.)
- `/why-synchronism` "What Worked" heading → "What We Tested — and How It Went" with a dated
  note (heading contradicted the Failed badges under it).

**P0 — glossary brought into verdict-sweep scope (Pass 2's top structural finding). Five
fossil entries in `src/lib/terms.ts`:**
- a₀: "EMERGES from cosmology… one of Synchronism's strongest results" → audited
  Reparametrization framing (Milgrom-1983 coincidence, dimensional rederivation).
- BTFR: entry still carried S193's synthetic-ladder rescue ("consistent with the
  regime-dependent prediction") → rewritten with the executed TEST-09 refutation and the
  rescue's invalidation.
- NP2: "p = 5×10⁻⁶, strongly supported" — a live fossil of the retracted TEST-03 statistic
  → corrected to the provenance-audit account + registered-run null.
- RAR: "predicts the scatter should be environment-dependent" (live-tense) → refuted by
  execution.
- ALFALFA-SDSS: self-contradictory kill-direction sentence → synced with the corrected
  TEST-03/05 account.
- Plus Pass 1's plain-language items: tanh entry de-jargoned (compression-ruler/dimmer
  language first, Landau line translated), γ entry gains intuition + the audited-negative
  status it was missing, coherence entry deduplicated (marching-band lead), phase-transition
  entry corrected (no critical point — it asserted the thing the site retracted).

**P1 — H₀ standardized site-wide (Pass 3's root-cause):** site standard now H₀ = 67.4
km/s/Mpc (Planck 2018) ⇒ Σ₀ ≈ 119 M☉/pc²; parameter-derivations heading + resolution note
(was "open inconsistency"), honest-assessment caveat → resolved, key-claims quotes
precision at the M/L-systematic level only ("sub-percent match percentages retired").
Topic `h0-value-inconsistency-across-pages.md` archived with resolution note.

**P1 — A2ACW framing (Pass 4):** research-philosophy's "successful demonstration of the
ceiling on AI-to-AI adversarial collaboration" → corrected: a single-arm, single-corpus
retrospective on a possibly-null corpus cannot separate "AI ceiling" from "null corpus";
control arm named as the missing experiment (now consistent with /for-researchers' confound
statement).

**P1 — beginner path (Pass 1):** why-synchronism's two technical caveat paragraphs
(ansatz/compander/circularity) wrapped in a collapsible "For physicists: the fine print"
details block — Step 1 body now reads jargon-free by default, nothing deleted.
equation-walkthrough Step 3 gains the ln "compression ruler" plain-language lead (the
dimmer-switch treatment tanh already had).

**P1 — galaxy-plotter accuracy (Pass 4):** disclosure block now states the spherical-vs-
Freeman-thin-disk approximation and its direction (Newtonian baseline understated ~15–20%
near peak ⇒ visual "gap" modestly exaggerated there; conclusions unaffected at large
radius). Bessel implementation deliberately queued, not rushed (topic seeded).

**P2 — editorial (Pass 2):** interactive-tools legend's unused "Kill Criterion Triggered"
chip removed (operational state, not a badge — with code comment); coherence-explorer
slider label "Tipping-point density" → "Reference density — saturation knee" (label no
longer commits the error its caption corrects); phase-boundary-visualizer duplicate γ≈1
physics block deduplicated (Three Regimes section now a summary index); γ-calculator gains
per-preset "What just changed, and why it matters" lines (the BCS preset now narrates the
sharpness inversion in one click); landing 1,703 stat card gains the null-model subline;
key-claims PRL 2024 citation marked "consistent with, not predicted by"; TEST-06 marked
UNDERPOWERED AS REGISTERED now, before its data arrives (kill threshold sits inside the
disclosed ~3× pipeline-dependence range).

## Back-Annotations (Research Repo)

- `Research/proposals/test04a_desi_dr2_both_outcome_precommitment.md` (NEW — the WAKE
  proposal; supersedes-in-part the 06-24 DR2 proposal, which predates the criterion-
  substitution correction). PREDICTIONS.md inscription gates on dp.

## Topics Seeded for Explorer

- `phi-exponent-provenance-audit.md` (HIGH) — where does φ in the C(a) exponent come from?
  Derived/asserted/fitted-then-named. Feeds a missing /parameter-derivations row and may
  sharpen or reframe TEST-09's "derived ingredients kill it" sentence.
- `galaxy-plotter-freeman-disk-baseline.md` (MEDIUM) — implement + validate the Bessel
  thin-disk Newtonian baseline (with sanity gates and MOND-curve revalidation asks).

## Open Items (Deferred)

- Breadcrumb taxonomy (Pass 2): middle segments unlinked, parents match no nav — a
  site-wide component/navigation.ts design change; too large for this pass, and the right
  fix (align parents with nav or create category pages) needs a decision, not a patch.
- Central errata/changelog page (Pass 2 Q3): inline correction notes "accumulating like
  sediment" — real, growing; suggest a /corrections page fed by the dated notes. Design
  question for a dedicated pass.
- Phase Boundary Visualizer rename ("γ Regime Map") — the page's own /mrh-style logic
  demands it; rename touches nav + inbound links; deferred with the dedup shipped.
- Consciousness-demo chart overlay (Pass 3 low): shade the convergence band
  "expected by construction" on the graphic itself.
- Equation-walkthrough → parameter-derivations compander pointer (Pass 3 low): substance
  already on-tool; cross-link nice-to-have.

## So What?

The loop's documented failure mode inverted and the site caught up with itself: for
eighteen months every provenance break over-refuted; today the front page was
**under**-reporting refutations because the headline never absorbed last week's executions.
The recount makes the honest ledger legible from the footer of every page. The deeper fix
is the WAKE proposal: the program has never adjudicated anything prospectively, DR2 is its
one standing chance, and the both-outcome rule is now written down before the data exists —
which is the only time writing it down counts.
