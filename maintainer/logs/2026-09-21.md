# Maintainer Session — 2026-09-21

*(The 2026-09-20 maintainer run did not happen: the runner hit a usage-credit limit at 06:00. Explorer 09-19 P1s
were carried into today.)*

## WAKE

Yes. One item is about the research, not the site, and it has a deadline.

**The program's one "genuinely prospective" test cannot fail.** The researcher persona said TEST-04a's DR2 branch B
("fσ₈ > 0.46 at ≥3σ") stacks two 3σ requirements, because 0.46 is already 0.418 + 3σ_forecast. I ran the arithmetic
rather than re-quote it (`maintainer/scripts/test04a_dr2_branch_power.py` + `_output.txt`). The persona's claim holds,
and the asymmetry is worse than stated. Under ΛCDM truth, at any σ_DR2 in 0.025–0.045:

| branch | fires under ΛCDM |
|---|---|
| B as adopted (obs > 0.46 + 3σ) | **< 1 %** |
| B clean ((obs − 0.418)/σ > 3) | 4–22 % |
| A (obs ≤ 0.46, "criterion met by suppression direction") | **29–38 %** |

Three more points came out of it:
- Session 107's own forecast σ at z = 0.51 is **0.018**, not the 0.014 the 0.46 threshold implies.
- Even a clean single bin needs σ ≈ 0.015 for real power. The five-bin table Session 107 already wrote down is the only
  form with a chance.
- The row tests Session 107's G_local/G_global mechanism, which the current framework no longer contains.

I checked that DESI DR2 galaxy full-shape is still unpublished (only the DR1 bispectrum and DR2 Lyα full-shape papers
are out, and the Lyα paper drops fσ₈), so re-registering is still prospective. The registration was adopted by dp, so
the change **gates on dp**.

Proposal (written before fixing friction):
`Synchronism/Research/proposals/test04a_dr2_branch_b_cannot_fire_reregister_before_data_20260921.md`.

**Frame:** the flagship prospective test got a timing check (before the data) and no power check. That is the explorer's
09-18 finding (specificity is the expensive half of pre-registration) landing on the one row that was supposed to be
the model. A mandatory line on every registration, "probability each branch fires under the null", would have caught
it on 2026-07-17.

## Feedback Reviewed
- **Visitor log 2026-09-21** (four personas). None of them found an overclaim in the physics verdict. The persona
  synthesis: the failure mode has moved from "claims too much" to "corrections don't propagate". HIGH items: the
  TEST-04a branch B problem (above); First Encounter Part D ("phase transitions happen… 1,703 phenomena cluster
  here. Not by design — by observation", with no caveat); /why-synchronism is ~4,000 words for Beginner step 1.
- **Forum:** nothing new since 09-18.
- **Explorer 09-19 finding** (a₀(z)): P1 ×2 on /parameter-derivations, P2 on /mond-unification.

## Changes Made (Site)
**The time-sensitive item**
- `tier-1-existing` TEST-04a alert: a dated flag says branch B as adopted cannot fire unless ΛCDM also fails, and gives
  the null rate of each branch. It tells readers to expect branch C and not to read branch A as support. The
  registration itself is unchanged because that is dp's call.

**HIGH friction**
- `first-encounter` Part D: "phase transitions happen… Not by design — by observation" is replaced with crossover
  wording plus "fitted, not predicted" and the γ Calculator's "neither placement follows from the equation". The
  highlight now carries the status (Reparametrization).
- `gamma-boundary/layout`, `sound-velocity/layout`, `navigation.ts`: related-link blurbs now use the body wording
  ("r = 0.982 with fitted γ; matched by a Z-polynomial null"). The `gamma-parameter` sibling card had the same
  Part D text; the new lint rule found it.
- `why-synchronism` (Beginner step 1): added a three-line "what happened" box with "Beginner path: you can stop here →
  Step 2". The Problem, Approach, What We Tested and What Failed sections are now in a "Details for the curious"
  `<details>`. The verdict (0 confirmed, 6 refutations, lost where it could be told apart) stays above the fold, so
  no failure is hidden. "Every prediction has a falsification criterion" is replaced with the measured 2-of-26 form.

**MEDIUM/LOW (two agents on disjoint files, both reviewed)**
- `for-researchers` A2ACW: "every known positive" → negative, with a dated correction. The J interval is
  [−0.71, +0.46] everywhere, and quoted history carries a correction note. Sensitivity is 3/3 per rule, and the
  pooled 6/6 is marked as not six independent trials. Bell item: "no construction exceeded the local bound S = 2
  (QM reaches 2√2)".
- `key-claims` claim 1: the lead-in is now "Proposed engineering consequences (current status per badge)";
  "privileged-frame" → "observer-privilege" (also on `observer-problem`, found by lint); added a Leggett–Garg / KS /
  PBR open question.
- `what-synchronism-is-not`: the superlative is replaced with a concrete fact.
- `parameter-derivations`: **explorer 09-19 P1 ×2 done**. The a₀(z) block is retitled to the current
  non-discriminating state with a revision trail; the Milgrom 2017 sentence is corrected; the stale legend sentence
  is fixed; the γ = ½ identity bullet now says which argument makes it MOND.
- `mond-unification`: **explorer P2 done**. Added why TFR zero points cannot decide a₀(z) (Milgrom 2017 §4; lever
  vs spread), and that the level-free ratio test has not been run.
- `coherence-function`: the stale "argument g_bar" sentence now reads acceleration (g_obs, μ-form); a₀′ is 2.25×
  below, 11% low after a₀ = 2a₀′.
- `honest-assessment`: "laboratory" is dropped from the five-external-data line, and the Bell/CHSH line is relabelled
  as a simulation run. TEST-10 is given in both units (579 points; 106/153 discs).
- `gamma-calculator`, `phase-boundary-visualizer`: the tool-type line matches the index's Kind/Status slots;
  "off-scale left" → at the left edge, indistinguishable from 0; the Ginzburg gloss is corrected.
- `terms.ts` "Outcome words" and the `tier-1-existing` legend: "Withdrawn" now has one definition, the HA one.
- `glossary`: every term also gets a lowercase-slug anchor (`/glossary#kill-criterion` now works; Δ → "delta" to
  avoid a ΔBIC/BIC collision). `handling-failure`'s post-diction link was pointed at its slug.
- `page.tsx` (landing): the demotion badge list is completed; I added that the per-claim split (identity/script vs AI
  prior-art mapping) has not yet been tabulated.
- `equation-walkthrough` γ row: now "the original proposal was… it failed…; γ today is fitted".
- `site_lint.py`: 89 rules (+14). Positive-controlled against HEAD. The new rules found 2 sibling-page copies,
  which are now fixed. Lint: 0 live. `next build`: passes (93 pages).

**Already present, not changed:** the w₀wₐ comparison the researcher asked for is on /honest-assessment:1789 and
/dark-energy:255 ("+11.0 behind w₀wₐCDM"). The persona missed it because it isn't next to the −0.3.

## Back-Annotations (Research Repo)
- `Research/proposals/test04a_dr2_branch_b_cannot_fire_reregister_before_data_20260921.md`: new, gates on dp.
- `PREDICTIONS.md`: a flag under the DR2/TEST-04a registration block with the branch null rates, the 0.018 vs 0.014
  forecast-σ discrepancy, and the DR2 publication check. The registration text is unchanged. No bucket moves;
  count 6; Bucket 0 = 0.

## Topics Seeded for Explorer
- `test04a-five-bin-growth-statistic-on-dr1-before-dr2.md`: **time-sensitive**. It only counts if done before DR2
  full-shape publishes.
- `temporal-scanning-vs-leggett-garg.md`: this could move claim 1 from "untestable as stated" to tested on existing
  data, in either direction. It flags the clumsiness-loophole trap ("measurement = synchronization" may itself be an
  invasiveness claim), to be decided before reading any data.

## Open Items (Deferred)
- Beginner path steps 2–3 (/first-encounter length, /core-idea's unglossed Ising/Hill/Landau names): step 1 was done
  first because the enthusiast quit there.
- A "function tested" column (C_ρ / μ-form on g_obs / floored C_a) on the HA and key-claims refutation tables. This
  depends on dp decision (i) from 09-08; still open.
- Shared-string generation (tool-type lines, badge definitions, "what differs from MOND" — three incompatible "only
  difference" sentences). The lint catches retired phrasings; it cannot generate the canonical one.
- Breadcrumb section crumbs (third day deferred); nav grouping; landing glossary links for Ω_m/DESI/ΛCDM; the footer
  ledger length; "Executed — 5" vs "6 refutations" nouns; the 1/C ≈ 1000 vs 1275 figures on the plotter; disc C
  10⁻⁵ vs 10⁻³ on /consciousness-demo; "0 ties" vs TEST-25 on Tier 1.
- An HA dated note (line ~460) still says "laboratory" inside a 07-30 history entry. It is left as a record.
- **Topic queue: 91 open topic files.** The explorer runs about one a day. Seeding faster than consumption turns the
  queue into a backlog nobody reads, so it needs a triage/archival pass (next maintainer session, or dp).

## So what?
The site work is maintenance. The finding that matters is that the program's single prospective registration was
built so that its kill cannot fire while its friendly branch fires a third of the time under the null, and that
there is still time to fix it. If dp re-registers before DESI publishes, TEST-04a becomes the first test this program
could actually lose on schedule. If not, the site now says so in the open. The lesson for the method is that a timing
check without a power check is only half a pre-registration. That also holds for the kill-criterion registry, where
power has never been computed for any row.
