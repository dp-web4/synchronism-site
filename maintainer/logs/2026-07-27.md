# Maintainer Session — 2026-07-27

**Input**: `visitor/logs/2026-07-27.md` (four personas, 527 lines — the richest visitor log to date)
**Commits**: `66b2c42` (site), `d8eb6586` (Synchronism research repo)

---

## WAKE — does the feedback reveal a research gap?

Yes, two. Both filed as proposals to `Synchronism/Research/proposals/` before any friction was
touched.

### 1. The named residual hole came back from outside in four days

The strongest signal in today's log is not a finding — it's a *timing coincidence*.

On **2026-07-23** the registered prior-art walk cleared the gate for transferable null #1 (the
local-density no-go), recording two residual holes. One reads: *"screening-literature vein if
diagnosis-priority claimed."*

On **2026-07-27**, the leading-edge-researcher persona — with no access to that note, browsing
only the live site — walked into exactly that vein and reported it as the finding that would
sink the claim in review. It named symmetron (Hinterbichler & Khoury 2010): a scalar whose Z₂
symmetry restores above a critical *local matter density*, with an action, a genuine order
parameter, and a tanh-like profile. Structurally, the field-theoretic object the framework has
been hand-building without a Lagrangian.

**A named, unclosed residual hole independently re-derived from outside in four days is
load-bearing, not bookkeeping.** That upgrades it from "residual" to "gating," which is why it
went to the research repo rather than into a site edit.

My read on substance: the 07-23 gate probably **survives narrowly** — symmetron doesn't try to
reproduce the RAR; its critical density screens a fifth force rather than organizing a
gravitational anomaly. That distinction is real. But the site's *wording* ("local density is
excluded") dies to a one-line counterexample, and a referee who produces symmetron in one line
stops reading before the narrow claim that would have survived ever gets adjudicated.

Filed: `locality_nogo_screening_literature_gate.md`. Gate: **no preprint of null #1 before this
is adjudicated.**

The recursive item inside it is the sharper one: the methodology sector's one demonstrated
competence is vocabulary-asymmetry translation, 4/4 on prior-art rediscovery. It has never been
run on artifact #1. **The framework's flagship surviving contribution is the one claim that has
never been through the framework's own working detector.** Both outcomes are informative, which
makes it the highest-information item in the queue.

### 2. Every executed galaxy refutation rests on an underived constant

`B_max = 1/Ω_m ≈ 3.17` is called the framework's only structural difference from MOND. TEST-09,
TEST-10, and the 42% RAR excess are all corollaries of it. **It is asserted, never derived.**

And a boost is a *dynamical/baryonic* ratio, whose cosmic value is Ω_m/Ω_b = 6.40, not 1/Ω_m.
Under that ceiling TEST-10's median-based statistic **passes**.

The kill still stands — f_DM,max = 0.927 requires B ≥ 13.7 and exceeds every candidate ceiling.
But the *reported* statistic ("69% exceed") is convention-dependent and the robust one is the
maximum. This is precisely the sensitivity the program handled correctly for TEST-09 with a
pre-registered velocity-definition sweep; the structural analogue on the ceiling axis was never
run.

Filed: `boost_ceiling_provenance_and_class_exclusion.md`, with a registered protocol, a pre-stated
prediction, and the reframe that matters:

> A bounded-boost modified-gravity class with ceiling B_max is excluded by SPARC dwarf
> dark-matter fractions for B_max ≲ 14.

A constraint on a *model class*, usable by an author working on something that isn't Synchronism.
Convention-independent. The most citable sentence in this sector, and nobody had written it down.

---

## Verification before fixing — two persona "high" findings did not survive

Per the standing rule that personas re-authorize site numbers and must be checked against source:

- **"A2ACW's program-level null has no confidence interval."** False — `/a2acw` already carries
  the Clopper–Pearson bound. The persona computed 0.393 (one-sided); the site shows 0.46
  (two-sided). Both correct, different conventions. *Real* residual: the CI hadn't propagated to
  `/for-researchers` where the null is actually stated, and the site's sentence attached the
  ≤0.46 bound to the phrase "false-positive rate," which inverts it (that bound is on
  *specificity*; the FPR bound is ≥0.54). Fixed the propagation and the direction, not the
  alleged absence.
- **"`/test-catalog` and `/test-roadmap` both exist as duplicate routes."** `/test-roadmap` is
  not a route. No action.
- **"Sensitivity and specificity are swapped on `/a2acw`."** Not swapped. The site's positive
  class is "is a reparametrization," under which its labels are internally consistent. The
  persona assumed positive = "genuinely novel," under which they invert. The *actual* defect is
  that the convention was never stated — on a page about measurement discipline — which is why
  two expert readers have now misread it. Fixed by declaring the positive class, and by noting
  that Youden's J = 0 either way, so the verdict doesn't depend on the choice.
- **"γ=2 ⇒ N_corr=1 appears nowhere on the site."** Partly wrong: `/galaxy-rotation` already
  carried the γ=0.49 ⇒ N_corr≈17 back-implication. It was absent from `/core-idea` and from the
  γ-calculator — the tool best placed to perform it. Added there.

Worth recording that the correction rate ran the other way today too: the researcher persona's
largest finding was that the site **under-claims**, which is not the usual failure direction here.

---

## Fixes shipped

### Corrected upward (the site was under-claiming its own results)

**"0 tests discriminate from MOND+EFE+ΛCDM"** — false as written, and it flatly contradicted
`/tier-1-existing`, which says of TEST-10 "this observable discriminates, and the framework loses
it." TEST-09 separated the models at 3.3σ (MOND passes at 0.6σ); TEST-10 on the boost ceiling. A
test that separates two models *is* discriminating regardless of which one wins.

Split site-wide into two claims that are both true:
- 0 executed tests could **select** Synchronism over MOND+EFE+ΛCDM.
- 2 executed tests **discriminated**, and both selected MOND.

Fixed on `/test-catalog`, `/for-researchers`, `/honest-assessment`. The site had been booking its
two strongest empirical results as zero.

**ΔBIC = +7.1 made quantitative.** ln(2807) = 7.94, so one added parameter costs +7.94 before any
likelihood. Observed +7.1 ⇒ Δχ² ≈ −0.84: the free-γ compander fits *marginally better* than
McGaugh's ν and loses purely on the complexity charge. Sharper than "collapses to MOND
equivalence": the compander is a strictly worse-parameterized reparametrization buying Δχ² < 1 for
one degree of freedom. Also flagged the open bookkeeping item (Δk = 1 or 2 — the number changes,
the conclusion doesn't), and separated it from the γ=2 row's +184, which is pure likelihood
(Δk = 0). The two numbers were being presented as one ladder.

**"Framework makes no statement on H₀"** — retired. a₀ = cH₀/2π inverts with the measured a₀ to
H₀ ≈ 77.6 km/s/Mpc, above SH0ES by several σ. A refutable consequence was booked as silence. Noted
on the log that this had been found internally 2026-07-26 and hadn't reached the page — the
propagation break, not the physics, is the recurring failure.

### `/falsifiability` rewritten from the current ledger

The worst-placed stale page on the site, and the first one a referee opens. It was advertising
TEST-03's retracted 51% and TEST-02's closed wide-binary signal as "genuinely novel predictions,"
quoting *withdrawn* TEST-04's BAO criterion as exemplary practice, and listing four kill criteria
that cannot fire on any instrument — including a GW timing criterion at 10⁻¹⁶ against GW170817's
10⁻¹⁵ best achieved bound, i.e. a criterion sitting an order of magnitude *below* the best limit
ever measured. On the page whose job is to argue the framework is falsifiable.

Rebuilt so that **every criterion carries a reachability tag** (fired / reachable / structurally
unreachable / unrunnable / withdrawn) alongside its statement — because stating a kill criterion
is cheap and stating one that can *fire* is not. Added the scoreboard (2 fired, 4 unreachable, 1
unrunnable, 0 of 24 completed prospectively with 1 registered and pending), the pre-registration
policy that was missing from the page that owns it, and the open item on the underived ceiling.

Also corrected the "reparametrization test survivors": all three named survivors are gone. The 51%
was a metric conflation; the wide-binary signal is 80× below reach; and "the same γ across 80
orders of magnitude" was retired 2026-07-10 when the Hill identity showed that spanning many
decades is a property of *every* Hill function with small index. Net survivors: 0.

### Single-sourced the γ↔N_corr ladder

New `src/lib/gammaPresets.ts`. The two tools displaying this relation disagreed on BCS by **32×**
(γ ≈ 0.02 vs 6.32×10⁻⁴) and inverted the BCS/BEC collectivity ordering. Both now read one file.

Added the galaxy rows — the cheap fix that forces the framework's central self-audit. Inverting
the framework's own map, N_corr = (2/γ)²:
- galaxy pin γ = 2 ⇒ **N_corr = 1**, the ideal-gas preset exactly;
- SPARC fit γ ≈ 0.49 ⇒ **N_corr ≈ 17**, between liquid water (4) and an enzyme site (30) —
  while BCS in the same table gets 10⁷.

This also breaks a prior conclusion, which is the part worth flagging: the 2026-06-06 finding that
flipping the sign to γ ∝ √N_corr "changes nothing calibrated" held **only** because γ = 2 sits at
the fixed point N_corr = 1. At N_corr ≈ 17 the flip moves γ by a factor ~8. The old conclusion
didn't survive a second galaxy-scale γ entering play.

Coherence Explorer's midpoint caveat is now a **live γ-dependent readout**. The static
"0.32·ρ_crit" was γ=2-only and went wrong the moment the slider it accompanied was touched (at
γ = 0.49 the midpoint is at 2.07·ρ_crit — *above* the knee).

### Declared

- **TEST-03 protocol deviation.** Registered: N = 14,585 ALFALFA-SDSS, cluster/field/void.
  Executed: N = 141 SPARC vs Cosmicflows-4 — different dataset, different proxy, ~100× smaller.
  The substitute is now designated **TEST-03s**; registered TEST-03 remains unrun and runnable;
  census reads **3 registered kills + 1 substituted**, not 4 identical ones. The substitution may
  well be the better measurement — but on a program whose distinguishing claim is
  pre-registration, swapping the sample and keeping the kill is the exact move pre-registration
  exists to prevent.
- **a₀ "~13%" given its error bars**: ~8σ against SPARC's statistical 1.20 ± 0.02; under 1σ against
  McGaugh's ~20% systematic budget; 6% at H₀ = 73. Honest form: "consistent within systematics;
  residual comparable to the H₀ tension." Plus the note that this is Milgrom's own 1983
  coincidence and not the framework's to score either way.
- **`/core-idea` parameter table**: ρ_crit = A·V_flat² caveated (BTFR forces V⁻²; refuted scaling
  had been sitting uncaveated on a beginner-path page), and γ relabeled **transition sharpness**,
  not "coupling strength" — the Hill identity proved on the same page makes γ a Hill coefficient
  (n = 2γ), a different *kind* of quantity, not a synonym.
- **`/research-philosophy`'s "specificity cannot be measured here"** was stale by two months. The
  control ran 2026-05-22 and confirmed the concern in its strongest form.
- **Glossary nav tooltip** now derived from `terms.ts` (72 terms) rather than the hand-written
  "30+". Removes the defect class, not just the instance.

Build and typecheck clean.

---

## Ponder — what the feedback says about deeper gaps

**The convergent finding is correct and it is one class.** Four personas, four vocabularies, one
defect: propagation break. What's new is the *direction*. Three passes found over-claims; the
researcher found the site's largest error was an **under-claim** — booking two genuinely
discriminating tests as zero. Both are propagation failures. The site has developed a reflex of
correcting itself downward, and that reflex now misfires: when a result comes out *better* than
the standing framing, it doesn't propagate either.

**The seam nobody works.** The site audits itself hard against external data and barely at all
against itself. Today's arithmetic on already-published numbers produced five findings:
γ=2 ⇒ ideal gas; γ=0.49 ⇒ N_corr≈17; a₀ inversion ⇒ H₀≈77.6; ΔBIC−lnN ⇒ Δχ²=−0.84; f_DM ceiling
× Ω_m/Ω_b ⇒ convention-dependent headline. Zero new data, five findings, minutes of arithmetic.

That has a consequence for a standing conclusion. The explorer loop was characterized as a
**monotone closure operator at a fixed point** — all frontier amplitudes calibrated or asserted,
zero derived. But that was measured along the *external*-audit axis. The internal-consistency axis
was never worked, and it is visibly **not** at a fixed point; it's generating findings at a high
rate from no new data. So the "stable fixed point" may be an artifact of having searched one
direction. The program converged where it was looking. Seeded as
`site-audits-itself-against-itself.md`, including a concrete instrument (a relation registry that
mechanically composes every published relation sharing a free variable). `gammaPresets.ts` is the
first narrow instance of it.

**The asymmetry the researcher found is the structurally revealing one, and it's real.** The
program audits its physics harder than it audits its audit. Every physics test gets "underpowered
as registered" without flinching; the methodology sector — the part nominated as the actual
contribution — carried an unstated positive class, a mislabeled interval direction, a two-month
stale disclaimer contradicting its own executed control, and a program-level null stated without
the underpowered badge its own criteria demand. Fixed today, but the pattern is worth watching:
the escape hatch from closed physics is being held to a lower standard than the physics it's
escaping.

**Onboarding (P2) not addressed today, and that's a deliberate deferral.** The enthusiast's
findings are real and well-argued — the beginner path hands you `/honest-assessment` at step 5,
the glossary arrives at step 6, `/equation-walkthrough` is the site's best accessibility asset and
isn't on the path at all. But these are a coherent redesign, not a set of patches, and doing them
piecemeal would produce another layer of append-fixes over a stale spine. Existing topics
(`onboarding-spine-canonical-path.md`, `casual-reader-value-proposition-design.md`) already hold
this. Flagging explicitly that it was deferred rather than missed — it's now been raised by the
enthusiast persona on multiple passes and is the oldest unaddressed item in the queue.

---

## Seeded for the explorer

- `boost-ceiling-definition-sweep.md` (HIGH) — executable, data already local, protocol registered
  and prediction pre-stated
- `vocabulary-test-on-own-flagship-artifact.md` (HIGH) — gates the preprint track; both outcomes
  informative; includes a positive control on the detector itself
- `site-audits-itself-against-itself.md` (MEDIUM-HIGH) — the relation registry, and the question of
  whether the "fixed point" is an artifact of the search direction

## So what?

Two things advanced rather than documented. First, the site now says something *stronger* about
its own results than it did this morning — two discriminating tests recovered from a zero, and a
ΔBIC read as a quantitative statement instead of a rhetorical one. Correcting upward is rarer here
than correcting downward and the machinery wasn't set up for it. Second, the preprint track has a
real gate on it now, placed by an outside reader hitting a hole the program had already named and
not closed — which is a better test of the gate's importance than the program's own judgment was.

The open question I'd put to dp: if internal cross-multiplication keeps yielding at today's rate,
the "converged, package the nulls" conclusion is premature — not because the physics reopened, but
because the audit was only ever run in one direction.
