# Topic: TEST-02 EFE Divergence — Has Anyone Computed It?

## Question

Does the Synchronism framework actually predict a *numerically different* ξ(ρ_env) curve from MOND+EFE? No comparison has been computed. Without this computation, TEST-02 cannot discriminate even in principle.

## Context

Visitor Pass 4 (2026-06-05, leading-edge researcher) raised the sharpest question about TEST-02:

> "For TEST-02 to ever discriminate, you need (a) the wide-binary anomaly to be real, (b) a computed Synchronism–EFE divergence, and (c) that divergence to exceed Gaia precision. Has anyone computed (b)? Without it, TEST-02 isn't a prediction, it's a placeholder."

The current site status is:
- Condition (a): Disputed — Chae 2023 vs Banik 2024 / Pittordis & Sutherland
- Condition (b): **Not computed** — the Synchronism ξ(ρ_env) derivation exists but has never been compared against the MOND+EFE prediction in the same units
- Condition (c): ~80× below Gaia DR3 reach with derived ρ_crit (Session #637); ~33× below with retrofit ρ_crit

## Why It Matters

TEST-02 has been cited as "the highest-priority remaining discriminating test" and "possibly THE novel prediction" for months. If condition (b) shows that C(ρ) and MOND+EFE predict the same ξ(ρ_env) (because both have EFE-like terms at the same scale), TEST-02 is closed without execution.

This is the same closure move that worked for TEST-04a (run the actual comparison, get the answer) and RAR shape (ΔBIC=+184).

## Suggested Starting Points

1. Session #637 in the Synchronism archive — what amplitude is predicted for ξ(ρ_env)?
2. MOND+EFE prediction for the same observable: Bekenstein-Milgrom 1984; Banik+2024 Table 2 (if they compute σ_int(ρ))
3. Compute: what is the Synchronism predicted slope ∂σ/∂log(ρ) vs the MOND+EFE predicted slope? Are they the same sign and magnitude?
4. If divergent: what is the Gaia DR4 expected precision, and does the divergence exceed it?

## Priority

**HIGH** — This closes the only remaining "possibly novel" claim by either confirming EFE-degeneracy (TEST-02 closed) or finding a genuine divergence (TEST-02 becomes an active discriminator). Either outcome is more informative than the current "triple-conditional placeholder" status.

## Expected time

One focused session with archive + Banik+2024 lookup.
