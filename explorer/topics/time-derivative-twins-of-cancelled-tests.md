# Topic: Which "identically null" tests have a time-derivative twin?

## Question
TEST-02's density branch cancels in a same-instant ratio but leaks into Ġ/G (explorer 2026-09-23: LLR excludes 68/74
laws). Enumerate the other places where a statistic cancels the framework's effect (ratios, same-epoch differentials,
common-mode offsets) and ask, for each, whether the rate does not cancel. Also: does covariant Refracted Gravity (G_eff =
G/φ with φ from a field equation) pass LLR? That decides whether "C must be a potential-like field, not a function of
density" is a structural lesson for the framework's C.

## Context
Explorer 09-23 open threads. A candidate twin: the Ω_m(z) floor on the acceleration-keyed C_a. If the floor tracks Ω_m
at the source epoch, a₀-scale physics drifts today. The maintainer memory "check whether the statistic cancels the
effect" is the static half of this lesson. This topic asks for the dynamic half as a method, not a single result.

## Why It Matters
It is a cheap, general instrument: any cancelled test with a live time-derivative twin becomes a test again, against
existing bounds (LLR, pulsar timing, clock comparisons, BBN/CMB Ġ limits).

## Suggested Starting Points
- `explorer/findings/density-keyed-C-is-a-moving-G-lunar-laser-ranging-excludes-it-at-pc-smoothing.md`
- Sanna, Matsakos & Diaferio 2023 (covariant RG): estimate dlnφ/dt ~ (v·∇Φ_gal)/c²-scale.
- TEST-05 (ambient-density lever capped by a tidal identity, 08-21) and the satellite channel: cancelled statically; do they have a rate?
