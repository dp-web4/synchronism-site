# Topic: LIV Preferred-Frame Leak — Does Sidereal Modulation Re-Open a Reachable Target?

## Question
The three-lock argument (PREDICTIONS.md B7/Phase-12) concludes that the discrete-substrate LIV channel is "structurally untestable": the refutable n=1 channel is symmetry-forbidden, the surviving n=2 channel is ~10⁷ below LHAASO reach, and n=2 is non-unique (LQG/causal-sets). But there's a noted potential leak: **the framework posits a preferred frame (absolute time / universal clock), which could generate sidereal/annual modulation of the LIV scale** — a different observable from isotropic time-of-flight.

## The Question
Does the preferred-frame structure of Synchronism generate:
1. **Anisotropic n=2 LIV** (modulation as Earth moves relative to the CMB frame at ~370 km/s), which would produce a quadrupole + dipole annual signature in GRB arrival times?
2. **Effective n=1 from the preferred frame** — if the isotropic dispersion is ω²=m²+2c²(1-cos k) in the lab frame, does boosting to the CMB frame generate a cross-term that effectively looks like n=1 in the CMB frame?

If (2): the phase-12 simulation already shows anisotropic n=2 (∝β), **not** effective n=1. So the boost gives a different kind of anisotropic n=2, not an n=1 term. This would be a different and potentially more constrainable signal.

## Why It Matters
If sidereal modulation gives a reachable target:
- The "structurally untestable" verdict is wrong — the lock leaks
- The framework has one genuinely refutable prediction after all
- The target would be: anisotropic time-of-flight at the ∝(E/E_Pl)²·cos(θ-θ_CMB) level

If not:
- Confirms the three-lock argument is complete
- Allows the for-researchers page to say "this is closed" not "this is a potential leak"

## Suggested Starting Points
- `explorations/2026-06-23-phase12-*.md` — the simulation that shows anisotropic n=2
- `Research/proposals/liv_frontier_symmetry_protected_untestable.md` — the filed proposal
- External: Hughes-Drever experiments, optical cavity bounds — what constrains anisotropic CPT violation?
- External: LHAASO anisotropy analysis — do they look for preferred-direction effects in GRB timing?

## Priority
HIGH — if the preferred-frame leak is real and reachable, it changes the /for-researchers page's central claim from "no testable unique prediction" to "one anisotropic test exists." The current site says this is "uncomputed."
