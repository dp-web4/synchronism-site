# Explorer Session — 2026-07-24

## WAKE

1. **What assumptions am I inheriting?** That the preprint strategy (locality no-go as the
   program's #1 transferable null, gated on dp) is the right frame; that the maintainer's
   HIGH-priority topic (`efe-chae-detection-nogo-crosscheck`) is genuinely the top of the
   queue and not just the newest; that "corroboration from the EFE literature" is a
   strengthening move rather than motivated reasoning.
2. **What if the frame is wrong?** The known failure mode of this loop (memory:
   explorer-loop-monotone-closure) is producing commentary instead of execution. The
   corroboration framing is exactly the kind of thing that *sounds* like an advance while
   being motivated reasoning — the proposal's own pre-stated refutation criterion (step-0
   collinearity) is the guard. Executing the guard is frame-safe either way: if it kills the
   framing, that's a productive dead end; if it passes, the writeup gains a scoped axis.
3. **Highest-information move**: run step-0. It was pre-registered yesterday, gates a
   strategic decision (preprint framing), needs data that turned out to be acquirable
   (Chae Table 2 via arXiv v2 source + in-repo TEST-08 artifacts), and no one had looked.
4. **What would falsify the current posture?** If e_env and ambient density were collinear
   (r² ≥ 0.5), the two-axis narrative dies and the writeup stays refutation-only — exactly
   the branch the pre-declaration commits us to accepting.

Decision: the queue's HIGH topic *was* the highest-value work (rare alignment). Executed it.

## Topic(s) Explored

- `efe-chae-detection-nogo-crosscheck` (→ done/): executed the step-0 collinearity check
  from `locality_nogo_efe_detection_axis_20260724.md`, plus the adversarial read
  (Paranjape & Sheth 2022, Freundlich 2022, Sargent et al. 2025) and the both-ways branch
  verification.

## Key Findings

- `efe-step0-collinearity-e-env-vs-ambient-density.md`: **SEPARABLE — framing survives
  step-0.** Pre-declared primary Pearson r = +0.432 (r² = 0.187 < 0.25 boundary; VIF 1.23;
  N = 141/141 joined) between Chae's erratum-corrected e_env and TEST-08's registered
  distance-corrected density metric. Holds in the low-acceleration subset (r² = 0.19).
  Shared variance 19–27% depending on scale/estimator — separable, not orthogonal.
  - Instrument validated both directions: TEST-08's r² = 0.0001 null reproduces exactly on
    the joined sample; Chae's headline (N=113, median e = 0.052, median e_env = 0.033)
    reproduces exactly from my parse of their Table 2.
  - **Load-bearing surprise**: the acceleration-vs-density contrast is *estimator*-dependent
    — under the site's whole-galaxy offset estimator, NEITHER variable shows signal
    (e_env: r = −0.11 ns; density: r = +0.01). Chae's 5σ lives in low-acceleration weighting
    and is a mean-level agreement; per-galaxy r(e, e_env) ≈ 0. The writeup may NOT ship the
    unscoped "acceleration wins, density loses" sentence.
  - Both-ways verification complete: real-EFE branch → acceleration (non-local); ΛCDM-mimicry
    branch (P&S 2022) → halo concentration via assembly bias (also non-local); inconclusive
    branch (Sargent 2025) → density null stands as the only executed registered test.
    **No branch rescues local-density coupling.**
  - Bonus: sign of r(e_env, RAR offset) is negative (EFE-directional), opposite to
    Synchronism's registered void-high prediction — both environment arms point
    anti-Synchronism.
  - Erratum trap documented: the widely-quoted golden-galaxy e_env values (0.094/0.102) are
    pre-erratum; corrected are 0.040/0.050. Corrected Table 2 archived in-repo
    (`explorer/data/chae2020_ms_r2.tex`).

## NotebookLM Usage

- Not used (auth known-expired per 07-23 log; not needed — single-paper data extraction via
  arXiv source tarball + curl worked).

## Rabbit Holes (Worth Revisiting)

- **P&S sign discriminator on the site's own instrument**: a low-acceleration-weighted
  per-galaxy offset (g_bar < g†/10 points only) over the existing TEST-08 pipeline would test
  whether the EFE sign signal has power where the whole-galaxy mean doesn't. Pre-declare
  before running. This is MOND-vs-ΛCDM literature work, not a Synchronism test.
- Chae 2021 Paper II (ApJ 921, 104) — e_env↔LSS provenance refinement for the writeup.

## Self-Seeded Questions

- The Spearman/Pearson gap (0.54 vs 0.43) on the primary implies a nonlinear e_env–density
  relation — does it saturate at cluster-scale counts, and does that matter for any
  estimator that would try to use both?
- Had the pre-declaration chosen Spearman as primary, the verdict would have been GRAY
  (rank-r² 0.29). The threshold-sensitivity is disclosed in the finding — is there a
  principled way to pre-choose the statistic when the predictor is log-range? (Protocol
  seed for `test-preregistration-protocol`.)

## So What?

Discovery, not documentation: a pre-registered refutation criterion was executed the day
after it was registered, on data nobody had joined before (Chae's erratum-corrected table ×
the site's own registered environment run), and it returned a verdict that *changes what the
preprint may claim* — the EFE axis survives but only in scoped form, and the unscoped version
that was drafted in the proposal is now demonstrably overclaimed (estimator-dependence).
The one-day register→execute turnaround is the pattern the execution-gap memory says this
loop lacks; today it closed.
