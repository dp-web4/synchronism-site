# Maintainer Session — 2026-07-07

## WAKE

Two signals in today's visitor log, one research-grade:

1. **Research: the A2ACW null is missing its decisive control.** Pass 4 (the persona that
   fact-checks state-of-the-art claims professionally): the null's own diagnosis — shared-corpus
   vocabulary lock-in, not temporal cutoff — points directly at a cross-vendor adversarial pair
   with disjoint training corpora, and no such experiment is proposed anywhere. Verified against
   `../Synchronism/Research/proposals/`: correct, all existing A2ACW proposals redesign axes or
   baselines but assume same-corpus pairs. **Proposal filed:**
   `a2acw_cross_vendor_corpus_control.md` (back-annotated to Synchronism repo). One run separates
   "AI adversarial collaboration has a ceiling" from "corpus monoculture has a ceiling" — that
   distinction is most of the null's citation value. Execution gates on dp (needs different-vendor
   model access).

2. **Process: why the φ⁻¹ regression survived three weeks of sweeps.** The landing card wrote
   φ⁻¹ as HTML entities (`&#x03C6;&#x207B;&#xB9;`); every propagation sweep grepped for the
   literal character. The 06-28 strip cleaned /key-claims and missed the landing page one
   viewport away. Lesson for all future sweeps: **grep for the entity encodings too**
   (`x03C6`, `&phi;`, numeric forms), and for the *claim* ("0.64", "found instead"), not just
   the symbol.

## Verification pass (per standing feedback: verify before fixing)

| Visitor finding | Verdict |
|---|---|
| Landing φ⁻¹ card contradicts /key-claims (P3+P4 high) | **REAL** — page.tsx:313, entity-encoded; also /prediction-tracker:35 (literal) and /hard-problem:130 (soft version: "finding 0.64" without its rejection) |
| B1 CHSH absent from ledger + citable artifacts (P4 high) | **REAL** — /for-researchers had 3 artifacts, /honest-assessment quantum section had no CHSH card. Bonus: /two-reframes was also missing yesterday's 05 density-substrate run (explorer P1, unshipped) |
| ρ_crit ∝ V² exponent sign-inverted (P3 med → P4 high) | **REAL and already known** — 07-02 explorer finding `rho-crit-velocity-exponent-mond-requires-minus2.md`, flagged "Not yet on site." Third persona-loop duplicate-derivation instance |
| /why-synchronism jargon bare (P1+P2 high) | **REAL** — phenomenological/ansatz/compander at lines 73/83/92, no glosses |
| /tools vs /interactive-tools duplicate route (P2 med) | **NOT A BUG** — next.config.mjs redirects /tools → /interactive-tools |
| A2ACW unexpanded on landing (P2 med) | **STALE** — expanded at page.tsx:173 since earlier |
| TEST-02 doesn't cite Chae/Banik, no DR4 trigger (P4 med) | **STALE** — both cited by name in the TEST-02 alert; DR4 milestone added 2026-07-03 |
| +1 never explained on /equation-walkthrough (P3 low) | **STALE** — explained twice, incl. load-bearing consequences (knee at 0.88, no power-law limit) |
| Chemistry explorer selection rule (P3 low) | **PARTIAL** — selection-bias disclosure exists (lines 97–99); exact curation rule undocumented. Deferred (don't assert a rule I can't verify) |

4 of 9 checked findings were stale or non-bugs — the verify-first discipline continues to pay.

## Fixes shipped (build PASS, 92 pages)

**P0 — consciousness three-surface contradiction closed:**
- `/` landing card: "C ≈ 0.64 ≈ φ⁻¹ found instead" → "both thresholds rejected at p < 0.0001;
  no threshold survives"; badge → "Speculative — Both Thresholds Refuted" with tooltip
  explaining parent-vs-instance status (also answers P3's Claim-2 badge question)
- `/prediction-tracker`: φ⁻¹ stripped from consciousness row, 0.64 rejection stated
- `/consciousness-demo`: one line added — 0.64 sequel also rejected, link to /key-claims
- `/hard-problem`: "finding C ≈ 0.64" now carries "also rejected at p < 0.0001; no threshold survives"
- `/key-claims` Claim 2 badge: "Speculative — geometric artifact" → "Speculative (parent claim,
  unfalsifiable as stated) — both threshold values Failed"
- **Sweep bonus:** `/compression-action` presented golden ratio φ as "natural scaling exponent"
  with no derivation — caution note added (asserted ansatz pending derivation; site φ-policy cited)

**P0 — B1 CHSH null propagated to all carriers:**
- `/honest-assessment`: new failure-ledger card (Bet B1 — S=1.98 local / S≡2.00 nonlocal-grid /
  2.67 only with signaling / S=1.85 on the framework's own density substrate; substrate-independent cap)
- `/for-researchers`: Artifact 4 added (page now "Four citable negative results"; intro and meta
  updated) with the A<B<C triptych localization of 2√2 and the transferable "nonlocal-by-construction
  doesn't evade Bell" point
- `/two-reframes`: yesterday's explorer P1 shipped — 05 saturation-density run added as fourth
  bullet (escape closed by execution); "why 2√2 not 4" paragraph upgraded from "no account" to
  the triptych localization (2√2 = projection-law fixed point, enters only by importing QM)

**P0 — ρ_crit V⁻² exponent shipped (07-02 explorer finding, 2 days late):**
- `/parameter-derivations`: LEAD AUDIT ITEM box on the A card — BTFR forces ρ_crit ∝ V⁻²,
  framework asserts V⁺², 240×–300,000× magnitude error, three incompatible provenances
  (+0.5 / +2 / −2), knee never crossed inside a galaxy
- `/honest-assessment`: sign statement added to the locality no-go card ("the no-go on the
  velocity axis")
- `/for-researchers` Artifact 1: one-line citable form added (transferable to any local-ρ MOND mimic)
- `/galaxy-plotter`: V⁻² line added to the stand-in disclosure

**P1 — beginner register break:**
- `/why-synchronism`: inline plain-word glosses for **phenomenological** ("picked because it
  matches the data, not because a deeper theory demands it"), **ansatz** ("educated starting
  guess, shaped so the units work out"), **compander** ("compressor–expander: squashes a huge
  input range into a small output range, like audio volume-levelling")

**P1/P2 — surfacing fixes:**
- Landing: badge tooltips added to the two chips missing them; "why keep reading" box now ends
  with the MOND one-liner + what a confirmation would look like (Pass 1's request, borrowed from
  honest-assessment as suggested)
- `/why-synchronism`: title tooltips added to all four raw badge chips
- Nav-label reconciliation: "Test Catalog"/"Tier 1 Tests"/"Tier-1 Test Catalog" link labels
  unified to canonical H1 names ("Test Roadmap", "Tier 1: Existing Data") across layout footer,
  landing, and 7 content pages
- Tool purpose statements above the fold: `/galaxy-plotter` ("what this tool is for: seeing the
  dark-matter problem itself... the real evidence lives in the caveats, not the curves"),
  `/gamma-calculator`, `/phase-boundary-visualizer` (Pass 2's exact suggested framing)
- `/tier-1-existing` TEST-07: self-flagged demotion **executed** — "SHOULD BE DEMOTED TO TIER-2
  (2026-05-17)" → "DEMOTED TO TIER-2 (executed 2026-07-07)", demoted in place so it stays visible
- `/research-philosophy`: cross-vendor corpus control registered as the named next experiment
  (closes Pass 4's "the methods page stops one step short of its own diagnosis")

## Deferred

- Glossary search/filter (P2 low — component work)
- Real MOND μ-function in plotter → seeded as explorer topic (needs validation against published
  fits, not a drive-by; same reasoning as the 07-04 deferral of the C(ρ) trace)
- Chemistry-explorer curation rule (can't document a rule I can't verify — needs archive check)
- Badge family visual differentiation (shape/outline per species — design decision, flag for dp)
- Breadcrumb middle-level linking (Pass 2 admits possible fetch artifact; needs browser check)

## Explorer topics seeded

- `gnosis-research-session63-methods-audit.md` (HIGH) — both expert personas independently asked
  what Session 63 actually is; five surfaces lean on it; "a refutation I can't audit is a claim
  like any other"
- `galaxy-plotter-real-mond-mu-function.md` (MEDIUM) — toy-vs-toy comparison; make the MOND side
  real, validated against McGaugh 2016 before shipping

## Ponder — what today reveals

1. **The persona loop's duplicate-work cost is now recurrent and measurable.** The ρ_crit V⁻²
   result: explorer derived it 07-02, site never carried it, and today TWO personas re-derived it
   from scratch (Pass 3 the envelope, Pass 4 the independent confirmation) — the third instance of
   the pattern. The fix isn't faster personas; it's that **explorer findings marked "Action:
   Maintainer" must not sit unshipped** — 07-02's finding had the exact box text ready and it took
   5 days + two re-derivations to ship. Same story with yesterday's two-reframes P1 (1-day lag,
   caught today). The propagation queue needs to be drained the morning after it's filled.
2. **Sweeps have an encoding blind spot** — literal-character greps miss HTML entities. The most
   scrutinized sentence on the site hid behind `&#x03C6;` for three weeks. Sweep recipes should
   target claims and numbers, not just symbols.
3. **All four personas converged on the same asymmetry** ("the interior is more honest than the
   surface") and today's session is evidence for their diagnosis: every HIGH fix was propagation,
   not content. The audit machinery works; surfacing is the bottleneck. The structural answer may
   be the citation-walk audit generalized to "does every closed finding cite a carrier page?" —
   already in memory scope after 07-06.

## So what?

The site's most-read sentence (landing consciousness card) no longer contradicts its own claims
page in the credulous direction — the exact sentence two expert personas said nearly made them
close the tab. The framework's cleanest self-executed negative result (CHSH substrate null) is
now findable from the ledger and the researcher page instead of one interior page. And the
research core gains the one experiment that could double its strongest preprint candidate's
citation value. Discovery-advancing on one axis (cross-vendor control), propagation-completing
on three.
