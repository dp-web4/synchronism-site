# Explorer Session — 2026-08-24

## WAKE

**1. What assumptions am I inheriting from the primer and topic queue?**

That the 68-item topic queue holds the highest-value work; that the maintainer will act on what I
find; that the persona loop is surfacing new problems. All three are questionable today. The
maintainer has now failed **11 consecutive days** (401 OAuth, `maintainer/logs/2026-08-24-0600.log`),
so nothing the explorer produced since 08-13 has reached the site. Most of the queue is
site-hygiene work that belongs to a track that isn't running.

**2. What if the current research frame is wrong?**

The frame is "audit the site's claims against its own archive and the literature." My memory index
holds ~40 closures and the corpus is saturated — my first three candidate investigations today
(quadrature-branch A-fork, SAGA/ELVES matched-pair discriminator, EFE=0 dSph channel) were all
already closed, two of them within the last 72 hours. The explorer loop is a monotone closure
operator and it has been approaching a fixed point.

The instinct I want on the record: **the site now has more meta-content (audits of its claims) than
object-content (claims), and I have personally contributed dozens of layers.** Pass 1's "am I reading
a theory or an obituary for one" and Pass 4's "this site over-refutes itself" are the same
observation from opposite ends of the readership.

**3. What experiment or investigation would most increase information right now?**

Today's visitor log ended with a note out of persona that named **one grep** that would decide two of
its own high-severity items in opposite directions — and identified a branch of the force-law fork
this program had **never enumerated** (an *implicit* C, keyed to `g_obs`). My memory had that exact
grep flagged as an OPEN P0. That was the highest-information action available and it cost one command.

**4. What would falsify the current research posture?**

If the persona loop's items turn out to be mostly rediscovery *because the site is stale* rather than
because the loop is exhausted, then re-tasking the loop is wrong and the propagation channel is the
only real bottleneck. Today's evidence: Pass 4's single best item was **not** rediscovery, and it was
right about the branch and wrong about which horn fired. So the loop is still productive — when the
persona states its own contingency.

**Decision**: abandoned the topic queue for the grep. It opened into the session's whole result.

---

## FOCUS — what was done

Ran the grep. Answer: **C's argument in every fit artifact is `g_obs/a₀`** — the implicit branch, in
4 files across site and archive. Then followed it, and it did not stop where I expected.

**Finding**: `findings/the-argument-of-C-three-functions-each-killed-by-its-own-distinguishing-feature.md`
**Scripts**: `findings/scripts/two_pillars_{argument_of_C,head_to_head_fit,neff_and_epsilon}.py` (+ outputs)
**Back-annotated**: `Synchronism/Research/proposals/argument_of_C_three_functions_ledger_not_commensurable_20260824.md`
**Topic archived**: `two-galaxy-laws-never-reconciled.md` → `done/`

### Result

The galaxy sector runs **three different coherence functions**, each carrying a *different* claim to
being the framework's structural difference from MOND — and **each is refuted by exactly the feature
that constitutes its claim.**

| | C_ρ (headline) | C_g (the numbers) | C_Ω (the ceiling) |
|---|---|---|---|
| novelty feature | EFE = 0 | return exponent q = 2γ | bounded boost B ≤ 1/Ω_m |
| killed by | that feature | that feature | that feature |
| ledger entries | #4 | #3, #5 | #1, #2 |

**Largest coherent sub-ledger: 2, not 6.**

Executed:

- **Only the implicit branch makes a galaxy rotate.** Deep-limit slopes measured: explicit +0.0014
  (⇒ `v ∝ √r`, never flat), implicit +0.5091 (⇒ MOND √ law, flat). **The implicitness that saves the
  rotation curve is the field-dependence that kills EFE = 0 — one property, not two.**
- **At γ = ½ the framework's EFE is MOND's identically**: `tanh(½ln(1+x)) = μ_simple(x/2)`, so the
  EFE response factor matches to **2.2×10⁻¹⁶** over six decades. The identity was known for the *fit*
  since 08-02; extending it to the EFE removes the last structural difference.
- **The a₀ factor-2 anomaly dissolves.** The identity forces `a₀ = g†/2 = 6.0×10⁻¹¹`; my own
  head-to-head fit returns **6.06×10⁻¹¹** (1.0%). Not a measurement — a reparametrization read back
  out of the fit.
- **The headline equation has now been fitted head-to-head** — first time. ΔBIC **+2843** (global
  ρ_crit) / **+3309** (the framework's own `ρ_crit = A·V_flat²`), sign-stable across 6 (h, ϒ)
  conventions. σ_int 0.23–0.25 dex vs the RAR's 0.13 dex *total*. **γ_ρ fits to 0.04** — the
  likelihood switches the density dependence off, independently reproducing the 08-19 "≤0.7% of
  variance" result by a different route. Pass 4's "should read *untested*, not *failed*" is answered:
  it is tested, and it loses.
- **Applied my own N_eff guardrail to my own kill**: deflated +142/+166, against the site's headline
  +184 → +11.5. The comparison the site never ran is the one that survives its own deflation.
- **ε = 2γ−1 is the only deformation, and SPARC does not constrain it.** Galaxy-level bootstrap:
  γ = 0.545 ± 0.129 ⇒ **ε = +0.090 ± 0.258 (0.35σ)**. The site's γ = 0.489 ± 0.02 is this interval
  inflated ~4.5× by treating 20 points/galaxy as independent. Same ε as the DE sector's.

### Two persona items resolved in opposite directions

- Pass 4's **P0 item 2** ("the RAR fit ran on `g_bar/a₀`, so refutation #3 is about a different
  model") — **REFUTED**. The fit is implicit in `g_obs`.
- Pass 3's **two separate `high` items** (ceiling derived backwards; plotter exceeds its own ceiling
  300×) — **one answer, not two fixes**. The premise `C ≥ Ω_m` is not unstated; it is
  `np.clip(C, Omega_m, 1.0)` inside C_Ω, the one function that never appears on the site. The
  "300× contradiction" is two different functions.

### Self-correction, same session

My first draft framed this as **two** functions and attributed refutations #1/#2 to C_ρ. The topic
queue (`two-galaxy-laws-never-reconciled.md`) and this program's own
`three-C-problem-existential-ambiguity.md` (2026-03-20) pointed at the third, and
`/tier-1-existing:122` states outright that TEST-09 is computed from it. Superseded stub left in
place. **The topic queue caught my error — after I had decided the queue held nothing worth doing.**
Worth remembering.

### Scope narrowing on yesterday's work

08-23's Crater II verdict (4.7σ short) uses the ceiling (C_Ω) *and* contrasts against MOND+EFE on the
grounds of EFE = 0 (C_ρ). It is a two-function verdict. Under C_g it is McGaugh's own prediction:
0.6σ, consistent. **Not withdrawn** — it correctly kills C_Ω — but it cannot be quoted as "the
framework fails Crater II" without naming which C.

---

## So what?

**Discovery, not documentation.** The honest galaxy-sector statement is a trichotomy, not a count:

> **C_ρ is novel and cannot fit galaxies. C_Ω is bounded and dies of its boundedness. C_g fits
> galaxies and is MOND.**

This closes the force-law fork — the site's oldest open structural question, open since 2026-06-07 —
not by choosing a law but by pricing each one. It also does what Pass 4 asked for (a recount) in a
stronger form: the entries aren't merely mis-weighted, they're **not commensurable**.

**What it does not do**: rescue anything. C_g being MOND is not a success; MOND-simple is excluded by
Cassini at +17.95σ, and that constraint is Desmond, Hees & Famaey 2024's. The site's account of its
own state is wrong **in both directions at once** — over-refuting C_g and C_Ω, under-refuting C_ρ.

**The methodological datum.** The multiplicity was found on **2026-03-20** and never reached the
site. Five months. Today's persona passes rediscovered its symptoms twice, independently, as fresh
`high` items. That is the propagation failure measured in months, and it is the same escalation that
has been open to dp since 08-19 — now **day 11** of maintainer downtime.

**Correction to my own WAKE.** I concluded the topic queue held nothing of value and then was saved
from a wrong taxonomy by a topic in it. The queue is not exhausted; it is *unread*, which is a
different problem with a different fix.

---

## → dp / supervisor

**Re-auth remains the single highest-value action**, and the case is now quantitative: today's
finding touches `/dark-matter`, `/mond-unification`, `/honest-assessment`, `/key-claims`,
`/galaxy-plotter`, `/tier-1-existing` and `/coherence-explorer`, and none of it can land. The 08-19
escalation still stands and is still dp's call: **may the explorer apply verbatim-specified
maintainer fixes after N days of downtime?** The explorer has again declined to route around
`explorer/CLAUDE.md` unilaterally.

**Housekeeping unchanged**: `AGENTS.md`/`CLAUDE.md` carry uncommitted supervisor gitnexus updates —
not explorer work, left alone. NotebookLM `status` still reports a false positive (verifies cookie
presence; token-fetch check is `⊘ skipped` without `--test`) — a check that cannot fail, which is the
defect this site documents in its own test catalog.
