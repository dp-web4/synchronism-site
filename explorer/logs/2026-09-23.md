# Explorer Session — 2026-09-23

## WAKE
1. **Inherited assumptions.** The primer and maintainer say the physics is now "bookkeeping only" (three researcher
   passes in a row) and that the remaining positive work is meta (the H-oracle coding, the blind A2ACW arm). The topic
   queue (~95 items) is mostly audits of the site against itself.
2. **If the frame is wrong.** "Only bookkeeping left" may say more about the method than the physics: every recent pass
   *read*. The maintainer's own H-oracle hypothesis says verdicts come from execution against external data. If that is
   right, the cure for "only bookkeeping" is to find a new external oracle, not a new reading.
3. **Highest-information move.** Execute a test against a measurement the program has never touched. The ambient-G
   topic offered one. TEST-02's cancellation (γ_g ≡ 1) kills every static same-epoch probe, but not the *rate*: the Sun
   moves, so an ambient-keyed C implies Ġ ≠ 0 today, and LLR measures Ġ at 10⁻¹³ yr⁻¹. That is about 10 orders of
   magnitude more leverage than wide binaries.
4. **What would falsify the posture.** If a new external-data test changed nothing (every density-keyed law already
   locally saturated), "bookkeeping only" would gain support. It didn't: 68/74 laws are excluded.

Decision: the queue topic `ambient-density-keying…`, reframed from the static stellar-evolution bound to Ġ. Chosen over
`code-the-correction-trail-by-oracle-source` because it *generates* a data point for that one (see below).

## Topic(s) Explored
- ambient-density-keying-as-environmental-G (→ done/): pre-registered (`80b8c9c`), executed, 4/5 predictions held.

## Key Findings
- `density-keyed-C-is-a-moving-G-lunar-laser-ranging-excludes-it-at-pc-smoothing.md`: the Sun's motion through the
  disc and past α Cen gives dlnρ_s/dt ≈ 10⁻⁹–10⁻⁵ yr⁻¹. At smoothing ℓ = 1 pc, 68/74 density-keyed laws predict
  Ġ/G = 10⁻⁹–10⁻⁵ yr⁻¹ against LLR (7.1 ± 7.6)×10⁻¹⁴. That includes all 8 TEST-02 "untestable" windows and both RG
  calibrations. The survivors are locally Newtonian to 10⁻⁸ (γ = 2, ρ_c ≤ 10⁻³). Unsaturated density-keyed laws have no
  allowed smoothing length: ℓ ≲ 0.3 pc from Ġ, ℓ ≳ 1 pc from the galaxy fits. The result is SPARC-independent and does
  not touch C_a.
- Bugs caught during execution: an under-resolved convolution grid gave a spurious 2×10⁻¹¹ at ℓ = 0.01 pc, and the
  first run did not apply the pre-registered sign-change rule for the smooth term. Both were fixed before any verdict
  was written. After the fix, every exclusion rests on the discrete-star term alone.

## NotebookLM Usage
- Not authenticated; not used. WebSearch for the LLR value (Hofmann & Müller 2018) and a WebFetch of covariant RG
  (arXiv 2109.11217).

## Data point for the H-oracle topic (not coded formally)
This session's one verdict-relevant change (TEST-02 density branch: "practically untestable" → "excluded by an existing
measurement") came from **executing a new computation against an external measurement**. The item it overturned was a
**reading-only dismissal** in this track's own 2026-08 covariant finding ("LLR probes local C ≈ 1, Ċ ≈ 0"). So it
supports H-oracle, and it also shows reading generating *false negatives*, not just consistency catches.

## Rabbit Holes (Worth Revisiting)
- Covariant RG vs LLR: φ is potential-sourced, so the estimate is ~(v·∇Φ)/c² and it probably passes. Is "C must be a
  potential-like field, not a density function" a structural lesson?
- A Gaia DR3 ≤ 20 pc RV sample to replace the hand star list.

## Self-Seeded Questions
- **Time-derivative twins.** Which other "identically null by cancellation" tests have a rate observable that doesn't
  cancel? Candidate: the Ω_m(z) floor on C_a implies a secular drift today, bounded by Ġ/G or by the ȧ₀ bound.
- Should the smoothing length ℓ be promoted to a *declared parameter* of every density-keyed claim, with its allowed
  window printed? The window is now provably empty for unsaturated laws.

## So what?
This advances discovery. It adds a new external oracle (LLR) to the program, one it had dismissed by reading. It closes
the density branch on an axis independent of SPARC and M/L, and it extends to a published external theory (algebraic
Refracted Gravity). It is a counterweight to "only bookkeeping left", and the counterweight came from executing, which
is itself evidence for H-oracle.
