# Maintainer Session — 2026-09-19

## WAKE

Yes, two research-level things, and one of them was runnable, so it was run rather than proposed.

1. **The researcher persona attacked the one transferable result on the site** (the globular-cluster knee
   window): Baumgardt & Hilker masses are Newtonian N-body fits to the same σ(r), so every alternative is graded
   against a Newton-tuned model. Reading the explorer's script showed the mechanism is real in form — the outer
   slope is mass-independent under Newton *only*, and the run used catalogue M with no free scale. Its size was
   unknown. Pre-registered (`d9d7beb`, before the script existed) and executed.
2. **The audit has been applied by traffic, not by claim.** The grad-student persona could not dent the flagship
   galaxy pages and then found four short pages still in the pre-audit voice, one with an outright wrong step.
   Coverage of the audit over *claims* has never been counted.

Proposal written before fixing friction:
`Synchronism/Research/proposals/gc_window_newton_conditioning_control_and_the_unaudited_side_pages_20260919.md`.

## Feedback Reviewed

- **Visitor log 2026-09-19** — four personas. Enthusiast: Beginner Path repeats equation + autopsy three times and
  teaches γ = 2/√N_corr one step after killing it. Tech writer: outcome vocabulary unmapped; history interleaved
  with state. Grad student: /mond-unification box yields cH₀/2 not cH₀/2π; /born-rule misuses Gleason;
  φ exponent badged two ways. Researcher: TEST-26 has no numeral; GC window Newton-conditioned; landscape table
  says MOND has "no governing equation" and omits Refracted Gravity.
- **Required reading**: `SPINE.md` and `PREDICTIONS.md` in full. One site sentence contradicted them: the landing's
  "novel bets no instrument has yet been pointed at" vs B7 (dim-4 LIV: refuted at natural value, open
  custodial-mechanism gap). Fixed, keeping both clauses (mostly untested; one commitment *has* been looked at).
- **Explorer 2026-09-18** (kill-criterion form audit): three P0s, all done below.
- Forum: nothing new.

## The Executed Result

`maintainer/scripts/gc_window_newton_conditioning_control.py` (+ `_PREREG.md`, `_output.txt`). Reuses the
explorer's solver and laws unchanged. **Identity control passed first** (−0.057 / −0.093 / −0.245 / −0.211, N = 42).

| King, 42 clusters | catalogue M | mass refit under each law | ratio to Newton | ⟨f⟩ |
|---|---|---|---|---|
| Newtonian | −0.057 | −0.057 (unchanged to 4 d.p. — internal control) | 1 | 1.36 |
| MOND + EFE | −0.093 | −0.094 | 1.65 → 1.67 | 1.18 |
| MOND, EFE off | −0.245 | −0.241 | 4.33 → 4.26 | 1.00 |
| density law, knee 0.161 | −0.211 | −0.195 | **3.73 → 3.45 (−7 %)** | 1.28 |

Pre-fixed rule R ≥ 3 ⇒ **the "manufactured exclusion" reading is REFUTED** at the mass-scale level. Why: the inner
bins sit far above the knee, where every law is Newtonian, and they pin the mass.

**Predictions: 3 of 5 held.** Missed (2): I expected density-law f ∈ 0.35–0.8; it is 1.28, and Newton's own is
1.36 — the analytic King profile under-predicts σ with catalogue M even under Newton. Missed (5): under Plummer
the *ratio* moved 9.45× → 5.13× while the residual moved −0.199 → −0.196.

**The second miss is the finding.** Plummer's Newtonian residual is +0.021 ± 0.027, consistent with zero, so
"× the Newtonian residual" is unstable exactly where Newton fits well. The site and the ledger quote the window as
"3.7–4.4×". The right statistic is the residual with its error (density −0.211 ± 0.027, stat only). The pages said
"no σ"; the script had printed one since 09-07. What has no number is the *systematic* budget.

Second day running that a careful persona's refutation-side claim failed on execution while exposing a real site
defect next to it. Same shape as yesterday: the reader's inference was wrong, the sentence that invited it was ours.

## Changes Made (Site)

- `mond-unification/page.tsx` — "Derivation Chain" → "Dimensional-Analysis Chain (not a derivation)". Step 2 now
  carries out the computation it used to describe: g = (4π/3)Gρ_crit·(c/H₀) = **cH₀/2**, a factor π above Step 3
  (verified by hand). Comparison card: which fits used McGaugh ν vs the compander → μ_simple.
- `born-rule/page.tsx` — new "Where the Three Steps Fail" (Gleason's premises are non-contextuality + additivity;
  dim ≥ 3 vs the qubit example; Step 2 assumes the conclusion; the conserved quantity is normalization and has no
  stated map to C(ρ)). "What This Adds" rewritten to match the badge; inverted Noether analogy and "speedup =
  parallelism" removed. Added the open question that would make it more than a relabel: non-contextual
  probabilities without non-contextual values (the framework's CRT model is KS-excluded on the value side).
- `compression-action/page.tsx` — badge Speculative → **Audited-Negative** (harder, matching
  /parameter-derivations). ξ(ρ) is undefined; the forced equivalence ξ^{1/φ} = [(1+x)^{2γ}−1]/2 holds for any
  exponent (verified), so 1/φ carries no content; floor and "action" mismatches stated.
- `galaxy-rotation/page.tsx` — badge Untested → **Failed + Reparametrization** (the plotter's pair); TEST-03's
  untested status kept as a sub-note.
- `honest-assessment/page.tsx` — landscape table: MOND row corrected (AQUAL 1984, QUMOND 2010); **Refracted
  Gravity added as first row**; superfluid DM added; Verlinde updated to Brouwer+2021; "all share a₀ ≈ cH₀/2π"
  corrected; re-stamped. GC section: today's control. Parallel-Paths definition de-circularised.
- `for-researchers/page.tsx` — γ = ½ double degeneracy promoted to the top *as a deflation, not a concordance*
  (ledger's no-power caveat carried: ±0.11 stat, 0.27–0.96 ϒ band). Artifact 5: item (vi), the control.
- `page.tsx` (landing) — "no instrument has yet been pointed at" reconciled with artifact 2; one-line bridge under
  the scoreboard on why 0 prospective and 6 refutations coexist.
- `first-encounter/page.tsx` — γ card now "the original idea, and what happened to it", verdict in the same card.
- `lib/terms.ts` — new entries: Prospective, Retrodiction, Outcome words (the refuted/killed/Failed/
  Audited-Negative/withdrawn/unrunnable/underpowered map). **Existing Kill criterion entry corrected** — it called
  a kill criterion "a numerical threshold" (11 of 26 have no numeral; its own example was one of them) and the set
  "the framework's most important methodological contribution".
- **Explorer P0s**: the unqualified "every prediction has a kill criterion" replaced with the measured form on
  test-catalog, research-philosophy, quantum-predictions, falsifiability meta — **plus two surfaces the explorer's
  list missed, found by the new lint rule** (`navigation.ts:500`, the /falsifiability blockquote).
  "/falsifiability: 4 criteria cannot fire" given its denominator (4 of 8 examples) and the by-construction class
  (11–15 of 26); specificity bullet added beside timing. TEST-15 restored to the registered 50+ / 3σ, with
  reachability.
- Small: tier-1 stray `/test-roadmap` link; cdm-discrimination "years" → "months".
- `maintainer/tools/site_lint.py` — **9 new rules**, positive-controlled: 14 hits on HEAD, 0 on the working tree.
- `next build`: passes, 93/93 pages. (First attempt failed on a duplicate glossary key — which is how I learned the
  persona's "kill criterion is undefined" was a fetch artifact pointing at a real defect, the stale entry above.)

## Back-Annotations (Research Repo)

- `PREDICTIONS.md` — new block: control executed, refuted at mass-scale level; the "3.7–4.4×" ratio is unstable,
  read residuals ± 0.027; the cH₀/2 computation. **No bucket moves, count 6, Bucket 0 = 0.**
- `Research/proposals/gc_window_newton_conditioning_control_and_the_unaudited_side_pages_20260919.md` — the run;
  the four unaudited pages; TEST-26 minimum content (gated on dp); the methodology-as-output frame sentence.
- **Flagged, not done:** archive session docs that attribute the 2π to horizon geometry (S217/S201/S380 per the
  07-05 citation walk) need the cH₀/2 note.

## Topics Seeded for Explorer

- `which-demotions-survive-a-worthless-auditor.md` — row-by-row partition: checkable identity/computation vs
  auditor judgement. Answers the enthusiast's "can I trust the failures either?"
- `eta-superconductivity-607K-provenance-and-is-eta-really-AG.md` — inputs for 607 K are nowhere; "η ≡ AG" is
  too strong (AG is a digamma suppression); one of the Bucket 2 / Bucket 3 rows is mis-stated.
- `a0-of-z-against-high-z-btfr-zero-points.md` — existing-data handle on the one non-MOND commitment. Carries a
  **do-not-cite** warning on the persona's from-memory result.

## Open Items (Deferred)

- **Beginner Path steps 1–3 rebuild** (enthusiast, high ×3). Only the Step 2 formula framing was fixed. "One job
  per step, meet the suspect before the autopsy" is right and is a content rewrite of three pages, not an edit.
  It also bears on the Questions-First culture: the path currently leads with verdicts. Next session's first item,
  ahead of the breadcrumbs.
- **Separate current-state text from change-log text, site-wide** (tech writer high; root cause named by three
  personas). Every new passage today follows the rule (body = now, history in a `<details>` fold). Retrofitting
  /tier-1-existing, /galaxy-plotter, /for-researchers is large. Splitting /honest-assessment (118k chars; a reader
  tool reported its last third as absent) is part of this.
- **Breadcrumb section crumbs unclickable; three IA schemes** — second day deferred.
- **/a2acw**: models and date ranges unnamed; J = 0 absent from HA's own A2ACW section. Needs archive lookup.
- **/superconductivity** — waits on the η topic. **/chemistry-correlation-explorer** γ-assignment fork ("a grep").
- **Tier-1 tile count 10/11/12; TEST-26's tier; /for-researchers TOC; bare acronyms; /dark-matter opening.**
- **TEST-26 numerals** and the **refutation recount** (bare "6" in footers): gated on dp.
- **r_c in the King-like GC model is also N-body-derived** — the control closed the mass-scale route only.

## So What?

A registered control ran and came back against the critic, and my own prediction about it failed in a way that
exposed the weak part: not the window, the *statistic* the window is quoted in. That is a better outcome than
either "the critique was right" or "the window is fine". On the site side, the honest summary is that the careful
reader is now more useful off the main path than on it: the flagship pages held, and everything wrong today was
on a page nobody had re-derived since May. The audit's coverage has no denominator. That is the next thing worth
counting.
