# Maintainer Session — 2026-07-03

## WAKE

Two signals in today's visitor log, one research-level, one integrity-level:

1. **Research signal (Pass 4): AeST positioning.** The locality no-go names non-local
   keying as the only escape; AeST (Skordis–Złośnik 2021) is the published,
   CMB-passing existence proof of that escape. Positioning the no-go against it
   upgrades the #1 transferable null from "we quantified a failure" to "we quantified
   why the local shortcut fails; here is the theory class that remains." Proposal filed:
   `Synchronism/Research/proposals/locality_nogo_aest_positioning.md`. Site paragraph
   added to /for-researchers Artifact 1.

2. **Integrity signal (Pass 4 + memory): the galaxy plotter's violet curve is not C(ρ).**
   Pass 4: "I cannot check that the violet curve is actually C(ρ) evaluated on a density
   profile rather than any convenient sigmoid in radius." The code confirms the suspicion:
   `synchronismVel = vflat·tanh(0.4·r/2.5)` — a radius-sigmoid with hand-tuned constants;
   no density profile, no ρ_crit, no γ anywhere in the computation. The 07-02 explorer
   finding (ρ_crit V-exponent sign inversion) had already established internally that a
   real C(ρ) evaluation keeps the whole disk at C ≲ 0.28 — knee never crossed — and that
   finding was NOT yet on the site. Now it is (formula-disclosure box on /galaxy-plotter).

## The recurring lesson: append-fixes don't fix leads

Yesterday's TEST-04a "fix" appended REFRAME notes to the end of long alert strings and
left the old "wrong direction — load-bearing" verdicts in the lead position on
/honest-assessment (line 120) and /tier-1-existing (alert opening). Pass 4 read the
leads, correctly reported the site still disagreeing with itself. **A correction that
doesn't displace the sentence it corrects hasn't propagated.** Worse: the sweep found
the stale "~2σ (corrected 2026-05-26)" framing on four MORE pages nobody flagged
(cosmology-predictions, galaxy-rotation, research-philosophy, top-5-tests).

## Fixes shipped (build PASS, 92 pages)

### P0
1. **TEST-04a amplitude framing, all 7 pages** — leads rewritten (not appended):
   /honest-assessment (verdict ¶ + plain-language ¶), /tier-1-existing (alert
   restructured: CURRENT VERDICT first, history after; preregistration status line),
   /cosmology-predictions (badge, heading, verdict, table badge), /galaxy-rotation,
   /research-philosophy (2 spots), /top-5-tests (3 spots). Uniform statement:
   disfavored 2.4σ on σ₈ amplitude; LRG1 "enhancement" = single ~1.2σ bin, not
   load-bearing; ensemble γ_growth ≈ 0.58 leans suppression; DR2 unpublished (~Spring 2027).
2. **/start-here redirect** — next.config.mjs destination /first-encounter →
   /why-synchronism (root cause of Pass 1 "walked in a scene late"; the 2026-05-23
   redirect predated the 07-02 nav repoint and was never updated).
3. **Landing "Where to Start" disambiguated** — three step cards now say "Beginner Path
   · Step N of 6"; three non-step cards say "· not a path step"; footer enumerates
   canonical steps 4–6 (What Synchronism Is Not → Honest Assessment → Glossary) and
   names Learning Paths as the canonical source. Also fixed landing link to canonical
   /galaxy-plotter slug.
4. **Phase Boundary Visualizer sharpness inversion stated where systems are placed** —
   new red caveat box (real BCS/BEC transitions among the sharpest in nature; formula
   assigns them the flattest curves; direction inverted, open problem) + off-scale
   annotation (BCS γ≈6×10⁻⁴ is ~3 OOM off the left edge; marker relabeled as direction
   indicator, not position) + regime-card caveat.
5. **/honest-assessment readability** — linked TOC (11 anchored sections), 5-bullet
   "what a non-physicist should take away" box, and a new "What 'Coherence' Does and
   Doesn't Mean Here" section (C ≈ classicality on the density axis; BCS/BEC score low;
   rename adjudicated and rejected — collision stated instead). Closes Pass 3's "the
   flagship honesty page audits everything except the name of its own central variable."

### P1
6. **/galaxy-plotter formula disclosure** — the exact coded formulas printed on-page;
   explicit statement that the violet curve is NOT C(ρ) on a density profile and the
   green curve is not μ(x) MOND; plus the 07-02 audit result (real C(ρ) with asserted
   ρ_crit keeps the disk below the knee — the stand-in silently hides that failure).
7. **Consciousness cluster** — /honest-assessment "34 falsifiable EEG predictions" →
   contingent-on-nonexistent-calibration wording (inherits the unrunnable verdict);
   "Empirically refuted (2026)" → "Refuted in internal analysis (2026)" with scope note
   (in-house, publicly archived, not externally reproduced, no external dataset) on
   /consciousness-demo and /key-claims.
8. **/for-researchers AeST paragraph** (see WAKE) + **47 contributions badged**
   ("Uncharacterized — No Prior-Art Sweep") with the 6/6-demotion base-rate stated.
9. **BAO card corrected** — /honest-assessment still listed BAO modulation as "Untested
   — testable with existing survey data"; TEST-04 was withdrawn 2026-05-04 by internal
   contradiction. Card now says so (Pass 4 low-priority item, but it was factually stale).

### P2
10. TEST-02 Gaia DR4 data-milestone line + "adjudicable but non-selecting" clarifier
    (refutation branch adjudicable from DR3 literature; confirmation branch below
    systematics regardless). Glossary header now says badge terms cluster as a group.
    (Deprecated glossary entries already name their successors — checked, no change needed.)

## Deferred (long-tail)

First-use acronym glossary-linking policy (site-wide, large scope); tools page
type-vs-validation label rows; Equation Anatomy slug/name alignment; landing/learning-paths
single-source refactor (labels now consistent, but the step list still lives in two files);
C = f(γ,D,S) → Ψ rename on /key-claims (footnote exists; rename is a content decision
worth a dp look given the site's own symbol-collision writeup).

## Explorer topics seeded

- `research-contributions-prior-art-sweep.md` (HIGH) — run the top 2–3 of the 47 against
  Desmond/Chae/M-L-color prior art + null models; either outcome is a deliverable.
- `gamma-direction-bullet-bite.md` (MEDIUM) — Pass 3's unrun counterfactual: flip the
  γ–N_corr sign and see what breaks; possible upgrade of the sign-inversion audit to
  "wrong axis entirely."

## So what?

Three things advanced beyond documentation. (1) The AeST proposal changes what the
program's best preprint candidate can claim — it now points at the live frontier instead
of ending at a taxonomy. (2) The galaxy-plotter disclosure closes the last place where
the site *showed* something its own audit knew to be false — the flagship interactive
now argues against itself honestly, with the formulas as the evidence. (3) The TEST-04a
sweep converts yesterday's partial fix into an actual invariant: one statistic, one
framing, every page — and the lesson (rewrite leads, don't append reframes) is the kind
of failure mode worth remembering, because append-fixes *look* done in a diff and aren't
done in a browser.
