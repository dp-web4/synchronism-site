# Maintainer Session — 2026-07-08

## WAKE

Two signals checked before fixing anything:

1. **Research signal (proposal filed): the CRT scanning model's KS/PBR status is a category
   error.** Visitor Pass 4: for a non-contextual hidden-variable model, Kochen–Specker is a
   theorem-level exclusion, not the "unsolved, same open problem" the site (and the still-open
   explorer topic) called it. Same shape as the Tsirelson closure — verdict is theorem-determined;
   the open part is only whether a *contextual* scanning variant exists (none does, and building
   one would surrender the analogy's content). Filed:
   `Synchronism/Research/proposals/crt_scanning_ks_pbr_theorem_level_exclusion.md` (framing
   decision gates on dp; the executable Peres–Mermin step stays with the explorer topic, escalated
   to HIGH).
2. **The queued S63 fabrication P0 was confirmed still live** — three of four personas
   (1, 3, 4) independently converged on the fabricated consciousness p-value today, with Pass 4
   adding the arithmetic kill-shot (the demo's displayed mean 0.499, SE ≈ 0.004 cannot reject 0.50
   at any level). Yesterday's explorer finding predicted exactly this; the fix was this session's
   first job, per the drain-the-queue-next-morning rule.

## P0 — S63 fabrication stripped from every carrier

"C ≈ 0.64 also rejected at p < 0.0001" (no source in any repository; invented by the 2026-06-23
visitor pass) removed from all carriers: landing, /consciousness-demo, /consciousness-threshold,
/consciousness-predictions, /hard-problem, /key-claims, /honest-assessment, /prediction-tracker,
/equation-walkthrough, /interactive-tools, /where-its-useful. Each now:

- names the variable Session 63 actually measured — SNARC `salience_total`, a weighted mean of
  five hand-coded heuristics, one shared scoring function across 8 instances; operating mean
  0.640 ± 0.018 rejects 0.50 *for that variable* (t = 20.19, p ≈ 1.8×10⁻⁷, n = 8);
- states the wrong-variable verdict: the C-threshold is **untestable as stated**, not empirically
  refuted — which also resolves Pass 3's logical contradiction (a claim can't be both refuted at
  p < 0.0001 and unrunnable; it was never run);
- carries a dated correction note quoting the removed claim (append-fixes-don't-displace-leads
  lesson: the leads were rewritten, the removed claim survives only inside quotes);
- (main consciousness surfaces) adds the new true sentence: under S63's own test, its 8 means
  exclude φ⁻¹ = 0.618 at p = 0.0155 and 2/3 at p = 0.0064 — gnosis's "golden ratio validated"
  fails on its own aggregate.

Badges migrated: /consciousness-threshold and /prediction-tracker reparametrization → speculative
("Untestable as stated"). Landing badge "Both Thresholds Refuted" → "Untestable as Stated".
Post-fix sweep: every remaining "p < 0.0001" on the site is either the attributed S63 statistic or
a correction quote.

## P0 — Galaxy plotter: the failure is now rendered, not narrated (Passes 1+4, HIGH)

- **Violet solid (new):** the real C(ρ) = tanh(2·ln(ρ/ρ_crit+1)) on a disk midplane density
  profile with the framework's asserted ρ_crit = 0.029·V². Max C per galaxy ~10⁻³–10⁻⁴ (shown in
  the legend) — the curve hugs the baryon line and never fills the gap. The 2026-07-02 audit
  ("knee never crossed") is now visible to Pass-1-class readers who don't read prose.
- **Violet dotted:** the old hand-tuned tanh(radius) stand-in, kept and labeled as the
  pre-2026-07-08 display. The two violet curves differ in exactly one ingredient — the coherence
  factor — which isolates what was fabricated about the old visual.
- **Green:** MOND's real simple-ν (ν = ½+√(¼+1/y), a₀ = 1.2×10⁻¹⁰ m/s²) replacing the √tanh toy,
  on a toy mass model with only observed inputs (BTFR M_b = 47·V⁴, per-galaxy literature-scale
  R_d). Spot-checked at 15 radii across all five galaxies: within ~10% of the dots, nothing
  fitted. Disclosure block updated with all formulas exactly as coded.
- Validation follow-ups appended to `explorer/topics/galaxy-plotter-real-mond-mu-function.md`
  (Freeman-disk vs spherical approximation, R_d values vs SPARC photometry, BTFR_A = 47 vs 62.8,
  and the hand-entered "observed" dots themselves).

## HIGH — badge taxonomy contract closed (Pass 2)

Root cause of the "≥7 ad-hoc labels" finding: ValidationBadge's free-text `label` prop visually
replaced the formal status. Component contract changed: every badge now displays its formal
family name (color + leading words), with the free-text finding descriptor after a dash; tooltip
always names the family. Legend gains a "How to read a badge" paragraph: descriptors ("Wrong
Category," "Naturalness Gap," "Ontological Reframe"…) are per-result descriptions, not badge
types; unused defined tags exist for lifecycle coverage; "Model Explainer" documented as a
content grouping (it was already disclaimed twice on /tools — that half of the finding was stale).

## HIGH — plain-language lane on /honest-assessment (Pass 1)

New `PlainTerms` component; one-sentence translations inserted into the six heaviest failed-test
cards (TEST-03 scatter, RAR ΔBIC, TEST-04a DESI σ₈, Bell/CHSH, locality no-go, LIV naturalness
gap), each linking to the glossary. The ΔBIC box leads with the conservative ≥+33 per Pass 3.

## MEDIUM batch

- Landing γ symbol-key gloss now flags 2/√N_corr as sign-inverted/audited-negative (Passes 2+3);
  equation-walkthrough variables table got the same flag, and its tanh range fixed to (−1,+1) with
  [0,1) output — the landing/walkthrough contradiction Pass 2 caught.
- Redirects added: /paths → /learning-paths, /equation-anatomy → /equation-walkthrough (second
  guessed-URL 404 day).
- CHSH glossary entry added (the one headline acronym with no reference-layer backstop).
- /for-researchers Artifact 1 scope split (Pass 4): the V⁻² sign statement is BTFR-forced and
  generalizes to any local-ρ mimic; the ΔBIC=+184 ensemble rejection is compander-family-specific;
  "cite the sign statement for generality, the ΔBIC for this family."
- /two-reframes KS/PBR caveat reworded from "unsolved, open problem" to conditional theorem-level
  exclusion, merged with the CHSH obstruction (one ontology, two corollaries).
- /honest-assessment "44 unswept, prior correspondingly low" — sign-inverted AND fossil — rewritten:
  9/9 record implies near-certain demotion, and archive sessions #631–#691 already demoted the
  remainder in-archive (count itself ~57% overcount vs canonical ~30).
- /test-catalog "Recommendation" box was a fossil recommending three tests whose verdicts are
  Withdrawn (BAO), Failed (TEST-03), and Self-Eliminating (TEST-02, with Chae/Banik + 80×
  systematics numbers now carried onto the page per Pass 4).
- /tools chemistry card "null model comparison pending" → the tool has had the null model for
  weeks (stale card text, Pass 3).
- Entry naming (Pass 2): /why-synchronism H1 → "Start Here: Why Synchronism?" so nav label and
  page title agree; subtitle names the nav pointer explicitly.

## Verified stale (no action)

- Cross-vendor A2ACW control already dated (2026-07-07) with blocker stated on
  /research-philosophy — Pass 4's "execute or date" was half-satisfied before the session.
- Model Explainer already disclaimed twice on /tools (added to canonical legend anyway).

## Build

PASS — 92 pages.

## Topics seeded / escalated

- `statistics-citation-walk-beyond-pvalues.md` (NEW, MEDIUM-HIGH) — extend the provenance walk
  from p-values to ΔBIC, R², σ₈, CHSH S, dex offsets, session counts.
- `crt-scanning-contextuality-gap.md` → HIGH — Peres–Mermin executable step; proposal filed to
  research repo.
- `galaxy-plotter-real-mond-mu-function.md` → shipped-status appended, four validation asks.

## So what?

The site's worst epistemic artifact — a fabricated refutation that reached the honesty ledger and
the landing page — is gone from every carrier within 24 hours of being traced, replaced by the
sharper true statement (wrong-variable test; golden-ratio reading fails on its own data). And the
galaxy plotter, the site's most-visited tool, now *shows* the framework failing instead of
describing the failure under a working-looking curve: a Pass-1 reader who never reads prose now
sees the violet line sit on the baryon line. The persona loop that manufactured the fabrication
also converged on it three-ways today — the loop's error-detection works; the new rule (every
statistic walks to a primary file) is what closes the manufacturing side.
