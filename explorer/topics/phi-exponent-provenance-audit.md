# Topic: φ in the C(a) Deep-Limit Exponent — Provenance Audit (Numerology Check)

**Priority: HIGH** — seeded 2026-07-17 (maintainer, from visitor Pass 4 Unanswered Question 3)

## Question

Where does the golden ratio φ in the framework's C(a) law actually come from? TEST-09's
no-rescue scan treats φ as one of the two "derived from cosmology" ingredients (deep-limit
exponent 1 − 1/φ = 0.382; "reaching the observed BTFR slope requires φ → 2.00, destroying
the golden ratio"). But `/parameter-derivations` — the page whose entire job is parameter
provenance, structured as a prosecution exhibit with a "zero first-principles parameters"
bottom line — **never mentions φ at all** (verified by grep 2026-07-17: zero occurrences).

A reviewing researcher put it precisely: "If a fitted exponent is being written as φ, that's
numerology the parameter-derivations page would normally catch — I couldn't find it addressed."

## Why It Matters

1. φ is the framework's most numerological-looking ingredient, on the page-of-record's blind
   spot. If φ is fitted-then-named, that is the same prestige-vocabulary failure the
   compander/Landau audit caught (a fit dressed as a constant). If it is derived, the
   derivation is uncited on the one page that should carry it.
2. TEST-09's refutation currently *leans on* φ's status: "its two derived-from-cosmology
   ingredients are precisely the two that put it off the BTFR" is a sharper sentence if φ is
   genuinely derived (the derivation kills it) and a different sentence if φ is a fit (then
   the framework had a free exponent and *still* couldn't reach 3.75 — arguably stronger).
3. The consciousness sector already had its φ⁻¹ = 0.618 claim excluded on S63's own data
   (p = 0.0155). If galactic φ is also unanchored, the golden ratio appears in two sectors
   with zero derivations — worth stating once, jointly, on /parameter-derivations.

## What To Do

- Trace φ's first appearance in the research archive (gitnexus cypher over Synchronism
  Section nodes; grep sessions for "golden ratio", "1.618", "0.382", "1 − 1/φ").
- Determine: derived (from what?), asserted, or fitted-then-renamed?
- Deliverable: a φ provenance row for `/parameter-derivations` (the page's summary table
  should gain a φ entry either way), plus a corrected sentence for the TEST-09 carriers if
  the "derived from cosmology" framing doesn't survive.

## Suggested Starting Points

- `explorer/findings/2026-07-14-btfr-bounded-boost-refutation.md` (how φ enters C(a))
- `explorer/scripts/test09_parameter_scan_no_rescue.py` (φ scan range [1.2, 4.0])
- Synchronism research repo: search for the C(a) = Ω_m + (1−Ω_m)x/(1+x) derivation sessions
- `/parameter-derivations` (the page missing the row)
