# Topic: Test-Page Sprawl — Write the Nesting Map

## Question
The site has four overlapping test inventories with no single explanation of how they relate:
- `/tier-1-existing` — 10 numbered tests plus TEST-04a sub-test
- `/test-catalog` — described as "24 experiments" in navigation
- `/top-5-tests` — "Top 5 Decisive Tests" linked from the Intermediate learning path
- `/test-catalog` (roadmap) — the Tier 1–4 hierarchy is "defined" here but lists no tests

A reader following the Intermediate learning path hits "Top 5 Decisive Tests" and has no idea whether this is a subset of the 10 Tier-1 tests, a different list, or the same tests renamed.

The specific audit questions:
1. Is Tier 1's list of 10 tests the complete Tier 1, or a curated subset?
2. What 24 experiments does "Test Catalog (24)" refer to? How do they map to Tier 1/2/3/4?
3. Are "Top 5 Decisive Tests" a subset of Tier 1, or independently selected?
4. Does any page currently explain the nesting?

## Context
Flagged independently by Pass 2 (Technical Writer) and Pass 3 (Grad Student) in 2026-06-02 visitor log.
The Test Roadmap page says "Tier 1–4 hierarchy is defined here" but shows no test list.
The Intermediate learning path links to /top-5-tests without stating whether those 5 are from Tier 1.

## Why It Matters
Test-page sprawl creates the impression the site has more active discriminating tests than it does.
The honest answer — that Tier 1 has ~10 tests, 0 discriminating from MOND+ΛCDM after audit — should
be findable within 2 clicks from any test page. Right now it requires piecing together 4 pages.

A single "how the test pages nest" paragraph, ideally on /test-catalog, would fix this.

## Suggested Starting Points
- Read /test-catalog, /tier-1-existing, /top-5-tests, /test-catalog (the roadmap) to enumerate the lists
- Check /learning-paths to see how the Intermediate path links to test pages
- Write a one-paragraph "map" and propose where to put it (probably /test-catalog as the hub)
- If the "24 experiments" count is wrong or outdated, correct it
