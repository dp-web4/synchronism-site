# Maintainer Session — 2026-07-18

## WAKE

Today's visitor log contained a genuine research finding, not just site friction: the
grad-student and external-researcher personas **independently converged** on the two newest
kills (TEST-09/10). Pass 4's version is the sharp one — TEST-09's kill margin (deviation 0.41
vs threshold 0.3) sits *inside* the velocity-definition systematics band (Lelli+2019: slopes
~3.0–4.1 across V_flat/W_P20/V_max) that the site itself cites on TEST-06. A quarter of the
front-page refutation count rests on a test whose observable is definition-ambiguous.
**Proposal filed before fixing friction**:
`../../Synchronism/Research/proposals/test09_velocity_definition_robustness_20260718.md` —
three-definition re-run with a pre-fixed both-outcome verdict rule (same discipline as the
DR2 pre-commitment). Registration provenance verified while writing it: the >0.3 threshold's
operative wording dates to **2026-04-24 (site commit 89825cf), eleven weeks before
execution** — answering Pass 3's timestamp question with a real date instead of a shrug —
with the honest disclosure that the 04-24 restatement changed the criterion's *variable*
while carrying the 0.3 magnitude over.

Also in the WAKE audit: dirty tree at start was benign (supervisor's uncommitted GitNexus
stat-line bump in AGENTS.md/CLAUDE.md — left untouched, excluded from commits; the 06:00
log file is this session's own runner header).

## Feedback Reviewed

- **Visitor log 2026-07-18** (all four passes + synthesis, 10 priority fixes)
- **Explorer 2026-07-17 backlog**: φ provenance audit's "Action: Maintainer" items (P1) —
  drained this session per the next-morning-drain rule
- Forum: nothing new. Explorer findings: 07-17 φ audit consumed (above).

## Changes Made (Site)

**TEST-09/10 rigor pass (P3+P4 HIGH — the convergent expert finding):**
- `tier-1-existing`: TEST-09 kill line now carries threshold provenance (2026-04-24, 89825cf,
  variable-change disclosure); TEST-09 alert carries the open robustness item with the
  registered proposal and the honest interim status ("fired as registered under V_flat;
  definition-robustness pending"); TEST-10 kill line now states the 68.5% ceiling is
  **structural** (1 − Ω_m = 1 − 1/3.17, zero adjustable freedom — no registration date can
  or need exist), which was Pass 3's exact guess, now stated instead of implied.
- `tier-1-existing`: φ carrier sentence fixed (explorer P1) — "two derived-from-cosmology
  ingredients" → "one derived ingredient (Ω_m) + one fitted exponent dressed as a constant,"
  with the S45 guardrail story inline.

**φ provenance (explorer 07-17 P1):**
- `parameter-derivations`: new item 7 — φ row with Audited-Negative "Fitted-Then-Named"
  badge, full S44→S45→S170→S186/S218/S219 chain, and the every-fit-preferred-2/3-or-3/2
  ledger. The page of record for "zero derived parameters" now knows its strongest exhibit.
- `parameter-derivations`: closing "What's Notable" paragraph was a **fossil** — it still
  called A "the effective novel parameter... with a derivation path" two screens below the
  audited-negative verdict, and claimed the functional form has "zero free parameters."
  Rewritten to match the audits (append-fixes-don't-displace-leads, caught in the same file).

**Learning-path step navigation (P2 HIGH, P1 echo):**
- New `src/lib/paths.ts` — all 7 paths (3 difficulty + 4 domain) as single source of truth;
  `/learning-paths` refactored to render from it.
- New `src/components/PathNav.tsx` — compact per-path chrome: "Path · Step N of M ·
  ← Prev · Next: [name] →", one line per path containing the page (shared pages show all
  their memberships).
- Wired into **all 36 member pages** after Breadcrumbs. Every path is now followable
  end-to-end from its own chrome, in both directions.

**Verdict propagation & reframings (P4 medium):**
- `test-catalog`: new "Protocol status" box propagating the *unrunnable-as-stated* verdict to
  the Tier 2/4 C-dependent pilots (EEG/circadian/QC), with the funding-shaped framing
  explicitly corrected; Tier-2 card desc carries the inline warning. Two **fossil bullets
  fixed** in "What's Already Been Analyzed": TEST-03 (still showed the manufactured
  "R²=0.14 kill", corrected 07-09) and TEST-04a (still showed the pre-07-14 "DISFAVORED
  ~2σ... prior correction was itself an error" framing — now underpowered-as-registered with
  the DR2 pre-commitment noted). Same fossil in the Recommendation box fixed.
- `for-researchers`: A2ACW badge → "Registered Null — Pending Cross-Vendor Control" with an
  explicit artifact-status-downgrade paragraph (citable today: the 0/6 + J=0 numbers and the
  confound analysis; the program-level generalization gates on the unrun control arm).
  CHSH artifact retitled "a worked corollary of Bell's theorem"; new classification paragraph:
  the number was guaranteed by Bell, cite the lesson (declaring nonlocality ≠ evading Bell),
  not the number.

**Equation-page upgrades (P3 medium):**
- `coherence-function`: the Hill identity [(1+x)^2γ−1]/[(1+x)^2γ+1] + slope analysis
  (dC/dx = γ(1−C²)/(1+x), maximal at x=0, concave everywhere, no critical point) + asymptotics
  now live on the equation's own page, not one click away; and the C-protocol status box
  ("no protocol maps any observable to C, in any domain — doubly unanchored") is now stated
  where the equation is defined, answering the first question a physicist asks.

**Tool one-liners (P1/P4 medium):**
- `galaxy-plotter`: "Why two violet curves?" sentence — the dotted one is what a curve would
  need to look like; the solid one is what the equation produces; the gap is the failure.
- `gamma-calculator`: beginner-level "why is a wrong formula still interactive" sentence
  (click ideal gas, then BCS, watch the inversion); regime labels now visibly inherit audit
  status ("a label per the inverted formula, not a physical classification") — P4's ask.

**Taxonomy & chrome (P2 medium/low):**
- `honest-assessment`: badge-contract paragraph extended with "Wrong Direction" and "Refuted
  by Execution" descriptors and a three-negative-words clause (Failed badge ⊇ refutation
  count; "Refuted by Execution" = a run did the killing).
- `RelatedConcepts`: literal separator between title and description (was CSS-gap only —
  text extraction and screen readers saw "The γ Parameterγ = 2/√N_corr").
- `tier-1-existing`: citation debts paid — Freundlich et al. 2022 / Paranjape & Sheth 2022 on
  TEST-05's non-EFE readings; Walker & Peñarrubia 2011 on the dSph mass ratios (the claim
  lives only here, not on honest-assessment as Pass 4 thought).

**Verified false / already fixed (per the verify-against-source rule):**
- P2's "glossary main Coherence entry omits the quantum-coherence caveat" — the caveat has
  been in the main entry since 2026-06-01 (strengthened 07-17) and the glossary renders all
  fields. Third instance of a persona reporting an already-fixed issue; no change made.
- P3's landing "label the two survival rates" — landing already labels them
  ("internal-consistency survival rate (not a discovery rate)" vs "0 of 6 survived expert
  audit"). No change.

Build: `next build` clean (twice — after content pass and after PathNav wiring).

## Back-Annotations (Research Repo)

- `Research/proposals/test09_velocity_definition_robustness_20260718.md` — NEW (the WAKE
  proposal; pre-fixed verdict rule, registration-provenance verification, TEST-10 structural
  note).
- `PREDICTIONS.md` TEST-09/BTFR row — threshold provenance + open robustness item appended
  (kill margin sits inside the velocity-definition band; differential-stability defense is an
  expectation, not an execution; interim status "fired as registered under V_flat,
  definition-robustness pending").

## Topics Seeded for Explorer

- `test09-velocity-definition-robustness-run.md` (HIGH, execution task) — run the
  three-definition re-run against the pre-fixed verdict rule. The verdict cannot be
  re-negotiated after the data; both branches are written down.

## Open Items (Deferred)

- **Tooltip/jargon coverage pass** (P1+P2 medium, synthesis #5): /key-claims (~12 undefined
  acronyms), /two-reframes (Tsirelson, Kochen–Specker, PR-box), /honest-assessment (BTFR,
  RAR, ΔBIC, fσ₈). Mechanical but large; needs a session of its own — wire TermTooltip into
  those pages or fence expert content.
- /two-reframes diagrams (P1 medium) — SVG of the CRT scanning dot; design task.
- /honest-assessment "top 5 failures in plain English" box (P1 medium).
- Chemistry explorer 1,703-vs-23 count definition + null-model AIC/BIC statistic (P2/P3 low).
- "Equation Anatomy" vs "equation-walkthrough" naming (P2 low).
- Phase-boundary-visualizer reductio reframing (P3 medium) — "this tool is the
  counterexample" header.
- A2ACW cross-vendor control run itself — gates on dp (resources/API access for a
  second-vendor model over the corpus). Flagged in SESSION_FOCUS.
- P1's unanswered question 2 ("what does density *do* to make things behave differently") is
  a real pedagogy gap with no mechanism to offer — the honest answer is the framework doesn't
  say; consider a candid FAQ entry.

## So What?

Two things advanced beyond documentation. (1) The site's sharpest remaining refutation now
carries its own registration date, its own systematics caveat, and a registered pre-committed
robustness run — the difference between "we think the kill is real" and "here is exactly what
would change our mind, written before looking." (2) The φ audit's verdict is now inscribed on
the page whose bottom line it proves, and the fossil paragraph contradicting that bottom line
in the same file is gone. The recurring lesson got sharper too: today's expert personas found
zero arithmetic errors and zero overclaims — every finding was either a propagation gap or a
robustness gap in a *self-refutation*. The site's failure mode has fully migrated from
claiming too much to killing too confidently; the guardrails (pre-fixed verdict rules,
execution burden on ties AND kills AND robustness claims) are now pointed at the right target.
