# Maintainer Session — 2026-09-10

## WAKE — the feedback contained three research-level items and one false premise

Required reading done in full: `../../Synchronism/SPINE.md`, `../../Synchronism/PREDICTIONS.md`.

Proposal written **before any site edit**:
`../../Synchronism/Research/proposals/test02_amplitude_is_knee_conditional_and_a2acw_positive_control_20260910.md`.
Four items, all routed to dp, **no bucket moved, executed count stays 6, Bucket 0 stays 0.**

### The one new result this session produced

Chasing the visitor's "TEST-02's mechanism and amplitude are reciprocals — pick a coupling"
complaint, I found the visitor's diagnosis **wrong and the real one sharper**. The coupling is not
ambiguous; it is `g_eff = g_N/C(ρ)`. What is unstated is **which ρ_crit the published 0.05–0.4%
amplitude was computed at** — and the answer is: one that appears nowhere else in the framework.

Script: `scripts/test02_amplitude_is_knee_conditional.py` (+ `_output.txt`, mirrored to the research
repo at `simulations/`). At ρ_local = 0.09 M☉/pc³, velocity excess `1/√C − 1`:

| ρ_crit (M☉/pc³) | provenance | γ = 0.489 | γ = 2 |
|---|---|---|---|
| 1.52×10³ | `0.029·V_flat²`, the **published** calibration (MW, V_flat = 229) | **+1.8×10⁴ %** | +9.1×10³ % |
| 0.161 | measured velocity-blind knee (2026-08-27) | +116 % | +18.6 % |
| 8.3×10⁻³ | Refracted Gravity E0 floor | +9.4 % | +0.0051 % |
| 3.2×10⁻⁴ | bottom edge of the refuted ρ_c grid | +0.40 % | ~0 |

The knee window that reproduces the card's quoted band is **ρ_crit ∈ [3.8×10⁻⁵, 3.2×10⁻⁴] at
γ = 0.489**, or **[1.6×10⁻², 3.0×10⁻²] at γ = 2** — disjoint from every knee the galaxy sector uses,
seven orders below the published calibration. So **"practically untestable / ~80× below Gaia reach"
is a property of an otherwise-unused knee**, and at the published calibration the solar-neighbourhood
prediction is a factor ~3.5×10⁴ boost in `g`, excluded by Oort and Solar-System ephemerides.

**This is not a seventh refutation and I recommended against counting it as one.** It is the same
physical content as the vacuum-singularity / AQUAL-1984 result `/key-claims` already publishes as
*"rotation curves do not fail to flatten — they blow up"* — evaluated at a radius where ρ is well
measured, so the vacuum limit isn't needed to reach it. Recommended ruling: scope TEST-02 into
02ρ / 02a, **count unchanged**. Over-refutation is this program's live failure mode and the 09-09
retraction of the "Oort ∩ GC disjoint" no-go was exactly this shape.

**A bisection-direction bug in the first draft of that script gave `ρ_crit = 1e6` for every window**
(the excess is monotonically *increasing* in ρ_crit; I had the comparison inverted). Caught by the
numbers being identical across γ, which is physically impossible. Fixed before anything was written.

### The false premise — recorded because it will be re-derived

The visitor's **#1 P0** was *"re-execute TEST-04a on DESI DR2/DR3 — DR2 (2025-03) and DR3 (2026-07-30)
have shipped; the registered criterion is two releases stale."* **It has not fired, and DR3 does not
exist.** Three DESI products were being conflated:

- **DR1 full-shape** (arXiv:2411.12021, Apr 2024) — what TEST-04a is adjudicated against.
- **DR2 BAO** (arXiv:2503.14738, Mar 2025) — published, and this program *already fitted it*
  (2026-08-12 dark-energy likelihood). Constrains **expansion**, not growth; cannot adjudicate fσ₈.
- **DR2 full-shape** — the **registered** statistic. Unpublished (~Spring 2027); preliminary results
  presented Apr 2026 (PIRSA:26040071), which `/for-researchers` already documented correctly.
- **"DESI DR3 (2026-07-30)"** — does not exist in this archive or its literature. DR3 appears only as
  the ~2027–2028 venue *proposed* for TEST-26. A fetch artifact.

**But the site earned the misreading**, and that is the actual defect: `/honest-assessment` cites
"DESI DR2 (arXiv:2503.14738)" prominently while "DR2 full-shape growth remains unpublished" sat at the
end of a ~2,000-word alert block on `/tier-1-existing`. A site whose entire method is pre-registration
cannot afford to look like it is sitting on an overdue trigger. Fixed at every point of use.

---

## Feedback Reviewed

- **Visitor log 2026-09-10** (4 personas, 394 lines) — primary input. 22 prioritized items.
  - *Casual enthusiast*: the Beginner path's "~30 min" with a ~16,300-word step 5.
  - *Technical writer*: three label families and a data dictionary buried in the long audit page; a
    cross-link chain aimed at one anchor out of three.
  - *Graduate physics student*: **12 of 12 numerical claims verified independently** — the badge
    severity inversion on the compander, and the scoreboard's "6" over-advertising coverage.
  - *Leading-edge researcher*: three refutation/test misclassifications, the TEST-02 amplitude, the
    galaxy-plotter calibration labelling, the A2ACW positive control.
- **Forum** (`../forum/`) — no new external feedback this session.
- **Explorer findings 2026-09-09** — implemented directives P1 (×3) and P2 (×2); see below.

### Convergences worth naming

Two pairs of independent inputs landed on the same correction from opposite directions, and in both
cases the pair is stronger than either half:

1. **The compander.** The grad student argued the badge severity is inverted (an *exact* identity with
   μ_simple at γ=½ is badged softer than 13%-off dimensional numerology). The explorer, a day earlier,
   produced the first number against the form itself (2.10× worse than MOND's μ). Together: at γ=½ the
   compander *is* what it would have to beat, and away from γ=½ it is 2.10× worse than what it
   reparametrizes. **There is no γ at which the form buys anything.**
2. **TEST-25.** The researcher argued it is framework-specific, not inherited. The explorer's 2.10×
   is what settles it: "inherited" means the other party can shed the failed component, and MOND can
   (its μ is free) while Synchronism cannot — leaving γ=½ costs it 2.10×. **The 09-09 number closes
   MOND's escape hatch for this framework**, and nobody had put the two together.

---

## Changes Made (Site) — build passes, 93/93 pages, zero errors

**Corrections that strengthen the negative**

- `cosmology-predictions/page.tsx` — **removed a stale over-refutation**: the page still read
  "Kill criterion (fσ₈ > 0.46) triggered," retired site-wide on 2026-07-14. Now states the criterion
  was **not met** (1.5σ against a >3σ bar) and that the 2.4σ lives on σ₈, a different GR-conditioned
  statistic. Plus the three-DESI-product clarifier.
- `honest-assessment/page.tsx` — DESI currency block now distinguishes DR2 BAO / DR2 full-shape /
  no-DR3, and says why the conflation matters.
- `tier-1-existing/page.tsx` — TEST-02: the full knee-conditionality arithmetic, the disjoint windows,
  the 3.5×10⁴ local boost, and the explicit statement that this is **not** a seventh refutation.
- `parameter-derivations/page.tsx` — compander badge **`speculative`/"Motivated Choice" →
  `reparametrization`/"exactly MOND's μ_simple at γ=½"**, with the γ=½ identity and the 2.10× penalty.
  Notes the explorer's own caveat that the compander knee has not been re-optimised self-consistently,
  so 2.10× is an upper bound on its advantage.
- `honest-assessment/page.tsx` — TEST-25 classification marked **contested**, with the escape-hatch
  argument in full and the consequence stated (3 framework-specific roots, not 2). **Not changed
  unilaterally** — classification is ledger governance and gates on dp.
- `honest-assessment/page.tsx` — ceiling refutation restated in **velocity-deficit** form:
  "B_max = 3.17 leaves a +14.1% median velocity deficit at the last measured point against 5.2%
  median measurement error," radius-graded (1.53× inner half, **3.08× last two points**). Includes the
  provenance of the 09-09 near-miss, because that is the reason to trust the number.
- `honest-assessment/page.tsx` — "three candidate arguments" corrected to **one candidate plus one
  alternative** (ρ_mid = Σ/2h identically, spread 1.0000; MRH-smoothed ρ = Σ/2λ to 0.4%), with the
  argument-ranking table at both floors (axis spans 1.41× at Ω_m, 5.5× freed; only g_N stable).
- `honest-assessment/page.tsx` — BTFR asymmetry: MOND's 3.81 labelled a **fit**, with MOND's a-priori
  n = 4 and the observed 3.75 ± 0.10 sitting 2.5σ from it. The Synchronism verdict is untouched; the
  manufactured contrast is gone. (This precision was on TEST-09's card since 2026-07-23 and had never
  reached the page where the bare pair is what readers see.)
- `for-researchers/page.tsx` — the **floor SPARC demands**: 90% of 153 discs need f ≤ 0.100, all 153
  need f ≤ 0.016, against the framework's f = Ω_m = 0.315 and an enumerated candidate list stopping at
  0.157. **No derivation route to a low-enough floor** — closes a 09-08 self-seeded question. Plus the
  Cesare+2020 prior art (same model class, same failure mode, never run on SPARC).
- `for-researchers/page.tsx` — A2ACW artifact 2 **demoted out of citable results**: retitled to name
  the underpowered statistic, badge `audited-negative/Registered Null` → `untested/Underpowered — No
  Positive Control Run`, with the H1/H2 ambiguity stated and why H2 would be the *more* interesting
  result. Page `description` corrected from "Five citable" to four.
- `galaxy-plotter/page.tsx` — "Synchronism's real equation" → "as published," with all three refuted
  choices named (γ=2 at ΔBIC +184; ρ_crit ∝ V_flat^**+2** against the BTFR's V^−2; per-system
  normalization), the inert-by-construction reading promoted to the headline, and the cross-link the
  plot owed: its green MOND benchmark uses the simple-ν function TEST-25 says Cassini excludes at 18σ.
- `what-synchronism-is-not/page.tsx` — added the **substrate/Lorentz row** (one-loop c_μν ~ 10⁻²–10⁻³
  vs bounds of 10⁻¹⁸–10⁻³⁰ = a 16–28 order gap, no custodial mechanism exhibited). The framework's
  largest fine-tuning problem was absent from the page whose job is enumerating what it is not.
  Includes the precision that cuts *for* the framework: the one-loop estimate's scheme-independence is
  disputed, so this is a naturalness problem, not a theorem.

**Honesty about the site's own promises**

- `src/lib/paths.ts`, `learning-paths/page.tsx`, `page.tsx` — **per-step reading times**, measured from
  rendered prose at ~200 wpm. The Beginner path's "~30 min" is now "~35 min on the short route; ~1 h
  50 m if you read step 5 in full," and step 5 carries "~80 min in full — or ~4 min for its 'If you
  read nothing else' box, which is the intended stop on this path." Measured counts: why-synchronism
  1,522 / first-encounter 972 / core-idea 1,635 / what-is-not 910 / **honest-assessment 16,330** /
  glossary ~5 min reference. A site whose pitch is honesty publishing a 3×-off time estimate is a
  credibility cost out of all proportion to the fix.

**Findability and terminology**

- `glossary/page.tsx` — **filter box** over 72 terms, matching name, expansion *and* definition text
  (so "dataset" finds SPARC), with a live match count and an empty state that points at the survey
  acronyms. Flagged by two personas independently.
- `tier-1-existing/page.tsx` — **fixed the broken anchor chain**: one link to `#validation-badge-definitions`
  became three, to badges / operational states / lifecycle verbs, with the reading order stated
  (badge → operational state → lifecycle verb). This answers the tech writer's unanswered question #1.
- `honest-assessment/page.tsx` — added `#operational-states` and `#status-lifecycle-verbs` anchors.
- `src/lib/navigation.ts`, `Breadcrumbs.tsx`, `layout.tsx` — category `'Getting Started'` → `'Start Here'`
  and top nav `Tools` → `Interactive Tools`. Two names for one section, twice.
- `equation-walkthrough/page.tsx` — "Term N of 6" → "Step N of 6" (the headings below already said Step).
- `consciousness-demo/page.tsx` — states plainly that **this C is the same C(ρ)** used site-wide, and
  then says the uncomfortable part: that one variable spanning quantum coherence, galaxy dynamics and
  consciousness is the framework's central ontological bet and its least supported claim.
- `src/lib/navigation.ts` — γ Calculator desc → "the formula that failed, and why we left it up."
- `first-encounter/page.tsx` — "Prefer analogies to equations?" → "Prefer pictures and stories? … some
  math does appear, including a square root and a cosine." The one page offered to equation-averse
  readers was not equation-light.
- `parameter-derivations/page.tsx` — BCS N_corr 10⁶ vs the γ Calculator's 10⁷ reconciled to 10⁶–10⁷,
  and turned into the point the page is already making: N_corr is a **counting convention**, so a factor
  of 10 in it is evidence the quantity is underdetermined, not a typo.

---

## Back-Annotations (Research Repo)

- `Research/proposals/test02_amplitude_is_knee_conditional_and_a2acw_positive_control_20260910.md` — new.
  Four items: (1) TEST-02 amplitude knee-conditionality, with the ledger ruling requested and **(b),
  count unchanged** recommended; (2) the A2ACW positive control — authorise or relabel the null
  permanently uninterpretable; (3) the DR2-BAO-vs-full-shape framing note, recorded so the conflation
  is not re-derived; (4) TEST-25 reclassification, with the 2026-09-09 compander number as the new
  argument.
- `simulations/test02_amplitude_is_knee_conditional.py` + `_output.txt` — the arithmetic, runnable.

**Nothing in PREDICTIONS.md was edited.** Every item above is either a site fix or a question routed
to dp. Count stays 6; Bucket 0 stays 0. Two of the four items move *against* the framework
(TEST-02 scoping, TEST-25 reclassification) and one moves *for* it (the DESI staleness was not real) —
noting that explicitly because a session that only ever moves one direction should be suspected.

---

## Topics Seeded for Explorer

- **`is-there-one-rho-crit-at-all-cross-sector-knee-scan.md`** (HIGH, new) — the generalization of
  today's finding: seven orders of ρ_crit are in live use across sectors, each sector having quietly
  chosen the knee that lets it survive. Is there *any* single ρ_crit consistent with SPARC ∩ ceiling ∩
  Oort ∩ ephemerides ∩ globular clusters ∩ wide binaries ∩ C₀ = Ω_m, at any γ? Carries explicit
  guards: sweep γ rather than fixing it (the 09-08 "disjoint windows" no-go was retracted on 09-09 for
  exactly that mistake), stay above the ~0.01 interpretability floor, run full samples not subsamples,
  and read each sector's registered text for the ρ_crit it was registered against.
- **`bulged-discs-are-where-sigma-and-rho-keying-differ.md`** (MEDIUM, new) — the 19 of 153 discs where
  ρ_mid = Σ/2h fails by >5% are the only place the 09-09 collapse-to-one-candidate can be falsified.
  The honest first question is whether 19 discs have the power to say anything; if not, that is the
  finding and it costs an afternoon.
- **`a2acw-positive-control-sensitivity.md`** — **escalated MEDIUM → HIGH**, not duplicated. This topic
  was seeded 2026-07-02 and has sat 70 days while the site kept citing the null it questions. Given a
  concrete protocol with pre-committed decision thresholds (≤20% demotion → protocol discriminates,
  H1 survives; ≥60% → the 1.4% yield is a property of the protocol and the prior-art-illusion rate
  becomes the finding). **Do not execute until dp rules** — it is a governance question about the
  program's own methodology claim.

---

## Open Items (Deferred)

- **The structural fix three of four personas asked for: split `/validation-taxonomy` out of
  `/honest-assessment`.** One page is serving as an 18,000-word confession, a 27-label data dictionary
  and a casual-reader summary — three documents with incompatible reading modes. I fixed the *routing*
  today (three anchors, stated precedence order) because that is patchable; the split is not, and doing
  it badly would scatter the canonical definitions that currently have exactly one home. **This is the
  fourth consecutive session the badge-vocabulary single-source-of-truth refactor has been raised. It
  needs a design session, not another patch, and I am going to stop re-deriving that conclusion.**
- **Extend glossary hover-definitions to survey/statistics acronyms** (SPARC, ΛCDM, BTFR, RAR, BIC, σ₈,
  χ²) — flagged by two personas. The terms are all correctly defined in the glossary; the defect is
  first-use glossing, and the mechanism already exists. Mechanical but touches many pages; deferred to
  keep today's diff reviewable.
- **Give the globular-cluster exclusion window a TEST- number and anchor** — citable result #6 has no
  card in Tier 1 or the 24-test roadmap, stops a factor 6.5 short of the SPARC-calibrated knee, and
  presupposes a universal ρ_crit that `0.029·V_flat²` does not define for pressure-supported systems.
  That last point is now part of the cross-sector knee topic, which should land first.
- **The Coherence Explorer cannot display the site's own central negative result** — the physically
  loaded ratio ρ/ρ_crit ≈ 4×10⁻⁵ is off the bottom of the slider range, so the tool built to show the
  coherence function cannot show the place where it is inert. Needs a log-range extension plus a
  "SPARC median" marker, not just a readout relabel.
- **Landing scoreboard collapse-behind-disclosure** (structural; do not remove honest content to make
  room). Dual-path prev/next footers. ρ gloss unification across `/first-encounter`, `/glossary`,
  `/equation-walkthrough`.
- **Tool state is not in the URL** on any of the five interactive tools — a reader cannot link a
  colleague to a slider position. Raised by the technical writer; genuinely useful for a site whose
  tools are its best pedagogy, and cheap per tool.

---

## So What?

**Does this advance discovery, or just document state?** One new result and one avoided error.

The result: **the framework's knee is not one number, and the test card with the softest verdict is
the one quietly using the smallest knee.** TEST-02's "practically untestable" was carried entirely by
an amplitude computed at a ρ_crit seven orders below the published calibration, where the same
calibration predicts a 3.5×10⁴ local boost that Oort excludes. The individual knees were all on the
site; the joint question was asked nowhere, and asking it is now a HIGH explorer topic with a clean
binary outcome — a surviving window or the empty set. Either publishes.

The avoided error: the visitor's top-priority item was to re-run a pre-registered criterion against
data that does not exist. Acting on it would have meant fabricating a DESI DR3 result. The *real*
defect was the site's own presentation making a live registration look overdue — which matters more
than the physics fix, because pre-registration is the one methodological claim this program still has.
**Reading the premise before executing it caught this; trying harder would not have.**

And the uncomfortable one, which I want on the record rather than in a topic file: the most valuable
remaining item on this site is not physics. It is the A2ACW positive control — a topic seeded 70 days
ago, never run, while the null it questions was cited as a result. If that protocol demotes genuine
post-cutoff discoveries at the same rate it demoted this framework's 6, then the headline finding of
3,308 sessions is a property of the *protocol*, and the publishable result is about AI-assisted
research rather than about gravity. That is the direction SPINE already says this program delivered
in. **A methodology null that nobody has controlled is not a null; it is an unexamined instrument —
and this site holds every other instrument to a standard it has not applied to its own.**
