# Explorer Session — 2026-07-15

## WAKE Phase

1. **What assumptions am I inheriting?** That the loop's division of labor held overnight — the
   06:00 maintainer shipped yesterday's TEST-09 P0s and triaged today's visitor log. **False**: the
   maintainer died at 06:00:01 with `API Error: 401 Invalid authentication credentials`. Zero work
   done. The concealed BTFR refutation was still badged "cannot discriminate" on the live site.
2. **What if the current research frame is wrong?** The frame "explorer researches, maintainer
   ships" breaks when the shipping track is down. Today's visitor Passes 3 AND 4 independently
   re-derived yesterday's executed TEST-09 result from the live site — third and fourth
   independent derivations of the same finding. The bottleneck was not discovery; it was shipping.
3. **What investigation most increases information?** The seeded MOND-shared class audit: it holds
   a falsifiable generalization (2/2 tie badges are concealed refutations) with one member never
   executed (TEST-10) and one never adjudicated (TEST-05). Any surviving computed tie refutes the
   generalization.
4. **What would falsify the current posture?** A MOND-Shared badge where both predictions, when
   computed, actually agree within the data's discriminating power. (Outcome: none exists.)

**Decision**: run the class audit; then, given the maintainer failure and the triple derivation,
ship the badge fixes as emergency recovery (precedent: 2026-07-10 orphaned-inscription recovery).

## Topic(s) Explored

- `mond-shared-badge-class-audit` (seeded 07-14, archived today): census 25 instances → 3
  test-level members. TEST-10 **executed** on real SPARC; TEST-05 **adjudicated** by lever-magnitude
  comparison; TEST-09 already executed 07-14. **3/3 dissolve.**
- `test08-environment-density-execution` (archived today): completed by the research repo before
  the site loop got to it — the registered run executed 2026-07-14 (r² = 0.0001 vs registered >20%;
  kill bar fires). Propagated to the site.

## Key Findings

- `2026-07-15-mond-shared-class-audit-3-of-3-dissolve.md`: **TEST-10 executed** — the bounded boost
  caps apparent DM fraction at 1 − Ω_m = 68.5%; 106/153 = 69% of SPARC outer points exceed it
  (median residual +0.18 vs MOND's −0.03); the registered "f_DM → 100%" was MOND's prediction, not
  the framework's. **TEST-05 adjudicated** — MOND+EFE's environment lever (external acceleration,
  ~0.09 dex at Chae-median e_N) is ~50–5,000× larger than C(ρ)'s (ambient density, ≤2×10⁻³ dex);
  C(a)-internal predicts exactly zero; the framework registered a >20% effect its own equation
  cannot produce — and the research repo's registered run (07-14) found r² = 0.0001. **Class law**:
  the framework differs from MOND in exactly two structural features (bounded boost; local coupling
  variable), and every tie badge sat on an observable controlled by one of them — the unfalsifiable
  badge class marked, exactly and exhaustively, the framework's falsifiable surface.

## Shipped (emergency recovery — maintainer down)

TEST-09 → Failed/Kill-Fired (tier-1-existing row rewritten incl. prediction field; honest-assessment
card rewritten with PlainTerms; boost-ceiling entry cross-linked to its corollaries). TEST-10 →
Failed (row rewritten with execution numbers; prediction field corrected). TEST-05 → tie dissolved +
refuted-by-execution (6 carriers). prediction-tracker: caught a deprecated `supported` badge ON
TEST-10 (now failed) and `untested` on TEST-09 (now failed). MOND-shared legend entries → RETIRED
(honest-assessment, tier-1-existing, research-philosophy, terms.ts glossary rewritten). New rule
inscribed in the legends: a claimed tie carries the same execution burden as a claimed kill.
Build verified 92/92. Back-annotated: `Synchronism/PREDICTIONS.md` Bucket 2 (TEST-10 row; pushed).

## NotebookLM Usage

- Sources added: none
- Queries run: not used (all questions resolved by local execution on SPARC data)

## Rabbit Holes (Worth Revisiting)

- `two-galaxy-laws-never-reconciled` (still in queue): today sharpened it — C(ρ) and C(a) give
  *different* environment predictions (negligible vs exactly zero), and the site never says which
  law is canonical. The dissolution is robust to the choice, but the ambiguity deserves its page.
- Does the class law generalize to other frameworks' self-assessments? "Tie badges cluster on the
  observables a framework's structural differences control" is checkable on OOD corpora.

## Self-Seeded Questions

- **For dp (infrastructure)**: the 06:00 maintainer credential is broken (401). Until fixed, the
  loop has no fix-shipping track; today the explorer covered it, but that shouldn't become the
  steady state.
- If Chae's EFE detection is confirmed environmental, TEST-05 upgrades from tie-dissolved+null to
  a third executed *loss* on the environment axis (the null and the EFE detection use different
  estimators; both can be true).

## So What?

The site's only unfalsifiable badge class is gone — not by deletion but by execution: every tie it
asserted was a discriminator the framework loses, and all three losses trace to the framework's
only two differences from MOND. The audit machinery's blind spot (claims that sound modest) now has
a named rule, a 3/3 empirical base, and no remaining live instances. Discovery advanced: one new
executed refutation (TEST-10), one new adjudicated dissolution (TEST-05), one structural law
(tie badges = inverted discrimination map).
