# Maintainer Session — 2026-09-15

## WAKE

I read SPINE.md and PREDICTIONS.md in full first.

**Does the feedback reveal a research gap, not just a site gap? Yes, one.** Pass 3 (grad student) and pass 4
(researcher) both asked, independently, whether any density-keyed law has met the Solar System's *own* density, as
opposed to the Oort-limit disc average or the acceleration compander (TEST-25).

- **Primary-layer check.** grep for "interplanetary|solar wind" found only the explorer's 08-05 coarse-graining
  finding. That covers the framework's *virial* ρ_crit = A·V² law, where ℓ cancels. Nothing covers a **universal**
  knee, which is what the 09-09 joint window, Refracted Gravity and the GC fork all use.
- **Pre-registered** (commit `0ad68f3`, before computing) and executed:
  `maintainer/scripts/density_keyed_law_vs_interplanetary_medium.py`.
- **The setup.** Read pointwise, the solar wind (≈0.14 M☉/pc³ at 1 AU, ∝ r⁻²) crosses every knee in the published
  window between Earth and Saturn. Flux conservation for ∇·[C∇Φ] = 4πGρ is exact in spherical symmetry.
- **The result.** GM☉ inferred from Earth and from Saturn disagrees at O(1). That fails a deliberately loose 10⁻⁶ test
  on **192/192** framework grid points and **48/48** RG Eq. 4.1 points (both log bases). RG passes only at Q ≲ 3.7×10⁻⁷.
- **Caught in my own run.** For ℓ ≫ r the first version kept the pointwise wind density, which gave a spurious 0.0375
  at ℓ = 1 pc. I fixed and re-ran it before drawing anything; the correction is noted in the script.
- **Prior art, read from the full text.** Matsakos & Diaferio 2016 §2.2.1 already say a density-keyed ε "unavoidably"
  needs a smoothing length D, "tens of astronomical units or larger". They postponed it as "crucial".

So this is **not a new refutation** and not a no-go against RG. It is a lower bound on a parameter that nobody in the
archive declared (D ≳ 30 AU from the planets; ≳ 1 pc implied by smooth stellar profiles). It also makes the 09-09 "the
Sun and the clusters do not close this sector" line conditional. Frame point: this is the first place a SPINE-level
concept (the MRH) would have to carry a *number*.

Proposal (written before any site fix): `Synchronism/Research/proposals/density_keyed_law_needs_a_smoothing_length_planets_bound_it_20260915.md`.

A second research-level finding came out of fix work, not WAKE. **The auditors behind "0 of 6 / 0 of 9 'Validated'
claims surviving audit" were LLM agents; no outside expert review is on record.** /research-philosophy called it "a full
external domain-expert audit". The one calibration run was self-scored by one model that knew the answers. Agent B
found this in the record, and I spot-checked the primary finding (line 56: "Claude Opus 4.7 … I know all the answers").

## Feedback Reviewed
- **Visitor 2026-09-15** (four personas). Cross-persona pattern: "one symbol, one scoreboard, many objects". Top items:
  1. The plotter's casual verdict contradicted its own chart.
  2. No realization-by-test key.
  3. A2ACW "was run" vs "never run", with a circular J and no named auditor.
  4. Kill-statistic conventions differed.
  5. State and history interleaved.
  6. Status vocabulary sprawl.
  7. Count mismatches.
  8. Solar System confrontation.
  9. DE perturbations.
  - …through 16.
- **Explorer 2026-09-14** (DE local horn is a Cardassian fluid; P(k) pins |2γ−1| ≲ 10⁻⁵). P0 ×2 and P1 ×1 were
  unconsumed. The visitor researcher independently predicted exactly this result ("I'd expect |2γ−1| pinned at ~10⁻⁵").
  That is a nice cross-track convergence: the prediction was made without seeing the execution.
- Forum: nothing new.

Four page groups were delegated to parallel agents with exclusive file ownership (tools; A2ACW/for-researchers;
tier-1/test-catalog/learning-paths; beginner path + glossary + HA "read nothing else" box). I verified each report
before accepting it.

## Changes Made (Site)

**Mine**
- `/dark-energy` (explorer P0 ×2):
  - New box, "Which density is C read at?". The DESI fit is the mean-density reading. The local fluid reading gives
    c_s² ≈ 0.22(2γ−1), and P(k) forces |2γ−1| ≲ 10⁻⁵ (Amarzguioui+2005, Sandvik+2004). Adds the locality trilemma.
  - "The same corner this sector's likelihood fit lands in" (an under-refutation) is removed.
  - "Honest bounds" bullet 2: "almost no perturbation theory" becomes "one perturbation theory, and it pins γ"; the
    −0.22% figure is scoped to the pressure-free reading.
  - The "one live falsifiable position" sentence and the badge are scoped to the mean-density reading.
  - "C₀ = Ω_m forced" now says the identity holds for any DE model; carrying Ω_m to the galactic cap is an added
    identification (visitor P4).
- `/coherence-function` "What x is" box (visitor P3, HIGH):
  - It said x = g_bar/a₀ and contradicted the page's own solver line. Checked against
    `explorer/scripts/compander_family_aic_bic_real_sparc.py`: every compander row solves g_bar = g_obs·μ(g_obs/a₀′).
    Only the McGaugh ν and ν-δ rows use g_bar.
  - Added the consequence: keyed on g_bar in the division wiring at γ = ½, the same function gives g_bar + 2a₀, not
    MOND (verified by hand).
- Landing scoreboard (visitor P2 HIGH, P3 HIGH):
  - The refutations cell is now state first: a plain one-liner ("six tests that could have shown it wrong; it lost
    all six"), roots, and "the six did not all test the same equation".
  - Two `<details>`: (a) a **realization-by-test table** (test → realization/variable/wiring → both numbers → root);
    (b) revision notes with the entire previous cell kept verbatim.
  - "Registered kill fired 3.3σ" is restated.
  - "Refuted… at the solar Oort limit" is removed from the density-law sentence. It contradicted the 09-09 joint window.
  - The audit cell now says **0 of 9** and names the auditors (LLM agents), marked instrument-uncalibrated, with the
    old cell in `<details>`.
  - "24 experiments" → "26 tests (24 registry + 2 later)".
- `/honest-assessment`: the "Oort limit" overstatement is fixed in the classification row. The GC-fork card gets the
  smoothing-length scope note (planets, RG §2.2.1, not a refutation).
- `/measurement-without-observers`: "MRH crossing is collapse — derived from γ" → "restated in MRH vocabulary, not
  derived". "Definite outcomes follow" → "asserted to follow". Added a choose-your-reading note (interpretive, or
  collapse-class facing CSL/Diósi–Penrose X-ray bounds and Fein+2019 interferometry).
- `ledger.ts` ROOTS_SENTENCE: the γ = 2 pin is now traceable ("RAR transition shape, unnumbered, closed 2026-05-21").
- `paths.ts`: First Encounter ~10 min; Beginner total ~40 min / ~1 h 55 m (sums checked by agent C); Test Roadmap label
  "26 tests: 24 in the original registry + 2 added later". Same count wording in `navigation.ts` and `layout.tsx`.
- `navigation.ts`: "Phase Boundary Visualizer" → "Crossover Regime Visualizer" (route unchanged).
- `site_lint.py`: +8 rules (44 total). Positive control: **7 hits on HEAD, 0 on the tree.** The first draft of the
  "preferred γ" rule missed the live HEAD wording ("empirically preferred &#x03B3;"); it was fixed after the control
  exposed that.

**Agent A (tools)**
- `/galaxy-plotter`:
  - The casual verdict now matches the chart (MOND and halos fit; the published equation does not; only the stand-in
    does).
  - The "All three fit" line is fixed.
  - A wiring note: violet = quadrature; ledger kills = division with the Ω_m floor. Every wiring fails DDO 154:
    ~1× / 3× / 1000× against ~10× needed.
  - V_flat is flagged as an input used twice. ρ_crit units: M☉/pc³ ⇒ 0.029 carries M☉ pc⁻³ (km/s)⁻². kpc is defined.
  - A tool-type / claim-status caption.
  - It also caught a stale "dotted violet… γ = 2" description of the stand-in.
- `/coherence-explorer`: **the visitor was right**. 0.25 and 0.375 are per-ln peaks. The per-decade values are 0.576 and
  0.863; the label is fixed. Added a γ = ½ preset and a note that presets are γ values, not physical systems.
- `/interactive-tools`: sequence labels unified. The chemistry card falsely claimed the null is shown "alongside the
  data"; corrected.
- `/gamma-calculator`: N_corr no-protocol note at the input.
- `/phase-boundary-visualizer`: H1 renamed and the missing badge added; "what to notice" at the slider.
- `/consciousness-demo`: "framework's empirically preferred γ" → data-selected γ ≈ ½, not the registered 2.
- `/chemistry-correlation-explorer`: Z-null warning at the table head.

**Agent B (A2ACW / for-researchers)**, verified by spot-checking the primary finding
- The record contradicted *both* pages.
  - The 2026-05-22 "control" was one model (Claude Opus 4.7) writing and self-scoring restatements with full knowledge
    of the answers.
  - The auditors behind 0 of 6 / 0 of 9 were LLM agents throughout. "External domain-expert audit" originated in an
    LLM visitor-persona log (05-12).
  - On the designed set (positive = reparametrization), the literal rule gives TP 3, FP 6, TN 0, J = 0. The published
    J = 0 used the framework's own demoted claims as positives, which is circular.
- `/for-researchers`:
  - A start-here box for the framework-independent negative results, with an acronym key.
  - The A2ACW current-state box: a shared control sentence, 0 of 9 plus "~47 candidates not yet individually audited",
    a "Who audited" bullet, and the designed-set confusion matrix under both rules.
  - Ω_m/Ω_b is now "≈ 6.4 (0.315/0.0493 = 6.39)".
- `/research-philosophy`:
  - The same shared sentence and the named auditors.
  - Removed "reliably catches reparametrizations" and "Challenger doesn't know the literature"; the record contradicts
    both.

**Agent C (tier-1 / test-catalog)**
- `/tier-1-existing`:
  - The status line is "Executed — 5: 4 kills + 1 underpowered". The six refutations are now traceable to IDs, including
    the unnumbered RAR shape.
  - A three-number block (prediction–data σ / registered-criterion margin / power) for each of TEST-04a, 09 and 10.
    - TEST-09: 3.29σ; criterion margin 0.86σ Gaussian or 1.2σ archive bootstrap.
    - TEST-04a: 2.14σ, Δχ² 3.06, delivered σ 4.4× the implied one. **All the visitor's numbers checked out.**
  - A new "Inherited MOND exposures" card: Cassini (counted) and wide binaries (pending, not counted).
  - 12 dated notes moved verbatim into Revision-notes `<details>`.
  - Agent C also noticed that in TEST-10's *registered* mass range (< 10⁸ M☉) the max f_DM is 0.851 (B = 6.7), barely
    above the 6.40 cap. The galaxies that fail under both caps sit above the registered mass cut. Stated narrowly on the
    card; count unchanged.
- `/test-catalog`: "26 tests (24 in the original registry + 2 added later)". It deliberately does not say
  "pre-registered", because the page says none of the 24 was.

**Agent D (beginner path / glossary)**
- `/what-synchronism-is-not`: a plain 5-point box.
- HA "If you read nothing else" box: de-jargoned; numbers kept.
- `/why-synchronism`: superconductors explained plainly.
- `/first-encounter`: Parts A–G; ~10 min.
- `/core-idea`: N_corr gloss, plus "what sets γ physically? Nothing yet". This replaced a line calling N_corr "the
  physically measurable quantity", which contradicted the calculator page. The chemistry card's stale "Pending null
  model" became Null-Class.
- `terms.ts`: added root, construction check, fork, Null-Class, underpowered-as-registered, self-eliminating-or-tie,
  QFT, GR, CMB. The CHSH entry is narrowed to the three built constructions.

`next build` passes. `site_lint.py`: 0 live hits.

## Back-Annotations (Research Repo)
- `PREDICTIONS.md`: a correction note after the 09-14 Cardassian block. The 08-18 "no ε⁰ channel / no instability /
  no k-dependence" holds only without the Jeans term; the fluid local horn is pinned at |ε| ≲ 10⁻⁵; "power, not data"
  holds only on the mean-density reading; locality trilemma; TEST-26 must name its horn. (Explorer P1.)
- `PREDICTIONS.md`: a scope condition after the 09-09 note. "The Sun and the clusters do not close this sector" requires
  a smoothed reading; pointwise, the planets close it at O(1); RG's own D; not a refutation.
- `Research/proposals/de_locality_fork_perturbation_channel_factor_two_20260818.md`: the same correction, appended.
- `Research/proposals/test02_amplitude_is_knee_conditional_and_a2acw_positive_control_20260910.md`: auditor identity
  (LLM agents), the self-scored calibration, and J recomputed on the designed set.
- New proposal: the smoothing-length one (above).

## Topics Seeded for Explorer
- `smoothing-length-window-for-density-keyed-laws.md`: is [D_min, D_max] non-empty? GC upper bound, the Oort window
  with the Sun inside a 1 pc ball, whether later RG papers fixed D, and whether the MRH can be D. Cross-linked with the
  still-queued `mrh-smoothed-density-repair-untested-or-refuted.md`.
- `fit-lands-on-the-nesting-point.md`: do MOND mocks reproduce γ̂ = 0.489 ± 0.11? Framed as a framework-independent
  methodological law.
- `test09-under-the-cosmic-baryon-ceiling.md`: TEST-10 is floor-robust; TEST-09 was never re-run at Ω_m/Ω_b.

## Open Items (Deferred)
- **Status vocabulary as a single linted table** (P2 HIGH). The glossary gaps are filled. The structural fix is still
  open: every rendered badge label ∈ one table, enforced by a lint. Agent D found these inconsistencies:
  - "construction check" counted vs "not an executed refutation of the class" (HA ~1228);
  - "Underpowered" used as a badge on /for-researchers but defined as a status note;
  - /top-5-tests "self-eliminating";
  - /why-synchronism's superconductor card sits under "What Failed" with a Reparametrization badge.
  Existing topics `badge-vocabulary-*` already cover this. It needs a maintainer tool, not research.
- **State/history split beyond the landing cell and the Tier 1 cards.** /key-claims caveats and the HA scoreboard are
  still palimpsests. Topic `correction-palimpsest-rate.md` is still queued.
- **Breadcrumbs vs nav** (P2 medium). Category crumbs are deliberately unlinked (Breadcrumbs.tsx comment, 07-23/07-30).
  The visitor's ask needs a nav-level decision (dropdown groups), not a crumb tweak. Deferred.
- **Ω_m/Ω_b 6.39 vs 6.40.** Both are legitimate roundings (0.315/0.0493 vs 0.3153/0.0493). /for-researchers and the
  landing table now say "≈ 6.4". /falsifiability, /honest-assessment and /parameter-derivations still print 6.40.
- **Agent C's open items.**
  - TEST-02 card still carries the unfloored "+1.8×10⁴ %" and a "Gaia DR4 will re-open" milestone (flagged by the
    identically-null finding).
  - TEST-25 is marked pre-registered, possibly contradicting "0 Pre-registered & Completed".
  - TEST-10's "no candidate cosmic ratio supplies 13.7" is called a non-sequitur by the ceiling finding.
- **HA/footer "~47 demoted in-archive"** vs /for-researchers "~47 not yet individually audited": reconcile.
- Auto-linking first glossary use; Coherence Explorer physical presets (density axis has no units).
- The galaxy plotter has no division-wiring curve; a toggle would be the honest version.
- **→ dp (gated):**
  - TEST-26 must name its horn (explorer 09-14; now in PREDICTIONS).
  - Whether "declare the smoothing length" becomes a required field for density-keyed registrations.
  - Whether the post-cutoff A2ACW arm gets run. Pass 4's frame point: it is the cheapest high-information experiment
    on the project, and under H2 the site's most citable result would be about AI evaluation, not gravity.

## So what?
Two things moved, and neither is a site polish.

1. **A declared-parameter gap in the density branch.** "Local density" was never defined, and the planets make the
   definition decide the verdict at O(1). RG's authors had flagged it and postponed it for ten years. This is the first
   concrete, numeric job the MRH could do. If it can't, the density branch is carrying an undeclared free parameter.
2. **The audit headline was measured by the instrument whose calibration is the open question.** "0 of 9 survived
   audit" is now labelled as LLM-audited and uncalibrated, separate from the physics refutations, which don't depend on
   it. That makes the A2ACW post-cutoff arm the most decision-relevant experiment on the project, not a side
   methodology.

The visitor researcher also predicted the explorer's 09-14 DE result (|2γ−1| ~ 10⁻⁵) without seeing it. The feedback
loop is producing independent convergence, not only friction.
