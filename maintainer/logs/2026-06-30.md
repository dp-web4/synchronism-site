# Maintainer Session — 2026-06-30

## Feedback Reviewed

- **Visitor log 2026-06-30** — all four personas; full synthesis at end of log
- **Forum**: no new input
- **Explorer**: no new findings pending

---

## WAKE Phase

**Research direction signal — YES.**

Pass 4 (leading-edge LIV/quantum-foundations researcher) asked: "The dim-4 LIV exclusion appears to kill not just Synchronism but *any* discrete absolute-time substrate without an added custodial symmetry. Is that being written up as a general no-go? It's the one place the program produced something I'd referee favorably, and it isn't Synchronism-specific."

This is a real research signal, not just a site complaint. The CPSU 2004 theorem (Collins–Perez–Sudarsky–Gambini–Pullin) generalizes beyond Synchronism, and the site currently frames the dim-4 result as a *defeat for the framework* rather than a *transferable result about a class of theories*. The distinction matters for what gets cited.

**Proposal filed**: `../../Synchronism/Research/proposals/dim4_liv_absolute_time_general_nogo.md` — outlines the general form of the no-go, what this program adds beyond CPSU 2004, and frames the decision gate for dp.

---

## Changes Made (Site)

### P0 — γ honesty parity (flagged HIGH by 3 of 4 personas: Tech Writer, Grad Student, Researcher)

**Problem**: `/equation-walkthrough` is the first place a learner meets γ = 2/√N_corr, and it softens the formula to "a motivated choice, not derived" with no Audited-Negative badge and no sign-inversion note. `/gamma-calculator` delivers the full reductio. The honesty level depends on which door you enter, which is the site's core credibility risk.

**Fix** (`src/app/equation-walkthrough/page.tsx`):
- Added `import ValidationBadge` and `import Link`
- Updated Step 4 `key` to say "γ is Audited-Negative — sign inverted vs. mean-field physics; see γ Calculator"
- Updated Step 4 explanation to note "See the Audited-Negative honesty note below"
- Added a static red warning box (always visible, after the step navigator) with:
  - Audited-Negative badge
  - Sign-inversion explanation (1/√N is a width, not a rate — larger N_corr → flatter, not sharper)
  - CLT self-contradiction (N_corr counts correlated variables; CLT requires independent ones)
  - Link to γ Calculator for the full interactive BCS reductio
- Fixed Step 6 C≈0.50 reference: removed "consciousness threshold" framing, added explicit note that both C≈0.50 and C≈0.64 were empirically refuted at p<0.0001

### P1 — Routing (Pass 1 HIGH severity)

**Problem**: Pass 1 typed `/learn` (natural guess for the beginner path) and got a 404.

**Fix** (`next.config.mjs`):
- Added `/learn → /learning-paths` redirect
- Added `/coherence-calculator → /gamma-calculator` redirect (anticipating the other common guess)

### P1 — Dim-4 LIV as citable result, not defeat (Pass 4 HIGH severity)

**Problem**: The dim-4 radiative LIV exclusion on `/for-researchers` is framed as "one of the framework's channels, and it falls on the refutation side" — i.e., a defeat. A LIV specialist reads it as a black mark, not a result to cite.

**Fix** (`src/app/for-researchers/page.tsx`):
- Renamed Artifact 3 heading: "Dim-4 radiative LIV exclusion of absolute-time discrete substrates — a citable no-go" (was "The only non-MOND, non-Zurek channel")
- Added a "transferable result" box above the technical details, stating: any discrete absolute-time substrate without a custodial boost-symmetry generates c_μν ~ α/π at one loop (CPSU 2004); existing bounds 16–28 OOM; "this is the one result here a LIV/quantum-foundations referee could cite without hedging"
- Updated page metadata description to mention all three citable results

---

## Back-Annotations (Research Repo)

**New proposal**: `../../Synchronism/Research/proposals/dim4_liv_absolute_time_general_nogo.md`
- What: the dim-4 LIV exclusion as a general no-go (not just a Synchronism defeat); the CPSU 2004 theorem generalized to a triage criterion for absolute-time discrete substrates; escape routes quantified; decision gate for dp on whether to write an arXiv note
- Why: Pass 4's WAKE signal that the site's most citable result is being presented as a defeat rather than a transferable finding — the research repo needs to reflect the general-theorem framing

---

## Topics Seeded for Explorer

- **`citable-negative-results-index-page.md`** (MEDIUM) — should the site have a `/citable-results` or `/negative-results` landing page consolidating the three transferable nulls with prior-art anchors and precise citable claims? Content exists on `/for-researchers`; the question is discoverability and citation hygiene.

- **`dim4-liv-general-nogo-writeup-feasibility.md`** (HIGH if dp decides to pursue arXiv note) — is there a non-trivial contribution beyond CPSU 2004 that justifies a standalone paper? Key question: do contemporary discrete-substrate proposals address CPSU 2004, or is the triage criterion already standard practice the community has absorbed?

---

## Open Items (Deferred)

**Long-tail items not addressed this session** (from the running list):

1. *Badge taxonomy collapse* — honest-assessment, research-philosophy, glossary all define badges in parallel prose. Deferred: requires careful audit of all three versions to find drifts before collapsing.
2. *Glossary hover-tooltip promise* — "on any page" but not consistently applied. Deferred: large scope (content pages need markup throughout).
3. *Acronym first-use sweep* — PCI*, CRT, PRI on `/key-claims`. Deferred: low risk, low impact.
4. *Landing page equation before analogy* — equation on first screen bounces casual readers. This is a UX redesign (landing page is long and complex); deferred to a focused session.
5. *TEST-04a post-hoc + single-bin clarifier* on `/tier-1-existing`. Deferred: minor wording addition, addressed in session if time allows.
6. *Saturation step-function reading guide* — still incomplete across concept pages.
7. *Learning-path Prev/Next navigation*.
8. *ρ_crit → ρ_knee rename*.

---

## So What?

The session's load-bearing change is the γ honesty parity fix: three of four independent expert personas converged on the walkthrough as the site's most misleading page (the only place learners first meet γ, and the gentlest version of the truth). The static warning box now makes the Audited-Negative verdict visible regardless of whether the reader navigates to the calculator.

The WAKE finding matters more: the dim-4 LIV result has been treated as a defeat since it was computed in June, and today a specialist recognized it as the site's most citable output. The reframing on `/for-researchers` and the proposal to dp are the research-level contribution. Whether it becomes a paper is dp's call — but the framing shift (defeat → transferable result) is the right move regardless.
