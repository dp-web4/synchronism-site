# Explorer Session — 2026-05-11

## WAKE Phase

**Assumptions inherited from primer / topic queue.** Topic queue is heavily
weighted toward site-framing fixes and compander-family bake-offs. The two
HIGH-priority topics seeded today by the maintainer were
`wide-binary-xi-rho-functional-form` (time-sensitive vs Gaia DR4) and
`coherence-function-governing-equation` (AIC/BIC across compander family,
already partially covered by `hill-vs-tanh-baseline-artifact.md` and
`sigmoid-family-variation-decisive-test.md`). Memory pointed to a separate
topic — `c-cosmic-vs-c-galactic-identity-gap.md` — flagging a *specific
computable test*: re-run Session 107 with the framework's own C(rho)
substituted for the silent Omega_m(z) identification.

**What if the frame is wrong?** Compander-family BIC bake-offs answer "is
tanh privileged?" but the framework's most-cited cosmology prediction (Session
107's fsigma8 = 0.418) is generated by C_cosmic = Omega_m(z), which is *not*
the framework's coherence equation. Asking which compander wins on chemistry
data is testing a function the framework doesn't actually use at the scale
where it makes its loudest prediction. So the more decisive single experiment
is the cosmic-coherence substitution test, not the BIC family bake-off.

**Highest-info experiment now.** Replace `C_cosmic(z) = Omega_m(z)` with
`C_cosmic(z) = tanh(gamma * ln(rho_cosmic_ratio * (1+z)^3 + 1))` in Session
107's growth integration. Sweep rho_cosmic_ratio. Two possible outcomes both
informative: (a) consistent equation gives unphysical prediction → the
substitution is load-bearing; (b) consistent equation gives a plausible
prediction → the substitution was just calculation convenience. Either way,
the framework's "one equation" claim has a precise test.

**Falsification of current research posture.** If the framework's actual
coherence equation, applied consistently at cosmic scale, predicts something
close to DESI DR1's measured fsigma8 — then the substitution was harmless
and the dual-C bridge problem is overstated. (Conversely if it gives anything
unphysical, the cosmology arc was built on the substitution.)

## Topic(s) Explored

- **c-cosmic-vs-c-galactic-identity-gap**: Ran Session 107's growth
  integration verbatim (`Synchronism/simulations/session107_desi_forecasts.py`),
  reproduced the published fsigma8(z=0.51) = 0.418 to 4 decimal places, then
  re-ran the integration with `C_cosmic` computed from the framework's own
  tanh-log equation instead of the silent Omega_m(z) substitution.

## Key Findings

- **`c-cosmic-substitution-load-bearing.md`**: Session 107's flagship
  prediction collapses in both directions when C_cosmic is computed
  consistently from C(rho). Same-density limit (rho_cos = rho_gal_ref)
  gives G_rat ≡ 1 → no growth suppression → fsigma8 = 0.444 (basically
  LCDM, only sigma8 differs). Realistic cosmic/galactic density ratios
  (1e-3 to 1e-5) give fsigma8 = 0.04 to 0.18 → structure growth shut off,
  refuted by DESI DR1 at 8-10 sigma. The 0.418 prediction exists *only*
  because Session 107 silently substitutes the LCDM Omega_m(z) form for
  "cosmic coherence." The cosmic ratio that would reproduce 0.418
  "consistently" is rho_cos/rho_gal ~ 0.48 — physically absurd. This is
  the **fourth** independent instance of the dual-C bridge problem and
  the most consequential one: it's the framework's most-cited cosmology
  prediction.

## NotebookLM Usage

- Not used this session (status not checked; computational task did not
  require multi-source synthesis).

## Rabbit Holes (Worth Revisiting)

- **Session 101 verification.** Session 107's comment says "C_cosmic
  derived in Session #101." Does Session 101 actually derive C_cosmic =
  Omega_m(z), or just assert the identification? Resolving this is the
  difference between a real bridge from coherence to LCDM (positive
  result) and a hidden substitution (negative result).
- **The 0.177 calibration.** ratio_0 = 0.177 is itself a tuning — set
  to make C_galactic(0) = Omega_m. Two free parameters (ratio_0 +
  substitution form) for one observable. Worth tracing whether the
  same dual tuning appears elsewhere (galactic rotation, BAO, void
  statistics).
- **Pattern check on other Tier-1 predictions.** If C_cosmic = Omega_m(z)
  substitution is the *mechanism* behind Session 107, what substitutions
  power the other Tier-1 predictions? Spot-check the SPARC and BAO arcs
  for analogous dual-form gaps.

## Self-Seeded Questions

1. Does ANY density assignment recover Session 107's 0.418 under consistent
   C(rho)? Answer from sweep: only ratios in (0.05, 0.10) work, and they
   correspond to cosmic ≈ 50% of galactic — nonphysical.
2. If the framework retracts the cosmology arc, what is left to predict
   on cosmic scales? The honest answer appears to be: nothing without a
   substitution.
3. Is "compander class" a more honest framing than "one equation"? Today's
   result, combined with the dual-C catalog (now 4 instances), suggests
   the front-of-site framing must change. The "one equation" claim is
   technically false at every domain boundary.

## So What?

This advances discovery. The 2026-05-09 finding identified the silent
substitution; today's finding shows it is **load-bearing** — without it,
the prediction either vanishes (same-density limit) or becomes
unphysical (realistic ratio). The cosmology arc cannot be repaired by
adjusting the suppression sign or magnitude; the failure is at the
substitution layer, one level upstream of the sign mismatch DESI saw.

The cleanest reframe is position (c) from the topic file: withdraw the
Session 107 cosmology arc and rebuild on a mechanism that does not
require the dual-form gap. Failing that, the framework owes the field
an explicit derivation of why Omega_m(z) is the right C_cosmic — or an
acknowledgment that no coherence function for cosmic-scale rho exists.
