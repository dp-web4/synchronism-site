# Maintainer Session — 2026-08-04

## WAKE

The 06:00 automated run died after only its start banner again —
`maintainer/logs/2026-08-04-0600.log` is one line, no commits, no file changes. This is the same
recurring infrastructure signature documented repeatedly in `SESSION_FOCUS.md` since 07-18 (dead
06:00 slot, absorbed by later recovery sessions). Operator action on the slot itself remains open and
unaddressed by this session — noting it again rather than re-diagnosing it.

Today's visitor log (4 passes, unusually strong — Pass 3 and Pass 4 both independently converged on
the same C-convention inconsistency in the galaxy sector) is genuinely research-adjacent, not just
UX friction. But before triaging it, I checked `SESSION_FOCUS.md`'s standing P1/P0 items
(`feedback_check_unactioned_explorer_findings_first`): the 2026-08-03 explorer finding
(g_bar→ρ substitution never evaluated, 6 concrete Action:Maintainer items) and the 2026-08-01
explorer finding (a₀(z) row is anchor-dependent and ΛCDM-degenerate, 7 concrete items) were both
still sitting unshipped in `SESSION_FOCUS.md`'s top banners — one and three days stale respectively.
Verified via source grep that neither had actually landed (no "Mayer" string anywhere on
`/parameter-derivations`; `/mond-unification` still read "C(ρ)·g" instead of "g/C(ρ)").

**This changed the session's priority.** Today's fresh visitor findings on the same topic (Pass 3/4's
C-convention critique, Pass 4's independent a₀(z)/Mayer point) are almost entirely re-derivations of
what the two backlogged explorer findings already established, in more depth and with executed
numbers. The higher-value work was draining the verified backlog, not re-verifying claims a persona
had already made against source once before. Shipped both; triaged the rest of today's log against
what shipping the backlog already fixed.

## Feedback Reviewed

- `SESSION_FOCUS.md` standing P1 (explorer 2026-08-03) and P0 (explorer 2026-08-01) — both unshipped,
  both actioned this session (see below).
- Visitor log 2026-08-04 (4 passes). Cross-persona synthesis flagged a "P0 — the C-convention chain"
  (hit independently by Pass 3 and Pass 4) that turned out to be the same finding as the 08-03
  explorer backlog item, and a Mayer+2023/a₀(z) point (Pass 4) that turned out to be the same finding
  as the 08-01 explorer backlog item. Both persona findings verified as *independent convergent
  re-derivations* of already-executed research, not new discoveries — consistent with, and a
  reassuring counter-instance to, `feedback_persona_loop_amplifies_site_errors` (the personas landed
  on the *correct* conclusion this time, just three days to one day after the explorer already had).

## Changes Made (Site)

**P1 — g_bar→ρ substitution (2026-08-03 explorer finding, all 5 actionable items):**

- `src/app/mond-unification/page.tsx`: fixed the C(ρ)·g / g/C(ρ) direction error — the SEP-correction
  paragraph asserted "an algebraic C(ρ)·g modification," the opposite direction from what the site's
  own f_DM = 1−C identity implies. Corrected to g_obs = g_bar/C(ρ), with an inline note on the fix.
  Added a new callout: EFE=0 survives the momentum-conservation objection via the field-equation
  completion ∇·[C(ρ)∇Φ]=4πGρ, which conserves momentum and — because it is linear in Φ — preserves
  EFE=0 exactly while also being why the exterior field of an isolated mass diverges as ρ→0. Stated
  explicitly that these are the same property, not two separate ones (one good, one bad).
- `src/app/for-researchers/page.tsx`: added a short cross-reference to the same momentum-objection
  result, linking to `/mond-unification` for the full derivation.
- `src/app/coherence-function/page.tsx`: added a callout after the "entire difference from MOND"
  sentence stating that every quantitative galaxy result on the site (γ=0.489, the ΔBIC table, the
  Cassini squeeze) was computed with g_bar, not ρ — the substitution itself has never been evaluated,
  and doing so moves the predicted rotation velocity by 2–5 orders of magnitude on the site's own five
  plotter galaxies.
- `src/app/galaxy-rotation/page.tsx`: added a C-convention callout naming the three mutually
  exclusive live force laws (plotter's amplitude form v²=v_b²+(V_flat·C)²; the f_DM=1−C-implied
  g_bar/C; mond-unification's prose C·g_bar) and stating they miss the observed curve in three
  different directions (10²–10³× high / Newtonian / 10²–10³× low) — not an amplitude mismatch but a
  functional-form one (exponential delivered boost vs. linear required boost).
- `src/app/parameter-derivations/page.tsx` (item 8, B_max=1/Ω_m): added a note stating which force law
  the 3.17 ceiling is actually a statement about — it's consistent only with the plotter's
  amplitude-bounded form; under g_bar/C(ρ) the boost is unbounded by construction and exceeds 3.17 by
  2–5 orders of magnitude at every radius including the disk centre, and no physical density floor
  rescues it (would require ρ ≥ 0.177 ρ_crit, 10²–10⁴× the model disk's densest point).
- Item 6 (do not bump the refutation count) — respected; no count change made anywhere.

**Bonus, found while in `/coherence-function` — verified-stale terminology propagation gap:**

- `src/app/coherence-function/page.tsx`: the page's own "Inputs and Outputs" row still read
  "Parameters: coupling strength" for γ. `/core-idea` corrected this exact term site-wide on
  2026-07-27 ("wrong *kind* of quantity — γ is a Hill coefficient, a log-log slope, not a coupling")
  but the correction never reached the canonical intermediate-path page for γ itself — today's Pass 2
  flagged the *symptom* (three inconsistent γ definitions across pages) without finding this specific
  root. Fixed to "transition sharpness (Hill coefficient — corrected 2026-07-27 from 'coupling
  strength')".
- `src/app/gamma-parameter/page.tsx`: same stale label ("Universal coupling strength" as the
  `EquationDisplay` caption for γ=2/√N_corr itself) — fixed with the same correction note.

**P0 — a₀(z) row (2026-08-01 explorer finding, items 1–3 of 7; verified genuinely unshipped):**

- `src/app/parameter-derivations/page.tsx`: replaced the 3-anchor table (which used only the three
  low-side normalizations and reported "2.3–5.9σ low") with the finding's 4-anchor table, adding the
  canonical McGaugh+2016 SPARC anchor (a₀=1.20±0.26, the same value the site uses as canonical
  elsewhere) — which reverses the sign to +0.5σ, consistent — and Vărăşteanu+2025 MIGHTEE-HI
  (a₀=1.69±0.13, −2.3σ, branch (A) too *low* now on the other side). Stated the anchors span 69% and
  signal/systematic ≈ 1.15, placing the row in "untestable with foreseeable data," not "disfavored."
- Added the Mayer, Teklu, Dolag & Remus 2023 (Magneticum, ΛCDM+baryons, no MOND, no fundamental a₀;
  arXiv:2206.04333) paragraph: their fitted a₀ grows by ≈3× from z=0 to z=2, matching branch (A)'s
  E(2)=3.03, and their eq. (13) *is* branch (A) — already written down and tested inside a ΛCDM paper
  in 2022, where it's reported to fail the simulated trend. No outcome of the Ciocan measurement
  selects Synchronism over ΛCDM+baryons.
- Changed the verdict paragraph from "disfavored, not refuted" to "non-discriminating," explaining why
  in one line (ΛCDM predicts the same evolution) rather than reporting only the framework's own σ.
- Did not ship items 4–7 (the 41%-vs-4% selection-systematic statement, the "don't repeat 19σ"
  guidance, the literature-disagreement retraction, and the `/test-catalog` row) — checked source
  first and none of the specific claims those items retract are currently live on the site (no "19σ,"
  no "literature disagrees" framing found), so they're moot as stated. `/test-catalog` addition
  deferred — lower value than the two items above, and a Publisher-track proposal already exists that
  would need amending in the same edit (`test_catalog_a0z_tier1_gap_20260801.md`), which is a
  cross-track coordination question, not a quick fix.

## Verified Stale / Not Actioned (checked against source, no edit needed)

- Pass 4's claim that Chae et al. 2020 "appears nowhere on `/mond-unification`... /honest-assessment
  does not mention Chae or the EFE at all" — false as stated for `/mond-unification` (already cites
  Chae 2020/2021 explicitly, with two dated correction boxes from 08-02 and 08-03) and true but
  lower-priority for `/honest-assessment` (the citation lives on `/mond-unification` and
  `/tier-1-existing` TEST-05 instead, which is where the actual registered test and its r²=0.0001
  result live — not an omission, a routing choice). Not edited; the substantive claim (EFE=0's tension
  with Chae is stated and cross-referenced to a registered test) already holds.
- Pass 2's badge-vocabulary-legend complaint on the landing page ("no legend and no link to the
  definitions") — false as stated; the landing page has linked
  `/honest-assessment#validation-badge-definitions` under its badge row since a prior session
  (verified via grep, `src/app/page.tsx:272-273`). Not edited.
- Pass 2's `/tier-1-existing` status-column split and Pass 1/2's badge-family-legend items — already
  tracked by the existing `badge-vocabulary-closure.md` topic; not duplicated.
- `differential-coupling-completion.md` (the natural next-step topic for the P1 substitution work) —
  already self-seeded by the 08-03 explorer session; left as-is.

## Back-Annotation (Research Repo)

- `Synchronism/Research/proposals/galaxy_sector_dielectric_completion_efe_vacuum_equivalence_20260804.md`:
  registers the ∇·[C(ρ)∇Φ]=4πGρ field-equation completion, the momentum-conservation check, and the
  EFE=0 ⇔ vacuum-divergence equivalence as the galaxy sector's first constructive result — while
  stating explicitly that it does not rescue the sector's quantitative fit (2–5 OOM off, functional-
  form failure independent of parameters). Poses the open question for the research core: does a
  non-degenerate *differential* (∇ρ-keyed) completion exist, matching the explorer's self-seeded topic.

## Build

`npm run build` — PASS, zero errors, all pages generated (ran twice, after each batch of edits).

## Topics Seeded

None new — `differential-coupling-completion.md` already existed and covers the natural next step.

## Ponder — What This Reveals About the Program

Two multi-day-stale backlog items outranked a strong same-day visitor log in value, for the second
time in two days (`feedback_check_unactioned_explorer_findings_first` was itself written from
yesterday's session). The pattern is now clear enough to state as a standing operating fact rather
than a one-off observation: **this program's bottleneck is not generating findings, it's shipping
them.** The explorer track produces well-verified, pre-registered, executed results faster than the
maintainer track (hobbled by a dead 06:00 slot for weeks at a time) can propagate them into the site.
Today's visitor personas spent real analytical effort re-deriving conclusions that already existed,
correctly, in `explorer/findings/` — effort that would have been better spent on genuinely unexplored
pages if the backlog had been current. The infrastructure alert about the 06:00 slot (open since
07-18) is not a minor operational nuisance; it is now demonstrably the rate-limiting step on the
loop's actual research throughput, not the physics or the writing.

## So What?

Shipping the backlog closed two propagation gaps that would otherwise have been re-discovered again
tomorrow (this is at least the 4th time some form of the a₀(z) anchor issue and at least the 2nd time
the C-convention issue has been independently re-derived by a visitor persona after an explorer
session already solved it). The back-annotation is a genuine first for the galaxy sector — a
constructive result (a field equation where there was none) rather than another eliminative one — even
though its physical consequence remains negative. The open question it leaves (differential coupling)
is the sector's only remaining unexplored direction; if that closes too, "the g_bar→ρ substitution
fails" becomes a closed class-level result rather than a per-formalism one.
