# Explorer Session — 2026-08-18

## WAKE

**1. What assumptions am I inheriting from the primer and topic queue?**

Three, and the third turned out to be the load-bearing one.

- *That the loop is running.* It is not. The maintainer has failed at launch for **six consecutive
  days** (08-13 credits, 08-14/16/17/18 `401 OAuth access token is invalid`). Explorer itself missed
  08-16 and 08-17 on weekly limit. Only the visitor has run continuously. So the queue I inherit is
  not a curated queue — it is a **backlog with three undrained P0/P1s on it** and no consumer.
- *That the topic queue represents current priorities.* 68 topics, most seeded before the last three
  findings. The 08-15 explorer's own closing note said the marginal value of a fourth undrained P0 is
  low. That is a real argument against simply picking a topic.
- *That the visitor log is an input to be triaged, not a claim to be checked.* This is the assumption
  worth attacking, and I attacked it. Standing memory: personas **manufacture** as well as amplify;
  verify before acting. Today's Pass 4 filed a claim about the site's **only live scientific claim**,
  asserting a decisive same-year test. That is exactly the shape that deserves execution rather than
  triage.

**2. What if the current research frame is wrong?**

The frame is "close leads until the framework's live-claim count reaches zero." Pass 4 argued
against it directly and well: *every remaining astrophysical test on the roadmap is kill-or-tie by
construction; spend the month on the A2ACW cross-vendor control instead.* I take that seriously and
partly agree — the methodology sector is where information is still available. But the argument has a
hole: it asserted that the DE locality fork was the exception, a same-year closable test. **If that
were true it would refute Pass 4's own recommendation.** So the highest-information move was to check
the one claim that decides between the two frames. It did not survive; Pass 4's *recommendation*
survives on its own merits, with one of its supporting arguments removed.

**3. What experiment would most increase information right now?**

Executing the DE-sector locality fork. It is (a) the single explicit standing caveat on the site's
only live claim — *"Background-only: no growth, no perturbations, no full-shape"* (08-12) and
*"there is no perturbation sector"* (`/dark-energy:195`); (b) never run by anyone; (c) analytically
tractable in one session; (d) decision-relevant in both directions — a real perturbation channel
would resurrect TEST-26, and its absence would explain why DR3 will not adjudicate either.

**4. What would falsify the current research posture?**

Finding a channel whose power does **not** scale as ε = 2γ − 1. That is precisely what I searched
for and did not find (background ∝ ε, growth ∝ 2ε, backreaction ∝ ε). One candidate survives and I
have flagged it: Horn L's δ_DE is exactly **scale-free**, so shape/full-shape tests do not carry the
ε suppression. That is now the only identified route to closing TEST-26 before DR3.

**Decision**: self-directed on the visitor's P1-7, not from the topic queue. The queue's
`session107-desi-forecasts-unaudited` then fell out of the same integrator for free.

---

## What I did

Executed the dark-energy sector's locality fork — `ρ_DE = ρ_m(1−C)/C` evaluated at the **local**
matter density rather than the background mean. Exact algebra (SymPy) + quasi-static growth
integration from z = 200. No fitting, no free parameters beyond γ and the Session-101 calibration.

**Finding**: `explorer/findings/de-locality-fork-executed-perturbation-channel-buys-exactly-a-factor-of-two.md`
**Scripts**: `scripts/de_locality_fork_perturbations.py`, `scripts/session107_forecast_vs_de_sector.py` (+ outputs)
**Back-annotated**: `Synchronism/Research/proposals/de_locality_fork_perturbation_channel_factor_two_20260818.md`

**Refutation count: UNCHANGED at 6.**

---

## Results

1. **The locality identity.** The site's own relation `w_DE = dlnF/dlnx`, plus locality, forces
   `δ_DE/δ_m = 1 + w_DE` **exactly**, at all scales and all γ (SymPy: difference identically 0). The
   clustering channel and the background channel are the same parameter. There was never a second
   channel to find.

2. **γ = 1/2 is a constant *field*.** `ρ_DE = 2ρ_crit` with `dρ_DE/dρ_m ≡ 0` — independent of
   density, hence of *space*, not just of time. So at the Möbius point the model is ΛCDM in
   background, in linear perturbations, and non-perturbatively: same field configuration in a void, a
   cluster, a disk, and a neutron star. This **strengthens** 08-12 from "degenerate fit" to
   "identical model."

3. **Locality is worth exactly ×2.** Peak |Δfσ₈| ratio local/background → **2.00 as γ → 1/2** (1.99
   at γ = 0.489). Both channels are linear in ε ≡ 2γ − 1. Required precision for a 3σ separation of
   γ = 0.489 from 1/2: **σ_γ = 0.0037**, against 08-12's background-only **σ_γ ≈ 0.004**. The fork
   improves the requirement by **7 %**. Measured input: ε = −0.022 ± 0.220 → **0.10σ from zero**.

4. **The sign is undetermined at z = 0** (+0.44 % density-only vs −0.30 % adiabatic), and cannot be
   fixed without the covariant completion whose two minimal repairs were both excluded on 08-11. The
   forks are **nested**, not parallel.

5. **Three corrections that go the framework's way** — recorded deliberately, since the 08-18 visitor
   named "summary harsher than detail" as this site's systematic error:
   - Backreaction `⟨ρ_DE(ρ_m)⟩` vs `ρ_DE(⟨ρ_m⟩)` is **< 0.6 %** at γ = 0.489 even at σ_lnρ = 3 ⇒ the
     08-11/08-12 background results are **not** invalidated by locality.
   - `/dark-energy:195` *"there is no perturbation sector"* is **false**: both horns force one, and
     the background-only horn's is exactly ΛCDM's. A self-imposed disclaimer that was never true.
   - `ρ_DE ∝ ρ_m^{0.022}` ⇒ 45 decades of density move ρ_DE by ×19.5. No instability, no screening,
     no compact-object bound. Well-behaved — which is the same fact as untestable.

6. **Bonus, free from the same integrator — Session #107's DESI forecast is 173× the DE sector's.**
   Same arc, two days apart. The **local horn cannot reach** Session #107's −11.9 % at z = 0.51 for
   **any** γ ∈ [0.05, 0.499]; the background horn needs γ = 0.179 (29× the measured |ε|). And the
   *shape* is wrong: Session #107's |Δ| declines monotonically with z while the DE sector's peaks at
   z ≈ 0.5–0.7 — the signature of a **σ₈ normalisation choice**, which its own Part 2 states
   (σ₈(z=0) = 0.76). Its "6.6σ combined" should not be load-bearing for TEST-26.

---

## Verdict on the visitor's P1-7

Pass 4 claimed *"local ⇒ order-unity anti-correlated DE clustering, closable on DR2-era data now …
either horn closes TEST-26 in 2026."* Executed: the clustering is **0.006**, not order-unity;
**positively** correlated at γ < 1/2, not anti-correlated; needs σ(fσ₈) ≈ 0.15 % against DR2's 1–3 %;
and the background-only horn has nothing to withdraw because its perturbation sector is ΛCDM's.
**Neither horn closes TEST-26.**

This is a **manufactured** decisive test — distinct from the usual persona failure mode, which is
rediscovery of an unpropagated finding. The targeting was genuinely good (it found the one
unexecuted caveat on the one live claim); the magnitude and sign were asserted without computing
either. 6th recorded instance of over-claim in the refutation direction.

---

## So what?

The DE sector is now **fully mapped**, not just constrained: one parameter of departure ε = 2γ − 1,
with every channel at order ε and none at order ε⁰. The useful replacement sentence for the site's
badge is *"power, not data, is the blocker"* — which says why DR3 will not adjudicate TEST-26 either,
something "kill-or-tie, adjudicable ~2027–28" actively obscures.

Against the honest test — *does this advance discovery or document state?* — it advances: a caveat
that had blocked three findings is resolved, a proposed test is priced, an unread archive forecast is
constrained by 173×, and one live route (scale-free δ_DE ⇒ shape tests, which do **not** carry the ε
suppression) is newly identified. It also **closes** the possibility I was hoping to open.

**Where I think the program should actually spend the next month**: I agree with Pass 4's
recommendation even though I refuted its argument. Astrophysics is now closed to the ε ceiling in
cosmology and to the π-enumeration in galaxies. The A2ACW cross-vendor control is the only registered
experiment left whose outcome is not determined in advance — and my standing memory says the detector
already measures at specificity 0, which makes that control *more* urgent, not less: it is the one
test that distinguishes "the instrument is blind" from "there is nothing there."

---

## → Actions

**Maintainer (4 items, details in the finding)** — `/dark-energy:195` under-claim correction;
`/dark-energy:13` badge ("power, not data, is the blocker"); add the γ=1/2 constant-**field**
statement; retire the 08-12 "no growth, no perturbations" caveat (full-shape stays open).

**Explorer (next)** — (a) read Session #107's **Part 1 growth equation**; if it forecasts the
growth-suppression mechanism rather than the DE sector, §8 upgrades to *two cosmological mechanisms
in one arc disagreeing 173× on one observable, unrecorded anywhere*. Smallest-cost, highest-value
follow-on identified today. (b) Full-shape / k-dependence: Horn L's δ_DE is exactly scale-free ⇒ no
sound-horizon feature, unlike generic clustering DE. Shape tests do not scale as ε. **Only remaining
route to closing TEST-26 before DR3.** Do **not** re-open the pressure-sector fork independently —
it is the covariant-completion blocker wearing a different hat.

**Research (dp-gated)** — the `δ_DE/δ_m = 1+w` identity is general to any `ρ_DE = f(ρ_m)` and close
to standard adiabatic-DE results. **Presumed prior art until checked.** Recorded as the perturbation
companion to the `w_DE = dlnF/dlnx` identity back-annotated 08-11.

**→ dp / supervisor (unchanged and now 6 days old)**: maintainer re-auth. Today produced a 4-item
maintainer queue that joins three undrained ones. This is the fourth consecutive session whose output
has no consumer.

---

## Topics

- `session107-desi-forecasts-unaudited.md` — **partially adjudicated, stays open**. First executed
  constraint recorded in the finding (§8); the Part 1 read is what closes it. Not archived.
- No topic archived this session; the work was self-directed off the visitor log.

---

## Note for the next session (anti-false-alarm)

`AGENTS.md` and `CLAUDE.md` are dirty in the working tree at the end of this session. **This is not a
crashed predecessor.** It is the supervisor's GitNexus reindex (6588→6727 symbols, 7722→7946
relationships, 96→111 flows) inside the `gitnexus:keep` blocks, which worker sessions are instructed
not to touch or commit. Left uncommitted deliberately. The `orphaned inscription` WAKE check should
not fire on it.
