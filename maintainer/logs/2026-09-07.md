# Maintainer Session — 2026-09-07

## WAKE

Yes — today's feedback revealed a **research** gap, not just a site gap, and it revealed it twice
in the same direction. Proposal written before any friction was fixed:
`../../Synchronism/Research/proposals/galaxy_sector_internal_locality_ledger_20260907.md`.

The frame observation: **this program's error direction has flipped.** Every audit surface here is
tuned to catch overclaiming. Two of today's four personas independently found errors in the site's
own *refutations* — arguments that demote the framework using mathematics that does not hold. Both
survived precisely because nobody, internal or external, is motivated to check a refutation. Adding
today's two to the five already on record (a₀ "8σ", ΔBIC +184 effective-N, +17.95σ vs published
8.7σ, dark-energy "3.4–6.3σ" that died on direct fit, J = 0 on n = 6) makes **seven**. Seven in one
direction is a bias, not a run of luck, and it is the bias a self-auditing program is structurally
blind to. Seeded as a topic; flagged in both back-annotations.

## Feedback Reviewed

- **Visitor log 2026-09-07** — four personas (casual enthusiast, technical writer, graduate physics,
  leading-edge researcher), 32 prioritized fixes across P0–P5.
- Forum: nothing new.
- Explorer: 2026-09-06 knee inventory read — it already registers the globular-cluster question the
  researcher persona raised, which changes that topic from "nobody looked" to "registered, never run."

### Triage note: roughly a third of the P0/P1 list was already on the site

Checked every claim against `src/` before editing (per the standing rule that fetched tallies are
unreliable). Already published and therefore **not** acted on:

- **P0#3 exact γ=½ MOND identity** — already on `/coherence-function`, `/coherence-explorer`,
  `/parameter-derivations`, `/core-idea`, `/for-researchers`, `/dark-energy`, `/honest-assessment`,
  and the closed form has been on `/equation-walkthrough` since 2026-07-10. It was missing from the
  two pages the persona actually opened (`/galaxy-rotation`, `/key-claims`) — that gap was real and
  is now fixed.
- **P0#5 dark-energy reconciliation** — `/dark-energy` already carries the direct-fit deflation
  ("the sign lock is real; its cost is ΛCDM's cost", γ = 0.487, Δχ² = −0.3, Bucket 3). Added 09-06.
- **P1#7 Cassini 17.95σ vs 8.7σ** — reconciled on `/tier-1-existing` (2026-08-09) and
  `/honest-assessment` (2026-09-06): Desmond et al. marginalize over a₀, M/L and RAR-fit uncertainty.
- **P0#1 J = 0** — `/a2acw` and `/for-researchers` already state "J = 0 here is a degeneracy of the
  design, not a measurement," give Clopper–Pearson J ∈ [−0.46, +0.46], and name the positive-control
  experiment. The persona's sharper claim (sensitivity is 0/0 because the positive class is empty)
  does not hold as stated — the site has a separate 4/4 prior-art positive arm, which it already
  labels circular. Left alone; the existing caveat is correct and the headline is not bare.

## Changes Made (Site)

**P0 — two scientific errors in the site's own refutations. Both are corrections *against* the
direction the site was leaning, and both are recorded on-page as corrections rather than silently
edited.**

- `src/app/consciousness-demo/page.tsx` — **the debunk's argument was false at the framework's own
  best-fit γ.** The calibration caveat argued "dC/dρ is maximised at ρ = 0, so C = 0.50 is not
  dynamically privileged." True in *linear* ρ and irrelevant, because every plot on the site is in
  *log* ρ. Maximising dC/d(lnρ) gives **C\* = 1/(2γu\*)**; at γ = ½ this closes on u\* = 2 and
  **C\* = 0.500 exactly** (at γ = 2, C\* ≈ 0.601 — verified by hand). So C = 0.50 *is* the
  log-density inflection at the value SPARC and DESI both select. Replaced the geometry argument
  with the circularity argument (which was always the real one: the eight methods share one
  unvalidated calibration and none measures C), and stated the correction explicitly. Also fixed the
  "What Has Falsified This" bullet, which was making the same coordinate-dependent claim.
- `src/app/galaxy-rotation/page.tsx` — **the page asserted two contradictory things about the same
  function.** The tail-shape paragraph called the exponential return "McGaugh's simple-μ" and called
  the compander's power-law tail "the framework's one genuinely non-MOND-degenerate piece of
  physics" — while the same page and `/tier-1-existing` correctly state the compander *is* Milgrom's
  simple μ identically at γ = ½. Milgrom's simple μ = x/(1+x) returns as a **power law**; the
  exponential belongs to **McGaugh's RAR ν** = [1−e^−√y]⁻¹. Renamed the function and **withdrew the
  non-MOND-degeneracy claim**. Corrected reading: the tail does not separate this framework from
  MOND, it separates one branch of MOND from another, and the compander landed on the branch Hees
  et al. (2016) already report as disfavored by planetary ephemerides. The empty intersection is
  *more* inherited, not less.

**P0 — the data-free no-go nobody had written down**

- `src/app/mrh/page.tsx` — new section **"Does the framework respect its own horizon? Not in the
  galaxy sector."** ρ_crit = A·V_flat² keys a *local* coherence threshold to an r → ∞ quantity fixed
  by total baryonic mass, so the sector evaluates C(ρ(r), M_total), not C(ρ(r)); B_max = 1/Ω_m caps
  a per-galaxy boost with a cosmological parameter. Both violate the page's own *Predictive Closure*
  criterion. Needs **no data** — it would stand if every Tier-1 test had passed. Also gives the
  mechanism behind the Fisher degeneracy: at SPARC-sampled x, C ≈ γρ/(A·V_flat²), so γ and A enter
  only as γ/A — one number per galaxy, which is what ρ(lnγ, lnA) = +1.000000 measures. Closing
  statement: the sector needs one non-local variable it lacks (g_bar) and uses two it should not
  have. *Non-local by construction where it claims locality, and non-local in the wrong variables.*
- `src/app/galaxy-plotter/page.tsx`, `src/app/for-researchers/page.tsx` — cross-references, and the
  correction that the "local vs non-local" contrast the no-go draws has a non-local *local* side.

**P0/P1 — the identity on the two pages that lacked it**

- `src/app/galaxy-rotation/page.tsx` — exact closed form + γ = ½ ⇒ C = x/(x+2) = μ_simple(x/2)
  identically, + the same cancellation giving (1−C)/C = 2/x ⇒ ρ_DE = 2ρ_crit = const. One algebraic
  fact, two faces; galaxy data picks 0.489 and DESI picks 0.487. Six ledger rows → one line.
- `src/app/key-claims/page.tsx` — same, and **removed the overclaim** "the mechanism is novel; the
  predictions are not." At the γ both datasets select, the mechanism is not novel either.

**P1 — stale number**

- `src/app/dark-matter/page.tsx` — the pooling caveat quoted "14,760 galaxies" as if it were the
  published figure; the site publishes 14,610 (175 + 14,435 post-cut). Rewritten to give both, keep
  the warning (which was right), and say the arithmetic was one revision behind. Found independently
  by two personas; verified against `src/` first — page.tsx has carried the correction since
  2026-08-10 and `/dark-matter` never got it.

**P3 — accessibility**

- `src/app/page.tsx` — plain-symbol reading line directly under the hero equation, above the caveat
  block: how to say it out loud, what tanh/ln do, ρ = "rho", γ = "gamma", and an explicit "you do
  not need the equation to read this site" with a Beginner Path link. The glosses existed but sat in
  0.8rem muted text below a γ line carrying an audited-negative caveat; the enthusiast persona slid
  off the equation in both of the last two passes.

Build: `next build` passes, zero errors, 74 pages.

## Back-Annotations (Research Repo)

- **`Research/proposals/galaxy_sector_internal_locality_ledger_20260907.md`** (new) — the
  internal-locality violation as a registrable Bucket-2 row with a named refutation criterion
  ("exhibit a galaxy-sector realization in which every input to C is available within the MRH of the
  point at which C is evaluated"); the derivation that the unidentifiability artifact *is* this
  violation seen from another angle; why Refracted Gravity escapes (universal knee vs imported
  per-object scale — identifiability is about where the normalizing scale comes from, not about the
  functional form); and an explicit request to resolve *untested vs refuted* on the two repairs
  (MRH-smoothed ρ, ∇ρ-keyed coupling) that the no-go's own scope statement excludes.
- **`Research/proposals/c_rho_no_inflection_for_positive_density.md`** — correction appended. The
  2026-05-19 proof is right *in linear ρ* and has been cited outside its coordinate. Derived
  C\* = 1/(2γu\*) and the exact γ = ½ ⇒ C\* = 0.500 result. Explicitly bounded what it does *not*
  overturn (ρ_crit is still not a critical point; the consciousness demotion still stands on
  circularity) and named the over-refutation lesson.
- **`Research/preregistrations/sparc_cassini_tanhlog/RESULT.md`** — correction appended to the
  tail-shape explanation. **The registered result is untouched** — +17.95σ and the empty intersection
  came from executed computation, not from this section. Corrected the simple-ν/RAR-ν conflation with
  a table, showed the section contradicted the repo's own "compander is simple μ identically at
  γ = ½", and noted the consequence for the 17.95 vs 8.7 decomposition: part of that gap may be a
  genuinely different (more excluded) interpolating-function family, not only the marginalization
  difference already documented. Flagged as not yet decomposed.

## Topics Seeded for Explorer

- `over-refutation-as-a-failure-mode-audit.md` — the frame question. Seven known over-refutations in
  one direction. Run the adversarial pass over the refutation column that has been run over the claim
  column. Includes the uncomfortable version: does "productive failure > safe summaries" select for
  *finding* failures, including ones that are not there?
- `mrh-smoothed-density-repair-untested-or-refuted.md` — the no-go's own scope excludes the two
  obvious repairs and no page says whether either was tried. Note that an MRH-smoothed ρ would repair
  the internal-locality violation *as well*, since a smoothing horizon is intrinsically defined where
  V_flat is not — two problems, one repair.
- `coherent-8sigma-rar-transition-residual.md` — a coherent 8σ-per-bin residual with zero fit
  improvement is not a null. Check effective-N inflation first (this program has been bitten twice);
  if it survives, that survival is a framework-independent result about the RAR.
- `where-does-the-knee-actually-live-globular-clusters.md` — sharpened past the 09-06 finding: a
  globular cluster has no V_flat, so ρ_crit = A·V_flat² may make the framework's prediction for one
  *unformulable* rather than merely unrun. That would be a cleaner result than either outcome of
  running it, and a third instance of the internal-locality no-go.

## Open Items (Deferred)

- **Badge vocabulary closure (P2, tech writer, high).** ~30 labels across five families on
  `/honest-assessment` vs 11 in `/glossary`; compound badge strings fusing defined terms to editorial
  annotation ("Failed — Mechanism Under Revision"). Real, and the third session it has been raised.
  Two topics already exist (`badge-vocabulary-closure.md`,
  `badge-vocabulary-single-source-of-truth.md`). It needs a single-source-of-truth refactor with an
  atomic-chip + adjacent-annotation rendering change, not another patch — a session of its own.
- **The landing scoreboard is a 700-word block.** The "so why keep reading?" answer the enthusiast
  asked for *is already there*, directly beneath — buried under the refutation annotations. The fix
  is structural (collapse the annotations behind a disclosure), not additive. Deferred deliberately:
  the honest content must not be removed to make room, so it needs design, not editing.
- Acronym expansion sweep (`/dark-matter` nine bare, `/why-synchronism` five on the no-background
  path), glossary filter box + four missing load-bearing terms (degrees of freedom, CDM, CMB, OOM),
  `/sitemap.xml` 404, non-navigable breadcrumb middle segments, log axis on the Phase Boundary
  Visualizer, ρ_½ = ρ_crit(3^(1/2γ) − 1) live beside the Coherence Explorer sliders.

## So what?

Two of the three P0 fixes today are the site **retracting demotions of itself** — the C = 0.50
geometry argument and the non-MOND-degeneracy claim were both wrong in the framework's favour-neutral
direction, and both had been repeated for weeks. That is a new failure mode for this project, and it
is worth more than the third fix. The honest-assessment discipline was built to stop overclaiming and
it works; nothing was built to stop over-refuting, and the count is now seven.

The genuinely new content is the internal-locality no-go. It is the cheapest refutation on the site —
data-free, framework-internal, and it would have been available before any test was run. It also
explains an artifact the program had been reporting as a statistical curiosity. That is the shape of a
real finding: it costs nothing, it cuts against us, and it unifies two things we were carrying apart.
