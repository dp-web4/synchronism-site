# Explorer Session — 2026-07-05

## WAKE

1. **Assumptions inherited.** The primer/memory frame says the physics is converged — "fixed point
   reached, 0 openings in 16 sessions" (2026-06-28), same-corpus auditing has ~0 marginal discovery
   value, and the only remaining work is *propagation completeness* + waiting for an OOD injection.
   Today's queue leads with `site-propagation-completeness-audit.md` (HIGH), a mechanizable
   site-vs-site cross-page sweep — squarely inside that frame.

2. **What if the frame is wrong?** The frame treats `PREDICTIONS.md` as a reliable anchor ("every
   site claim must be consistent with it"). But today's maintainer, while seeding that topic, found
   a citation defect *in the ledger itself* — Bucket 2 cites S438 for an a₀ "wrong sign" refutation
   that S438 doesn't contain. If the anchor has provenance defects, the frame's assumption that
   auditing is "just propagation" is too complacent: nobody has ever walked the ledger's *own*
   internal edges. That's a genuinely un-audited corpus, not more same-corpus re-auditing.

3. **Highest-information investigation.** Turn the citation-walk discipline (2/2 on site→archive:
   TEST-04a, CDM) *inward* on PREDICTIONS.md Bucket 2 — does each REFUTED row's cited session
   actually contain the refutation it's credited with? This tests the reliability of the document
   that governs every framing decision, on a corpus never checked.

4. **What would falsify the current posture?** If the ledger's Bucket 2 is provenance-solid, the
   "anchor is reliable" assumption holds and the aside is a one-off typo. If multiple rows are
   defective, the anchor needs the same walk it enforces downstream — and "the physics is cleanly
   closed" would be resting partly on mis-cited eliminations.

**Decision:** redirect from the broad site sweep to the ledger-internal citation walk. Higher
information, questions the frame, directly seeded by today's maintainer finding.

## Topic(s) Explored

- `site-propagation-completeness-audit.md` — **embedded aside only** (the a₀/S438 ledger-citation
  defect). Main-body site cross-page sweep left open; topic not archived.

## Key Findings

- `predictions-ledger-citation-walk-a0-row-misbucketed.md` — PREDICTIONS.md Bucket 2's a₀ row is a
  **three-way conflation**: wrong session (S438 = RC-RMS, unrelated), wrong failure phrasing ("wrong
  sign" is the γ=2/√N_corr result, S430/S437 — a₀ is a positive scalar with no sign), and wrong
  bucket (a₀=cH₀/(2π) reproduces Milgrom's coincidence → Bucket 3 reparametrization, not a
  tested-and-failed Bucket 2 refutation). **The ledger over-refutes; the site's a₀ framing is the
  correct one** — the rare direction (core wrong, site right). Rest of Bucket 2 is **12/13 clean**,
  so isolated, not systemic. Resolves the maintainer's open question: the site does *not* under-state
  the a₀ failure — the ledger over-states it.

## Back-Annotations (Research Repo)

- `Synchronism/Research/proposals/predictions_ledger_a0_row_misbucketed_20260705.md` — P0 bucket
  move with exact edit text: remove the bad Bucket 2 a₀ row, add a₀ to Bucket 3 (cite S217/S201/S380),
  give the γ "wrong sign" result its own clean Bucket 2 row (cite S430/S437). Not edited directly —
  a bucket move to the anchor doc is stewardship territory; flagged for the maintainer with
  ready-to-lift text.

## NotebookLM Usage

- Not used — this was archive-internal provenance verification (grep + file reads), not multi-source
  synthesis.

## Rabbit Holes (Worth Revisiting)

- **Walk Bucket 1 and Bucket 3 citations too.** Only Bucket 2 was checked. Base rate looks good
  (92%) but the one defect sat on the *first row* undetected indefinitely.
- **Mechanize the ledger-internal walk.** ~20-line script: parse each `| … | S### |` row, resolve
  the citation, grep the file for the row's own keywords, flag mismatches. The archive-internal twin
  of the maintainer's proposed site-propagation script — same discipline, different corpus.
- **The broad site cross-page sweep** (the topic's actual main body) is still unrun.

## Self-Seeded Questions

- The anti-oscillation device is itself a document with citations, and citations rot. Should
  PREDICTIONS.md carry a self-referential discipline — "this ledger's own citations get walked on
  every edit" — the way it demands the README/STATUS stay pinned to it?
- Every propagation defect logged to date runs site-over-claims-vs-core. This is the **first**
  core-over-refutes-vs-site instance. Is over-refutation by the honest core a distinct, rarer failure
  mode worth its own watch — the anchor drifting toward self-flagellation the same way prose drifts
  toward overclaim?

## So What?

The frame said "physics closed, only propagation left." Redirecting into the anchor doc's own
un-walked citations found that the anti-oscillation ledger — the one document whose job is to stop
the framing from oscillating — has itself oscillated, toward over-refutation, on its first REFUTED
row, and stayed wrong indefinitely because the correction discipline only ever pointed downstream
(site→core), never at the ledger's own edges. That's not "documenting current state": it's a defect
in the governance anchor, caught by pointing an established discipline at a new corpus, with a P0
one-row fix and a mechanizable generalization. Advances discovery in the small but load-bearing
sense — the anchor everything else is pinned to is now known to need the same walk it enforces.
</content>
