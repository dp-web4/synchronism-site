# Explorer Session — 2026-07-18

## WAKE

1. **Assumptions inherited**: that the topic queue's HIGH item (TEST-09 velocity-definition
   robustness) is genuinely the highest-value work, and that the registered verdict rule is
   sound. Checked both: the rule was fixed 07-18 *before* any data, the threshold provenance
   (89825cf, 04-24) was verified by the maintainer, and two independent expert personas
   converged on the gap. The assumption held.
2. **What if the frame is wrong?** The standing risk is the program's documented failure mode
   in reverse: an audit culture that over-refutes can also over-*defend* — adjudicating a
   robustness run with post-hoc scope choices. Guard applied: adjudicate exactly the
   registered three definitions, report everything else as exploratory including the rows
   that land under threshold, and disclose P(≤0.3) even where it weakens the headline.
3. **Highest-information experiment**: this run. A quarter of the front-page refutation count
   rested on a definition-ambiguous observable; the run either made it airtight or caught a
   fourth manufactured-kill instance. Either outcome is a discovery.
4. **What would falsify the current posture?** If the kill had failed under W_P20 or V_max,
   the refutation count drops to 3 and the "strongest direct refutation" language on
   /honest-assessment becomes an overclaim — the site's central honesty claim would need
   restating. (It fired everywhere, but V_flat's own exceedance turned out to be only
   ~1.2σ_dev — the airtight form is V_max, which is itself a finding.)

Queue vs. WAKE: queue won — the HIGH topic was the right work.

## Topic(s) Explored

- `test09-velocity-definition-robustness-run.md`: EXECUTED the registered three-definition
  robustness run on real SPARC (123/153 galaxies), plus exploratory coverage of the full
  Lelli+2019 definition family (W_M50, V_2.2, V_2Re, V_last) and an 8-point W_P20
  generator-sensitivity grid, with paired-bootstrap errors on every differential.

## Key Findings

- `test09-velocity-definition-robustness-executed-kill-stands.md`: **Kill stands under the
  pre-fixed rule — all 11 adjudicated runs exceed 0.3** (min 0.32±0.08). Refutation count
  stays 4. V_max is the statistically airtight form (0.56–0.72, P(≤0.3) ≤ 0.001); W_P20's
  margin is thin (0.34±0.10, P=0.36); inner-disc/single-point measures (V_2.2 0.28, V_last
  0.25) sit under threshold but are outside the registered outer-velocity scope and don't
  probe the bounded boost — MOND passes the differential under every definition, Synchronism
  fails the entire outer-velocity class. Observed arm externally validated against
  Lelli+2019 Table 1 anchors (fetched and read the actual paper).

## NotebookLM Usage

- Not used — single-paper anchor (Lelli+2019, fetched directly); no multi-source synthesis
  needed. Did not check status this session.

## Rabbit Holes (Worth Revisiting)

- Paired-bootstrap P(≤threshold) as a standard companion statistic for every registered
  numeric kill on the site (feeds `test-preregistration-protocol` topic).
- Real archival W_P20 (EDD cross-match) to replace the synthetic-profile proxy — polish, the
  generator grid already brackets it.
- Orthogonal-ML fitter re-run as belt-and-suspenders (Lelli+2019 say definition, not fitter,
  drives the differences; our vertical fits reproduce their ordering).

## Self-Seeded Questions

- The V_flat baseline kill fires at only ~1.2σ_dev over its threshold. How many of the other
  registered numeric kills would survive publishing their exceedance probability? (TEST-10's
  ceiling violation is definition-free and 69%-of-sample wide — that one is safe.)
- Is there a general lesson: registered point criteria should be co-registered with an
  exceedance-probability statistic, else "fires" and "barely fires" are indistinguishable in
  the ledger.

## So What?

Discovery, not documentation: the site's loudest kill was defensible-but-unexecuted on the
robustness axis both expert personas attacked; now it is executed, with its strongest form
(V_max) identified, its thin margins disclosed, and the fourth-manufactured-kill scenario
ruled out by run rather than by argument. The registered rule was adjudicated exactly as
fixed, on data that could have gone the other way.
