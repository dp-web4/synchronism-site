# Maintainer Session — 2026-08-11

## WAKE

The visitor log's top P0 (Pass 4: "no dark-energy sector anywhere — the loudest omission I found")
was **already answered** — the explorer derived the sector on 08-10 and left a six-item maintainer
action list. So today's research-direction question was already routed
(`Research/proposals/dark_energy_sector_exists_and_forbids_desi_quadrant_20260810.md`); no new
proposal needed. What the visitor log actually demonstrated is the propagation clock: the site's
lead sentence was false for one more morning, and a competent researcher persona found it in one
pass. This is confirmation #6 that persona reports track the site's leads, not its appendices —
the fix that matters is always the lead rewrite.

Dirty tree at start: supervisor GitNexus count updates in AGENTS.md/CLAUDE.md (benign, committed
with this session) + my own runner log. Not an orphaned-inscription case.

## Feedback Reviewed

- **Visitor log 2026-08-11** (4 passes). Key items: Pass 4 dark-energy gap (rediscovery — explorer
  closed it 08-10); Pass 4 audit-0/6 instrument-limitation caveat missing at headline (real, matches
  standing detector-null-class finding); P2 numeric drift (three galaxy totals on landing — partially
  real: the tile was corrected 08-10 but line 347 of the same page still said 14,760); P1 beginner-path
  jargon (**verified manufactured** — ansatz/phenomenological/Hamiltonian/compander all carry inline
  plain-word glosses, three inside a collapsed "For physicists" block; only MOND's first use at the
  top of why-synchronism genuinely preceded its gloss); P3 B_max conditionality + ΔBIC N_eff
  (real, standing memory items, deferred — count reconciliation gates on dp).
- **Explorer finding 08-10**: `w-eff-the-archive-has-a-dark-energy-sector-and-it-forbids-the-desi-quadrant.md`
  — the session's primary work order.
- **Forum**: no new items.
- **Required reading**: SPINE.md + PREDICTIONS.md in full. PREDICTIONS.md:152 retraction (08-10)
  confirmed in place; it explicitly routes Sessions #100/#101 errata to the maintainer lane (done
  below) and gates TEST-26 adoption + refutation-count reconciliation on dp.

## Changes Made (Site)

- `src/app/honest-assessment/page.tsx` — the false "C(ρ) contains no dark-energy sector" sentence
  **rewritten as the lead, not annotated**: DESI item removed from the "silent" list; new
  `#dark-energy` block states the sector (Session 100, 2025-12-08), the corrected w(z) (−2γ → −1
  monotone, w_eff(0) = −1.24 at γ=2), the sign-lock sign(w₀+1)=sign(wₐ) forbidding the DESI DR2
  quadrant in all four combinations, both honest bounds (γ=1/2 IS ΛCDM — no over-refutation; the
  lock is conditional on the non-covariant substitution), refutation count unchanged, and the
  provenance of the failed negative (compilation-layer verification) with the standing rule.
- `src/app/top-5-tests/page.tsx` — TEST-26 added under a new section "Proposed — prospective
  registration pending adoption" (kill-or-tie stated in the card; adoption gates on operator);
  scorecard row added; top red box notes the candidate without repopulating the (still honestly
  empty) discriminating list. **Fossil fixed**: the Strategy card still directed "immediate
  discriminating work" to TEST-02 — a test closed as self-eliminating higher on the same page since
  08-10. Rewritten to the actual strategy: two prospective kill-or-tie positions (DR2 fσ₈
  pre-commitment + proposed TEST-26) and the covariant-00-component derivation as the one route
  back to a discriminating test.
- `src/app/page.tsx` — (1) 0/6-audit headline: instrument-limitation caveat now carried at point of
  headline in both occurrences (audit false-flags 6/6 genuine discoveries; kept separate from
  data-driven refutations), linked to the calibration on /research-philosophy. (2) "14,760" → 14,610
  in the What-It-Covers Cosmology card — the 08-10 tile correction had not propagated 35 lines down
  the same file.
- `src/app/why-synchronism/page.tsx` — MOND's first use (line 27) now glossed/tooltipped; it
  previously preceded the page's own gloss by ~120 lines.
- Build: `npx next build` passes, zero errors.

## Back-Annotations (Research Repo)

- `Research/Session100_Modified_Friedmann.md` — dated erratum: stated w_eff formula has a sign error
  (unit tests: matter → −2, radiation → −7/3 under their form); tabulated column follows neither
  formula (leading −1 dropped); corrected w_eff(0) = −1.24 not "> 0"; closed form and sign-lock
  consequence recorded; covariant-substitution conditionality named.
- `Research/Session101_Cosmic_Coherence.md` — dated erratum: premise was an arithmetic artifact;
  "different cosmic form" is identically the galactic form at γ=1/2 (Möbius degeneracy — same fact
  that makes γ=1/2 MOND's simple μ); "category error" framing withdrawn; verification table is the
  imposed assumption read back. Original text preserved in both files.
- (PREDICTIONS.md:152 retraction was already inscribed by the explorer 08-10 — verified, no edit.)

## Topics Seeded for Explorer

- `covariant-00-component-does-the-sign-lock-survive.md` — the highest-value follow-on: derive the
  00-component from an action; if Ċ terms don't break the lock the no-go is unconditional, if they
  do it's the site's only candidate discriminating prediction.
- `session107-desi-forecasts-unaudited.md` — Session #107's 3.1–3.2σ forecasts predate the TEST-04a
  "~1.5σ as registered" correction; ~16 cosmology-arc files remain unread by any track.

## Open Items (Deferred)

- `/falsifiability` 0→1 prospective count — **gates on dp** (TEST-26 adoption).
- Refutation-count canonical figure (6 vs ≤5 vs 3–4 roots) — **gates on dp** per the 08-08
  independence audit in PREDICTIONS.md; until then the landing cell keeps its inline subtraction.
- Correction-stack lead-rewrites site-wide (P1+P2's #1 item): today's edits follow the rule; the
  backlog (40+ dated stacks on /honest-assessment alone) needs a dedicated pass — flagged in
  SESSION_FOCUS.
- B_max conditionality in the /honest-assessment ledger rows themselves (Pass 3); ΔBIC=+184 with
  N_eff sensitivity range (Pass 3, standing memory item); consciousness-demo axis to [0,1) (Pass 4
  low); phase-boundary rename (P2 medium).

## So What?

The loop closed at its intended speed for the first time on a research-grade item: explorer derives
(08-10) → visitor independently demands (08-11, 05:00) → maintainer propagates (08-11, 06:00). The
site moved from *silent* on the dominant live cosmology anomaly to *forbidding* it — a falsifiable,
currently-disfavoured, pre-registrable position, which is strictly more scientific content than the
"honest silence" it replaces. The remaining discovery lever is now clearly identified and seeded:
whether the sign lock survives a covariant derivation. That is an advance, not documentation.
