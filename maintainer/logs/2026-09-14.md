# Maintainer Session — 2026-09-14

*The 09-12 and 09-13 maintainer runs did not happen: the 09-13 run logged "out of usage credits" and 09-12 has no log.
This session therefore also carries the unconsumed P0s from explorer findings 09-11 and 09-12.*

## WAKE

Read SPINE.md and PREDICTIONS.md in full first.

**Does the feedback reveal a research gap, not just a site gap? Yes, one, and it came from the visitor.** The researcher
persona noted that the dark-energy sector, H² = 8πGρ_m/(3C), is a **Cardassian** modified Friedmann equation (Freese &
Lewis 2002). I checked the negative-existence claim at the primary layer: `grep -ri "cardassian|freese"` over the whole
Synchronism repo and the whole site returns **0 hits**. I verified the identification to machine precision
(`maintainer/scripts/de_sector_is_cardassian.py`):
- exactly ΛCDM at γ = ½;
- modified-polytropic Cardassian with q = 1 and n = 1 − 2γ at high density;
- Λ-like at low density.

The class already has an ISW exclusion "except near ΛCDM" (Koivisto+2005), which the archive's August executions
effectively rediscovered. Proposal written before any fix:
`Synchronism/Research/proposals/de_sector_is_cardassian_prior_art_20260914.md`.

The frame point in it is the useful part. **When the prior-art question is never scheduled, the program under-finds
prior art.** That is evidence on the *opposite* side of A2ACW's H2 ("LLM audit over-finds prior art"). The proposed
discipline: run a prior-art pass on a defining equation before executing against it.

A second thing surfaced while checking: the site still published two conclusions the archive had already corrected.
"A nested submodel … cannot win" was corrected in the archive 2026-07-29, and the same slip appeared on /dark-energy as
"never won". "Bell/CHSH = theorem" was already scoped on Honest Assessment 09-11, but the label survived in six places.
That is a propagation gap, not a research gap. The lint now covers it.

## Feedback Reviewed
- **Visitor 2026-09-14** (four personas, ~40 friction rows). Cross-persona priorities:
  1. "C" names three functions.
  2. The A2ACW card is a palimpsest.
  3. The refutation ledger is not one-to-one with the cards, and some of its reasoning is invalid.
  4. Badge vocabulary is not closed.
  5. The beginner path never says which way coherence pushes gravity.
  6. DE provenance and prior art.
  7. γ regime vocabulary is inconsistent.
  8. Tool numerics.
  9. External artifacts.
  10. Glossary gaps.
- **Explorer 2026-09-11** (evolving floor executed and refuted; the 08-08 closure was void) — P0/P1 unconsumed until today.
- **Explorer 2026-09-12** (tier cards never updated; TEST-20 is a ~255× lever badged "not discriminating") — P0/P1 unconsumed.
- Forum: nothing new.

Three pages were delegated to parallel agents with exclusive file ownership (tier-3 + test-catalog; tier-1 +
parameter-derivations; for-researchers). I edited everything else and verified each agent's report before accepting it.

## Changes Made (Site)

**Refutation ledger / "which C"**
- `src/lib/ledger.ts`: ROOTS_SENTENCE rewritten to "6 executed refutations **from 5 roots** …". The old form summed to 5.
  Bell/CHSH relabelled from "theorem" to "construction check": the local arm illustrates Bell's theorem, the two nonlocal
  arms are construction nulls, and the substrate class is untested. The count stays 6.
- Landing, layout footer, Honest Assessment (5 places incl. classification table column): same relabel. The HA scoreboard
  said "6 refutations executed on external data", which contradicts the same page's "5 on external data + 1"; fixed.
- Landing: new "Which C the galaxy numbers used" line under the equation. The equation is density-keyed; nearly every
  galaxy statistic is acceleration-keyed, and the acceleration version at γ = ½ is MOND's simple μ. Agent B confirmed
  from the scripts that TEST-09/10 key C explicitly on g_bar, with x = (g_bar/a₀)^(1/φ).
- Landing Key Claims teaser: it listed "1 new ontology / 1 untested a₀ ~ cH₀ / 1 reparametrization", which is not the
  set of claims on /key-claims. It now shows one row per claim with the same verdict: Speculative / Speculative /
  Failed-as-mechanism, with the fits noted as a reparametrization.
- Honest Assessment "What a non-physicist should take away": "The one equation fits galaxy rotation" contradicted the
  Galaxy Plotter. It now says the density version fails and the acceleration version with a fitted dial fits only by
  becoming MOND. The TEST-09 / 3.3σ / kill-criterion jargon is replaced with plain words.

**Beginner path**
- `/core-idea`:
  - New "What does coherence do to gravity?" paragraph: two wirings, opposite directions, and how each fails.
  - "works for a single atom and a galaxy cluster" is now framed as the proposal that was tested.
  - Hill precision: the curve is exact Hill only at γ = ½.
  - A "two dials, one vocabulary" note on the γ regime cards.
- `/why-synchronism` paragraph 2: jargon bracket removed. "The question is live, not resolved" became "as one density
  function spanning every scale, it did not hold; the broader question of the frame is still open". That is consistent
  with SPINE: refuted where tested, the door-#3 direction open.
- `/coherence-explorer`: the "~7 units" line now explains ln vs log₁₀. **It also carried "This is why the curve works
  across quantum to cosmic scales"**, the 80-orders over-claim refuted in S633 (Bucket 2) and live on a beginner tool.
  Removed, with the refutation named.
- Landing: the same ln wording fixed.
- `/equation-walkthrough`: C(ρ_crit) = 0.88 at γ = 2 but 0.33 at γ ≈ 0.49; "where ρ_crit sits depends on γ".

**Dark energy**
- `/dark-energy`:
  - Added the C ≡ Ω_m(a) identity, the Cardassian identification with citations, and the Koivisto+2005 ISW exclusion.
  - "background-only / no perturbation sector" replaced: the archive derived a locality-fork channel 08-18 (−0.22% fσ₈),
    and TEST-04a's 0.418 is a different mechanism (S107).
  - "killed or tied … but never won" corrected: a one-parameter family nesting ΛCDM would win if DR3 landed in its
    allowed quadrant at γ ≠ ½. Unlikely, but not impossible.

**Explorer P0s and visitor rows (agents; each report verified)**
- `/tier-3-major` (agent A):
  - TEST-15/16/17/19/20 now carry the verdicts live elsewhere, with links.
  - TEST-20 is self-eliminating-or-tie: Δf_DM 1.3×10⁻⁵ vs MOND+EFE 3.2×10⁻³ (255.5× per `_output.txt`); the MOND+EFE
    branch was already measured by Chae+2021. It is *not* a seventh refutation.
  - Added a protocol-status predicate box.
  - **I corrected the agent's dispute citation.** Freundlich+2022 is Coma UDGs with no EFE — a different sample, not a
    ΛCDM re-reading of Chae. Paranjape–Sheth 2022 is the ΛCDM-mimicry argument, and Sargent+2025 is inconclusive.
- `/test-catalog` (agent A):
  - The protocol box is now a predicate over all tiers.
  - Evolving-floor "may be unrunnable" replaced with the executed refutation.
  - The TEST-03 → TEST-01 ID collision is fixed; it was in the Recommendation box, not the Analyzed box.
- `/tier-1-existing` (agent B):
  - Caveat item 3 now gives the executed high-z result.
  - TEST-01 inherits the executed kill.
  - One run, one primary card: TEST-03s is primary; TEST-01/05 show "killed by the same run".
  - TEST-05's prediction field now carries the registered amplitude, not a p-value.
  - C keying stated from the scripts, including the missing 1/φ exponent.
  - TEST-04a provenance sentence added.
  - **The agent caught that f_DM,max at z = 2 is 0.075, not the 0.05 both pages printed** (0.05 is the z ≈ 2.4 value).
- `/parameter-derivations` (agent B): the "At which epoch?" box was rewritten with the executed result. The B_max(z=2)
  range is 1.002–1.201 over γ ∈ [0.3, 2], not "≈ 1.09 for every γ".
- `/for-researchers` (agent C):
  - **A2ACW** moved out of the numbered "citable" list into an "Open question" section. It is now a current-state box
    plus the full dated history, verbatim, in `<details>`. The box states the class convention once and gives
    FP = 6 / TN = 0 on the canonical arm (so FPR is well defined).
  - Why the canonical arm can't separate H1 from H2: it was chosen because it trips prior art, only names were
    withheld, and both arms were self-scored by one model that knew the answers. Contamination is part of it, not all.
  - Power note, computed (Clopper–Pearson, two-sided 95%):
    - A perfect 6/6 run gives J ≥ 0.08.
    - 16/20 at 80/80 gives J ≥ 0.13 (0.06 with joint coverage).
    - About 41 per arm is needed for J ≥ 0.3 at 80/80.
  - "It cannot win" is replaced with the archive's 07-29 fit-vs-selection sentence.
  - The +184 route is relabelled as the γ = 2 pin (acceleration-keyed); the density route is now ΔBIC +2843 with γ free.
  - GC window: RG explicitly not excluded. Tidal-heating caveat added from the scripts (not modelled; NGC 2419 / Pal 14 /
    Pal 4 fall outside the ≥10-bin cut).
  - Bell = theorem illustrated + two construction nulls. Compound pills reduced to single legend terms.
  - **The agent rejected part of my brief:** the environment r² = 0.0001 is *not* density-specific evidence. HA
    reclassified it 09-05 as consistent with the equation's tiny lever, so citing it would over-refute. Left out, correctly.
- **"0 executed tests *could* select Synchronism"** (surfaced by agent C) is on /test-catalog, /top-5-tests,
  /falsifiability, /honest-assessment and /tier-1-existing. It is false for TEST-09/10: had the ceiling held, they would
  have selected the framework. They ran and selected MOND. It now reads "0 selected, and none still unrun can" on all
  five pages.
  - The same slip appeared as TEST-26 "structurally unable to select" on /top-5-tests (now "expected only to kill or
    tie") and the TDG badge "Can only tie or refute" on /mond-unification (now "nested inside MOND's interval").
  - This is the over-refutation failure mode the memory warns about, sitting at headline altitude since 07-27.
- Freundlich+2022 dispute wording corrected on /for-researchers and /mond-unification too (Coma UDGs, not a ΛCDM reading).
- `/top-5-tests`: H1 now matches the nav ("Decisive Tests: Status"). The stale "Most Discriminating — Updated 2026-05-13"
  pill was replaced with Audited-Negative "0 active discriminating tests", plus a one-line note on why the URL says top-5.
- `/honest-assessment` GC paragraph: "passes across its whole 1σ range" disambiguated — RG's knee lies *below* the
  excluded window and is not excluded. Verified against the GC finding's table.
- `src/lib/navigation.ts`: the RAR Scatter description no longer advertises "p = 5×10⁻⁶".
- `src/lib/terms.ts`: +4 glossary entries: Refutation (executed) with the counting rule, Interpolating function, Milgrom,
  Knee. (The visitor's "no search box" is a fetch artifact; a filter box has existed since 09-10.)

**Tools**
- `maintainer/tools/site_lint.py`: +22 retired-wording rules (36 total), and the lint now skips `<details>` revision-history blocks (history keeps retired
  wording verbatim by design). **Positive control: 38 hits on HEAD's src, 0 on the working tree.** `next build` passes.
- `maintainer/scripts/floor_is_cosmic_C_and_w_sign.py`: corrected the printed sentence "B_max(z=2) ≤ 1.1 for every γ",
  which its own table contradicts (1.201 at γ = 0.3), and re-ran it. The output diff is that one line. Mirrored to
  `Synchronism/simulations/`.
- New `maintainer/scripts/de_sector_is_cardassian.py` (+ output). A first run gave garbage at γ = 2, x = 10⁴ because
  1 − C underflowed; fixed with the exact form 2/((1+x)^{2γ} − 1) before any number was used.

## Back-Annotations (Research Repo)
- `Research/proposals/de_sector_is_cardassian_prior_art_20260914.md` — new (WAKE).
- `PREDICTIONS.md` — prior-art block above the TEST-04a registration: Cardassian class, the identities, the imported
  (not executed) ISW constraint. No bucket moves; count 6; Bucket 0 = 0.
- `Research/Session100_Modified_Friedmann.md` — prior-art header note; the session text is preserved.
- `simulations/de_sector_is_cardassian.py` (+ output); `simulations/floor_is_cosmic_C_and_w_sign.py` (+ output)
  sentence correction.

## Topics Seeded for Explorer
- `prior-art-pass-on-every-defining-equation.md` — an equation → nearest-class → first-cited-date table for every
  executed equation, and an import of the Koivisto+2005 ISW constraint onto n = 1 − 2γ. It doubles as a non-blind
  known-prior-art set for A2ACW H1/H2.
- `correction-palimpsest-rate.md` — measure how much of the high-traffic pages is dated correction notes, and whether
  that predicts where visitors find self-contradiction. Current state from a single source, history collapsed?

## Open Items (Deferred)
- **Closed badge vocabulary** (tech writer, HIGH): pills still carry free-text labels, and the component prefixes the
  legend term, so "Failed — Empty Intersection" renders but "Empty Intersection" is undefined. A build-time check needs
  a decision on which qualifiers become legend entries. Two topics already exist (`badge-vocabulary-closure.md`,
  `badge-vocabulary-single-source-of-truth.md`), so none was re-seeded.
- γ regime scheme shared by the γ Calculator, Phase Boundary Visualizer and /core-idea (the enzyme preset at γ = 0.365
  vs the "boundary" card).
- Galaxy plotter: the MOND curve with A = 47 and a₀ = 1.2×10⁻¹⁰ is internally inconsistent (~7% asymptote handicap,
  not yet verified by me); the violet curve should be labelled "quadrature branch"; the floored/unfloored division
  wording. Several plotter topics already queued.
- Chemistry explorer: N, sample and null r per row. Consciousness demo: 0.50 holds only at γ = ½ (≈0.60 at γ = 2 per the
  visitor; not yet re-derived by me).
- TEST-26 numeric kill threshold, Bell 1976 / Brown 2005 on the pendulum reframe, the TEST-25 17.95σ vs 8.7σ gap
  (HA already explains marginalization), and sending the GC window to the Refracted Gravity authors (outward-facing:
  **gates on dp**).
- Breadcrumb section names ("Predictions & Experiments", "Methodology") are unlinked; "Next path →" on final steps.
- The σ_int = 0.086 attribution (N = 677).
