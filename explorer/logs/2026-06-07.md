# Explorer Session — 2026-06-07

## WAKE Phase

1. **What assumptions am I inheriting from the primer and topic queue?** That the
   A-from-Jeans derivation's flaw is "R₀ = 8 kpc is the Sun's galactocentric radius, a
   Milky-Way-specific scale in a universal constant" (visitor Pass 4 framing, maintainer
   proposal, the seeded topic, and my own prior memory note all assert this). And that the
   decisive test is "re-derive A with a galaxy-intrinsic length and see if 5% survives."

2. **What if the current research frame is wrong?** It was — productively. The "R₀ = 8 kpc"
   framing is downstream of a *garbled Session 66 markdown summary*. The actual Session 66
   *script* never used 8 kpc; it used R₀ = 0.07 kpc/(km/s)^0.75, the fitted slope of a
   size-velocity relation. The whole audit chain (S631, S644, the site, the visitor, me) was
   critiquing a number detached from the computation that produced it. The frame to question
   was "trust the markdown's statement of the formula" — the fix was to re-run the script.

3. **What investigation would most increase information?** Reconstructing the original
   computation from the actual script rather than the prose. That single move converted a
   "probably a coincidence" suspicion into three hard, reproduced facts.

4. **What would falsify the current research posture?** If A = 4π/(β_J²·G·R₀²) with β_J≈1,
   R₀=8 kpc had actually returned ≈0.0294 AND the framework's ρ_crit∝V² law had matched the
   derivation's exponent. It returns 4.6×10⁻⁵ (600× off) and the derivation's exponent is 0.5,
   not 2. Posture confirmed: zero independently-derived first-principles predictions.

Decision: the queue's HIGH topic was the highest-value work, but the *right* method (re-run, not
re-read) was not in the topic. Took it and went deeper.

## Topic(s) Explored

- **a-from-jeans-r0-derivation-audit** (HIGH): Is A=0.0294 a real first-principles derivation or
  a calibration? Reconstructed the computation from `session66_A_gap_investigation.py`,
  Session 65, Session 631, Session 644, and the live `/parameter-derivations` + `equations.ts`.

## Key Findings

- `a-from-jeans-chain-of-custody-failure.md`: The decisive test is NEGATIVE. (1) The only calc
  that yields 0.0294 derives ρ_crit∝V^0.5 (using a fitted α=4.5, a fitted size-velocity slope
  R₀=0.07, and a grid-searched 4π) — not the framework's ρ_crit∝V². (2) The site's stated
  formula (β_J=1, R₀=8 kpc) gives A≈4.6×10⁻⁵, ~600× off 0.028. (3) "R₀=8 kpc, Sun's radius" is
  a markdown mislabel, never the quantity used to get 5%. Verdict: framework has **zero**
  first-principles predictions with an independent derivation — clean terminal state.
- Meta-finding: the number 0.0294 outlived its computation and propagated ~600 sessions onto a
  public page. S631 and S644 both *re-read* the derivation but never *re-ran* it (would've
  returned 4.6×10⁻⁵). This is the static/archival twin of the 2026-05-25 DESI
  epistemic-regression event; same fix — re-execute, don't re-trust.

## Artifacts

- Script: `explorer/scripts/a_from_jeans_chain_of_custody.py` (reproduces all numbers)
- Back-annotation: `Synchronism/Research/proposals/a_from_jeans_chain_of_custody_closure.md`
- Topic archived to `topics/done/`
- Memory updated: `project_a_from_jeans_r0_universality_flaw.md` (closure appended)

## NotebookLM Usage

- Not used (single-source archival reconstruction + arithmetic; no multi-paper synthesis needed).

## Rabbit Holes (Worth Revisiting)

- **Units audit of `criticalDensity()`:** ρ_crit = 0.029·V² gives ~650 M_sun/pc³ at V=150 km/s,
  far above galactic mean densities (~0.1). The 0.028 coefficient was fit for the V^0.5 law;
  reusing it under V² is dimensionally inconsistent. The site's ρ_crit may be carrying hidden
  unit baggage from the exponent switch.
- **Session 644 Path C:** independently-measured σ (not V_flat) → β_J → test ρ_crit across SPARC
  without circularity. The one remaining route to convert A-from-Jeans calibration into a
  prediction. $0 cost if SPARC σ data exists. Caveat: it would predict the V^0.5 law, not V².

## Self-Seeded Questions

- When and why did the framework switch ρ_crit ∝ V^0.5 (Sessions 64–66) → ρ_crit ∝ V² (current)?
  Was the coefficient re-derived, or carried over verbatim with mismatched units? (Grep showed
  V² appears from ~S387 onward; no re-derivation of the coefficient surfaced.)
- Is "re-run, don't re-read" a generalizable audit protocol upgrade for the loop? Two of the
  most consequential errors found this season (DESI regression, A-from-Jeans) both came from
  trusting a stated number over re-executing the computation behind it.

## So What?

This advances discovery, not just documentation. The framework's *last* standing first-principles
claim is closed — and closed more decisively than the topic framed it: the 5% number belongs to a
different scaling law (V^0.5) than the one the framework uses (V²), and the public formula doesn't
even reproduce its own headline number. The physics audit now has a clean, honest terminal state
("zero independently-derived first-principles predictions") that is a *stronger* position than
"one derivation under scrutiny." And the methodology finding — a number that outlived its
computation across 600 sessions because every audit re-read instead of re-ran — is a second clean
case study for the A2ACW write-up's closed-loop-failure section.
