# Maintainer Session — 2026-04-28

## Feedback Reviewed

- **Visitor log**: 2026-04-28 — 4-persona pass, 10–13 pages each. Cross-persona synthesis identified 25 issues (Tier A: 10 high, Tier B: 10 medium, Tier C: 5 low). Key pattern: honest pages are the best on the site; front-of-site claims have not propagated what the honest pages already concede.
- **Forum**: No new feedback this session
- **Explorer**: No new findings since 2026-04-27

---

## WAKE Phase Finding

Today's visitor feedback reveals three **research gaps**, not just site gaps:

1. **RAR σ_int(ρ_env) slope is unspecified** — the framework's one genuinely novel galactic claim lacks a numerical slope. The Pass 4 researcher explicitly: "not yet a paper; possibly a paper if σ_int(ρ_env) is formalized." Filed research proposal to Synchronism repo.

2. **MRH-crossing vs. Zurek einselection** — the researcher asks for one experimental scenario where they differ. If none exists, the measurement section should be re-badged Reparametrization. Seeded as explorer topic.

3. **Tanh Landau saddle-point** — does `tanh(γ·log(ρ/ρ_crit + 1))` arise from a free-energy saddle-point, or is it two saturating functions stacked? The answer changes the "motivated by mean-field" framing. Seeded as explorer topic.

---

## Changes Made (Site)

### High-severity fixes

1. **`src/app/research-philosophy/page.tsx`** — Fixed A2ACW canonical expansion. Was "AI-to-AI Collaborative Whiteboarding"; now "AI-to-AI Adversarial Collaboration Workshop" (matching `src/lib/terms.ts` line 70). Pass 2 (tech writer) found at least three different expansions across the site — canonicalized to one.

2. **`src/app/galaxy-plotter/page.tsx`** — Added amber disclaimer note: "V_flat is taken from existing SPARC/MOND fits, not independently predicted by Synchronism. The violet curve demonstrates shape recovery — it is a consistency check, not a parameter-free prediction." Pass 3 (grad student) and Pass 4 (researcher) both flagged this as high severity: casual readers see the violet curve and read it as a prediction.

3. **`src/app/top-5-tests/page.tsx`** — Reframed Test #4 (GW-DM): removed "future decisive test" framing; now states "GW170817 already constrained Synchronism to GR-equivalent at |α| < 3×10⁻¹⁵; this test discriminates only if Synchronism predicts a positive signal, which it does not currently." Reframed Test #5 (Cosmic Interference): clarified "Candidate only — derivation pending" language. Updated strategy section accordingly.

4. **`src/app/parameter-derivations/page.tsx`** — Two changes:
   - Re-badged Σ₀ = cH₀/(4π²G) from "Validated | 12% Error" to "Reparametrization | Freeman's Law Re-expressed". Updated text to explain this is Freeman's (1970) empirical surface density law re-expressed via cosmological constants — dimensional bookkeeping, not first-principles derivation.
   - Added badge-taxonomy clarifier note at top of derivation chain: "Motivated Ansatz and Motivated Choice are Speculative sub-types; Freeman's Law Re-expressed is a Reparametrization sub-type." Pass 2 (tech writer) flagged these as looking like official badges that aren't in the six-tier taxonomy.

5. **`src/app/first-encounter/page.tsx`** — Added learning-structure disambiguator note: "New here? This 10-minute intro is the fastest path. Want a structured journey? Learning Paths offers Beginner/Intermediate/Advanced routes." Pass 2 found two parallel learning structures (/first-encounter 7-step vs. /learning-paths 3-track) with no explanation of their relationship.

6. **`src/app/key-claims/page.tsx`** — Upgraded dark-matter claim badge from "Speculative — Structural Problems" (amber) to "Failed — Mechanism Under Revision" (red). Changed card border from blue to red. Pass 4 (researcher): "Leaving a fatal failure on /honest-assessment while keeping the claim active in /key-claims as Speculative is incoherent." The honest caveat already documents the Bullet Cluster sign error as a structural failure; the badge should match.

---

## Back-Annotations (Research Repo)

**Filed**: `Synchronism/Research/proposals/rar_sigma_int_environment_slope_derivation.md`

The Pass 4 researcher identified environment-dependent RAR intrinsic scatter as the framework's one candidate genuinely novel prediction — and noted it lacks a numerical slope. This is the highest-leverage research task: derive dσ_int/d(log ρ_env) from C(ρ), compare to MOND baseline (σ_int ≈ 0.13 dex constant) and ΛCDM-hydro (σ_int increases in clusters). If the Synchronism prediction has the opposite sign from ΛCDM-hydro, existing SPARC + ALFALFA-SDSS data could test it. Proposal includes derivation path via Sessions #64-65 (Jeans criterion).

---

## Topics Seeded for Explorer

1. **`ncorr-operational-definition-recipe.md`** — N_corr has no recipe for deriving it from observables. Every γ-based prediction is unfalsifiable in practice until this exists. HIGH priority; directly blocks the framework's testability claims.

2. **`mrh-crossing-vs-zurek-einselection.md`** — Is there one experimental scenario where MRH-crossing differs from Zurek einselection? If not, `/measurement-without-observers` should be Reparametrization. The site currently claims "dissolution of the measurement problem" — this is either a major contribution or an overclaim.

3. **`tanh-landau-saddle-point-derivation.md`** — Does C(ρ) arise from a Landau saddle-point derivation or is it two saturating functions stacked? Relevant to whether the site can ever claim Landau critical exponents near γ_c ≈ 1.

---

## Open Items (Deferred)

- **Acronym hygiene** (Pass 2 HIGH): MOND, SPARC, BAO, DESI, Gaia DR3, etc. used without first-use expansion on body pages. The promised hover-tooltip system either needs implementation or body pages need parenthetical expansions. Multi-page effort; deferred.
- **Two parallel learning structures**: Added disambiguator on /first-encounter. The deeper fix (merging or clearly demoting one) would require restructuring navigation — deferred pending operator decision.
- **Glossary Greek-letter sort**: γ, ρ_crit sort by symbol not by spelled-out name — deferred (LOW severity, small fix but requires glossary restructuring).
- **Top-5-tests quantitative slopes**: BAO amplitude not derived; wide-binary slope not specified. These are research gaps, not site fixes — addressed via research proposal.
- **Chemistry r=0.982 template-contamination**: Site admits this on /gamma-boundary but the landing page still advertises "1,703 phenomena." A dedicated session on the pre-registered vs. post-hoc split is needed before the landing page can be updated.
- **`tanh` definition on first use**: Never explained as "hyperbolic tangent" on body pages. Medium fix; deferred.

---

## So What?

The pattern the researcher identified is correct: the site has done the honest epistemic work but hasn't propagated the conclusions to where they're most visible. Today's fixes push the propagation one step further — the dark-matter claim is now visibly Failed, the Σ₀ parameter is now visibly a reparametrization, and the GW test is now correctly framed as already-constrained.

The more interesting observation from today: the framework's one genuinely novel claim (environment-dependent RAR scatter) is real enough that a leading-edge researcher would return when — and only when — the numerical slope is posted. That's actionable. The research proposal is the highest-leverage output of this session.
