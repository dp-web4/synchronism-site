# Topic: Does Any Operational Protocol for Measuring C Exist — In Any Domain?

**Priority**: HIGH
**Seeded**: 2026-06-11 (maintainer)
**Proposal filed**: `Synchronism/Research/proposals/c_observable_calibration_gap.md`

## Question

Is there — anywhere in the 3,308-session archive, or derivable from the framework — a candidate
protocol for computing C from a measurable quantity, *independent of the data C is used to predict*?

## Context

2026-06-11 visitor Pass 3 (grad physicist) and Pass 4 (researcher) converged independently, both HIGH:

- Pass 3 Q3: "What observable *does* correspond to C, operationally? Every proposed test I found
  either measures phase coherence (which C isn't) or fits C's parameters to the data being predicted."
- Pass 4 Q5: "Is there a protocol — even a sketch — for computing C from any measurable, in any
  domain? Every falsifiability claim on the site ultimately bottlenecks on this absence."

The symptom fixed this session: key-claims offered "EEG phase coherence during anesthesia" as the
consciousness kill criterion while the site insists C is *not* phase coherence (BCS at C≈0), and
consciousness-demo admits no EEG/fMRI/Φ→C calibration exists. Claim 2 is now labeled unfalsifiable
as stated. But the gap is general, not consciousness-specific.

Combined with `ncorr-ladder-never-anchored` (2026-06-08), C is **doubly unanchored**: input
(N_corr asserted on all 17 rungs) and output (no measurement protocol in any domain). The only
data contact is galaxy rotation, where C's parameters are fit to the data being predicted.

## Why It Matters

If no protocol exists or is derivable, most "Untested" badges outside galaxy rotation should
become a third category — **unrunnable as stated** — which changes the test catalog's claimed
exposure to data by an order of magnitude. If one IS derivable, it's the framework's first
operationally grounded quantity. Either resolution advances the audit. Note the trap: any honest
protocol at the galaxy rung must return N_corr ≈ 17 (back-implied from fitted γ≈0.49),
contradicting the independent-stars premise — so derivation may refute rather than rescue.

## Decisive Test

Survey the archive for proto-protocols (correlation-length recipes, density-field estimators,
any C-from-data attempt). Classify each: (a) genuinely independent measurable → derive and run
on one system; (b) circular (fits C to the prediction target); (c) wrong observable (phase
coherence, synchrony). If all fall in (b)/(c), write the closure: "C is a fitting intermediary,
not an observable" — and enumerate which site badges flip from Untested to unrunnable.

## Suggested Starting Points

- Site: /consciousness-demo (the admission), /key-claims (today's fix), /coherence-function,
  /gamma-calculator (Caveat 3: no Hamiltonian→N_corr protocol)
- Done topics (input-side twins): `ncorr-from-hamiltonian-protocol.md`,
  `ncorr-operational-definition-recipe.md`, `consciousness-D-S-operational-definition.md`
- External calibration exemplar: PCI* = 0.31 (Casali et al. 2013, Sci Transl Med) — what an
  empirically calibrated threshold looks like; the contrast object for any C protocol.
