# Maintainer Session — 2026-07-28

## WAKE

Found a crashed predecessor session: `maintainer/logs/2026-07-28-0600.log` (untracked, 2 lines,
timestamp only) and a memory entry (`project_galaxy_sector_is_nested_submodel_of_mond.md`, written
12:24 UTC today) show an earlier instance of this exact task read the visitor log, extracted the
nested-submodel insight, and died before making any site edits, proposals, or commits. `git status`
was otherwise clean (only the gitnexus-count auto-diffs in AGENTS.md/CLAUDE.md), so nothing else
was lost. Picked up from the visitor log fresh.

Yes — the feedback revealed a research-direction question, not just a site gap: whether the
site's headline "4 refutations" undercounts a structural fact (TEST-09/10 are corollaries of one
boost ceiling) and whether a fifth, already-executed result (the SPARC×Cassini squeeze) had simply
never been shipped to the site. Both are addressed below; the count question itself is deliberately
left open for dp, per the site's own discipline for headline-number changes.

## Feedback Reviewed

- Visitor log 2026-07-28: 4-persona pass (casual enthusiast, technical writer, grad-student
  physicist, leading-edge researcher). Exceptionally dense — 41 friction items, 18 priority fixes
  cross-persona-ranked. The grad-student and researcher passes independently audited the
  refutation ledger and both concluded it over-counts in the self-critical direction; the
  researcher pass additionally found two runnable tests the site had never registered.
- Verified several visitor claims against source before acting (per standing practice): the
  "4 refutations" figure is current and deliberate (commit 69248a4, 2026-07-17), not stale; the
  TEST-05 "underpowered/90×" characterization does not match the page's actual wording
  ("undetectable," "~900× under," "~50× smaller lever"); the claimed duplicate routes
  (`/the-core-idea`, `/methodology`) do not exist in the current tree — those three visitor
  findings were not acted on.
- Checked `Research/proposals/boost_ceiling_provenance_and_class_exclusion.md` (filed 2026-07-27
  from yesterday's visitor pass) and confirmed its Branch 1/2 arithmetic is illustrative
  back-of-envelope, not an executed per-galaxy sweep — flagged this explicitly since a memory
  shorthand had blurred the distinction.
- Checked `PREDICTIONS.md` Bucket 1 (B2) in the research repo and found the "Solar System
  ephemeris squeeze" the researcher persona flagged as missing/unregistered had actually already
  been **executed** (2026-07-23, re-verified 2026-07-24): a pre-registered joint SPARC×Cassini
  constraint with verdict "robust empty intersection," +17.7–18.0σ across the retained γ interval.
  It existed only in the research repo — zero site presence (`grep -rl "Cassini" src/` → nothing).

## Changes Made (Site)

- `src/app/parameter-derivations/page.tsx`: added item 8, B_max = 1/Ω_m ≈ 3.17 — the parameter
  carrying every discriminating result on the site, confirmed absent from the "complete chain"
  inventory. Badged Speculative / "Asserted, Not Derived." States the Ω_m/Ω_b alternative and the
  tail-vs-median distinction, linking the open proposal. Updated the page's bottom-line summary
  paragraph to mention it.
- `src/app/for-researchers/page.tsx`: added the nested-submodel framing to the opening section —
  Synchronism's galaxy sector = MOND ∩ {B ≤ 3.17}, a strict submodel that can only tie or lose,
  available a priori from the model's own stated structure. No data claim changed; this is a
  reading of *why* the existing "0 tests could select Synchronism" result had to come out that way.
- **`src/app/tier-1-existing/page.tsx`: added TEST-11**, the SPARC×Cassini joint squeeze — an
  already-executed (2026-07-23/24), decisive, previously-unshipped research result. Full alert
  with exact figures from `Research/preregistrations/sparc_cassini_tanhlog/RESULT.md`: γ = 0.489
  optimum, +17.95σ discrepancy, +17.71σ–+18.00σ across the retained interval, zero survivors at
  every tested external-field strength and BIC threshold. Scoped honestly per the source document:
  closes the scale-universal tanh-log QUMOND realization only, not modified inertia, dark matter,
  or the umbrella ontology.
- `src/app/test-catalog/page.tsx`: updated "TEST-01 through TEST-10" → "through TEST-11"; added a
  note distinguishing TEST-11 as an internal-consistency kill, not Synchronism-vs-MOND
  discrimination, alongside the existing 0-of-24/2-executed-discriminators framing.
- `src/app/galaxy-rotation/page.tsx`: added a paragraph after the γ≈0.49→MOND-simple-μ mechanism
  section connecting it to TEST-11 and citing Blanchet & Novak (2011) / Hees et al. (2016) as the
  literature basis for why a slow high-acceleration return is disfavored by planetary ephemerides.
- Build verified clean (`npx next build`, zero errors) after each batch of edits.
- **Deliberately not changed:** the footer/landing "4 refutations" count. TEST-11 is a fifth
  galactic-sector result and a plausible case for either bumping the count or creating a separate
  category, but the existing 4-count went through a dedicated, reviewed recount commit
  (2026-07-17) — bundling a count change into the same session that added the new test skips that
  review. Left as an explicit open question in the back-annotation below.

## Back-Annotations (Research Repo)

- `Research/proposals/boost_ceiling_provenance_and_class_exclusion.md`: added **Branch 3** (the
  nested-submodel argument, stated structurally — MOND ∩ {B≤3.17} can only tie or lose, no data
  needed), a **status note** clarifying Branch 1's arithmetic is illustrative not executed, and
  **two new open questions**: (4) whether TEST-09/TEST-10 should count as one structural refutation
  measured twice or two independent ones, and (5) whether TEST-11 should join the site's headline
  refutation count. Both gate on dp per the proposal's existing discipline.

## Topics Seeded for Explorer

- `boost-ceiling-registered-sweep-execution.md`: the registered per-galaxy convention sweep
  (1/Ω_m vs Ω_m/Ω_b vs (Ω_m−Ω_b)/Ω_b) proposed 2026-07-27 has not been executed — only
  back-of-envelope arithmetic exists. Follows the TEST-09 velocity-definition-robustness precedent
  (same-day execution after registration).
- `badge-vocabulary-closure.md`: the tech-writer persona's P1 finding — three legends with three
  different badge subsets, ~14 badge-shaped labels defined nowhere. Frames the open design
  question (extend the closed taxonomy, or name a second tier for event-shaped test-outcome
  labels) rather than prescribing an answer.
- `beginner-path-errata-before-comprehension.md`: the enthusiast persona's P2 finding — dated
  correction boxes and refutation caveats arrive mid-explanation, before the reader has learned
  the base idea. Includes the specific praise for Honest Assessment's "what a non-physicist should
  take away" voice as a model to extend backward.
- `test-id-cross-page-inconsistency.md`: TEST-03 (`/test-catalog`) and TEST-05
  (`/tier-1-existing`) both appear to describe the same r²=0.0001/N=141 environment-null result
  under different IDs; not resolved this session, flagged for a careful side-by-side read.

## Open Items (Deferred)

- The refutation-count question (4 vs. a possible 5, and whether TEST-09/10 double-count) —
  intentionally not resolved this session; needs dp's review per the existing recount discipline.
- Badge vocabulary closure (P1, tech-writer persona) — large IA-level fix, seeded as a topic
  rather than attempted piecemeal.
- Beginner-path pedagogy restructuring (P2, enthusiast persona) — same; a `/two-reframes` split
  and errata-resequencing is a multi-page content change better done as a dedicated session.
- Galaxy-plotter "Synchronism (real)" curve — verified the page already discloses V_flat as an
  input and states the curve fails to reach flatness for this galaxy; did not find the gap the
  researcher persona described as clearly as claimed. Not acted on; worth a second look with more
  time.
- TEST-02 relabeling ("self-eliminating-or-tie" vs "one-sided kill, executable now") — verified
  the page's `alert` field already carries exactly this nuance in detail (added 2026-06-12); the
  visible badge chip may still read as flatter than the prose. Low priority, not acted on.
- A2ACW specificity-zero reframe — verified `/research-philosophy` already carries substantial
  nuance in this direction (0/6 detection, "specificity cannot be measured here," vocabulary vs.
  temporal asymmetry breakdown); did not find a clean, uncovered gap worth a same-session edit.
