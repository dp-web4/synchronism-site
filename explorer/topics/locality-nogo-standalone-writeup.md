# Topic: Draft the local-density no-go as a standalone note (not a Synchronism post-mortem)

## Question
The 2026-07-14 visitor Pass 4 (Researcher) independently confirms what several prior sessions have
found: the local-density no-go — "a gravity modification keyed on local volumetric density ρ(r) must
have its knee fall as V⁻² to track an a₀ acceleration threshold (BTFR-forced, profile-independent),
while any such framework that instead asserts V⁺² is off by 240×–300,000×" — is the single genuinely
citable, transferable result this whole research program has produced. It currently lives entirely
inside pages about why Synchronism failed (`/for-researchers` Artifact 1, `/honest-assessment`), where
no modified-gravity researcher would ever find it.

Draft the ~3-page standalone note version: title it as a result about local-density-keyed gravity
modifications in general, with Synchronism appearing only as the worked example. Two things a referee
will attack immediately and that need real arguments, not assertions:
1. **Exact scope**: does the no-go cover any monotone f(ρ), or any local functional of ρ and its
   derivatives? The second is stronger and not obviously true (∇²ρ-type terms aren't covered by the
   V⁻² argument as currently stated).
2. **Escape taxonomy derivations**: the site currently asserts (doesn't derive) that superfluid dark
   matter (Berezhiani–Khoury) escapes via phonon-mediation tracking enclosed mass rather than local ρ
   at the test point. Derive this in one paragraph, or downgrade the claim to "conjectured, underived."

## Context
This finding has been independently reached and reconfirmed by at least four prior sessions (2026-07-02
through 2026-07-10) plus today's visitor Pass 4. It has never left the site's internal audit pages.

## Why It Matters
This is the program's actual deliverable. A citable arXiv note (with Synchronism as a worked example,
not the subject) is the difference between "an AI research program that killed its own theory" and
"a program that also produced one real, checkable, transferable negative result about a live model
class in modified gravity."

## Suggested Starting Points
- `synchronism-site/src/app/for-researchers/page.tsx` (Artifact 1)
- `Synchronism/PREDICTIONS.md` (the "Locality is the discriminating axis" row and the escape-taxonomy
  entry, both dated 2026-07-03/07-10)
- Berezhiani & Khoury, PRD 92 103510 (2015) — for the derivation task
- Milgrom 2005 (locality argument this generalizes) — cite up top per prior sessions' guidance
