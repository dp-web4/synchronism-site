# Maintainer Session — 2026-07-09

## WAKE

Today's visitor log (four personas) converged on one shape of finding, independently, with
no cross-persona coordination: **the site's audit machinery is aimed exclusively at physics
overclaims.** Every failure mode found today lives somewhere that machinery isn't pointed —
onboarding (nobody audits the beginner path), badge vocabulary (nobody diffs rendered strings
against the legend), negative statistics (nobody re-derives a number that makes the framework
look bad), and the audit's own methodology claims (A2ACW's null is inflated 550× on the page
written for citers). This is a research-direction finding, not just a site-content finding —
see the WAKE-phase proposal filed to the research repo before any fixes:
`Research/proposals/symmetric_audit_discipline_directional_bias_confirmed.md`. It also closes
a loop: the 2026-07-08 explorer citation-walk found the TEST-03 manufactured kill from the
research-repo side; today's visitor pass re-derived the identical conclusion from the
site-reader side, one day later, with no shared context. That's a strong signal the finding
is real, not an artifact of one investigator's framing.

## Feedback Reviewed

- Visitor log: 2026-07-09 (four personas, casual/tech-writer/grad-student/researcher) — by far
  the densest single-day log this project has produced: 26 cross-persona priority items,
  converging heavily on "the site over-refutes its own physics and over-claims its own
  methodology."
- Explorer findings (2026-07-08, carried via memory): TEST-03 manufactured-kill citation-walk,
  Hill-identity/mass-cancellation/Σ₀ finding — both directly actionable today.
- No new forum input this session.

## Changes Made (Site)

**TEST-03/TEST-05 manufactured-kill correction** (9 pages: `tier-1-existing`,
`honest-assessment`, `galaxy-rotation`, `rar-scatter`, `cdm-discrimination`,
`cosmology-predictions`, `mond-comparator`, `research-philosophy`, `why-synchronism`,
`prediction-tracker`, `key-claims`) — the site's "R²=0.14, N=14,585, p=5×10⁻⁶, kill criterion
triggered" claim for TEST-03 is internally impossible (that R²/p pair implies N≈130–175, not
14,585 — a t-statistic of ~48.7 vs the quoted p). The statistic belongs to TEST-05's
SPARC-scale sample. TEST-03's registered ALFALFA-SDSS environment-density test has never
actually been run. TEST-05, which does own the statistic, met its own registered criterion
(reject independence) and is reclassified MOND-shared/non-discriminating rather than failed.
Verified three ways independently: archive Session 639 (2026-04-30), explorer citation-walk
(2026-07-08), and today's visitor Pass 3 + Pass 4 (2026-07-09, by direct computation).

**Σ₀ arithmetic error** (`parameter-derivations`, `freemans-law`, `honest-assessment`,
`critical-density`, `key-claims`, `tier-1-existing`) — Σ₀ = cH₀/(4π²G) was reported as ≈110
M☉/pc² (12% error vs Freeman's 124) in most places; the correct value from the site's own
stated constants is ≈119–123 M☉/pc² (≈4% to 0.5% error, depending on which page's H₀ is used
— see new topic on that inconsistency). Also added the caveat that Σ₀ = a₀/(2πG) exactly, so
this is not independent evidence from a₀'s own ~10% MOND gap.

**Bullet Cluster strawman → Clowe 2006** (`galaxy-rotation`, `key-claims`) — the site refuted
Claim 3 ("dark matter is incomplete decoherence") via a CFD-viscosity sign error that assumes
dark matter is a substance with a viscosity to get wrong; Claim 3's headline explicitly denies
that. The CFD viscosity ansatz is real and stays documented (`/dark-matter-failure`,
unchanged) but is now correctly scoped as a specific historical sub-hypothesis, not evidence
against the headline claim. The headline claim is now refuted via the standard, stronger
argument (lensing-mass/baryonic-gas offset, Clowe et al. 2006) plus the mass-cancellation
locality no-go, and flagged as the same local-vs-non-local failure at cluster scale, not a
second independent one.

**A2ACW null denominator** (`for-researchers`, `a2acw`, `honest-assessment`) — the citable-
artifact page (`/for-researchers`) quoted the null as "0 across 3,308 sessions," a 70–550×
inflation over the honest 0/6 (audited) or 0/47 (candidates pending audit) figures used
correctly elsewhere on the site. Standardized to 0/6 audited (0/47 pending) and added the
missing caveat: no control arm, and a corpus confound (the audited corpus is Synchronism
itself, already known to be a MOND reparametrization — "AI can't generate novelty" and "this
seed had none" are observationally degenerate in a single-arm design).

**LIV citation fix** (`for-researchers`, and back-annotated to `PREDICTIONS.md` in the
research repo — the error originated there) — PRL 93, 191301 (2004) was attributed to
"Collins–Perez–Sudarsky–Gambini–Pullin." The actual authors are Collins, Perez, Sudarsky,
Urrutia & Vucetich; Gambini and Pullin are not on this paper.

**Claim 3 badge reasoning** (`key-claims`) — a₀ itself is Reparametrization (unchanged); the
"Failed" badge on Claim 3's headline interpretation is now grounded in the mass-cancellation
locality no-go (ρ/ρ_crit is mass-independent for a disk with ρ_crit∝V², so no galaxy crosses
the knee for any calibration constant) and the corrected Bullet Cluster argument, replacing
the retracted TEST-03 statistic and the CFD-viscosity strawman as the stated evidence.

**Deprecated "Validated" badge** (`what-synchronism-is-not`) — the section-top badge on the
page whose entire job is not to overclaim was itself using the site's own deprecated,
"verdict-shaped" tag. Reclassified to `active-mrh`. 15 more live instances found site-wide
(`autonomous-research`, `chemistry-phase-transitions`, `coupling-experiment`,
`critical-density`, `electronegativity`, `falsifiability`, `handling-failure`,
`phase-transitions`, `publication-roadmap`, `publisher-track`, `research-philosophy`,
`sound-velocity`, `status-dashboard`, plus `prediction-tracker`'s entire data table) — too
large a reclassification to do safely in one pass; seeded as a topic instead.

Build verified clean (`npx next build`, zero errors) before commit.

## Back-Annotations (Research Repo)

- `PREDICTIONS.md`: fixed the same LIV citation author-list error at its source (Bucket 1,
  row B7) — the site's error was inherited from here, not introduced independently.
- `Research/proposals/symmetric_audit_discipline_directional_bias_confirmed.md`: WAKE-phase
  proposal naming the directional bias in the review process itself (6/6 provenance breaks
  found so far over-refute the framework; 0 over-claim the physics — the one overclaim found
  is on the methodology track). Recommends a standing symmetric-audit rule and a dedicated
  explorer citation-walk on the A2ACW methodology's own claims.

## Topics Seeded for Explorer

- `test08-environment-density-execution.md`: the registered TEST-03/TEST-08 environment-
  density correlation (classify SPARC by cluster/field/void, correlate against RAR residuals,
  apply the real r²<0.09 threshold) is runnable with public catalogs and has never been run.
  One afternoon per the research-repo proposal's estimate — would give TEST-03 a real verdict
  instead of the corrected-but-still-open state it's in now.
- `deprecated-validated-badge-full-sweep.md`: 15 remaining live instances of the deprecated
  `validated`/`supported` badge status, each needing individual research to assign the correct
  replacement (not a mechanical find-replace).
- `h0-value-inconsistency-across-pages.md`: `/freemans-law` uses H₀=67.4 km/s/Mpc;
  `/parameter-derivations`' a₀=1.08×10⁻¹⁰ implies H₀≈70. Minor but real, undetermined which
  the framework actually commits to.
- Two stale topics resolved and archived to `topics/done/`: `test03-test05-statistical-
  reconciliation.md` and `test03-kill-criterion-archive-audit.md` — both asked exactly the
  question this session answered.

## Open Items (Deferred)

- The full deprecated-badge sweep (15 instances) — seeded as a topic rather than rushed.
- The H₀ inconsistency — minor, needs a decision on canonical value, not urgent.
- Visitor's onboarding/beginner-path friction (Pass 1: "no equations required" step 1 shows
  `tanh`; missing step map; coherence-inversion warning arrives late) — genuine, high-severity
  for the target persona, but this session's time went to the physics/methodology-accuracy
  items, which affect every reader including the ones who get past onboarding. Carrying
  forward for next session; not fixed today.
- Undefined badge terms flagged by Pass 2 ("Kill Criterion Triggered," "Demoted," "Model
  Explainer" allegedly undefined) — checked against source: "Kill Criterion Triggered" and
  "MOND-shared" are in fact already defined inline on `/tier-1-existing` and in the
  `/honest-assessment` legend; "Model Explainer" and "Demoted" were not checked this session
  and remain open.

## So What?

Today's real find isn't any single arithmetic error — it's that the same shape of error (site
over-refutes its own physics) showed up independently from two unrelated investigators on
consecutive days, and that the site's *methodology* claims (A2ACW) have never been audited
with the rigor its *physics* claims receive. Fixing six instances is bookkeeping; the WAKE
proposal filed today is the actual question — whether the review process itself needs a
symmetric-audit rule, or whether "an alert visitor eventually catches it" is an acceptable
steady state. That's dp's call, not mine to resolve by fixing more pages.
