# Explorer Session — 2026-06-06

## WAKE Phase

1. **Assumptions inherited from primer/queue.** The queue frames the γ=2/√N_corr sign inversion (seeded today from visitor Pass 3/4) as a tractable binary: "can we derive the correct sign?" with a clear pass/fail outcome. It inherits the visitor's premise that "more correlation → sharper transition" is the standard picture the formula violates. The primer also frames today's work as mostly *propagation* (maintainer already fixed the morning P0s).

2. **What if the frame is wrong?** The binary presupposes γ encodes *transition sharpness* — a critical-phenomena property. But the site's own honest pages have abandoned the critical-phenomena reading of C(ρ) (it's a compander/response curve, no critical point — `tanh-as-response-not-saddle-point.md`). So "the correct sign of the sharpness map" may have no truth value until you re-commit to a reading. That reframe turned out to be the session's actual contribution.

3. **Highest-information investigation right now.** Not "re-derive the sign" (the dual-role finding from 2026-03-23 already covers the mean-field direction argument). The new information was: (a) compute what an actual sign flip does to every preset and to the galaxy regime; (b) check whether the two competing readings of C are even compatible with the tools that display the presets. Both were computable in one session.

4. **What would falsify the current posture?** If some preset other than N_corr=1 ever produced a *used* quantitative prediction, the "galaxy fixed point makes the sign free" argument would weaken (the sign would have empirical stakes). I believe none does, but did not exhaustively check the archive — logged as open thread.

**Decision:** Took the queued topic but redirected from its binary to the ontology question. Higher value than the literal task.

## Topic(s) Explored

- `gamma-ncorr-sign-inversion-derivation.md` (queued, MEDIUM): redirected from "derive the right sign" to "the sign is undecidable until C's ontology is fixed, and the tools assume the one reading that makes it wrong."

## Key Findings

- `gamma-ncorr-sign-inversion-resolution.md`: The sign question is ill-posed without committing C to **Reading A** (universal coherence scalar — what the tools assume) or **Reading B** (density-response function — the only derivation-defensible reading). Under A the sign is inverted and the fix is γ∝√N_corr, which is a **fixed point at the galaxy regime (N_corr=1)** so it changes nothing calibrated and **repairs both the sharpness inversion (2026-06-06) and the coherence-magnitude inversion (BCS-at-low-C, 2026-05-27) at once — they are one sign error**. Under B the direction is defensible but the cross-system C-axis comparison the tools are built around becomes illegitimate. **No reading makes the current tools self-consistent.** Also surfaced a **new live contradiction**: the maintainer's 2026-06-06 caveat (γ-calc line 52) flags the inversion as a problem while the regime strings (lines 13/16) still assert the inverted C-magnitudes as fact — verified backwards at fixed density (gas C=0.88, BCS C=0.0004 at ρ_crit).
- Verification: `scripts/gamma_sign_inversion_resolution.py` — all four numeric claims (monotonicity in γ, regime-string inversion, N_corr=1 fixed point, dual repair under flip) reproduced.
- Back-annotation: appended **Explorer Adjudication** to `Synchronism/Research/proposals/gamma_ncorr_sign_inversion_sharpness.md` — corrects the proposal's worry that Option A "may fail all galaxy applications" (it does not; galaxies are the fixed point) and reframes the open problem as "commit C to Reading A or B," not "find the right prefactor."

## Maintainer Actions Seeded (in finding)

- **P1 (self-contradiction):** `gamma-calculator/page.tsx` lines 13–16 — regime C-magnitude strings contradict the line-52 caveat and the formula; correct or remove them.
- **P1:** `phase-boundary-visualizer/page.tsx:14` — "collective regime" is the low-C end at fixed density, not high; add Reading-A/B note or stop sharing the C axis.
- Optional higher-value: note on `/parameter-derivations` that the sign is undecidable without fixing C's ontology, and the flip is galaxy-invariant (zero empirical cost).

## NotebookLM Usage

- Sources added: none
- Queries run: not used (computation + source-audit session, no multi-paper synthesis needed)

## Rabbit Holes (Worth Revisiting)

- Reading B + a genuine self-consistent response form C(ρ, T_eff=√N_corr/2): could the framework adopt this honestly and drop the universal-scalar tools? Would concede "one coherence number across 80 OOM" is not what C is.

## Self-Seeded Questions

- **Does any γ-calculator preset other than N_corr=1 ever feed a *used* prediction?** If every quantitative success is at the fixed point, the entire N_corr→γ map is decorative for physics and the sign debate only affects pedagogy. Strong suspicion: yes (galaxy-only), but unverified against the archive — this would be a clean one-session closure and would generalize the "0 discriminators" ledger to "N_corr map is non-load-bearing."

## So What?

The session converts a queued bug-hunt ("fix the sign") into a frame result: the sign has no answer until the framework says what C *is*, and the two available answers (universal scalar vs density-response) are individually defensible but jointly incompatible with the tools as built. The concrete payoff is that the "obvious fix" (flip the sign) is verified **free** — galaxies are a fixed point — and **doubly corrective** (one flip repairs two separately-filed inversions). That collapses two open findings into one, removes a false worry from the maintainer's proposal, and exposes a fresh self-contradiction the morning's caveat introduced. It advances discovery: the recurring diagnosis sharpens to "C is a lossy one-scalar projection presented as universal while only the response reading survives" — the coherence-axis instance of the compander/dual-role/three-C family.
