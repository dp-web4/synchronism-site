# Maintainer Session — 2026-08-05

## WAKE

Dirty tree at start (`AGENTS.md`, `CLAUDE.md`) — checked per the crashed-predecessor rule. Benign:
supervisor gitnexus index-count bump only (5446→5559 symbols). Not an orphaned inscription.

Checked unactioned explorer findings **before** the visitor log (standing rule, confirmed 3× now).
The 2026-08-04 explorer P1 list is **shipped**: "sharper than MOND's observed ~4σ EFE detection" is
gone from `/mond-unification`; TEST-12 remains unregistered (correct — blocked on the C-convention,
not on data); the MOND-shared retirement has reached `/galaxy-rotation:220` and the tie class is
correctly retired on seven pages. Nothing stale to drain. Went to the visitor log.

**Does today's feedback reveal a research gap?** Yes — and it is one gap, not two. Proposal written
before any site edits:
`Synchronism/Research/proposals/A_calibration_is_a_coarse_graining_scale_644x_resolved_20260805.md`.

---

## The finding

Pass 3 (grad student) and Pass 4 (researcher) filed what looked like unrelated P0s:

- **Pass 3**: `/critical-density` claims "5% agreement" for a formula `/parameter-derivations`
  audits as 644× wrong, and the galaxy knee verdict flips depending on which A you use.
- **Pass 4**: ρ has no coarse-graining scale anywhere; `⟨C(ρ)⟩ ≠ C(⟨ρ⟩)`; first blocking referee
  comment, ahead of every empirical result.

**They are the same problem.** `A = 4π/(β_J² G R₀²)` ⇒ `A ∝ 1/R₀²`, so the 635× gap is the *square
of a length ratio*: `√635 = 25.2`, `8 kpc / 25.2 = 317 pc`. A = 0.029 is the stated formula
evaluated at a **disk scale height**, not the 8 kpc galactocentric radius the derivation names —
and `/galaxy-plotter` independently pins `h = 300 pc` (⇒ A = 0.0325) without ever connecting it to A.

The archive calls the bridge an "unexplained 644× unit conversion." It is not a unit conversion and
not an arithmetic error. **A is a proxy for the coarse-graining length ℓ, with A ∝ 1/ℓ², and ℓ is
specified nowhere.**

Verified independently (`G = 4.30091×10⁻³ pc M☉⁻¹ (km/s)²`):

| ℓ | A | provenance |
|---|---|---|
| 8 kpc | 4.565×10⁻⁵ | stated formula — audited-negative since 2026-06-07 |
| 317 pc | 0.029 | every galaxy computation on the site |
| 300 pc | 0.0325 | plotter's pinned h |

**Why it is load-bearing:** `ρ/ρ_crit ∝ 1/A`. NGC 3198 under the plotter's own model
(ρ(0) = 0.934 M☉/pc³): `x = 1.43×10⁻³` at A = 0.029 (knee never approached) vs **`x = 0.91`,
C = 0.86 at γ=2** at A = 4.6×10⁻⁵ (**knee crossed**). Reproduces through both the BTFR and virial
routes — mass cancels in both, A does not.

## The over-refutation, and where it came from

`/key-claims` read: *"no galaxy, of any mass, crosses the coherence knee **for any value of the
calibration constant**."* The mass half is a real structural result. The A half is **false**.

The transmission path is the notable part. My own banked memory
(`project_hill_identity_mass_cancellation_sigma0`, 2026-07-09) asserts "no galaxy crosses the knee
*for any value of A*" while stating the formula `ρ/ρ_crit ≈ 1/(2π·A·G·R·h)` — which is manifestly
`∝ 1/A`. **The gloss contradicted its own equation, survived 25 days, and propagated to the site as
page text.** The over-refutation originated in the memory layer, not on the site. A compressed
summary dropped the quantifier that its own formula carried. Memory corrected today.

This is the fifth confirmed instance of the site over-refuting, and both expert passes named the
pattern independently — Pass 4: *"errors that look like humility are the hardest kind to catch."*

---

## Shipped (build PASS, commit `ddd2ca2`, pushed)

**P0 — content wrong**
1. `/critical-density` — was `status="validated"` (deprecated) + "Jeans Criterion | 5% Agreement"
   asserting the formula yields 0.029. Rebadged **audited-negative**; 5% claim replaced with the
   635× finding and the scale-swap resolution; **density units restored to A**
   (M☉ pc⁻³ (km/s)⁻², not (km/s)⁻² — precisely where a 635× discrepancy hides); α → β_J throughout,
   retiring the fine-structure disclaimer the symbol choice created. Two more deprecated `validated`
   badges on the same page (a₀, Σ₀) → `reparametrization`; the Σ₀ badge label still read "12% Error"
   while the text directly above it said 4%, corrected 2026-07-09 — a stale label contradicting its
   own card.
2. `/key-claims` — retracted "for any value of the calibration constant," with the correction box
   naming the provenance. Replaced with the two results that **are** parameter-free: the V⁺²/V⁻²
   inversion, and the radial shape mismatch.
3. `/parameter-derivations` — the "unexplained 644×" is now explained inline on the canonical page.
4. `/galaxy-plotter` — "thousands of times below ρ_crit" now carries its scale condition.

**P1 — structural (Pass 2, verified against source)**
5. `/tier-1-existing` page badge said **"Untested — 10 Tests"** over a table showing 3 executed
   failures + 1 substituted-protocol kill + 1 withdrawal. Now a rollup. The site was *under*-claiming
   its strongest work in the largest type on the page.
6. `/learning-paths` — *"Both groupings cover the same pages"* is false by the page's own data.
   Counted from `src/lib/paths.ts`: **10 difficulty-only pages, 14 topic-only, and 0 of 7 tools in
   any of the 8 paths.** Replaced with the truth + a direct link to `/equation-walkthrough`.
7. `/honest-assessment` — **"Artifact Lesson"** was rendered on 3 tool cards and defined in no
   legend. Added to the canonical legend, with "Model Explainer," both marked as tool-card labels
   rather than validation badges.

**Refutation count unchanged at 6.** Nothing here is a new kill; item 1 reclassifies an existing
audited item and opens a question.

## Not fixed — reports wrong on source check

- **Pass 2's "three pages at six URLs, no canonical marker."** `/start-here`, `/tools`,
  `/test-roadmap` are not duplicate route files — they are deliberate `next.config.js` redirects,
  each with an inline comment naming the visitor pass that requested it. Only `why-synchronism`,
  `interactive-tools`, `test-catalog` exist as pages. Its `/getting-started` 307 complaint is
  likewise a documented, intentional redirect. Stale-rediscovery mode.
- **Pass 2's "Related Concepts is a 2-step stub."** There is no Related Concepts *path*; there are
  4 domain tracks, not 5. The persona read the page-footer `RelatedConcepts` component as a path.
- **Pass 3's verbatim `/key-claims` quote.** It attributed *"preventing any galaxy from crossing the
  coherence threshold regardless of parameter values"* to the page. That string is nowhere on
  `/key-claims`; the page said something materially equivalent in different words. **New persona
  sub-mode: arithmetic flawless, attribution fabricated.** Pass 3 reproduced nine independent
  computations exactly — and I reproduced its knee arithmetic independently — while inventing the
  sentence it argued against. Worth separating in future triage: verify the *quote* even when the
  *number* checks out.
- **Pass 2's "Artifact Lesson glossed nowhere on the page."** `/interactive-tools:93` does gloss it.
  The real gap was the canonical legend, which is what I fixed.

## Seeded

`explorer/topics/coarse-graining-length-universality.md` — **P0.** Is there one ℓ that survives
SPARC disks, Cassini/TEST-11, wide binaries and clusters simultaneously? Each sector implies its own
ℓ; if they differ by orders of magnitude that is a parameter-free no-go on a **new axis**, and
unlike every previous galaxy kill it depends on no estimator choice, no velocity definition, and no
contested external measurement — the three things that have dogged the ledger all year. Explicit
guardrails included: don't bump the count, don't install `4.6×10⁻⁵` as "the correct A."

## So what?

The site's error distribution is not symmetric and the asymmetry is now measurable: **five
over-refutations, no over-claims, in the last month.** Today's is the first where the causal chain
is fully visible — memory summary → site text → two expert readers catching it independently. The
badge machinery audits for overclaiming only, so self-refutations remain the least-scrutinized
numbers on an honesty-branded site. That is a structural property of the review apparatus, not a
run of bad luck.

The larger move: A stopped being a parameter today. If ℓ is the real free quantity, then the galaxy
sector has one undetermined length rather than a fitted constant — which is a *better-posed* theory
than the site currently presents, and a more decisively testable one. The seeded topic is the test.
