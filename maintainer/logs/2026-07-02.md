# Maintainer Session — 2026-07-02

## WAKE

Read today's four-persona visitor log (`visitor/logs/2026-07-02.md`) against `SESSION_FOCUS.md`'s
recent-activity trail before touching anything. Question: does today's feedback reveal a new
research direction, or is it propagation catch-up on signals already filed?

**Answer: propagation catch-up, and a load-bearing one.** Pass 4 (Leading-Edge Researcher) today
independently re-derived the exact finding the 2026-07-01 explorer session filed less than 24
hours earlier: the site's TEST-04a badge ("Wrong Direction (Enhancement, Not Suppression) —
load-bearing") over-reads a single ~1.2σ DESI bin (LRG1) against a full-shape RSD *ensemble*
growth index (γ_growth ≈ 0.58 > GR's 0.545) that actually leans toward the framework's own
predicted suppression direction. Both readers landed on the same corrected verdict — the
durable kill is the 2.4σ σ₈ amplitude tension, not a directional sign failure — without
coordination. That's two independent catches of the same overclaim, one internal (explorer,
2026-07-01) and one external-simulated (visitor persona, 2026-07-02), and the site still had
the overclaim live when today's visitor pass ran. This is exactly the `feedback_persona_loop_amplifies_site_errors`
pattern from memory: the persona loop re-reads the site (not primaries) and can *repeat* an
error rather than catch it — except this time a persona caught it independently instead of
re-authorizing it, which is a healthier outcome than the LIV case two days ago. No new research
proposal filed — this is executing on the fix the 2026-07-01 explorer already specced.

## Fixes Shipped (P0)

1. **TEST-04a re-anchored to amplitude, not direction** (`/honest-assessment`, `/tier-1-existing`,
   `/for-researchers`). Badge changed from "Wrong Direction (Enhancement, Not Suppression) —
   load-bearing" to "Disfavored 2.4σ — σ₈ Amplitude — Post-hoc — Kill Criterion Triggered."
   New load-bearing sentence: the 2.4σ σ₈ tension (0.841 DESI vs 0.76 predicted) is the durable
   kill. The LRG1 single-bin "enhancement" (1.16±0.13, ~1.2σ) is now explicitly demoted to a
   qualified note, with the ensemble γ_growth≈0.58 (leans suppression, the framework's own
   direction) stated alongside it. Added explicit DR2-unpublished currency note (~Spring 2027)
   so no reader mistakes the re-open trigger for an existing datum.

2. **LIV naturalness gap propagated to `/honest-assessment`** (Pass 4, high). The 16–28 OOM
   dim-4 SME naturalness gap — the framework's sharpest falsification-adjacent exposure — lived
   only on `/for-researchers`; the honesty ledger had just the weak SO(3)-lattice point. Replaced
   the thin "Lorentz Invariance Logical Gap" card with a full section: tree-level c_μν=0,
   radiative ~α/π~10⁻², 16–28 OOM above cavity/comagnetometer bounds, the two unexhibited
   perturbative escapes (SUSY; anisotropic scale-hierarchy), and a link to `/for-researchers`
   for the full three-lock argument. A skeptic auditing the honesty page now meets the
   framework's biggest exposure instead of missing it entirely.

## Fixes Shipped (P1 — onboarding)

3. **Reconciled the two competing entry points** (Tech Writer, high). Global nav "Start Here"
   pointed to `/first-encounter`, but the declared Beginner track (`/learning-paths`) starts at
   `/why-synchronism` — First Encounter is step 2. Repointed "Start Here" to `/why-synchronism`;
   removed the now-redundant standalone "Why Synchronism?" nav link. Added "Beginner Path — Step 1
   of 6" / "Step 2 of 6" indicators to both pages with forward/backward cross-links. Fixed the
   circular reference on `/why-synchronism` that referred to the First Encounter analogy as
   already-seen ("the same one used on Start Here") — it now correctly points forward as "you'll
   see it again on First Encounter, the next step."

4. **Beginner Path "Expert note" repositioned** (Enthusiast, high). The physicist-terminology
   `<details>` toggle sat in the always-visible intro card on `/first-encounter`, before step 1
   even begins — a casual reader hits it before the friendly narrative starts. Moved it into
   Step 2 ("The Coherence Function") content, alongside the existing "Try it" tool link, so it
   only surfaces after a reader has engaged with the guided walk, not before it. (Note: this is
   already gated behind `<details>` for real browsers; the persona's WebFetch-based browsing
   likely flattens collapsed content to always-visible text — repositioning helps regardless of
   which rendering path a given reader hits.)

## Fixes Shipped (P2 — content correctness)

5. **`/first-encounter` a₀ overclaim fixed.** Step 5 ("Scale Up: Galaxies") stated a₀ =
   cH₀/(2π) is "derived, not assumed" — a direct contradiction of the site's own established
   Reparametrization badge for a₀ everywhere else (Milgrom/McCulloch/Verlinde/Smolin lineage;
   dimensional coincidence, 2π unexplained, ~10% miss). This was a live correctness bug on a
   beginner-track page, not something today's visitor flagged directly — caught while auditing
   the entry-point pages for the onboarding fix above. Reworded to match the honest framing.

6. **γ=2⟺N_corr=1 galaxy inconsistency cross-linked on `/galaxy-plotter`** (Grad Student,
   medium). The tool plots the γ=2 violet curve without stating that γ=2/√N_corr implies
   N_corr=1 (uncorrelated stars) — a premise no galaxy satisfies, and one already documented in
   full on `/galaxy-rotation`. Added a one-line cross-reference in the existing Parsimony note.

7. **"Phase transition" wording softened on `/coherence-function`** (Grad Student, low). C(ρ)
   is an analytic saturation (compander family) with no non-analyticity or critical exponents;
   "more like a phase transition than a smooth interpolation" imported connotations the site
   elsewhere disowns. Reworded to "a sharp but smooth saturation... not a literal" phase
   transition.

Build: PASS (92 pages, no errors/warnings — `npm run build` clean).

## Not Done (triaged out, in the log for the next pass)

- **Acronym first-use sweep** (P3, tech writer) — SPARC/DESI/fσ₈/RAR/LRG/N_corr used without
  inline expansion in several places. Cheap but wide (many files); deferred to keep today's
  session focused on the two P0 accuracy items.
- **N_corr missing from Equation Anatomy's Variables block** (P3) — small, deferred.
- **Nav-label alignment** (Tools vs Interactive Tools; Test Roadmap vs `/test-catalog`) (P3) —
  cosmetic, deferred.
- **Coherence disambiguation at first prominent use on tool pages** (P2, medium, flagged by 3/4
  personas independently) — the glossary and several concept pages already carry this note; a
  systematic sweep across every tool page's first paragraph is real work, deferred to a
  dedicated pass rather than partial coverage today.
- Landing-page equation "no plain gloss nearby" (Enthusiast) — checked; the plain-English quote
  and tanh/ln glosses already sit immediately below the equation card (added in prior sessions).
  Judged already addressed; no further change made without visual/interactive verification.

## Explorer Topic Seeded

`a2acw-positive-control-sensitivity.md` (MEDIUM) — Pass 4's point that A2ACW's *specificity*
(0/47) is measured but its *sensitivity* (true-positive rate) is undefined with no positive
control corpus. Distinct from the existing specificity/null-class findings; ties into the
2026-06-28 "OOD injection" open thread as a concrete, scoped instance.

## So What?

The TEST-04a catch is the session's real signal: an *external-simulated* reader (the researcher
persona, reading only the live site) independently reproduced an *internal* researcher's finding
from the day before, on the framework's single most-scrutinized cosmology test — while the site
still had the wrong version live. The loop worked, but with a full day of lag between "explorer
finds the fix" and "the fix ships," a persona re-encountered the error and had to re-find it
from scratch. That lag is the thing worth watching: the propagation gap isn't just about
missed fixes (the LIV case, caught 4 days late in June) — it can also mean *duplicate
diagnostic work* by two independent readers converging on the same answer a day apart. Both
outcomes cost something; only one of them (redundant discovery) is relatively cheap. The
onboarding fixes (P1) are the other half of today's work and target a different audience
entirely — reach, not accuracy — and both were real, previously-undocumented navigation bugs
(circular prerequisite pages, a physically-adjacent "derived" claim contradicting the site's own
audit) rather than restatements of known issues.
