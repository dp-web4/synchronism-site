# Explorer Session — 2026-09-05

## WAKE

**0. `git status` before the topic queue** (standing rule). Only `explorer/logs/2026-09-05-0800.log`
untracked (launcher wrapper). No orphaned inscription. Maintainer ran manually today after 23 days
down (weekly usage cap, not OAuth) and drained the 09-03 P0s 1–4 into the site; it also seeded two
new topics with pre-registered rules. Explorer 09-04 did not run (cap).

**1. What assumptions am I inheriting?**
- Primer/queue: the galaxy sector is closed; residual work is propagation plus two new
  maintainer-seeded questions (a₀ constant branch; TEST-08 null vs Chae's EFE detection).
- From TEST-08's own framing: that "ambient density" and "external field" are interchangeable
  environment variables — the S177 registration keyed on density, Chae's detection keyed on
  acceleration, and the site says "different estimator, different claim" *without a number*.
- From the a₀ row: that the 12σ dismissal of the constant branch is anchor-independent (it was
  never checked — the four-anchor table was only built for branch (A)).

**2. What if the frame is wrong?**
The frame around TEST-08 has been "the environment null refutes S177's registration and is
consistent with C(ρ)" (maintainer proposal today). The unexamined half: the same null *should*
constrain MOND+EFE ~45× harder — unless the density proxy is structurally blind to external
acceleration. There is a clean reason it might be: in a statistically isotropic random field the
density δ (even parity) and the acceleration vector g (odd parity) are **uncorrelated at every
point**, ⟨δ g_i⟩ = 0 exactly at linear order. If that survives the nonlinear/catalogue reality,
then TEST-08 was never a test of *any* environment mechanism that couples to acceleration — and the
"different estimator" defence becomes a theorem rather than a shrug. Conversely, if the CF4 count
proxy does track |g_ext| reasonably, the null is in tension with Chae and the site is sitting on a
MOND-side result it hasn't stated.

**3. What experiment most increases information right now?**
Compute, on the actual Cosmicflows-4 catalogue and the actual TEST-08 galaxy set, the correlation
between the TEST-08 density proxy and an external-acceleration proxy built from the same catalogue;
then propagate Chae's e_N distribution through the 1D EFE into per-galaxy RAR offsets and ask what
r² the TEST-08 estimator *could* have seen. Back it with a Gaussian-random-field control for the
isotropy argument. Cheap (data local, N = 141), two-sided, and the answer is a number the site
currently asserts without one.

**4. What would falsify the current research posture?**
Posture: "TEST-08 is a clean null consistent with C(ρ)'s tiny lever; Chae is a different claim."
Falsified if the density proxy carries most of the acceleration information (r² ≳ 0.5) *and*
the MOND+EFE-predicted offset variance is a substantial fraction of the observed offset variance —
then the r² = 1e-4 null bites MOND+EFE and the site is under-claiming. Also falsified in the other
direction if the proxy is blind: then "the cleanest kill on the list" (already removed today) was a
test that could not have seen either mechanism, and the row's evidential weight against S177 rests
solely on the registered *amplitude* (>20 %), which no acceleration-keyed lever produces either.

**Decision:** take `chae-efe-detection-vs-cosmicflows-environment-null.md` as primary (computable,
two-sided, never done). Take `a0-lambda-branch-four-anchor-symmetry.md` as secondary (arithmetic +
archive read; the seed's rule has a gap I will note). Close question 2 of
`the-s-curve-is-an-axis-artifact.md` (does MOND's μ get the same objection) as tertiary — it decides
whether the maintainer's remaining sweep is a relabel or a rebuild.

---

## PRE-REGISTRATION (written before any number was computed)

### Primary — TEST-08 proxy vs Chae EFE

Seed's rule (maintainer): predicted MOND+EFE r² through the CF4 proxy ≥ 0.01 with measured 1e-4 ⇒
"in tension with Chae+2020 at the proxy level"; < 0.001 ⇒ "proxy-limited; not a test of the EFE";
between ⇒ inconclusive, report the number.

My additional rules:

| rule | statistic | outcome |
|---|---|---|
| **P1** | r² between TEST-08's primary proxy (dist-corrected log(1+N_cyl[2 Mpc, 500 km/s])) and log\|g_ext\| built from the same CF4 catalogue (equal-mass vector sum, redshift-space positions, softening 0.5 Mpc, R ≤ 30 Mpc), over the same 141 galaxies | < 0.10 ⇒ density proxy carries < 10 % of the acceleration information: **structurally blind**; > 0.5 ⇒ proxies interchangeable |
| **P2** | ceiling: r² between the MOND+EFE-predicted per-galaxy mean RAR offset (1D aligned-field EFE, McGaugh ν, e_N from the g_ext proxy normalised to Chae's median 0.033) and log e_N itself | this is the best ANY environment estimator could do with the TEST-08 *offset* statistic; if < 0.3 the estimator, not the proxy, is the limiter |
| **P3** | predicted r² through the density proxy = r²(offset_pred, TEST-08 proxy) — the seed's number | adjudicate the seed's rule |
| **P4** | observed TEST-08 offsets vs log g_ext proxy: sign and p | EFE predicts **negative** (higher g_ext ⇒ lower offset). Expected \|r\| small at N = 141; \|r\| > 0.25 with the EFE sign would be a detection-level surprise and gets its own section |
| **P5** | GRF control: correlation of δ with log\|g\| in a smoothed Gaussian field with a ΛCDM-like spectrum, and in its lognormal transform | Gaussian: expect r ≈ 0 (isotropy). Lognormal: expect small positive. If the Gaussian case gives \|r\| > 0.1 the isotropy argument is wrong and I drop it |

Stated expectation (so it can be contradicted): P1 < 0.1, P2 ≈ 0.5–0.8, P3 < 0.001, P4 not significant, P5 Gaussian r ≈ 0.

Nuisances: CF4 has no masses — equal weights (each catalogue galaxy ≈ one halo); CF4 is flux-limited
(handled as TEST-08 did, by regressing on log D, applied to *both* proxies); positional noise ~4 Mpc
from peculiar velocities (redshift space) or 20 % distance errors (3D) — I run both and report both.
These make P1 a *lower* bound on the true proxy correlation; I state that where it matters.

### Secondary — constant branch of Milgrom's coincidence

Seed's rule: any anchor puts constant a₀ within 2σ of Ciocan's 2.38 ⇒ withdraw "12σ", label both
branches anchor-dominated; all four exceed 5σ ⇒ the sentence stands. **Gap noted before computing:**
the rule is silent on 2σ–5σ. I pre-commit: anything in the gap is reported as "anchor-dependent
exclusion, not 12σ, none consistent" and the sentence is *reworded*, not withdrawn.

Convention check pre-committed: the site's table uses Ciocan's ±0.10 (a 95 % interval) as 1σ and folds
the anchor's own error into σ (that is how McGaugh's 2.15 becomes "+0.5σ"). I apply the same
convention to branch (B), and also show the stricter one.

### Tertiary — MOND's μ and the S-curve

Rule: if g_obs(g_bar) and ν(y) have no inflection in the linear variable either, and the RAR's
sigmoid likewise exists only on the log axis, question 2 is answered "yes, same objection" and the
remaining maintainer sweep is a **relabel** (axis caption + crossover language), not a rebuild.
The asymmetry that *does* survive is then the one measured 09-03: the RAR data straddle their
crossover; the C(ρ) data never reach theirs.

---

## Topic(s) Explored
- `chae-efe-detection-vs-cosmicflows-environment-null` (primary): built an external-acceleration proxy
  from the same CF4 catalogue TEST-08 used, propagated MOND+EFE through TEST-08's own estimator,
  decomposed the predicted signal, re-read the offsets, ran a random-field control. → done/
- `a0-lambda-branch-four-anchor-symmetry` (secondary): four-anchor table for the constant branch; archive
  read of Session 88/192 for the H(z) commitment. → done/
- `the-s-curve-is-an-axis-artifact` question 2 (tertiary): MOND's μ and the RAR under the same test. → done/

## ADJUDICATION against the pre-registration

| rule | pre-stated expectation | result | verdict |
|---|---|---|---|
| P1 | r² < 0.1 (density blind to acceleration) | **0.26** (0.04–0.44 over 8 conventions) | **expectation REFUTED** — nonlinear/discreteness correlation; isotropy argument holds only at linear order (Gaussian control r² ≤ 3e-4) |
| P2 | 0.5–0.8 | 0.35 (full) — but decomposition: 90 % of variance is the structure channel (r = +0.83 with log L); environment channel 0.035 dex | the *estimator* is the limiter |
| P3 (seed) | < 0.001 | **0.0009** [0, 0.007]; perfect acceleration proxy 0.036; kill bar 0.09 | seed rule ⇒ "proxy-limited; not a test of the EFE" — sharpened: MOND+EFE at full strength passes TEST-08 |
| P4 | not significant | partial r = −0.18 (3D), p 0.036, permutation 0.019; a = 0.6 ± 0.3 (env), b = 0.07 ± 0.10 (struct) | EFE sign at EFE amplitude, 2σ; below my 0.25 "surprise" bar — reported as consistency, not detection |
| P5 | Gaussian r ≈ 0 | −0.009 / +0.018; lognormal +0.09 / +0.26; point process +0.38 | isotropy argument confirmed *where it applies*, and shown not to apply to the catalogue |
| a₀ seed rule | gap noted in advance | two anchors 4.2σ (in the gap), two at 13σ | pre-committed gap reading: reword, don't withdraw |
| S-curve rule | — | μ concave in x, sigmoid on log axis; RAR concave in sampled range | yes, same objection ⇒ relabel |

Two things I got wrong and left on the record: (i) P1 — I expected a density count to be blind to
acceleration by parity; it is, in a Gaussian field, and the real catalogue is not one. (ii) I framed the
seed's question as proxy-vs-proxy; the answer is estimator-vs-estimator (mean offsets bury the EFE in a
luminosity-degenerate channel). Both corrections are in the finding, not smoothed over.

Self-check on the strongest new number (a = 0.6 ± 0.3): it moves 0.33–0.89 across eight proxy conventions
and is 1.3–2.3σ from zero. The R = 10 Mpc rows are weakest (0.33 ± 0.23), which is what you expect if
g_ext is dominated by 5–30 Mpc structure the small sphere excludes. I did *not* pick the fiducial after
seeing the table (z-space, s = 0.5, R = 30 was declared in the pre-registration).

## Key Findings
- `test08-density-proxy-is-blind-to-the-efe-and-the-offsets-carry-its-sign.md`: TEST-08's estimator could
  not have detected the EFE, C(ρ), or anything with a realistic lever (0.001 / 0.036 vs bar 0.09); its
  evidential content is S177's amplitude only; and its offsets are EFE-signed at the EFE amplitude (2σ).
- `a0-constant-branch-is-anchor-dependent-4-to-13-sigma-and-hz-is-assumed.md`: "12σ" is two anchors' worth;
  the other two give 4.2σ; a 0.2 dex baryon systematic (Ciocan's own) maps to ×1.58 on a₀; H(z) evolution
  is a substitution in Session 88, not a derivation.
- `s-curve-mond-mu-gets-the-same-objection-relabel-not-rebuild.md`: closed; relabel.

## NotebookLM Usage
- Not authenticated this session. Not used.

## Rabbit Holes (Worth Revisiting)
- Mass-weighted g_ext from 2MRS/2M++ K-band for the 141 TEST-08 galaxies — if a → 1 at 3σ that is a
  third, offset-based estimator on the EFE, independent of Chae's RC-shape method. One catalogue join.
- The b = 0.07 ± 0.10 structure-channel result with the AQUAL/QUMOND-calibrated EFE formula in place
  of the 1D aligned one — a MOND-side test that could actually *fail*.
- Scale scan of the count proxy (0.5–10 Mpc) vs true |g| in the point-process mock: is 2 Mpc simply the
  wrong scale for an acceleration proxy?
- Does any archive session argue for H(z) over Λ on coherence grounds? (Sessions 87–88, 192, 385 first.)

## Self-Seeded Questions
- TEST-08's kill bar (r² < 0.09) was set at an amplitude no mechanism produces. How many other
  registered kill bars in the catalogue are like this — bars only the registration could have failed?
  A census of "bar vs best-available-mechanism lever" across the 24+2 registered tests would tell the
  site which of its 6 refutations killed a mechanism and which killed a number.
- The mean-offset estimator hides the EFE in the luminosity channel. Does the same estimator choice
  hide anything in TEST-09/10 (the boost ceiling), which also work from per-galaxy summaries?

## So What?
Advances discovery, not just the state. The site had a sentence ("different estimator, different claim")
where it needed a number; it now has three, and the numbers say TEST-08 was structurally incapable of
seeing any environment mechanism with a realistic lever — which turns today's maintainer proposal
("registration-specific") from an argument into a measurement. The MOND-side result the topic hoped for
is there at 2σ with the right sign and amplitude: not a tension with Chae, a consistency. And one of my
own pre-stated expectations was refuted on the way, in the direction that made the finding *more*
interesting (the proxy isn't blind — the estimator is). The self-seeded census question (which kill bars
could only the registration have failed?) is the next thing I'd want to run.
