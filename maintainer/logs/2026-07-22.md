# Maintainer Session — 2026-07-22 (recovery session)

## Context: four failed runs preceded this session

The scheduled 06:00 maintainer runs failed on 07-19 and 07-20 ("out of usage credits"),
07-21 never ran, and today's 06:00 run died after printing its start header. This session
ran later on 07-22 as the recovery pass. Consequence observed in the wild: the 07-18
explorer's P1 (TEST-09 card resolution) sat unshipped for four days, and today's expert
visitor personas re-derived several queued findings from the live site as if new.
SESSION_FOCUS carries a re-raised infrastructure ALERT.

## WAKE

Does today's feedback reveal a research gap? Mostly no — it revealed a **propagation
backlog**. Both of Pass 4's research-level signals already exist in the research repo
(PCI* = 0.31 is named in `c_observable_calibration_gap.md`; the S8-dissolution is
`test04a_s8_calibration_target_dissolved_kids_legacy.md`), and the DESI DR2 pre-commitment
Pass 4 asked for was adopted by dp on 07-17 — the site just didn't say so. The one
genuinely unregistered item became today's proposal: the framework's silence on DESI DR2's
evolving-dark-energy preference (w₀wₐ) is undocumented scope, and undocumented scope is
what makes future retro-fits possible. Proposal filed **before** fixing friction:
`../../Synchronism/Research/proposals/w0wa_evolving_dark_energy_scope_registration.md`.

## Feedback Reviewed

- Visitor log 2026-07-22 (4 personas). Verification-against-source results:
  - **REAL — fixed**: a₀/Σ₀ H₀ incompatibility (Pass 3, HIGH); tanh curve never drawn
    (Pass 1, HIGH); learning-paths topic tracks invisible without JS (Pass 2, HIGH — the
    tracks existed behind a client-side toggle); MRH undefined at first use on `/`,
    `/research-philosophy`, `/honest-assessment` (Pass 2, HIGH); TEST-09 robustness status
    stale (P1 from 07-18); 1.4% missing denominator (Pass 3); plotter changelog-style
    legend label (Pass 1); w₀wₐ + PCI frontier gaps (Pass 4).
  - **STALE / ALREADY FIXED**: TEST-04a S8-weakening note (already in the card's FRAMING
    sentence and the honest-assessment S₈ item); first-encounter MRH gloss (already
    gloss-before-name); Model Explainer disclaimer (already on the tools page, twice);
    phase-boundary and gamma-calculator framing boxes (shipped 07-18).
  - **NOT IN SOURCE**: "Specificity (false-positive rate): 0/6 = 1.0" mislabel — the
    rendered source says the correct thing; likely a WebFetch paraphrase artifact. One
    genuinely ambiguous parenthetical ("no measured false-positive rate") tightened anyway.
- Forum: nothing new.
- Explorer findings: TEST-09 robustness §Action (drained, 4 days late).

## Changes Made (Site) — commit 185e49f

- `tier-1-existing`: TEST-09 "definition-robustness pending" → **definition-robust by
  execution (2026-07-18)** with PREDICTIONS.md's honest nuance (significance carried by
  V_max, V_flat alone ~1.2σ, W_P20 thin, inner-disc out of scope) + prefer-V_max citation
  guidance. TEST-04a: **DESI DR2 three-branch prospective registration (adopted by dp
  2026-07-17) added to the card** — answers Pass 4's #1 unanswered question.
- `parameter-derivations`, `mond-unification`, `galaxy-rotation`, `key-claims`,
  `dark-matter`, `critical-density`, `first-encounter`, `freemans-law`,
  `honest-assessment`, `why-synchronism`, `lib/terms.ts`: **a₀ standardized to
  1.04×10⁻¹⁰ m/s² at H₀ = 67.4 (~13% below Milgrom), consistent with Σ₀ = 119** —
  closes Pass 3's algebraic-incompatibility catch with dated correction notes.
- `core-idea`: **tanh S-curve drawn as inline SVG** where the equation lives — dimmer
  labels, the C = 0.5 midpoint at ≈0.32·ρ_crit, and the C ≈ 0.88 saturation knee at
  ρ_crit marked honestly; caption flags γ = 2 as refuted.
- `learning-paths`: client-side toggle removed; **By Difficulty and By Topic both render
  statically** (server component now) — the nav's "Physics, Chemistry, Philosophy"
  promise is now visible to no-JS readers and crawlers.
- `coherence-explorer`, `equation-walkthrough`: "what this shows / what it gets wrong"
  framing boxes above the controls (sign inversion + knee-vs-midpoint stated before the
  reader builds the wrong intuition). `phase-boundary-visualizer`: N_corr values labeled
  **asserted, not counted** in the lead.
- `page.tsx` (landing): MRH expanded at first use; 1.4% survival rate now carries its
  denominator (≈47 of ~3,300) and links to /for-researchers.
- `research-philosophy`, `honest-assessment`: MRH expanded inside the "MRH-relationship
  tags" taxonomy headings — the taxonomy's own name no longer contains an undefined
  acronym. `honest-assessment`: **w₀wₐ evolving dark energy added to the cosmology
  no-prediction list** with the DESI DR2 numbers and the scope verdict.
- `consciousness-demo`: **PCI* = 0.31 engaged** (Casali 2013; Casarotto 2016 validation)
  — the sector's gap reframed from "no calibration exists" to "untested against the one
  calibrated instrument available," with what a C→PCI mapping would need.
- `for-researchers`: ambiguous specificity parenthetical → "a measured false-positive
  rate of 6/6 = 1.0 on the control set." `galaxy-plotter`: legend label humanized
  ("Old illustrative curve (hand-tuned; kept for comparison)").

Build: passes with zero errors. Pushed → Vercel deploy.

## Back-Annotations (Research Repo)

- `Research/proposals/w0wa_evolving_dark_energy_scope_registration.md` (new): register
  "no w(z) coupling" as a dated scope negative before DR2+ data can invite a retro-fit.
  Action gates on dp (PREDICTIONS.md wording/placement).
- (TEST-09 robustness was already back-annotated by the 07-18 explorer; PREDICTIONS.md row
  verified current during required reading.)

## Topics Seeded for Explorer

- `badge-vocabulary-single-source-of-truth.md` — **updated, not duplicated**: today's
  Pass 2 is the third independent re-derivation; added the four-family map angle and the
  sharp question (why does the "preferred" MRH-relationship family have zero adoption on
  claim pages?). Highest-recurrence unfixed structural finding on the board.

## Open Items (Deferred)

- **TEST-10 dSph lead promotion** (Pass 4, low): the 2-OOM dSph impossibility
  (M_dyn/M_bar ~ 10²–10³ vs ceiling 3.17) is still a closing aside; promote to the galaxy
  sector lead. Small fix, next session.
- **Citable artifact freeze** (Pass 4, HIGH, re-raised): no arXiv/Zenodo DOI exists;
  "What's Citable" answers "nothing, procedurally." This is the standing
  `stable_fixed_point_preprint_strategy.md` decision — **gates on dp**, re-raised today
  with a sharper formulation (the three nulls evaporate the moment the site is edited).
- **TEST-02 Gaia DR4 wording** (Pass 4, low): "DR4 resets systematics floor" slightly
  misreads a methodology dispute as a precision one; one-sentence reword.
- **Galaxy plotter progressive disclosure** (Pass 1, medium): default to Newton vs
  observed with other curves opt-in — needs a UI decision, not a text edit.
- Badge single-source-of-truth refactor — topic updated, awaiting explorer/dedicated
  session (component architecture).

## So What?

The instructive failure this session wasn't on any page — it was the **four-day maintainer
outage making the feedback loop's value visible by subtraction**: with no drain running,
expert personas started independently re-deriving the queue from the live site (three of
today's eight "findings" were already fixed or already known). The loop's compounding value
is the cadence, not any single fix. Second lesson, again: verify persona findings against
source before fixing — 3 of 8 were stale and 1 didn't exist; the fabrication-adjacent
failure mode now includes *WebFetch paraphrase artifacts presented as quotes*.
