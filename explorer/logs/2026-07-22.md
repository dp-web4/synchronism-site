# Explorer Session — 2026-07-22

## WAKE Phase

**1. What assumptions am I inheriting from the primer and topic queue?**
- That the ~53-topic queue is the right menu. Most of it is commentary-shaped (audits, write-ups, framing fixes) — the maintainer's recovery session this morning already drained the visitor backlog, so UX/propagation topics are low-marginal-value today.
- That findings are the deliverable. Memory says the explorer loop's failure mode is exactly this: "monotone closure operator... explorer commentary only (since RUN)." The sessions that changed anything (RAR discriminator, TEST-09 BTFR, velocity-definition robustness) were *executions*.

**2. What if the current research frame is wrong?**
The frame risk today is not overclaiming — the site has fully migrated to killing-too-confidently as its failure mode, and the guardrail (pre-fixed verdict rules) is pointed there. The residual frame risk is that "everything reduces to MOND" is asserted at the *function* level but has only been tested for tanh. If some other compander member actually beat McGaugh's ν on SPARC, the "curve-equivalent to MOND" verdict would be wrong in an interesting direction. Nobody has looked.

**3. What experiment or investigation would most increase information right now?**
The compander-family AIC/BIC comparison (queued topic, open since 2026-06-01, Grad Student request 2026-07-01). Every ingredient exists locally: real SPARC mass models (`MassModels_Lelli2016c.mrt`), the fitting/BIC pipeline (`rar_transition_shape_real_sparc.py`), and a pre-statable verdict rule. Both outcomes are informative:
- tanh wins at ΔBIC < −10 vs the family → first (weak) functional-form support the framework has ever had;
- family degenerate → "tanh carries no content" becomes a statement **by execution**, not assertion, and the governing-equation gap closes.

**4. What would falsify the current research posture?**
The posture is "the compander is curve-equivalent to MOND; the functional form is arbitrary within its class." Falsified if: (a) any compander member with correct asymptotics is *distinguishable* from the others on SPARC (family not degenerate), or (b) any member beats McGaugh ν decisively (ΔBIC < −10), which would be new information even for the MOND literature.

**Verdict rule, fixed before running** (per the 07-18 protocol seed): models compared on identical data, identical noise model, BIC = N·ln(SSR/N) + k·ln(N). "Distinguishable" = |ΔBIC| > 10 between family members with the same k. "tanh privileged" requires tanh to beat every same-k family member by ΔBIC > 10. Anything less ⇒ "form-selection null: tanh statistically indistinguishable from its compander alternatives."

**Decision**: Execute `tanh-compander-family-aic-bic` on real SPARC. Queue contains higher-listed topics but none runnable today with this information yield.

## Topic(s) Explored

- `tanh-compander-family-aic-bic` (archived to done/): ten functional forms fit to the real SPARC RAR, 2,807 points, identical pipeline to the ΔBIC=+184 run (which this run reproduces exactly as sanity check). Robustness confirmed by independent shape-grid scan with a vectorized bisection inverter.

## Key Findings

- `compander-family-selection-executed-tanh-not-privileged.md`: **(1)** tanh carries no statistical content — indistinguishable from erf-log/Hill/ν-δ (mutual ΔBIC ≤ 8.9 < the pre-fixed bar of 10) and finishes *last* among the four viable members; form-selection null by execution. **(2)** The falsifier from WAKE §4(a) actually FIRED: the family is NOT degenerate — SPARC refutes arctan-log (+46.7), algebraic-log (+23.8), Gompertz (+58.0), all of which satisfy the site's four qualitative constraints. The site's `/coherence-function` sentence listing arctan among the acceptable forms is now wrong by execution (and "logistic" double-counts tanh — identity: logistic-in-log-argument ≡ tanh; Hill ≡ logistic in ln y). The data selects the *asymptotic pair* (deep-MOND μ∝y; Newtonian return at least power-law fast), and only that. **(3)** γ→0.49 mechanism found: tanh(γln(1+y)) = 1−2(1+y)^(−2γ), so γ sets the Newtonian-return exponent q=2γ; the fit pins q=0.98, cross-validated by free-Hill n=0.975 — both converge on MOND's simple μ (q=1), which ties McGaugh at k=1. γ=0.49 is not a mystery number; it's the tanh encoding of the simple μ-function. γ=2 = q=4 = premature re-Newtonianization (the +184 kill restated). **(4)** Cross-dataset squeeze (inferred, hedged): the galaxy-preferred q≈1 class is exactly what Cassini excludes in modified-gravity MOND (Hees+2016 MNRAS 455,449 — conclusions verified against the PDF; Desmond 2024); tanh-log at its SPARC-fit γ plausibly inherits the exclusion while Cassini-safe γ is SPARC-killed. Runnable Q₂ computation seeded as new HIGH topic.

## Actions Left for Maintainer

In the finding §Action: P1 fix `/coherence-function` compander sentence (arctan refuted, logistic double-counted) + selection table; P2 γ=0.49 mechanism sentence on `/gamma-calculator` + `/galaxy-rotation`; P3 optional Hees/Desmond frontier note.

## Back-annotation

Research repo: `Research/proposals/compander_family_form_selection_executed_20260722.md` (pushed, 6fbdfad1). Governing-equation gap (form chosen, never selected) is now CLOSED as a gap — selection performed.

## NotebookLM Usage

- Status: storage/cookies present but **session auth expired** — `add-url` redirects to Google login. Operator action: re-run `notebooklm login` on CBP.
- Sources added: attempted arXiv:1510.01369 (Hees+2016), failed on auth. Verified the paper via WebFetch/PDF read instead.
- Queries run: none (single-execution session; two primary sources verified directly).

## Rabbit Holes (Worth Revisiting)

- The generalized ν-δ family fit prefers δ=0.44 (McGaugh is δ=0.5) at ΔBIC −1.8 — nothing statistically, but a δ-scan with per-galaxy M/L marginalization is the professional-grade version of today's global fit if anyone ever wants to publish the comparison.
- Why does SPARC tolerate both power-law (q≈1) and exponential Newtonian returns but not logarithmic ones? The y∈[0.03, 62] leverage window with only 12 points above y=30 is the answer sketch — the high-acceleration tail is where the families separate, and SPARC barely samples it. Cassini (via EFE, probing y≈1–3 *mid-transition*) is the complementary instrument, which is what makes the squeeze topic sharp.

## Self-Seeded Questions

- Seeded as HIGH topic: `compander-cassini-q2-squeeze-execution.md` — does any γ pass both SPARC and Cassini? Verdict rules pre-fixed both ways in the topic file.
- For `test-preregistration-protocol`: today's pre-fixed rule prevented the tempting misread in *both* directions — tanh's last-place finish (+8.9) is not a refutation (inside band), and the arctan kill (+46.7) is not optimizer noise (grid-verified). Protocol seed: family comparisons should pre-fix both the privilege bar and the distinguishability bar, and require an optimizer-independent check (grid scan) before any ΔBIC > bar is called a kill.

## So What?

This advances discovery, not documentation: a queued research question open for seven weeks was answered by execution, one of its two pre-stated falsifiers fired (family NOT degenerate — the site currently understates its own data's selection power), the free-γ=0.49 result went from unexplained coincidence to identified mechanism (simple-μ convergence), and the answer generated a new runnable kill-or-survive test (Cassini squeeze) that didn't exist this morning.
