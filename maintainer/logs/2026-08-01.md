# Maintainer Session — 2026-08-01

## Infrastructure note

The 06:00 automated slot died again after printing only its start header
(`maintainer/logs/2026-08-01-0600.log`, one line, no file changes) — same signature documented
repeatedly in `SESSION_FOCUS.md` since 07-18. This is a recovery session run later in the day.

## WAKE

Today's visitor log (4 passes, unusually deep — Pass 3 and Pass 4 both checked identities and tool
arithmetic by hand) contained one item worth verifying before anything else: Pass 4 cited a specific
paper, **Ciocan et al. 2026 (MUSE-DARK III, A&A 709, L16, arXiv:2604.22613)**, measuring a₀(z)
evolution and finding it runs opposite to the framework's prior expectation. Given this project's
history of persona-fabricated citations and refutations (3+ prior confirmed instances — see memory
`feedback_persona_loop_amplifies_site_errors`), I verified the paper via WebFetch/WebSearch before
treating it as real. **It is real** — title, authors, and all quoted numbers (a₁ = 1.59×10⁻¹⁰,
a₀|z~1 = 2.38×10⁻¹⁰, "faster than that of H(z)") match the actual paper.

More importantly: this exact finding was **already fully researched and actioned** by the 2026-07-30
explorer session (`explorer/findings/a0-epoch-branch-A-tested-disfavoured-for-evolving-too-slowly.md`),
which left 6 specific `Action: Maintainer` items. No maintainer session ran 07-31 to drain them (git
log jumps directly from the 07-30 explorer commit to today's visitor commit). Today's Pass 4 persona
independently re-derived the same finding from scratch, which corroborates it rather than duplicates
it — this is a real, overdue, well-specified backlog item, not new research. WAKE verdict: the
research direction is right; the gap was pure propagation lag. Fixed the lag (below) rather than
writing a fresh proposal for already-completed research.

## Feedback Reviewed

- Visitor log 2026-08-01 (4 passes): unusually strong. Pass 3 (Grad Student) and Pass 4 (Researcher)
  both verified their own claims against live-site arithmetic and external sources before writing
  them up — noticeably lower fabrication risk than typical passes, and it showed: every high-severity
  item I checked against source turned out to be real.
- Explorer finding (07-30, previously unshipped): `a0-epoch-branch-A-tested-disfavoured-for-evolving-too-slowly.md`
- No forum input this session.

## Changes Made (Site)

- `src/app/parameter-derivations/page.tsx`: shipped the 07-30 explorer's 6-item action list on the
  a₀ epoch-fork card — corrected the arXiv:1703.06110 citation (it tests/disfavors an evolving a₀,
  it does not propose one; the actual proposals are arXiv:0801.3133 and ApJ 698, 1630), closed the
  false fork (branch (B) requires a fundamental a₀, which the site's own "emergent scale" framing
  elsewhere rules out), added the Ciocan+2026 result with the full normalization table (2.3σ–5.9σ
  low depending on anchor), added the unstated Λ-half of Milgrom's coincidence (2πa₀ ≈ c²(Λ/3)^½,
  zero evolution predicted, ~12σ against Ciocan — worse than the H₀ branch), and softened the
  "13% below Milgrom" framing with the extrapolation caveat. Refutation count stays at 6, stated why.
- `src/app/mond-unification/page.tsx`: this was a fossil page contradicting the site's own ledger.
  Fixed the stale "10% Error" badge (site-wide value is 13%). Added an update note pointing to the
  epoch-fork resolution. Corrected the TDG "Untested — Novel Prediction" badge, which claimed a
  standing discriminator while `/for-researchers` and `/tier-1-existing` both state zero
  discriminating tests remain — added the nesting caveat ([10.5,14.5] sits almost entirely inside
  MOND's [10.9,40.9]; only an outcome that falsifies Synchronism is discriminating) and noted the
  0.3–0.4× EFE factor is a corollary of the boost ceiling TEST-09/TEST-10 already killed, not an
  independent prediction.
- `src/app/for-researchers/page.tsx`: (1) added the missing "algebraic coupling" scope word to
  citable-artifact #1's opening sentence — the correction already existed lower in the same card for
  a different sentence but never reached the summary line a referee would read first; without it the
  claim is false as written (symmetron-type gradient screening is a published, real counterexample).
  (2) Added an integrity note on the DESI DR2/TEST-04a pre-registration: checked git history and
  found the fσ₈≤0.46 threshold was committed 2026-07-01/07-14 — *after* the April 2026 DESI DR2
  PIRSA presentation (PIRSA:26040071) that Pass 4 also flagged. This doesn't invalidate the
  registration (adopted formally by dp 2026-07-17, per `PREDICTIONS.md`) but means "first genuinely
  prospective test" can't be verified as prospective relative to that talk without checking what the
  talk actually showed — filed as a research-repo proposal (see Back-Annotations).
- `src/app/dark-matter/page.tsx`: Pass 3 claimed this page carries no validation badge — false, it
  already has `Failed — Mechanism Under Revision` (persona error, noted for the record, not acted
  on). What was real: the closing paragraph still framed the galaxy-scale mechanism as merely
  "incomplete," pending confrontation with clusters/CMB/LSS, when the mechanism itself is refuted at
  galaxy scale (ΔBIC=+184, TEST-09 3.3σ, TEST-10 69% ceiling violation, no algebraic chain to g_obs).
  Rewrote the lead and split the "14,760 galaxies" figure (175 SPARC + 14,585 ALFALFA-SDSS TFR
  objects from a test that never ran as registered — these measure different things).
- `src/app/dark-matter-failure/page.tsx`: "What Survives" section claimed TEST-05 and the McGaugh RAR
  fit stood independent of the CFD viscosity sign error. Both have since failed on their own terms
  (TEST-05 retired with the MOND-shared badge class; the RAR fit itself is ΔBIC=+184-rejected).
  Corrected to state nothing in that section survives anymore.
- Verified and declined to act on: the `/key-claims` badge/tooltip "concatenation" Pass 2 flagged —
  the JSX already separates them with flex+gap; this is a WebFetch text-flattening artifact, not a
  live rendering bug. The Bullet Cluster "over-refutation" Pass 4 flagged on `/key-claims` — read the
  live section in full; it already frames this as the locality no-go at cluster scale ("same failure,
  not two"), not as a computed lensing prediction. The TEST-09/TEST-10 "counted twice" item Pass 3
  flagged — `/honest-assessment` already presents them as a single combined item, "(0) The
  boost-ceiling pair," in its sharpest-events list. All three were already fixed or were persona
  misreadings; no action needed, which is itself useful signal about which parts of today's log to
  trust at face value vs. verify first.
- Build verified clean (`npx next build`, zero errors) after all edits.

## Back-Annotations (Research Repo)

- `Synchronism/Research/proposals/test04a_dr2_preregistration_timing_check_20260801.md`: new
  proposal flagging that the DESI DR2/TEST-04a pre-registration (adopted 2026-07-17 per
  `PREDICTIONS.md`) postdates a public April 2026 DESI DR2 presentation, and recommending someone
  check whether that talk showed a threshold-relevant fσ₈ figure before treating the registration as
  fully prospective. Does not change the registration itself — flags a question, doesn't answer it.
- Noted, did not duplicate: `Synchronism/Research/proposals/test_catalog_a0z_tier1_gap_20260801.md`
  already exists, filed today by a "Publisher (Phase 0)" track I hadn't previously seen reference to
  in this track's CLAUDE.md — it independently proposes the same a₀(z)/Ciocan test-catalog gap the
  07-30 explorer finding covers. Two independent tracks converging on the same finding same-day is
  good corroboration; no further action needed from this session beyond what's already in the site
  fix above.

## Topics Seeded for Explorer

- `gamma-gloss-coupling-vs-steepness-consistency.md`: γ is glossed as "coupling strength" on 2 pages
  and "steepness/sharpness" on 3 others, with the site's own sign-inversion self-criticism only
  legible under the steepness reading. Escalated to the top of today's cross-persona synthesis by
  the Technical Writer pass.
- `galaxy-plotter-shows-abandoned-gamma2-not-gamma049.md`: the Galaxy Curve Plotter hard-codes the
  refuted γ=2 configuration instead of the site's own best-fit γ≈0.49, which — per the Hill-function
  identity already on `/core-idea` — lands exactly on MOND's curve. Flagged as possibly a direct
  maintainer fix (code + already-published numbers) rather than new research, if picked up.

## Open Items (Deferred)

Today's visitor log surfaced ~20 more medium/low-severity items not addressed this session, given
the size of the P0 backlog that took priority (the a₀ epoch-fork fix alone accounts for most of this
session). Notably still open: the four-copies badge-taxonomy duplication (Tech Writer, P1 in
synthesis — topics for this already exist in the queue: `badge-vocabulary-closure.md`,
`badge-vocabulary-single-source-of-truth.md`); the `/coherence-function` acronym-expansion gap;
landing-page reordering (marching-band analogy before the equation); `/core-idea`'s beginner/technical
splice at step 3. None of these are urgent enough to have required same-session verification the way
the Ciocan citation did, and the existing topic queue already covers most of them.
