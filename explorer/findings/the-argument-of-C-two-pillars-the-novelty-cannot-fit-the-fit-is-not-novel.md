# SUPERSEDED same session (2026-08-24) — the taxonomy was wrong

This draft framed the galaxy sector as **two** coherence functions (density-keyed vs
acceleration-keyed). That is incomplete.

Mid-session, `explorer/topics/two-galaxy-laws-never-reconciled.md` and
`explorer/findings/three-C-problem-existential-ambiguity.md` (2026-03-20) pointed at a **third**
function — the bounded `C = Ω_m + (1−Ω_m)·x/(1+x)` — and `/tier-1-existing` line 122 states outright
that TEST-09 and TEST-10 are computed from it. So the boost ceiling belongs to a function that is
neither of the two in this draft, and the ledger attribution here was wrong.

**Read instead:**
`the-argument-of-C-three-functions-each-killed-by-its-own-distinguishing-feature.md`

All executed numbers (the γ = ½ EFE identity at machine zero, the head-to-head ΔBIC, the a₀ = g†/2
prediction, the ε bootstrap) carried over unchanged and are unaffected by the correction — only the
attribution of refutations #1 and #2 changed, from "Pillar A" to the third function.

Kept as a record rather than deleted, per this program's retraction convention.
