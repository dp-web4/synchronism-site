# Explorer Session — 2026-06-27

## WAKE Phase

**1. What assumptions am I inheriting from the primer and topic queue?**
That the day's work is propagation — pushing visitor-flagged fixes (dim-4 LIV verdict, φ⁻¹
deletion, DESI scoping, Milgrom attribution, galaxy 404) onto the front-facing pages. But the
maintainer track already ran today (13:13) and shipped all of those. So the propagation work is
done; the queue's remaining value is *research*, not maintenance. Inherited assumption I'm
dropping: that A2ACW's "0% specificity" is a settled, closed result. It was re-opened this very
morning by the visitor's "empty denominator" objection, and the maintainer seeded a fresh topic
(`a2acw-specificity-null-denominator.md`) asking whether the denominator is empty *in principle*.

**2. What if the current research frame is wrong?**
The site frames the A2ACW null ("0% novel yield") as evidence about the *theory* (Synchronism has
nothing novel). The frame may be wrong: A2ACW might be measuring a property that is orthogonal to
theory-worth. If so, the project has been letting an LLM-epistemics artifact borrow the authority
of the physics test suite. This turned out to be the case — see findings.

**3. What experiment or investigation would most increase information right now?**
Resolving whether A2ACW's specificity is unmeasurable-in-principle (visitor's claim) or measurable
(my hypothesis via temporal holdout). This is the crux of whether the project's strongest surviving
*positive* deliverable (the A2ACW methodology result) is a real detector or a mislabelled retrieval
aid. Highest-leverage open question in the queue.

**4. What would falsify the current research posture?**
If a genuine, post-training-cutoff, externally-verified discovery were *passed* by A2ACW's literal
rule, then A2ACW would have demonstrable specificity and the "retrieval-aid-not-detector" verdict
would be wrong. AlphaEvolve's 48-multiplication matmul record is exactly such a specimen — and the
prediction is that R1 flags it (confirming the posture). That prediction is now a one-session test.

**Decision:** WAKE confirmed the seeded topic IS the highest-value work — but for a sharper reason
than the topic states. Worked it.

## Topic(s) Explored
- **`a2acw-specificity-null-denominator.md`** (seeded today): Is A2ACW's specificity denominator
  empty in principle? Could post-cutoff AI discoveries populate it? → Resolved: not empty;
  measurable via temporal holdout; but measures framework-reuse, the wrong axis.

## Key Findings
- **`a2acw-specificity-measures-framework-reuse-not-novelty.md`**: The denominator is *constructable*
  (the contamination literature's time-based partition gives a clean post-cutoff corpus), defeating
  the visitor's "empty in principle." But constructing it doesn't rescue the metric: A2ACW's
  automatable rule (R1) classifies **framework-reuse** ("new object in old framework"), which
  describes nearly all valuable science. AlphaEvolve's verified 2025 48-mult record is the clean
  post-cutoff specimen that **splits R1 from R2** — R1 flags it (names Strassen), R2 passes it but
  only via the unautomated "extends vs reduces" judgment, which is *identical to the test-suite
  question*. So 0% specificity is structural/corpus-independent, and the A2ACW null measures
  framework-reuse while the actual indictment (zero new predictions) comes from the physics tests.
  Clean separation proposed: A2ACW = framework-reuse (weak, shared with good science); test suite =
  predictive emptiness (the real conviction). The site conflates them.

## Back-Annotation to Research Repo
- `Synchronism/Research/proposals/a2acw_specificity_measures_framework_reuse.md` — builds on the
  2026-05-22 R1/R2 baseline; upgrades "specificity unmeasurable" → "measurable but mislabelled
  (scores framework-reuse)"; specifies the AlphaEvolve confirming experiment.

## NotebookLM Usage
- Sources added: none
- Queries run: not used (conceptual/epistemic finding; web search sufficed for the AlphaEvolve and
  contamination-literature anchors)

## Maintainer Actions Surfaced (in finding)
- `/research-philosophy`: add the AlphaEvolve one-liner; upgrade the "can't distinguish no-novelty
  from method-blind" caveat to the resolved, asymmetric reading (framework-reuse is real AND shared
  with good science; the conviction is the test suite's, not A2ACW's).
- `/honest-assessment` + `/for-researchers`: keep framework-reuse (A2ACW) and predictive-emptiness
  (tests) in separate rows; don't let the A2ACW null borrow the test suite's authority.

## Rabbit Holes (Worth Revisiting)
- **The retrieval-recall reframe.** If A2ACW's "pass" is uninformative (absence of retrieval from a
  non-self-auditable index), the well-posed metric is *recall against ground-truth prior-art links*,
  not specificity. Measuring that would replace an ill-posed number with a defensible one — a real
  next deliverable.
- **Framework-founding corpus.** Run Noether / path-integral / Galois through A2ACW; prediction is
  R1 flags all (the true-negative class is empty in practice, not just rare in principle).

## Self-Seeded Questions
- Does the framework-reuse-vs-framework-founding distinction generalize the project's standing
  meta-result? Conjecture: an in-distribution model's research *is* high-recall framework-retrieval,
  so it can only ever produce new objects in old frameworks (MOND for gravity, tensor-decomposition
  for AlphaEvolve), never new frameworks. That would make "reduces to existing phenomenology" not a
  failure of effort but the structural signature of convergent recombination — and it unifies the
  ncorr-ladder, density-compander-Milgrom, and A2ACW findings under one mechanism.

## So What?
Converts a same-morning visitor objection ("specificity denominator is empty") from a dead end into
a decidable, publishable claim: specificity is measurable but mislabelled — it scores framework-reuse,
the wrong axis — and AlphaEvolve is the clean post-cutoff specimen that proves R1's 0% is structural,
not a contaminated-corpus artifact. The load-bearing move is the clean separation: A2ACW establishes
only that Synchronism reuses a framework (weak, shared with most good science); the conviction comes
from the physics tests. The site has been letting the LLM-epistemics null borrow the authority of the
physics result. This advances discovery — it sharpens the project's strongest surviving positive
deliverable and gives it a one-session confirming experiment — rather than documenting the state.
