# Explorer Session — 2026-06-05

## WAKE Phase

1. **What assumptions am I inheriting from the primer and topic queue?**
   That TEST-02 (wide binaries) is the "last possibly-novel prediction," a triple-conditional
   placeholder whose condition (b) — "does C(ρ) predict a different ξ(ρ_env) than MOND+EFE?" —
   is merely *uncomputed*. The maintainer seeded it as HIGH today; both expert visitor personas
   flagged it. The implicit assumption: compute one slope, get one number, and TEST-02 either
   activates or closes.

2. **What if the current research frame is wrong?**
   It is, in a productive way. The frame assumes TEST-02 has *a* prediction whose divergence
   from MOND just needs computing. The reality: the framework has **three** wide-binary
   mechanisms (Session #238 C(a); #579 C(ρ); #369 γ∝1/√ρ) that don't agree, and the live site
   states the density-dependence with the **sign reversed** relative to all of them. Condition
   (b) can't be a single number when the prediction itself is underdetermined.

3. **What experiment would most increase information right now?**
   Computing condition (b) directly — the same "run it, don't argue it" move that closed
   TEST-04a (DESI), the RAR shape (ΔBIC=+184), and the EFE/TDG sector (boost ceiling). Done.

4. **What would falsify the current research posture?**
   Finding a locus where C(ρ) and MOND+EFE diverge *measurably*. I looked for it (vertical
   gradient at fixed R, where ρ and g_ext decouple). It exists in principle — opposite-sign
   gradient — but is unmeasurable (amplitude 80× below reach; divergent regime = worst-data
   regime). Posture survives, now with a mechanism instead of an assertion.

**Decision:** the queued HIGH topic *was* the highest-value work, but WAKE redefined what
"doing it" means — not computing one slope, but resolving why no single slope exists.

## Topic(s) Explored

- **test-02-wide-binary-efe-divergence-computation** (HIGH, queued): computed condition (b).
  Result: the wide-binary sector is the **third independent C(a)→C(ρ) fork** (after the RAR
  transition shape and the EFE/TDG boost ceiling). The acceleration form C(a) predicts the
  anomaly but *is* MOND (and is the RAR-refuted bounded form); the density form C(ρ) is distinct
  from MOND but predicts the **Newtonian null** (≈0.05–0.4% vs MOND's ~18% in the clean sample).
  → TEST-02 can refute Synchronism-C(ρ) but cannot confirm it over GR. Plus: caught a **sign
  error** on the live site (TEST-02 says "higher density → stronger deviation"; the framework's
  own mechanism and Sessions #369/#579 give the opposite).

## Key Findings

- `test02-wide-binary-efe-divergence-closure.md`: condition (b) computed; the C(a)/C(ρ) fork
  unifies the wide-binary sector with the galaxy/EFE closures; live-site density-sign is
  backwards; the only direction-level discriminator (vertical gradient at fixed R) is
  unmeasurable. Reproducible: `scripts/test02_wide_binary_efe_divergence.py`.

## NotebookLM Usage

- Sources added: none
- Queries run: not used (archive + primary-source lookup sufficed)

## Rabbit Holes (Worth Revisiting)

- Session #238's C(a) "fits slightly better than MOND" (χ²=9.6) vs the same bounded C(a) being
  RAR-refuted at ΔBIC=+184 — not a contradiction (4 coarse bins vs 2807 points), but worth one
  explicit reconciling sentence on the site.

## Self-Seeded Questions

- Does any current/forthcoming Gaia wide-binary catalog span |z| ≳ 1 kpc with good parallaxes?
  That is the only way the opposite-sign vertical gradient becomes a real test — the wide-binary
  analogue of the cluster bridge.
- The framework should commit to ONE wide-binary mechanism. Three live mechanisms with two
  different sign conventions is the deeper problem behind "no specific prediction written."

## So What?

This closes the last "possibly-novel" prediction by computation, not assertion — and it does so
by showing TEST-02 is not a special placeholder but the *third* instance of the single recurring
structure: the C(a)→C(ρ) migration trades a measurable-but-MOND prediction for a distinct-but-null
one, with no position on the axis that is both. It also catches a live correctness bug (the
density-dependence sign) that three visitor personas read past because they didn't cross-check it
against the framework's own mechanism. The "0 discriminators" ledger is now complete across all
three galactic sectors with a uniform diagnosis.
