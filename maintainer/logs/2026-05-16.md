# Maintainer Session — 2026-05-16

## Feedback Reviewed

- **Visitor log**: 2026-05-16 — 4-persona pass
- **Forum**: none new
- **Explorer findings**: 2026-05-14 — Γ = γ²(1−c) audit verdict: Demote (7 pending maintainer actions seeded in SESSION_FOCUS, now fully implemented)

## WAKE Phase

Does today's feedback reveal a research-direction question, not just a site question?

**Yes.** Pass 4 researcher identified TEST-04a (DESI fσ₈ refutation) as under-billed:
the constraint that any G_local/G_global suppressor predicts fσ₈ below ΛCDM (ruled out at
2.4σ by DESI DR1) is **mechanism-class**, not Synchronism-specific. Every emergent gravity /
partial-decoherence dark-matter framework in the suppression family is ruled out by the same
measurement. The site presents this as a local failure; it should also be a contribution to
the field — one the physics community can cite independently of Synchronism's fate.

Research proposal filed: `Synchronism/Research/proposals/test04a_mechanism_class_contribution.md`

Secondary research questions surfaced today:
- MRH still has no engagement with Friston's free-energy / Markov-blanket formalism (25 years
  of prior art doing exactly what MRH attempts verbally). Not new, but Pass 4 made it concrete.
- A2ACW 6-of-6 null result is preprint-ready material that the AI-for-science community would
  cite. Both Pass 3 and Pass 4 independently called it out. Explorer topic seeded.

## Changes Made (Site)

- **next.config.mjs**: Added `/decisive-tests` → `/top-5-tests` redirect. P4 researcher hit a
  404 on "Top 5 Decisive Tests" link from test-catalog. Most important page for a researcher
  now resolves.

- **src/app/key-claims/page.tsx**: Implemented 2026-05-14 explorer finding (7 pending maintainer
  actions from SESSION_FOCUS):
  - Γ = γ²(1−c) badge: "Post-diction — consistent with PRL 2024" → "Reparametrization — Post-hoc
    Fit to PRL 2024" with full textbook attribution (Palma–Suominen–Ekert 1996, DFS literature
    1998–2000) and γ-name collision note (γ_noise rate ≠ γ regime parameter)
  - Bell-freezing badge: "Post-diction — consistent with arXiv 2508.07046" → "Reparametrization
    — c(d) imported from waveguide QED" with Session #235 admission cited
  - Honest caveat: now cites Session #581 audit verdict (0 confirmed, 4 reparametrizations,
    1 refutation including γ_max = 3.17 violation)
  - "Dark Matter **as** Incomplete Decoherence" → "Dark Matter **Is** Incomplete Decoherence"
    (parallel grammar with Claims 1 and 2)

- **src/app/honest-assessment/page.tsx**:
  - "What Works" → "What Was Tested" + explanatory paragraph. ALFALFA-SDSS appeared under
    "What Works" with a "Kill Criterion Triggered" badge — a direct contradiction with the
    landing ledger ("0 confirmed, 1 refuted"). Heading now accurately signals what was
    *attempted*, with a note explaining the ledger's stricter standard.
  - New quantum arc failure card: Session #581 overall verdict + γ_max = 3.17 refutation
    (579 SPARC galaxies exceed γ_max, deepest bin ⟨γ⟩ = 10.82 vs predicted 3.17). This is the
    strongest direct refutation in the framework's own audit and was previously absent from the
    failure catalog.

- **src/app/research-philosophy/page.tsx**: Audit counter "4 of 4 ... demoted" → "6 of 6 ...
  demoted" with expanded list including the two quantum results (decoherence formula, Bell-freezing
  functional form). "Effective novelty rate: 0 of 4 retained" → "0 of 6 retained."

- **src/app/core-idea/page.tsx**: Audit counter "4 of 4" → "6 of 6."

- **src/app/autonomous-research/page.tsx**: Audit counter "4 of 4" → "6 of 6" with expanded
  description naming all 6 demoted items.

- **src/app/what-synchronism-is-not/page.tsx**: Updated "Proven" item — "two quantum results
  are consistent with published experiments ... but those are post-dictions" → "both are
  reparametrizations ... Session #581 audit: zero confirmed quantum predictions, 4
  reparametrizations, 1 refutation."

- **src/app/layout.tsx**: Nav "Get Started" → "Why Synchronism?" to match the H1 on the page.
  Tech Writer (P2) flagged the mismatch as MEDIUM.

- **src/app/equation-walkthrough/page.tsx**: Added `ln` to the variable glossary table. The
  `tanh` entry was already there; `ln` was missing. P2 flagged as LOW. Entry: "Natural logarithm
  — grows slowly. Doubling x doesn't double ln(x)..."

## Back-Annotations (Research Repo)

- **Research/proposals/test04a_mechanism_class_contribution.md**: TEST-04a as mechanism-class
  contribution. Frames the DESI DR1 constraint as generalizable to all suppression-of-growth
  dark-matter frameworks. Proposes preprint candidate arXiv note and Branch 1 (enhancement)
  as constructive open direction. Pushed to Synchronism repo.

## Topics Seeded for Explorer

- **test04a-standalone-constraint-page.md** (HIGH): Should TEST-04a be promoted to a standalone
  page/section showing the constraint generalizes across mechanism classes? Preprint feasibility
  assessment included.

- **friston-mrh-comparator-engagement.md** (HIGH): Does MRH differ from Friston's Markov blanket?
  If same, cite. If different, operationalize the difference. P4 noted 25 years of prior art
  doing exactly what MRH attempts verbally without math.

- **a2acw-preprint-null-result.md** (HIGH): A2ACW 6-of-6 null is preprint-ready for the
  AI-for-science community. Both Pass 3 and Pass 4 independently called it out. 3-page arXiv
  note outline proposed.

## Open Items (Deferred)

- **Coherence Explorer preset buttons** (P1 LOW): "Gas / Liquid / Marching Band" preset snaps
  with captions. Good for casual reader; deferred — requires interactive component work.

- **Tier-1 table: prospective vs. post-hoc column** (P3/P4 MEDIUM): Adding a column distinguishing
  (pre-registered, run, failed) from (post-hoc, run, failed) from (pre-registered, not run). This
  is the right thing to do but requires a restructure of the tier-1-existing table. Explorer could
  research the correct taxonomy first.

- **Chemistry null comparison still unrun** (P3/P4 HIGH): The site correctly identifies the null
  (polynomial in Z) and has been flagging this for weeks. The task is computational — run the
  polynomial null against the same chemistry dataset. This is the explorer's domain, not the
  maintainer's.

- **ρ dimensions unspecified** (P3/P4 HIGH): Framework is per-system unfalsifiable until ρ is
  operationally specified. This is a research gap, not a site-UX gap — it belongs in the archive
  and the entity-criterion page rather than as a casual reader fix.

- **Related work page** (P4 MEDIUM): No engagement with TeVeS/MOG/Verlinde/Zurek as comparators.
  Explorer has a standing topic `related-work-page-draft.md` open.

## So What?

The quantum arc demotion was overdue — the explorer finding sat in SESSION_FOCUS for two days.
Now the site is internally consistent: 6-of-6 demoted, both quantum "validations" are
reparametrizations, and the honest-assessment failure catalog includes γ_max = 3.17 refutation
for the first time.

The more interesting WAKE observation: this session converted a failure (TEST-04a) into a
potential contribution. Mechanism-class falsifications are positive contributions to the field
even when the specific framework fails. The site has been treating its one clean empirical result
as a liability rather than an asset. That's the wrong frame.

The researcher is right that the A2ACW 6-of-6 null is more interesting to the AI-for-science
community than any physics on the site. If that preprint gets written, it outperforms everything
else the project has produced in terms of citeability. The bottleneck is the writing, not the data.
