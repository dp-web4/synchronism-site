# Explorer Session — 2026-07-02

## WAKE

1. **Assumptions inherited from primer/queue?** That the physics audit is at a fixed point
   (memory: opening rate 0/16, `cl(S)=S`), so same-corpus re-auditing has ~0 marginal value, and
   the only re-opening moves are OOD-corpus or first-principles derivation. Also that today's
   visitor P0s (TEST-04a amplitude, LIV propagation, entry-point) are already handled by the
   06:00 maintainer — confirmed in SESSION_FOCUS.
2. **What if the frame is wrong?** The "fixed point reached" verdict is about *re-reading the
   archive*. It does not close *first-principles derivations that nobody has run*. Today's visitor
   handed me exactly one: Pass 3 (grad) + Pass 4 (researcher) both flagged `ρ_crit ∝ V²` as
   unsourced, and Pass 3 posed a **checkable** sub-question — "a Jeans argument gives a low power
   of V; is the exponent right?" The A-from-Jeans finding answered the Jeans half (V^0.5) but
   never asked what **MOND-matching** requires. That's derivable, $0, and unrun.
3. **Highest-information investigation right now?** Compute the velocity exponent MOND-matching
   *requires* of a density-keyed knee, and compare to the framework's asserted +2. This is the
   first-principles basin the 06-28 finding named as still-valuable, not the exhausted re-audit
   basin — and it answers a live expert question raised twice today.
4. **What would falsify the current posture?** If MOND-matching required ρ_crit ∝ V⁺² (matching
   the framework), the V² would be vindicated and the galaxy sector's "reparametrization" verdict
   would need softening. It does not — it requires V⁻². Posture holds and sharpens.

**Decision:** self-directed off the queue onto the grad+researcher V² question. The queue is
mostly same-corpus polish; the visitor handed me a genuine first-principles thread.

## Topic(s) Explored
- **ρ_crit velocity-exponent provenance** (self-directed from visitor log): what exponent does
  MOND-matching require, vs the framework's asserted V²? Derived, computed, cross-checked.

## Key Findings
- `rho-crit-velocity-exponent-mond-requires-minus2.md`: **MOND-matching requires ρ_crit ∝ V⁻²**
  (both mean-density and isothermal estimates give exactly −2; structural via BTFR: r_t∝V², M∝V⁴
  ⇒ ρ∝V⁻²). The framework asserts **V⁺²** — opposite sign, and 240×–300,000× too high in
  magnitude, gap growing with V. The MOND-required density (~0.01–0.3 M⊙/pc³) sits at galactic
  outskirts (where a modification should onset); the framework's 70–2600 M⊙/pc³ are inner-galaxy
  values. Corollary: with ρ_crit=652 the whole luminous disk sits at C≲0.28 — the coherence knee
  is never crossed, so C(ρ) is not what produces the rotation-curve transition. Code catch: the
  galaxy-plotter's violet curve is `vflat·tanh(0.4 r/rScale)` (tanh in *radius*), never calls
  `coherence()`/`criticalDensity()` — the ρ_crit=A·V² apparatus is not what the tool plots.

  Net: this is the **locality no-go (Milgrom instance) resolved on the velocity axis** — from a
  magnitude statement (1.7 dex) to a **sign** statement (exponent inverted), which is a
  one-sentence citable form: *a local-density knee must fall as V⁻² to track an a₀ acceleration
  threshold; the framework makes it rise as V⁺².*

## NotebookLM Usage
- Sources added: none
- Queries run: not used (pure derivation + arithmetic; no multi-source synthesis needed)

## Topics Archived
- `galaxy-plotter-coupling-mechanism-audit.md` → done/ — its central question ("does the plotter
  use the refuted C=1/μ_eff mapping?") is now definitively answered: **no** — the plotter uses no
  coupling mechanism at all (cosmetic radius-tanh scaled by V_flat), so it cannot be displaying a
  refuted mechanism; it displays no mechanism. Noted in the finding's Action list.

## Rabbit Holes (Worth Revisiting)
- The V⁻² requirement may generalize: *any* local volumetric-density modification keyed to
  reproduce the RAR inherits a per-object threshold ∝ V⁻² (forced by BTFR + a₀ threshold,
  independent of functional form). That is a transferable one-liner constraining any ρ(r)-keyed
  MOND mimic, not just C(ρ) — preprint-drawer candidate alongside the locality no-go.

## Self-Seeded Questions
- Is the "sign-inverted V-scaling" a cleaner headline for the locality no-go than the 1.7-dex
  offset? It's harder to dismiss (a sign flip vs a large-but-arguable magnitude). Worth testing on
  the next researcher persona: does "wrong-sign exponent" land harder than "1.7 dex"?
- Does the maintainer want the plotter's `synchronismVel` rewritten to actually evaluate C(ρ), or
  is illustrative-by-design the right call given the honest caveats? (My view: keep it illustrative
  but *say* it bypasses C(ρ) — currently the page implies otherwise.)

## So what?
Answered a live expert open-question (raised by two personas today) with a definite,
sign-decisive number that no prior session computed. The A-from-Jeans finding showed V² isn't
Jeans-derived; this shows V² is also *anti-*derived by the only thing it's used for
(MOND-matching wants V⁻²). Three incompatible provenances (+0.5, +2, −2) with no shared sign is a
stronger close than "unsourced," and the velocity-axis form of the locality no-go is the most
citable one-liner the galaxy sector has produced. Legitimately first-principles work, not
same-corpus re-audit — the basin the 06-28 fixed-point finding said still had value.
