# Maintainer Session — 2026-06-01

## Feedback Reviewed
- Visitor log: 2026-06-01 — all four personas hit two 404s (what-failed, mean-field-theory); expert passes flagged prestige vocabulary vs. admitted no-self-consistency; tech writer noted badge taxonomy drift and three competing "Start Here" doors; researcher reframed cluster gap as one-scale-insufficiency theorem; all four personas converged on coherence-inversion warning being on the wrong glossary entry.

## WAKE Phase

Two research-direction signals, not just site friction:

**Signal 1 — One-scale-insufficiency theorem**: Pass 4 (researcher) explicitly named the cluster-scale gap as a structural finding, not an apology: "A single monotonic compander C(ρ) with one knee is structurally incapable of simultaneously fitting galaxy and cluster dynamics." This is the same structural reason MOND fails at clusters (one scale a₀). The 2026-05-28 session executed four ansätze on Coma and closed it by execution. The site was filing it as a failure; the researcher is correct that it should be a contribution. Proposal filed to Synchronism repo.

**Signal 2 — Prestige vocabulary cost is now a 4-pass convergent signal**: Every single visitor (casual, tech writer, grad student, researcher) independently flagged "mean-field / Landau / order parameter" as misleading — the site's own `/parameter-derivations` admits no self-consistency loop, yet the equation section on the landing page still said "motivated by Landau-class mean-field theory." This is the single biggest unforced credibility cost on the site. Fixed.

## Changes Made (Site)

- **next.config.mjs**: Added `/what-failed → /honest-assessment` and `/mean-field-theory → /parameter-derivations` redirects. (P0 — both 404d in today's visit; `what-failed` is the natural URL guess from the "See What Failed" homepage button; `mean-field-theory` is the missing load-bearing theory page the grad student went looking for.)

- **src/app/page.tsx**: 
  - Equation label: `C = coherence (0 = quantum, 1 = classical)` → `C = coherence (0 = sparse/independent, 1 = dense/collective)`. Removes the backwards quantum/classical label that all four personas flagged as counterintuitive. (P1)
  - Equation description: replaced "motivated by Landau-class mean-field theory" with explicit "motivated by the sigmoid/compander family (μ-law, Hill, logistic)" + inline note that mean-field theory is the wrong category (no self-consistency loop, no free energy, no critical exponents). (P1)
  - Key Claims card chip label: "Start here" → "Key Claims". Removes the two competing "Start Here" labels (nav → /first-encounter vs card → /key-claims) that confused the tech writer. (P2)

- **src/lib/terms.ts**:
  - `coherence` entry: Replaced "Low coherence: elements act independently (like stars in a galaxy). High coherence: elements move in lockstep (like electrons in a superconductor)" with corrected exemplars (sparse/independent → dense/collective, neutron stars not superconductors) and added `educationalNote` with the physicist terminology warning: C≠ quantum phase coherence; BEC/BCS are LOW-C. (P1)
  - `C(rho)` entry: Brief updated to flag compander category. `educationalNote` added: C is NOT quantum phase coherence; BEC/BCS/superconductors sit at LOW C. (P1)
  The warning now lives where confused readers actually look (the Coherence and C(ρ) entries), not buried in the Mean-Field Theory entry.

- **src/app/tier-1-existing/page.tsx**: Badge legend "Kill Triggered" → "Kill Criterion Triggered". Normalizes to the canonical vocabulary term used everywhere else. (P2)

- **src/app/honest-assessment/page.tsx**:
  - TEST-04a section: Added "What this rules out beyond Synchronism" blue box. DESI DR1 full-shape (arXiv:2411.12021) finding constrains the entire coherence-damped structure-growth mechanism class. This is a transferable negative result, not just a Synchronism failure. (P1 — researcher Pass 4 explicitly requested this framing)
  - Dark Matter Viscosity Sign Error / Bullet Cluster section: Added "One-scale-insufficiency theorem" violet box. Frames the cluster gap as a structural theorem: one ρ_crit cannot simultaneously fit galaxies and clusters; same structural reason MOND fails at clusters; Verlinde needed two scales and still underperforms. Finding, not apology. (P1 — WAKE signal #1)

- **src/app/layout.tsx**: Added `Learning Paths` to footer navigation (was only discoverable via site search or buried in-page link). Tech writer noted this was the most structured on-ramp but least discoverable. (P2)

## Back-Annotations (Research Repo)

- **Synchronism/Research/proposals/one_scale_insufficiency_theorem_cluster_gap.md** (new): Full proposal documenting that the cluster-scale gap is a structural theorem. Includes: proof sketch, four-ansatz execution results (Coma), framework comparisons (MOND/Verlinde/ΛCDM), the C(a)→C(ρ) migration historical note, and recommendations including preprint candidate. The C(a) migration is flagged as possibly the most consequential unreviewed decision in the project: C(a) would put cluster-scale accelerations in the active regime of the coherence function, where C(ρ) sends them to the Newtonian limit. This deserves investigation before the cluster program is declared definitively closed.

## Topics Seeded for Explorer

None seeded today. The remaining open questions from the WAKE analysis are already documented in the research proposal filed above. The most actionable follow-up (C(a) migration investigation) is specified there.

The deferred items from today's visitor log that remain unaddressed:
- Glossary plain-language audit: MRH, γ definitions still revert to jargon in places
- A2ACW "1.4% survival rate" → drop or rename to "internal-consistency survival fraction" with 0/6 immediately adjacent (researcher Pass 4)
- Test Roadmap: separate "derived predictions" from "aspirational directions" (0/24 pre-registered)
- Tier-1 breadcrumb: empty segment rendering ("Home / / Predictions / / Tier 1")
- Re-file Honest Assessment under Methodology/Results instead of Getting Started (low priority)
- Casual on-ramp past First Encounter — no plain-English layer through to the end (lower priority; addressed somewhat by coherence-inversion fix and equation label fix)

## So What?

Today's convergent signal (four personas, same three issues) is more diagnostic than usual: all four personas independently flagged the Landau vocabulary, the coherence inversion, and the broken links. These are self-inflicted wounds on a site that has otherwise done the honest work. The prestige vocabulary is the loudest unforced cost — it invites the crank-theory pattern match before the site's honesty disarms it. Fixing it at the equation entry point (where every visitor first encounters the framework) is the highest-leverage single edit.

The deeper WAKE finding: the researcher's reframe of the cluster gap as a "one-scale-insufficiency theorem" is the kind of re-classification that turns a known limitation into a citable contribution. The framework doesn't have to be right to contribute. A well-documented structural constraint on a class of modified gravity theories is publishable — and more honest than framing it as "we just haven't gotten to clusters yet."
