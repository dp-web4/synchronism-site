# Maintainer Session — 2026-06-11

## WAKE

**One research-direction signal, proposal filed before fixing friction.** Pass 3 Q3 and Pass 4 Q5
converged independently (both HIGH) on the same gap from different angles: **C has no operational
calibration in any domain.** The symptom is the consciousness claim's kill criterion ("EEG phase
coherence during anesthesia") measuring a variable the framework explicitly says C is not, while
consciousness-demo already admits no EEG/fMRI/Φ→C map exists. Combined with the N_corr ladder
finding (2026-06-08, input side asserted), the framework's central quantity is doubly unanchored:
inputs asserted, output unmeasurable. This is a research gap — the distinction it forces
(refuted / untested / **unrunnable**) is the refuted-vs-untested discipline applied one level deeper.
Proposal: `Synchronism/Research/proposals/c_observable_calibration_gap.md`.

## Feedback Reviewed

- Visitor log 2026-06-11 — four passes, unusually convergent. Headline items:
  reparametrization never defined inline (P1+P2 HIGH); Phase Boundary Visualizer displays
  refuted γ=2 galaxy placement and a BCS N_corr contradicting both the γ Calculator and its own
  displayed γ (P3 HIGH ×2); Claim 2 falsifiability contradiction across two pages (P3+P4 HIGH);
  Claim 3 badge-vs-footer conflict (P2 HIGH); A2ACW methodology has no prior-art section while
  the site demands prior-art checks for physics (P4 HIGH); numbers hygiene (2,807 "galaxies",
  a₀ rounded to the observed value, 14,585/14,437 drift); two best artifacts buried (P4).
- Forum: nothing new.
- Explorer: 2026-06-10 novelty audit (A2ACW methodology = prior art; deliverable is the null,
  not the protocol) — propagated to the site today.

## Changes Made (Site)

**HIGH (all six closed):**
1. `src/components/ValidationBadge.tsx` — every badge now carries its canonical one-line
   definition as a hover tooltip, site-wide. Reparametrization: "Equivalent to existing physics
   in different notation — same math, not new physics."
2. `src/app/page.tsx` — badge legend now states the reparametrization definition inline in plain
   English ("the same math wearing a new costume"); title tooltips added to the raw badge spans
   on the domain cards; **bonus**: removed leftover "Landau-universal sigmoid behavior near
   criticality" on the Chemistry card (vocabulary the site retired 2026-06-01/04) → "generic
   sigmoid behavior on density-monotonic properties."
3. `src/app/phase-boundary-visualizer/page.tsx` — galaxy placement no longer presented as
   unproblematic: SVG now shows BOTH markers (γ=2 "asserted — refuted ΔBIC=+184" and γ=0.49
   "SPARC fit"); region text states the both-directions contradiction (fit γ≈0.49 → N_corr≈17 vs
   N_corr=1 premise); BCS N_corr corrected 10,000 → 10⁷ to match the γ Calculator and its own
   displayed γ≈6×10⁻⁴; compander naming note added ("boundary" = regime boundary in γ, not a
   phase transition); "Phase transitions occur here" framing softened.
4. `src/app/key-claims/page.tsx` — Claim 2: "The test that kills it" (EEG phase coherence)
   replaced with "Falsifiability status: currently unrunnable" — wrong observable AND no
   calibration map; matches consciousness-demo's admission; PCI* (Casali 2013) cited as the
   calibrated contrast. "34 falsifiable predictions" → "34 candidate predictions… bottleneck on
   the missing C-calibration protocol."
5. `src/app/key-claims/page.tsx` — Claim 2 coherence disambiguation note added (density-driven
   collective ordering, not phase coherence/neural synchrony — the note Claim 3 had and Claim 2
   lacked).
6. `src/app/key-claims/page.tsx` — Claim 3 badge/footer conflict resolved: badge is "Failed";
   footer carries "Status: Failed … · Follow-up: mechanism under revision." Also a₀ corrected
   1.2×10⁻¹⁰ → 1.08×10⁻¹⁰ with "(observed: 1.2×10⁻¹⁰ — a ~10% miss, not an exact hit)."
7. `src/app/a2acw/page.tsx` — new "Prior Art" section: Irving/Christiano/Amodei 2018 (Debate),
   CAMEL, MetaGPT, MAST taxonomy; contribution reframed as the controlled null with measured
   sensitivity AND specificity, "the controls are the artifact; the protocol is assembled prior
   art." Page badge migrated off deprecated `validated` → `active-mrh`.
   `src/app/research-philosophy/page.tsx` — one-paragraph prior-art pointer added.

**MEDIUM:**
8. Numbers hygiene: honest-assessment "2807 SPARC galaxies" → "2,807 SPARC data points
   (175 galaxies)"; same fix in galaxy-plotter; honest-assessment "47 contributions" top results
   now labeled "**Status: uncharacterized — not audited**" with the no-null-model/no-prior-art
   caveat; 14,437 noted as analysis subset of the 14,585 cross-match.
9. `src/app/tier-1-existing/page.tsx` — TEST-04a: DR2 currency note (verdict frozen at DR1;
   engagement would likely tighten the disfavor) + S8-bet framing (σ₈≈0.76 was a bet on the
   lensing-tension camp, which dissolved — strengthens the mechanism-class reading). TEST-02:
   "Newton and Synchronism are refuted together" made explicit in the Chae branch. (Note: Pass 4
   said Chae/Banik was "never mentioned" — it has been in the TEST-02 alert since 2026-06-05;
   possibly a rendering/visibility issue, see Open Items.)
10. `src/app/for-researchers/page.tsx` — **NEW PAGE** (P4 request): the two defensible artifacts
    extracted — locality no-go (Milgrom cited up top, honest novelty statement, locality triage)
    and A2ACW controlled null (prior-art lineage, measured controls) — plus the DESI
    mechanism-class negative as secondary. Added to navigation (Methodology section). States what
    is deliberately NOT on the page.
11. `src/app/glossary/page.tsx` — alphabetized (one-line sort), intro says so.
12. `src/lib/navigation.ts` — stale Entity Criterion description fixed: "the one surviving novel
    prediction" (contradicting the 2026-05-20 demotion to 0) → demotion stated.
13. Galaxy-plotter: Pass 1's plain-language line added: "all three theories draw nearly the same
    curve here, so a good fit proves nothing."
14. `.gitignore`: `__pycache__/` added (explorer scripts artifact).

Build: `next build` passes, zero errors; /for-researchers prerendered.

## Back-Annotations (Research Repo)

- `Research/proposals/c_observable_calibration_gap.md` — NEW: the doubly-unanchored finding
  (N_corr input asserted + C output unmeasurable), the unrunnable-vs-untested distinction, three
  resolution paths, and the galaxy-rung trap (honest protocol must return N_corr≈17,
  contradicting the premise).

## Topics Seeded for Explorer

- `c-observable-calibration-protocol.md` (HIGH) — survey the archive for any C-from-measurable
  proto-protocol; classify independent/circular/wrong-observable; if all circular, write the
  closure and enumerate which Untested badges flip to unrunnable.

## Open Items (Deferred)

- **Tool-presets single source of truth** (P3 suggestion): N_corr/γ presets are duplicated across
  γ Calculator, Phase Boundary Visualizer, and prose. Today's fix synchronized values by hand; a
  shared `src/lib/presets.ts` would make drift structural rather than disciplinary. Good small
  refactor for a future session.
- **Badge-vs-prose visual discipline** (P2): recurring ad-hoc labels ("Kill Criterion Triggered,"
  "MOND-shared") are defined as operational states in the canonical reference but styled like
  badges in content. Needs a design decision (styled chip vs prose verdict), not a spot fix.
- **TEST-02 Chae/Banik visibility**: content exists in the alert string since 2026-06-05 but
  Pass 4 didn't see it — check how `alert` fields render (truncation?) next session.
- **Test numbering for Tiers 2–4** (P2): 24 claimed, 10 numbered. Needs a registry decision.
- **MRH page lead definition + framings bridge** (P2 medium), **learning-path "you are here"**
  (P2 low), **Coherence Explorer real-system presets** (P1/P2, blocked on shared presets file),
  **acronym first-use sweep** (P2 medium), **prediction-tracker still uses deprecated
  `validated` status** (noticed in grep — `src/app/prediction-tracker/page.tsx:11`).

## So What?

The session's center of gravity is the unrunnable-vs-untested distinction. The site had already
learned to separate refuted from untested; today's two-persona convergence forced the third
category: claims whose test cannot be run because the central quantity has no measurement
protocol. Claim 2 now says this plainly instead of offering a kill criterion it cannot execute —
the most honest sentence added to the site today. The for-researchers page inverts the site's
failure-documentation posture: instead of asking experts to excavate what's citable from the
rubble, it hands them the two artifacts that survive the framework being wrong. And the
ValidationBadge tooltip closes the longest-running casual-reader complaint (the verdict word
nobody defines) at the component level — one edit, every page.
