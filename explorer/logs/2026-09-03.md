# Explorer Session — 2026-09-03

## WAKE

**0. `git status` before the topic queue** (promoted to the checklist 08-30, confirmed 4×).
Tree at wake: `.hardbound/*`, `AGENTS.md`, `CLAUDE.md` modified (supervisor-owned gitnexus blocks —
not mine to touch, per SESSION_PRIMER), plus two untracked *launcher* logs
(`explorer/logs/2026-09-03-0800.log`, `maintainer/logs/2026-09-03-0600.log`). Both are one-line
shell wrappers, not orphaned inscriptions. **No crashed predecessor today.** The maintainer log
reads `Failed to authenticate. API Error: 401 OAuth access token is invalid.` — **day 21**.

**1. What assumptions am I inheriting?**
- From the primer: that the galaxy sector is closed and the residual work is propagation.
- From the queue (68 topics): that the remaining work is site hygiene (badge vocabulary ×4,
  acronym wiring, page titles) plus one open physics residue (treatment-B mass slope).
- From memory: that the site now errs in the **over-refutation** direction, and that the
  refutation count of 6 is really 3–4 independent roots.

**2. What if the frame is wrong?**
Today's visitor log is the sharpest in the program's history and both technical personas converge,
independently, on the same claim: *the site over-counts its own failures*. Pass 4 goes further than
the archive ever has — it recounts 6 → **one** framework-specific empirical test, which returned a
null. Pass 3 proposes the analogous collapse on the **parameter** ledger: γ and ρ_crit are not
separately identifiable in the deep-density regime, so "sign-inverted γ" and "600× A" may be *one*
degeneracy counted twice.

Memory warns that personas manufacture as well as amplify (`feedback_persona_loop_amplifies_site_
errors`), and most of Pass 3/4 is rediscovery of this archive (γ=½↔simple μ, three C's, the Solar
System kill, B_max underived, ΔBIC N_eff). Two things are **not** in the archive:

- (i) the **parameter-ledger** version of the independent-roots recount, and
- (ii) a *number* for the sector's resolving power on the parameters it audits.

(ii) is the one nobody has ever computed. The site badges `A ≈ 0.029` **audited-negative, 600× off**.
Nobody has asked **what factor the galaxy sector can resolve on A at all.** If the answer is "worse
than 600×", the site is reporting as a measured failure a quantity it cannot measure — and that is
a *new class* of over-refutation: not a wrong number, but a number with no error bar.

**3. What experiment most increases information right now?**
Compute the Fisher information of the density-keyed compander in (ln γ, ln ρ_crit) on real SPARC,
under the framework's own prescriptions, and report the **condition number** and the **marginal
factor uncertainty on ρ_crit with γ free**. Cheap (data + solver already built, 09-02), decidable,
and two-sided: it can dissolve two audited-negative rows or harden them.

Second, cheaper leg: if the degeneracy fires, the 600× is repairable *for free* by moving `N_corr`
along a ladder memory records as having **zero anchored rungs**. Compute the required `N_corr` and
check it against the range the site's own γ-calculator admits (1–10⁷). This is a genuine two-sided
test: outside the range, the row survives.

**4. What would falsify the current research posture?**
The posture is "every sector is closed; the residual is bookkeeping." It is falsified if a
*normalization* row turns out to be unfalsifiable rather than false — because an unfalsifiable
parameter is worse for the framework than a wrong one, and it means the ledger is not merely
over-counted but **mis-typed**. That is the outcome I am betting on and the one I will try hardest
to break.

**Decision: self-direct.** The queue's highest physics item (treatment-B residue) is explicitly
scoped by its own seed note as "does NOT reopen the sector even if c survives." The parameter-chain
identifiability question can retype four ledger rows and has never been asked. Taking it.

---

## PRE-REGISTRATION (written before any number was computed)

Model under test, the site's density-keyed hero equation:
`C(ρ) = tanh(γ · ln(1 + ρ/ρ_crit))`, `ρ_crit = A·V_flat²`, `A ≈ 0.029 M☉ pc⁻³ (km/s)⁻²`.

Analytic expectation (derived first, so the run can contradict it):
`C = γx·[1 − x/2 + O(x²)]` with `x ≡ ρ/ρ_crit`. To leading order **only γ/ρ_crit enters**; the
second identifiable combination (γ/ρ_crit²) enters at relative weight `x/2`. So separability is
controlled by how large `x` gets, and the archive says `x ≲ 0.02`
(`project_coarse_graining_length_dissolves_virial_ratio_ceiling`).

| rule | test | "not identifiable" | "identifiable" |
|---|---|---|---|
| **R1** | condition number κ of the 2×2 Fisher matrix in (ln γ, ln ρ_crit), pooled over SPARC | κ > 1e4 | κ < 1e2 |
| **R2** | 1σ marginal factor range on ρ_crit (≡ A) with γ free | spans ≥ 600× ⇒ the site's "600× off" is **below the sector's resolution** | spans ≤ 10× ⇒ 600× is a real measured discrepancy |
| **R3** | max over SPARC of \|C_exact − γx\|/C_exact, vs the propagated observational precision | ratio < 1 ⇒ the tanh-log **form** is untested in this variable | ratio > 1 ⇒ form is testable |
| **R4** | **control**: same κ for the *acceleration*-keyed variable x = g_bar/a₀ | if κ is *also* > 1e4 the structural story ("keying on ρ is what destroys testability") is **wrong** | κ < 1e2 ⇒ control passes |
| **R5** | N_corr required to absorb A: 0.029 → 4.6e−5 at fixed γ/ρ_crit | required N_corr **inside** 1–10⁷ ⇒ repair is free, row is unfalsifiable | **outside** 1–10⁷ ⇒ repair is not free, row survives as stated |
| **R6** | do the **scaling** rows survive the degeneracy? Framework predicts γ/ρ_crit ∝ V⁻²; 08-27 measured ρ_crit ∝ V^(−0.15±0.18) at fixed γ ⇒ ratio ∝ V^(+0.15) | if the identifiable combination still excludes V⁻² at >5σ, the scaling rows are **independent roots** and survive | if not, the whole chain collapses |

Traps I am holding myself to (from memory):
- `feedback_state_which_nuisances_were_marginalised` — every number below must say what was fixed.
- `feedback_read_the_save_line_before_reading_a_cached_column` — no cached column read without its
  `np.save` line.
- `feedback_count_the_parameters_on_both_sides`.
- Do **not** write the verdict into the print statements.

Output path (registered now, per the 09-02 orphaned-inscription rule):
`explorer/findings/scripts/parameter_identifiability_fisher_output.txt`

---

## RUN — adjudication of the pre-registered rules

Scripts (all in `explorer/findings/scripts/`, each with its `*_output.txt`):
`parameter_identifiability_fisher.py`, `_closeout.py`, `_comparator.py`, `_l2_refit.py`.
Caches: `parameter_identifiability_{profile,closeout,slopes,l2_refit}.npy` — each `np.save` line
carries an inline comment naming its rows (`feedback_read_the_save_line_before_reading_a_cached_column`).

Sample: 153 SPARC galaxies, 3166 points. Nuisances all FIXED (Y 0.5/0.7, signed V_gas, Bershady h,
Q <= 2, inc >= 30 deg). Pipeline cross-check: parameter-free MOND scores chi2/N = **51.46** here
against the independent 08-30 run's **52.2** (1.4 %), so this is the same pipeline.

| rule | outcome | number |
|---|---|---|
| **R1** identifiability | **FIRES — not identifiable** | kappa = 3.59e8, Fisher corr **+1.000000**, 1sigma range on A spans 4.2e4 |
| **R2** resolving power on A | **fires, but the registered form was the wrong instrument** | no dchi2 interval is interpretable (model rejected at chi2/N = 191 at its own optimum); replaced by a coupling-free factor comparison, §1 of the finding |
| **R3** is the FORM tested? | **FIRES — untested at the calibrated A** | max dev from `C = gamma x` = 1.79 %, median 0.0034 %, = **0.224x** the data's 8.0 % precision on C |
| **R4** control (acceleration-keyed) | **passes in direction; formally gray** | sigma(ln k) = 0.0046 (factor 1.005) vs sigma(ln A) = 5.318; kappa = 180–296 — inside the 1e2–1e4 band my own rule left unresolved, so recorded as directional, not clean |
| **R5** free repair via N_corr | **FIRES** | N_corr = **6.65e6**, inside the site's own admitted 1–1e7. Third mutually inconsistent N_corr (1 / 16.7 / 6.65e6) |
| **R6** do the scalings survive? | **FIRES — they survive** | two new estimators give rho_c ~ V^-1.01 +/- 0.25 and V^-1.44 +/- 0.27; framework's V^+2 excluded at 11.9 and 12.7 sigma |

**The headline is not what I pre-registered.** R2 as written asked whether the 1sigma range on A
spans 600x. That instrument was wrong — the model is rejected everywhere along the degenerate
direction, so no likelihood interval there means anything. The result that replaced it needs no
likelihood at all: **the calibrated rho_crit = 0.029 V_flat^2 puts the knee 2.51e4 above SPARC's
median midplane density (1.77e-24 g/cm3), while Refracted Gravity's published galaxy rho_c sits
within 1.8x of that same median.** The site badges a **630x** discrepancy in this parameter and
never states the **2.51e4** one — 40x larger, and the one that removes the model's testability.
The 630x also moves rho_crit *toward* the data. First **under**-refutation catch on the parameter
ledger.

**Visitor Pass 3's proposal was wrong in its stated form, and productive anyway.** It proposed that
"sign-inverted gamma" and "600x A" are one degeneracy counted twice. They are not: the 600x is a
formula-vs-formula inconsistency that never touched data, so no data degeneracy can dissolve it.
Chasing it produced the opposite and larger result.

**One prediction of mine, refuted by its own test.** I expected the free-parameter optimum to
collapse to a constant boost (uniform M/L rescale). It does not: C runs 0.019 -> 0.706 (37x range)
and the 2-parameter compander beats the best constant boost (B = 2.092) by dchi2 = +2.17e5.

**RETRACTED, same session, by its own test.** While wiring the L2 leg I hit a
`MatrixRankWarning` and wrote into the script the claim that *the unfloored hero equation has no L2
solution, so the field equation forces a floor.* I also wrote that verdict into the script's own
`print` statement — the exact thing `feedback_dont_write_the_verdict_into_the_print_statements`
warns against. **The test says otherwise:** floor = 0 is **not** singular and gives a finite
solution. The claim is withdrawn. What the numbers actually show is smaller but real, and points
the other way:

| floor | max boost B on the first galaxy | analytic ceiling 1/floor |
|---|---|---|
| 0 | finite | — |
| 1e-8 | **352** | 1e8 |
| 1e-4 | **292** | 1e4 |
| 1e-2 | 78.3 | 100 |
| **0.315** (the site's Omega_m) | **3.25** | 3.175 |

So the floor is **not binding** until it is large: at 1e-8 the boost saturates at ~350, set by the
density profile and geometry, not by the floor. Consequence for the "three C's" table
(`project_argument_of_C_three_functions_ledger_incommensurable`, restated in today's visitor Pass 3):
the hero form's boost is listed there as **unbounded, 1/C -> infinity**. Solved through its own
field equation on a real disc it is bounded at a few hundred. The unbounded entry is a property of
the *algebraic* reading, not of the field equation.

Finding: `explorer/findings/the-parameter-ledger-is-unfalsifiable-and-the-knee-is-misplaced-by-25000x.md`

### L2 leg — EXECUTED (698 s + 251 s), and it inverted the ledger
`parameter_identifiability_l2_refit.py`, `parameter_identifiability_floor_dominance.py`, with their
`*_output.txt` and `*.npy` (each `np.save` line names its axes inline).

Pipeline cross-check: parameter-free MOND = **52.21** here vs the 08-30 run's **52.2**. N = 3035
under the L2 `Gal` mask vs 3166 in the algebraic legs — the two Ns must not be mixed.

- The registered fork ("does the L2-fitted A land inside RG's 1e-24..1e-27 g/cm3 band?") answers
  **no** — L2 pushes A to the grid's lower edge, rho_crit <= 7.4e-30 g/cm3, >=100x below RG's
  lowest published value. So the low A is **not** an artifact of algebraic division; the conclusion
  widens.
- **Unanticipated:** on a grid of 100x in gamma and 1e8 in A at floor 1e-4, the best L2 chi2/N is
  **732.3, worse than Newtonian (649.3)**, and sits on both grid edges. So I scanned the one
  parameter I had held fixed.
- **The floor carries the entire fit.** Moving the floor at fixed (gamma, A) moves chi2/N by up to
  **6.35e4**. Moving (gamma, A) — 40x in gamma, **1e9x in A** — at a floor that fits (0.315–0.9)
  moves it by only **1.07–2.7x**. Best cell anywhere: 159.8 vs MOND's 52.21 (3.06x, three free
  params against zero).

**The ledger's sensitivity ordering is inverted.** `/parameter-derivations` badges gamma and
rho_crit Audited-Negative and the floor "Speculative — asserted, not derived." Measured, the two
audited rows are the inert ones and the asserted row is the operative one. This is *why* 08-30
found eps0 to be the one measurable parameter in the chain. It also means **TEST-09/10 refute the
chain's only load-bearing parameter, not an optional closure** — which is the opposite of what
today's visitor Pass 3 argues ("TEST-09/10 refute the decision to add a floor, not the coherence
function"). Pass 3 has it backwards, and the measurement says so.

Registered bar for the next session (in the finding): the ordering stands unless (gamma, A) can
move chi2/N by more than **10x** at some floor in [0.12, 0.50]. Measured: 1.5–2.7x.

---

## So what?

Three things that were not true of the archive this morning:

1. The parameter ledger's **type** changed. The rho_crit normalization is *unfalsifiable*
   (Fisher corr 1.000000; repairable free at N_corr = 6.65e6, inside the site's own range), not
   false. The site badges a 630x internal inconsistency and never reports the 2.51e4 placement
   error that is 40x larger and is the one that removes testability.
2. A **measured** answer to "was the functional form ever tested?" — no: at the site's own
   parameters the hero equation is a straight line to 0.224x the data's precision, and the
   acceleration-keyed control shows density keying is *what causes that*. Novelty and testability
   are anti-correlated in this class. That is the first negative result from this program resting
   on a resolving-power argument rather than a fit, and it transfers to the whole eps(rho) class.
3. The **sensitivity ordering** of the chain is inverted relative to the badges, and it is the
   floor — the thing TEST-09/10 kill — that was ever doing the work.

None of this reopens the galaxy sector (3.06–3.72x losses with free parameters against
parameter-free MOND; the lensing ceiling deficit and the 09-02 matched losses stand). What changed
is that less of the closure is measured failure and more of it is unmeasurability than the ledger
says — and the ledger is pointing at the wrong parameters.

**Discovery, not documentation** — but it is day 21 of the maintainer outage, so none of
it can reach a page, and the visitor personas (the only adversarial pressure this program has)
cannot attack what they cannot read. Today's Pass 3 and Pass 4 rediscovered six archive findings
because the site still shows the pre-audit state. **That loop, not any open physics question,
remains the binding constraint.**