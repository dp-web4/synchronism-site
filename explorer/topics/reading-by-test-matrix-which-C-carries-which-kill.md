# Topic: A reading × test matrix: which reading of C does each executed refutation actually bite?

**Priority:** MEDIUM. A researcher persona says the "6 refutations from 5 roots" headline does not correspond to any one theory.
**Seeded:** 2026-09-22 (maintainer), from visitor log 2026-09-22, Pass 4 (and Pass 2 implicitly)

## Question
Build the matrix. Rows are the readings of C in live use:
- C(g_obs) implicit (acceleration-keyed; this is MOND at γ = ½)
- C(g_bar) explicit
- C_ρ algebraic (density-keyed; EFE = 0)
- C_ρ with the field equation (L2 and L3 separately)
- C_Ω / the floored forms (Ω_m floor vs E0 floor)
- the cosmological C (C = Ω_m(a), Session 100)

Columns are every executed test: the 6 headline refutations plus TEST-08, TEST-25, P611.2, TEST-26 (fit), and the
2026-09-22 baryons-only check. Each cell is one of: kills / survives / not evaluable (and why) / not run. Is any single
row alive *and* non-MOND/non-ΛCDM?

## Context
- The Tier 1 caveat box already admits TEST-09/10 presuppose one of "three mutually exclusive readings". TEST-25 is an
  EFE-quadrupole test and cannot bite a reading with EFE = 0. The environment kill (TEST-08) bites density-keyed amplitude.
  The +184 was acceleration-keyed.
- The 2026-08-24 three-C's triage (`Synchronism/explorations/2026-08-24-efe-zero-is-phi-independence-and-three-C-functions.md`)
  found "largest coherent sub-ledger = 2". The matrix is the full version of that sentence.
- Memories: "three C's, not two", "two C(ρ) forms are in live use — 78.2% / 3.17× is the tell", "read the registration
  text before executing it". Most cells need you to read which C a script actually evaluates, not which C its doc names.

## Why It Matters
The ledger count is under dp's independence review (open question 4). A matrix turns "6" into per-reading verdicts a
referee can check. It is also the single source the site's status lines could be rendered from. The same visitor log
found TEST-09 carrying four different statuses on four pages.

## Suggested Starting Points
- /tier-1-existing caveat box, /for-researchers, /coherence-function
- `Synchronism/PREDICTIONS.md` Bucket 2 table and the dated blocks above Bucket 1
- Output: a findings doc whose core is the table. Every cell cites a script and its `_output.txt` line, or says "not run".
