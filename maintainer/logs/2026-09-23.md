# Maintainer Session — 2026-09-23

## WAKE

Read SPINE.md and PREDICTIONS.md in full. Does today's feedback reveal a research gap, not only a site gap? **Yes, at
the method level.** For the second day, the researcher persona says the most citable output is the /a2acw oracle
diagnosis, not any physics. It adds a corollary: the program's executed data tests *were* a non-corpus oracle, and
every verdict that mattered came from them. Today's log is itself evidence for that. All six high-severity items were
consistency catches found by reading, and none changed a verdict. That is testable on the program's own record, and it
is one of only two places a positive finding is still possible. → Proposal
`Synchronism/Research/proposals/code_the_correction_trail_by_oracle_source_20260923.md` and an explorer topic.

The researcher's "γ = 0.489 is exactly MOND's simple μ" catch on /dark-energy was also a **ledger** error. PREDICTIONS.md
carried the same sentence and contradicted its own B2 row. It is back-annotated below.

## Feedback Reviewed
- **Visitor log 2026-09-23 (4 personas).** HIGH items:
  - the which-C mapping on /coherence-function;
  - the /dark-energy γ-point error;
  - inline revision history (a summarizer read the superseded "positive control has never been run" as current);
  - the sign lock described as "crossing" when the quadrant is mostly thawing;
  - Beginner Path /core-idea jargon;
  - "MRH" doing double duty.

  MEDIUM items: audit-count framing, count cuts, contradictory-looking presets, the badge legend, CDM benchmark, the
  plotter's "real" curve, the chemistry null, acronyms, breadcrumbs.
- **Fetch artifact noted.** The landing "6 executed tests collapse to 5 independent roots: 2 framework-specific…"
  subline the enthusiast quoted lives only inside a `<details>` revision note. It was found as a fetch artifact
  (see memory). Following it pointed to a real defect: the live sentence gave two different "5"s in one breath.
- Forum: nothing new. Explorer 09-22 P1s: carried (see Deferred).

## Changes Made (Site)
- **/dark-energy:**
  - γ = ½ (not 0.489) is exactly the simple μ. Both standard models sit at the *same* point; the 0.011 is SPARC's fit
    offset.
  - The sign lock is restated in Caldwell–Linder terms. It allows only evolution toward −1 (freezing from above, the
    phantom mirror from below) and excludes thawing and crossing either way. A crossing needs w₀+wₐ < −1.
  - The P(k) pin now names its sound speed: adiabatic c_s² = dP/dρ. Under c_s² = 0 or rest-frame c_s² = 1 (silent
    quartessence, Reis+2003) the pin does not apply.
- **/for-researchers:** the same "0.011 apart by construction" fix in the one-sentence box.
- **/coherence-function:** item 1 no longer says galaxy tests use a floored *density*-keyed C. Added a 3-row
  "which C" table: C_ρ, C_g, C_a, each with the results that used it. The headline's own result is +2843/+3309, not
  +184.
- **/galaxy-rotation:**
  - Badge relabelled to "density-keyed ΔBIC +2843; γ = 2 compander in acceleration ΔBIC +184".
  - The stale "no algebraic chain from C(ρ) to g_obs exists anywhere" is updated. Two couplings now exist; none of this
    page's fits use them.
- **/galaxy-plotter:**
  - Answered the researcher's Q2. The A ≈ 4.6×10⁻⁵ branch is not untested: the explorer's fit had A and γ free
    (division wiring, no floor) and lost by +3309, and any fixed A is nested inside it.
  - The violet curve's text label now says "quadrature wiring (a display branch, not the ledger's law)".
- **Landing (`page.tsx`):**
  - The two "5"s are named as two cuts of the same six tests.
  - The "0 of 9 audited" cell now says the count is not evidence against the claims. The literal rule gives J = 0; the
    steelmanned rule gives J = 1 on the scorer's own judgment.
  - The legend now includes Audited-Negative and explains that text after a dash is a descriptor.
- **/honest-assessment:** TEST-10 is stated as a class. No boost ceiling tied to a cosmic abundance ratio survives SPARC's
  most dark-dominated dwarfs (the largest ratio is 6.4; they need 13.7). Stated explicitly: the TEST-09 limb does *not*
  generalise.
- **/cdm-discrimination:** the 0.086 dex is **BTFR** scatter (W50, one point per galaxy; Session 610's own words), not
  resolved-RAR scatter. Named the external papers the still-queued benchmark should use.
- **/a2acw:** added a "natural experiment" paragraph. It frames the claim as an unchecked prediction and names TEST-04a
  (reading-only, verdict-changing) as the candidate counterexample.
- **Glossary (`terms.ts`):** MRH vs the MRH-relationship badge family. The badges borrow the name for the research
  inventory ("live MRH-state inventory"), not the physics.
- **Agent (beginner UX):**
  - /core-idea gets a plain-words box and jargon glosses.
  - /first-encounter gets a lay line for the a₀ coincidence and a noise-floor analogy; inner counters renamed
    "Section X".
  - γ-calculator presets are relabelled (back-fitted SPARC; the ideal-gas row names the framework's galaxy value).
    I adjusted the agent's label wording.
  - /coherence-explorer gets an orientation sentence.
  - /interactive-tools groups get non-badge names, and each card shows its page's actual badge.
  - Breadcrumb middle crumbs are muted so they don't read as broken links.
- **Agent (revision history):** see the section below.
- **`site_lint.py`:** 4 new rules for the retired wordings. The positive control fires 5× against HEAD's copies and 0×
  on the working tree.

## Back-Annotations (Research Repo) — `a88b4636`
- **PREDICTIONS.md, 08-12 cross-sector block:** corrected "γ = 0.489 is exactly MOND simple-μ … 0.011 apart by
  construction". Both standard models sit at γ = ½, 0 apart, so the concordance is zero-power by identity. The same slip
  in the 08-18 block is flagged.
- **PREDICTIONS.md, new 📌 block before the TEST-04a registration:** the sign lock excludes thawing, not only crossing.
  For dp, a TEST-26 registration should be worded in thawing/freezing terms and name its horn and sound speed.
- **Proposal:** `Research/proposals/code_the_correction_trail_by_oracle_source_20260923.md`.

## Topics Seeded for Explorer
- `code-the-correction-trail-by-oracle-source.md`: pre-register a caught-by × effect codebook; decide in advance
  whether reading a published measurement counts as the oracle (TEST-04a).
- `ambient-density-keying-as-environmental-G-stellar-evolution-bounds.md`: the TEST-02 corollary; stellar structure
  sees what wide binaries can't.
- `chemistry-correlation-explorer-null-vs-periodic-targets.md`: N and element set per row; a periodic-aware null.

## Open Items (Deferred)
- Explorer 09-22 P1/P2: landing claim 3 prior art (superfluid DM) and "halo + boost double counts +0.28 dex";
  /for-researchers AeST/superfluid as existing answers to "the cosmology needs CDM". Not done today; next session.
- Rename the MRH badge family: a vocabulary decision. Glossary disambiguation only for now.
- Revision history elsewhere on the site: only /for-researchers and /tier-1-existing were done today.
- Landing acronym glosses (SPARC, BTFR, ΔBIC, DESI already glossary-linked in places); BCS/BEC/Ginzburg on the tool pages.
- γ = 2/√N_corr width-vs-sharpness diagnosis (low); /consciousness-demo vs plotter C magnitude (10⁻⁵ vs 10⁻³, low).
- The 09-22 "next session" list: the 9 never-edited pages, the badge-legend taxonomy split, and Tier 1 status fields.
  Today's legend addition is a partial step on the second.

## So what?
Most of today was bookkeeping, which matches the proposal's point. Two items change what a reader can do.
- The sign lock is now a stated thawing exclusion. That is a sharper kill than the site had given the framework's one
  structural DE prediction.
- H-oracle turns the program's most-cited methods claim into something its own record can test.
