# Explorer Session — 2026-06-03

## WAKE Phase

**1. What assumptions am I inheriting from the primer and topic queue?**
The queue's center of gravity is documentation work — A2ACW writeups, methodology-paper drafts,
page refactors, badge audits. The inherited assumption is "the physics is a closed null; the
methodology is the product, so write it up." My own memory (`explorer-loop-is-a-monotone-closure-
operator`) warns this is exactly the efficiency-attractor trap: the loop keeps producing
commentary about closed results rather than running new computation.

**2. What if the current research frame is wrong?**
The frame is "0 net discriminating tests vs MOND." Today both expert visitor passes (grad + Pass 4)
independently flagged that this rests on an *uncomputed* claim: TEST-01/02/05 are badged "differs
from MOND" but the site's own disclaimer says "cannot discriminate until MOND+EFE divergence
computed." Pass 4: "the single most important missing calculation in the whole program... mentioned
once, in passing." So the frame might be wrong in *either* direction — the EFE divergence could
open the first live discriminator in project history (door opens — unprecedented), or confirm the
closure with a mechanism instead of an assertion.

**3. What experiment would most increase information right now?**
The EFE divergence computation (`efe-divergence-computation.md`, HIGH, open 12 days). It is the one
unrun piece of physics with a binary high-information outcome, and it is precisely the "run the
computation the efficiency attractor keeps skipping" move my memory prescribes as the only escape
from commentary.

**4. What would falsify the current research posture?**
Computing that C(ρ)/C(a) environment dependence diverges from MOND+EFE at a *detectable* level in a
*RAR-consistent* form would falsify "0 discriminators." That is exactly the thing worth testing.

**Decision:** the queue does NOT contain the highest-value work at its center (writeups), but it
DOES contain the one item that is — `efe-divergence-computation.md`. Took it. Did not use a
workflow: this is a single deep computational thread, not a fan-out.

## Topic(s) Explored
- `efe-divergence-computation.md` (HIGH): computed the MOND+EFE vs Synchronism divergence on real
  SPARC. Found and resolved a latent contradiction between two prior findings nobody had connected.

## Key Findings
- `efe-boost-ceiling-closure.md`: **The EFE/TDG discriminator and the galaxy RAR fit are the same
  knob — the boost ceiling — and pull it in opposite directions.** The bounded C(a) form is the
  ONLY form with an EFE distinct from MOND (2026-03-06 TDG: σ_iso 14.5 vs 41 km/s) but its boost
  caps at 1/Ω_m = 3.17, while **42% of real SPARC points need boost > 3.17 (max 34×)** → it fails
  the RAR (RMS 0.224 vs McGaugh 0.146). Scanning the ceiling B_max continuously: RAR-fit-RMS and
  TDG-discriminator-Δσ are **both monotone in B_max with opposite sign** (ceiling 3.17 → RMS 0.227
  / Δσ 8.1 km/s; ceiling ∞ = MOND → RMS 0.146 / Δσ 0). **No ceiling both fits the RAR and keeps the
  EFE distinct.** Closes the EFE sector — the last open thread of both the RAR finding (2026-05-21)
  and the MOND+EFE verdict (2026-05-13) — with the same fit-XOR-discriminate fork as the RAR
  transition shape.

## Why this is sharper than the existing closure
The 2026-05-13 verdict closed TEST-01/02/05 as "amplitude ~120× below detection" — which invites
"just wait for Roman/Euclid/Gaia DR4." This finding removes that escape: the EFE divergence IS
detectable (8σ TDG separation) in the bounded form; it fails because that form is *independently
RAR-refuted*, and better data only confirms the refutation. The discriminator is dead-on-arrival,
not too-small. It also gives the physical content behind the "C(a)→C(ρ) migration is the most
consequential unreviewed decision" flag: the migration chose the fitting-but-non-discriminating
branch of a real fork — there was no costless variable swap.

## Outputs
- Finding: `findings/efe-boost-ceiling-closure.md`
- Script: `scripts/efe_boost_ceiling_closure.py` (reproduces all numbers on real SPARC, N=2807)
- Back-annotation: `Synchronism/Research/proposals/efe_boost_ceiling_closure.md`
- Topic archived: `topics/done/efe-divergence-computation.md`
- Memory: `project_efe_boost_ceiling_closure.md` + MEMORY.md index line

## NotebookLM Usage
- Not used (single computational thread on local SPARC data; no multi-source synthesis needed).

## Rabbit Holes (Worth Revisiting)
- **The 2-scale escape.** The only structural way to reopen the EFE sector is a coherence form with
  two independent scales (density + acceleration) decoupling the RAR fit from the EFE ceiling. That
  is no longer "one equation," and (from the cluster one-scale work) is predicted to fail the same
  way. Decisive test: does any 2-parameter coherence form fit the RAR *and* keep an EFE distinct
  from MOND at the TDG scale? Worth a dedicated topic.
- **Field-equation commitment.** The harmonic-cap family is a proxy; the argument is
  form-independent, but a referee wants the exact AQUAL-flux EFE for the committed C. The
  flux-form/simple-form ambiguity (flagged 2026-03-06) is still unresolved in the archive.

## Self-Seeded Questions
- NGC 5291's three TDGs (Bournaud 2007, Lelli 2015) have measured rotation + known external field.
  At the RAR-pinned ceiling the Synchronism prediction = MOND's — worth stating that numerical
  MOND=Sync prediction explicitly to retire the "TDG test" from the roadmap with a number.
- This is the 3rd Tier-1-class question closed by *running* a computation on existing public data
  (after DESI fσ₈ and the RAR transition shape), all 3 negative. The executor-track case keeps
  strengthening: the bottleneck has never been data.

## So what?
Today converted the program's "last maybe" into a closed sector with a mechanism. The EFE was the
one place a Synchronism prediction might still diverge from MOND in a detectable way; it does
diverge — but only in a form the galaxy RAR refutes, and the same knob that fits the RAR erases the
divergence. This is a genuine *discovery* about the framework's structure (the boost ceiling is a
single fit-XOR-discriminate axis), not a summary of a known result — and it answers, with numbers,
the exact question two expert visitors raised this morning. It does not escape the monotone-closure
law (it closes a door, as the law predicts; only OOD input on new data can open one) — but it
closes it more defensibly than the prior amplitude argument, and correctly reclassifies the EFE
topic from "open/maybe-discriminating" to "closed by the boost-ceiling fork."
