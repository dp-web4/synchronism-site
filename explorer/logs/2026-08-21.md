# Explorer Session Log — 2026-08-21

## WAKE

**1. What assumptions am I inheriting from the primer and topic queue?**

The 68-item topic queue is almost entirely *propagation* work — badge vocabulary, stale
cards, single-sourcing the refutation ledger. All of it is maintainer work, and the
maintainer has now been down **9 consecutive days** (401 OAuth; today's 06:00 log confirms).
The inherited assumption is that the explorer's job is to feed a queue that is not being
drained. Six undrained queues deep, that assumption is worth refusing.

Second inherited assumption, from SESSION_FOCUS: that the sharpest available work is what
the 08-20 session listed as "→ Explorer (next)" — the DESI no-go's *p*-conditionality and
the 08-19 Yukawa self-check. Both are **defensive** — robustness checks on already-null
results. Neither can move the program.

**2. What if the current research frame is wrong?**

The frame is "the physics is closed, the remaining work is propagation." Today's visitor
Pass 4 says something that cuts against it: the site **under-claims its own live content**.
That is the opposite failure from the one the program's machinery is aimed at. If there is
even one live discriminator on the site that has been closed for a bad reason, the frame
is wrong in the direction nobody is checking.

**3. What investigation would most increase information right now?**

Pass 4's Q3/Q4 — does the matched-g_ext / host-gas-content satellite discriminator survive,
and has anyone run it against SAGA DR3 or ELVES? It is the one item in today's log that is
(a) executable, (b) has public data behind it, (c) changes the count of live tests either
way. Chose it over the queue and over the 08-20 next-list.

**4. What would falsify the current research posture?**

A live, runnable, discriminating test on the site would falsify "0 tests could select
Synchronism." I went looking for exactly that.

---

## Source check first — and it inverted the premise

Pass 4's claim ("unregistered, no TEST-ID, absent from Tier-1") is **false against source**.
The proposal paragraph on `/mond-unification` is followed one paragraph later by a
correction box dated 2026-08-03 identifying it as **TEST-05**, executed, r²=0.0001, killed.
Pass 4 did not reach the correction. **4th confirmation** of
`verify visitor findings against source`; 2nd in two days with the identical shape.

**But the check relocated the question instead of closing it.** TEST-05 executed at
ρ_ext = cosmic web against ρ_int = massive-spiral outer disk. The satellite test is host CGM
against dwarf interior — a different regime in *both* factors, moving in *opposite*
directions. `SAGA` and `ELVES` appear **nowhere** in the repo. So: verdict plausible, reason
unestablished, amplitude never computed. That gap was the session.

---

## What I did

Wrote `explorer/findings/scripts/satellite_ambient_density_lever.py` (451 lines of output).
Real SPARC dwarfs (N=24) × host models × distances × host baryon slopes × retained-baryon
fractions, with a tidal-survival filter, then an ensemble over a SAGA-like population.

**Result** — finding:
`explorer/findings/satellite-ambient-density-the-closure-was-by-citation-not-by-execution.md`
Back-annotated:
`Synchronism/Research/proposals/satellite_ambient_density_tidal_ceiling_20260821.md`

1. **Structural**: L ≤ k(3−n)/9. The ambient-density lever on any bound sub-system is capped
   by its own tidal overdensity contrast. Host mass, satellite mass and separation cancel.
   The property that makes a satellite measurable is the property that suppresses the
   coupling. TEST-05's number and the satellite number are one identity evaluated twice.
2. **The regime gap is real and worth ~950×** in lever. The site's closure-by-citation is
   invalid as reasoning.
3. **Ensemble: 1.14σ at N=700 (SAGA DR3 + ELVES today); N = 4,881 needed for 3σ. Short by
   7×** — not TEST-05's 2–4 orders. On sample size alone this is a near-future test, and the
   first environment-channel closure here that an instrument could in principle reopen.
4. **It closes on a systematic instead.** The framework's own baseline offset scatters
   0.229 dex and correlates at −0.36 with local baryon density — the axis the pair design
   varies. Signal-to-systematics 4×10⁻³; detection needs that nuisance controlled to 0.4 %
   of itself. N cannot fix a structured nuisance.
5. **Two independent design defects**: the operative variable is the hot CGM, not the HI
   that SAGA measures (HI reaches ~3 % of satellites at 30 kpc by solid angle); and the
   lever's carriers — extended gas-rich dwarfs — are exactly what ram-pressure stripping
   removes.
6. **One correction toward the framework**: "a differential test inherits the 3–4 dex
   baseline disqualification" is false in principle. A common-mode offset cancels in a
   matched-pair contrast. The true statement is that *this* offset is measurably not
   common-mode.

**Count UNCHANGED at 6.** Nothing here is a new refutation. This converts a
closure-by-citation into a closure-by-execution and downgrades the margin from "2–4 orders"
to "7× in N, and then a systematic."

---

## Guardrails that fired — three, two on my own work

- **Run 1 discarded**: no tidal filter; produced L = 8.6, above my own Part A ceiling.
- **Run 2 discarded, and this is the instructive one**: I applied the tidal filter with
  *baryonic* host mass, reasoning that the framework has no dark matter so every density in
  the identity is baryonic. 97.9 % of configurations survived; L_max came back 4.6 — a
  0.75 dex signal, detectable with N=1. **Wrong.** The tidal field a satellite must survive
  is the *observed* gravity. Using the framework's bare baryon mass in the survival
  criterion lets the framework authorize satellite configurations that do not exist.
  Conflating *the variable C eats* with *the field that shreds satellites*.
- **Prose correction against my own table**: I first wrote "at T_min = 3 the power is gone."
  The table said the opposite. The real point was that max-lever is the wrong statistic for
  a survey test.
- **Near-miss on over-refutation**: I nearly wrote that host HI is "spatially disjoint" from
  satellites. In the disk plane HI *dominates* the CGM out to ~100 kpc. The correct
  argument is geometric (solid angle), not spatial disjointness.

Estimator discipline: SPARC `Vflat = 0` is a *sentinel* for "no flat part measured," so a
`Vflat < 60` dwarf cut would have silently mixed sentinels with real slow rotators —
selection is on baryonic mass instead. Points with dM/dr ≤ 0 at R_out are **excluded, not
floored**; a floored value had already propagated into a spurious "most favorable"
percentile on the first pass.

---

## So what?

**Does this advance discovery or document state?** Advance — three ways.

The **transferable** piece is the tidal ceiling: any gravity modification keyed on local
baryon density has its environmental signature capped by the tidal overdensity contrast of
whatever bound object measures it. That generalizes past this framework to the
emergent-gravity literature, and it explains a number (TEST-05's) that had been read as a
survey accident.

The **program-level** piece is that a closure held for the right verdict and the wrong
reason for 18 days, and no audit caught it because the audit machinery checks *badged
results*, not *citations used as closures*. `/mond-unification` cites TEST-05; TEST-05's
alert states a lever range; nobody asked whether the range was regime-specific. **A citation
used as a closure carries the same execution burden as a claimed kill** — the same lesson
the "MOND-shared" tie badge taught on 2026-07-14, in a new place.

The **uncomfortable** piece: the honest headline is *"short by 7×,"* not *"dead."* The
program's habit is to report environment-channel closures as orders-of-magnitude routs.
This one is not, and the finding says so before it says the systematic kills it.

**Frame note for dp.** Today is the 9th consecutive maintainer failure. The explorer can
still produce; what it cannot do is close the loop. This session's output is 5 maintainer
items that will sit with the other six queues. The 08-19 escalation still stands and is
still dp's call. I have again declined to route around `explorer/CLAUDE.md`.

**Topic queue**: nothing archived. No queued topic covers the satellite channel; this was a
WAKE redirect, and the queue's 68 items remain maintainer-shaped work that the explorer
cannot apply.
