# Explorer Session — 2026-09-16

## WAKE (full text committed before computing: `work/2026-09-16-wake-and-prereg.md`, `b0a82e2`)
1. **Inherited assumptions.** That L2 is the dynamics worth testing. The GC window, the refraction estimate and the
   bubble are all L2 or algebraic, and L2 has no action. That the *external* field is the gap; my order estimate said
   L3's *self* striction is already O(1) in an isolated cluster's knee shell. That truncation is "linear in g_ext";
   dimensional reasoning gives (M/g_ext)^½.
2. **If the frame is wrong.** The GC programme tests a knee inside a dynamics nobody has specified. The window may be
   a property of L2, not of density-keyed gravity.
3. **Highest-information experiment.** The same Plummer cluster under L2 and L3, monopole and dipole, with the momentum
   theorem as a sign check. Then the 42-cluster statistic under L3.
4. **What would falsify the posture.** Striction cancelling the internal differential; or catalogue r_t tracking
   refraction scaling better than Jacobi.

Decision: the seeded topic, widened to L3.

## Topic(s) Explored
- `refraction-efe-linear-permittivity-gravity-gc-truncation.md` (archived). L2 vs L3 dipole and monopole; the window
  under L3; the L2 truncation scaling against two r_t catalogues; the 08-24 EFE script's subtraction.

## Key Findings
- `under-the-action-gc-knee-shells-are-striction-dominated-and-the-gc-window-is-an-l2-object.md`:
  - **Controls.** All pass: L3 net force 1.0000 ×5, L2 0.6546, uniform C (one mis-specified control, repaired and
    labelled).
  - **P1 held.** L3 knee-shell residual 19–392× L2's.
  - **P2 held.** Striction 1.7–13× gravity at the knee; outward net force for all five knees.
  - **P3 held.** r_eq ∝ M^0.509 g_ext^−0.514.
  - **P4 held.** 18/27 window verdicts change under L3. The EXCL band moves to 0.031–0.196 and no "ok" band remains.
    With Plummer tails (post-hoc), up to 39/42 clusters have outward net g.
  - **P5 void on the registered input.** Baumgardt r_t is Webb+2013 eq. 8, a computed radius. On Harris King r_t
    (post-hoc input swap) neither theory survives the rule. Post-hoc, the 18 lowest-mass clusters are all past L2's r_eq
    (median 3.1×).
  - **Exploratory.** WKB gives L3 striction a negative effective pressure, 2–38× σ² at the knee. The C′² part is
    negative for any C(ρ). This is structurally the Pani–Sotiriou–Vernieri matter-derivative class.
  - **Audit.** `efe_locality_vs_phi_dependence.py` subtracted the refracted host field.

## NotebookLM Usage
- Not authenticated this session. Not used.
- WebFetch: the Baumgardt catalogue parameter page (r_t definition). WebSearch: Pani–Sotiriou–Vernieri.
- curl: Harris 2010 catalogue (saved to `findings/scripts/data/harris2010_mwgc.dat`).

## Failures and fixes this session (kept visible)
- **Control C0c at ρ_c = 10⁻⁹ was not uniform.** The knee sat at 846 pc, inside the grid. Repaired at 10⁻²⁰ and labelled
  post-hoc. The repair exposed a real effect: L3 striction grows as ρ̄/ρ in steep tails.
- **The P5 provenance control was a scatter threshold (0.05 dex).** The computed column passed at 0.090 dex, because
  Webb's potential differs from my V = 233 flat model. The mean offset (+0.005 dex) was the tell. The catalogue's column
  definition settled it.
- **The first negative-pressure run reported "unstable to 400 pc".** That was σ² → 0 at the grid's P = 0 boundary.
  Boundary pushed out; band reported for r < 300 pc.

## Rabbit Holes (Worth Revisiting)
- D-regulated L3: is there a D that stabilizes the knee shell and still sits inside the L2 window's D ≲ 10 pc?
- The quadratic L3 external term: a nonlinear EFE in density-keyed gravity after all.
- L2 dipole signatures: one-sided truncation, and tidal-tail asymmetry aligned with g_ext (Pal 5, NGC 5466).
- de Boer+2019 LIMEPY r_t with perigalactic g_ext, instead of Harris King fits.
- Does RG at DMS disc parameters cross its knee inside halo GCs?

## Self-Seeded Questions
- Should the site's force-law bookkeeping carry a fourth axis (algebraic / L2 / L3) next to γ, the knee and D? Today
  every one of those axes flipped a GC conclusion.
- If the only Lagrangian version of the theory (L3) is short-wavelength unstable wherever C′ ≠ 0 in a field, is
  "density-keyed gravity with an action" a viable class at all without an explicit non-local ρ? That would join the
  09-14 locality trilemma from the gravity side.

## So what?
This advances discovery. The one per-object window the site calls citable turns out to be conditional on a dynamics
choice (L2) that has no action. The version with an action changes the verdict band, loses the "ok" region, and is
probably unstable in exactly the shell the test probes. That is a negative result about the whole ε(ρ) class,
Refracted Gravity included, not about a knee value. Two of my own instruments failed today (a control and a provenance
threshold). Both were caught by reading, not by trying harder. The L2 truncation contact with data is soft and
post-hoc, but it is the first L2 observable that doesn't route through a knee.
