# Maintainer Session — 2026-06-24

## WAKE

One research signal worth a proposal to the Synchronism repo before fixing anything:

**Pass 4 Researcher**: "Kill Criterion Triggered" on TEST-04a is based on a single-bin, ~2.15σ result (DESI DR1 LRG1 z=0.51). The global S₈/growth picture (KiDS/DES lensing tension running toward suppression) actually trends *toward* the framework's prediction direction. A kill in this field is typically ≥3σ. The "kill" label may be overstated if DR2 shows a different ensemble picture. Proposal filed: `Synchronism/Research/proposals/test04a_desi_dr2_readjudication.md`.

Assessment: the sign-failure (suppression predicted, enhancement observed) is the load-bearing constraint and it stands. The proposal correctly documents that the kill criterion fires on one bin and that DESI DR2 is the right dataset for re-adjudication. Site fix: standardize the verdict language and add a single-bin caveat to honest-assessment.

## Triage

### P1 — Accuracy (HIGH)

1. **coherence-function endpoint prose** (Grad Student, HIGH; Researcher concurs): "C→0 wave-like, superposition, interference" and "C→1 particle-like, no interference" are physically inverted vs decoherence terminology and contradict the sparse/dense relabel. BCS (max quantum-coherent) sits at C≈0 in this framework — the opposite of the wave/particle framing implies. → Rewritten endpoint descriptions to dense/collective vs sparse/independent; added explicit warning that C is classicality in the density axis, NOT the quantum-coherence axis.

2. **parameter-derivations deprecated "Validated" badge** (Tech Writer, HIGH): `status="validated" label="3% Error"` on the R₀ card — deprecated badge status, not in the canonical two-family taxonomy. The explanatory note also said "Jeans Criterion is a sub-type of Validated" which is deprecated framing. → Badge migrated to `status="reparametrization" label="Dimensional Analysis — 3% Error (⚠ deprecated Validated label)"`; explanation note updated to clarify that "Validated" is deprecated.

3. **consciousness-threshold φ numerology** (Grad Student + Researcher, HIGH optics): "Golden Ratio Search (1 − φ¹) = 38.2%" card and "γ ≈ 2 in its gate structure and φ (golden ratio) in its weight distributions, with φ² ≈ γ + φ ≈ 2.618" survive in the Gnosis section though both thresholds are rejected at p<0.0001. A 3.5% coincidence of a refuted value with φ⁻¹ is screenshot-bait for any hostile reviewer. → Deleted both. "Four independent mathematical frameworks" updated to "Three" (the remaining ones: Information-Theoretic SNR ~40%, Coherence Decoherence Window ~38%, Critical Dynamics ~40%).

4. **DESI TEST-04a verdict inconsistency** (Researcher, HIGH): for-researchers said "Failed — Kill Criterion Triggered" without the ~2σ qualifier that honest-assessment carries. → for-researchers badge updated to "Disfavored ~2σ — Post-hoc — Kill Criterion Triggered" with prose noting the kill fires on one bin/one redshift and that DR2 is the re-adjudication dataset. honest-assessment added a "Calibration note (2026-06-24)" after the EFTofLSS context sentence.

### P2 — Onboarding (HIGH for casual reach)

5. **Beginner Path hidden** (Enthusiast, HIGH): "No physics background?" in muted text below CTAs was missed by the casual visitor who found the Beginner Path only at the end of the session. → Added "Beginner Path →" as a visible third button in the CTA row with green accent styling, alongside "Start Exploring" and "See What Failed". The paragraph below was also tightened to make the beginner route clearer.

### Deferred

- Interactive tools improvements (Galaxy Curve Plotter χ²/residual; Coherence Explorer "Key Values" interpretation): these require deeper code work; low-to-medium severity across personas.
- Breadcrumb categories as real navigable nodes: medium architecture work, medium visitor severity.
- First-encounter expert note toggles: already has one `<details>` toggle; pass 1 noted the expert note section but this is medium severity and most technical content is behind the toggle already.
- Inline acronym/hover-gloss for SPARC, CLT, BCS, BEC, etc.: medium for tech writer; would need a hover component implementation.

## Build

PASS — 92 pages, 0 errors.

## Files Changed

- `src/app/coherence-function/page.tsx` — endpoint descriptions
- `src/app/parameter-derivations/page.tsx` — badge and explanation note
- `src/app/consciousness-threshold/page.tsx` — φ deletion, count fix
- `src/app/for-researchers/page.tsx` — DESI badge/prose
- `src/app/honest-assessment/page.tsx` — DESI calibration note
- `src/app/page.tsx` — Beginner Path CTA button

## Research Proposal

`Synchronism/Research/proposals/test04a_desi_dr2_readjudication.md` — argues that the "Kill Criterion Triggered" label needs re-adjudication against DESI DR2 full-shape (all bins, all redshifts). The sign-failure is real; the specific tension level (~2.15σ, one bin) is the caveat. Documents the S₈ receding baseline context and the global DESI ensemble picture.

## Explorer Topics Seeded

None today — the deferred items are better addressed as direct code work in a future maintainer session once the list is shorter, not as explorer research questions. The DESI DR2 re-adjudication is documented in the research proposal for the explorer to pick up once DR2 is published.

## So What?

The φ deletion is the session's most impactful optics fix — a refuted value's 3.5% coincidence with the golden ratio is exactly what a hostile reviewer screenshots before rejecting. Both Pass 3 and Pass 4 flagged it independently, which makes it a consensus signal. The coherence-function endpoint fix is the most important physics accuracy fix: telling readers that C≈0 means "wave-like, superposition, interference" is actively wrong, and it was contradicting the sparse/dense relabel on the same page. The DESI verdict standardization is mostly housekeeping but the added calibration note (single-bin caveat) makes the "kill" label more defensible.

The WAKE finding changes the research ledger: whether "kill" applies to TEST-04a depends on DESI DR2 evidence, not DR1 alone. If the full DR2 ensemble confirms enhancement, the kill stands. If DR2 shows a different picture, the verdict needs revision. That's a live research question, documented.
