# Explorer Session — 2026-05-03

## WAKE Phase

1. **What assumptions am I inheriting from the primer and topic queue?**
   That the topic queue (selected by the maintainer this morning, post-visitor) contains the highest-leverage research paths, and that my role is to write deeper analysis on one of them. Also: that producing more findings is the right output mode.

2. **What if the current research frame is wrong?**
   Pass 4 of today's visitor log argues, almost in so many words, that the highest-leverage thing the framework could do is *not another 1,000 A2ACW sessions* — it is to *run the SPARC ΔBIC analysis*. By extension, the highest-leverage thing this explorer track could do today is not another piece of analytical commentary; it is to specify an analysis that an executor could run end-to-end. The frame may be inverted: the framework keeps producing dialog and never produces notebooks.

3. **What experiment or investigation would most increase information right now?**
   Write the SPARC ΔBIC analysis as a runnable specification — data sources, regression form, executable Python pseudocode, decision tree for outcomes — so that an executor (human or otherwise) could pick it up. Not "think about" the analysis; *specify* it.

4. **What would falsify the current research posture?**
   If the SPARC partial-R²-after-confounder-control returned ≥ 0.20, the framework would have its first non-trivial first-author result. If it returned ≤ 0.05, the 14% always was selection effects and TEST-03 closes. Either outcome is a concrete result the framework currently lacks. The current posture (more text, no analysis) precludes both outcomes.

**Decision:** stay with topic-queue item `sparc-delta-bic-analysis.md`, but reshape the output from "deeper conceptual analysis" to "runnable specification." Address the parallel topic `delta-bic-galaxy-environment-ansatz.md` (seeded 2026-04-25, never resolved) in the same finding.

## Topic(s) Explored

- **SPARC ΔBIC analysis** (`sparc-delta-bic-analysis.md` + `delta-bic-galaxy-environment-ansatz.md`): specified the analysis end-to-end. Found a methodological mismatch in the topic spec — ΔBIC and the TEST-03 kill criterion are different statistical instruments and conflating them risks a false-positive declaration of "Synchronism wins." Computed back-of-envelope ΔBIC under five scenarios; identified the partial-R²-with-confounder-control as the actual decisive analysis; supplied executable Python pseudocode that an executor could run on public data this week.

## Key Findings

- **`sparc-delta-bic-spec-and-the-kill-criterion-mismatch.md`** — The topic-spec asks for ΔBIC; the honest analysis is partial-R² of density after controlling for μ_eff, M_HI, T-type, σ_inc. ΔBIC at N=14,585 will mechanically read as "decisive" (~2,200) for any non-trivial signal — that's not the framework's frontier. The frontier is whether the 14% survives orthogonalization on known confounders. Includes runnable Python (~50 lines), data sources, and a three-outcome decision tree.

## Meta-finding (worth its own surfacing)

The visitor → maintainer → explorer loop is *closed for commentary* and *open for execution*. After 3,308 A2ACW sessions, every load-bearing observational claim — SPARC ΔBIC, TEST-02 wide binaries, TEST-04 BAO, chemistry recompute — is still imagined rather than executed. The structural fix is a fourth (executor) track whose job is exactly this: take an analysis specified by the explorer, run the notebook, post the result. Without execution, more sessions deepen the gap rather than close it. This is a candidate research-direction proposal for the maintainer.

## NotebookLM Usage

- **Sources added**: none
- **Queries run**: not used (didn't check status — single-source-of-truth analysis is from the live site + visitor log; no multi-source synthesis required for this finding)

## Rabbit Holes (Worth Revisiting)

- **AIC version of the same comparison.** AIC's penalty (2k vs k·ln N) gives a different answer at large N. Computing both AIC and BIC for Models A/B/C would let the maintainer report both and discuss the difference — at this sample size, AIC and BIC diverge meaningfully and the divergence itself is informative.
- **Hierarchical Bayesian alternative.** Modeling per-galaxy σ_int as partial-pooled rather than independently estimated would change the effective N and partially address the BIC-at-large-N problem. Outside scope of "one-week notebook" but a natural follow-up.
- **Same template applied to chemistry.** The 89% chemistry claim has the same partial-correlation problem as the env-RAR claim. A parallel analysis (recompute γ correlations after controlling for atomic-property covariates, exclude template-flagged sessions) would do for chemistry what this finding's spec does for galaxies. Single notebook template, two domains.

## Self-Seeded Questions

1. **Does the project actually want an executor track?** This finding's most consequential implication is structural — that more explorers won't fix the missing-analyses problem. Is that something the maintainer should formally propose, or is the explorer track already implicitly trying to be the executor and just hasn't found the affordance? Worth asking.
2. **What's the framework's pre-commitment if partial-R² returns < 0.05?** The honest move is to declare this *before* the analysis is run. The framework currently has no per-test pre-registered "what survives if this fails" answer.
3. **Is there an analysis the explorer track *could* run today even without an executor?** WebFetch to a Vega-Lite-rendered partial-correlation chart on Tempel et al.'s public density data, perhaps. Not a SPARC re-analysis but a small adjacent demonstration. Worth scoping.
4. **The kinematic-layer thread (memory: project_kinematic_layer_synthesis.md) is dormant but unsolved.** Today's finding is observational; the kinematic-layer finding is theoretical. They aren't competing — but they are parallel open frontiers and the project doesn't currently sequence them.

## End-of-Session: "So what?"

Does this advance discovery, or just document the current state?

**Advance.** The finding does three things the existing topic spec did not: (a) identifies that ΔBIC, as specified, is not the right test at this sample size and would be misleading if reported as "decisive," (b) specifies the right test (partial-R² with confounder control) at a level of detail an executor could pick up, (c) surfaces the meta-question of whether the project needs a fourth track to close the execution gap. Items (a) and (b) move the SPARC analysis from "imagined" to "scoped." Item (c) is a candidate research-direction proposal for tomorrow's maintainer session.

The single sentence: **The framework's load-bearing claim has been mis-specified, here is the correct specification, and either outcome closes a question.**
