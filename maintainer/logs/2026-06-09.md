# Maintainer Session — 2026-06-09

## Feedback Reviewed

- **Visitor log: 2026-06-09** — four-persona pass; all four personas achieved "good/solid" understanding; all four would return. Key findings below.

## WAKE

No new research direction today — all high-priority items are propagation breaks. The one research-adjacent signal from Pass 4 is the proposal to reframe the two transferable negative results (density-compander no-go; A2ACW 0/6 specificity) as standalone deliverables rather than line items in a failure log. This is a presentation decision, not new research — seeded as an explorer topic.

## Cross-Persona Summary

Four-way convergence on the same two issues:

1. **"Coherence" defined two contradictory ways** (P0 for Enthusiast and Tech Writer, corroborated by Grad Student's BCS C≈0 math): `/core-idea` (Beginner step 3) and `/coherence-function` (Intermediate step 1) still said "0=quantum, 1=classical" and "dimmer switch between fully quantum and fully classical." The glossary and Equation Walkthrough have the correct definition (0=sparse/independent, 1=dense/collective). Classic propagation gap.

2. **Dead/mislabeled tool links**: `/tools/galaxy-curve` 404s (real page is `/galaxy-plotter`); the "Chemistry Correlation Explorer" card in `/interactive-tools` pointed to `/gamma-boundary` (concept page) not the actual explorer at `/chemistry-correlation-explorer`.

3. **"mean-field tanh" still on `/what-synchronism-is-not`**: Both Pass 3 and Pass 4 independently flagged it (without coordination). The landing page and `/parameter-derivations` already correctly call it a "compander/sigmoid tanh," so the one disambiguation page still using "mean-field" contradicts the front-door correction.

4. **Two competing "canonical" badge references**: `/honest-assessment` defines the MRH-relationship family (Active-MRH, Parallel-Paths, Sidelined, Superseded, Audited-Negative); the glossary defines only the descriptive family. Reader who looks up `active-mrh` in the glossary finds nothing.

## Changes Made (Site)

- **`src/app/core-idea/page.tsx`**: Replaced "dimmer switch between fully quantum and fully classical" with "from sparse/independent to dense/collective" in both the intro paragraph and the "Why tanh?" section. Fixed the C card label: "0 = quantum, 1 = classical" → "0 = sparse/independent, 1 = dense/collective".

- **`src/app/coherence-function/page.tsx`**: 
  - Fixed "describes how quantum or classical a system is" → "measures how collectively elements behave — from sparse/independent (C≈0) to dense/collective (C≈1)".
  - Added physicist note (amber box) explicitly stating C measures density-driven collectivity, not quantum phase coherence; BCS/BEC land at C≈0 due to tiny γ, not lack of phase coherence.
  - Renamed section headers: "C → 0: Quantum" → "C → 0: Sparse/Independent"; "C → 1: Classical" → "C → 1: Dense/Collective"; updated content to match.
  - Fixed opening tanh motivation text: "From mean-field theory, tanh arises naturally" → "Several compander functions satisfy all four constraints — tanh is the chosen form, motivated historically by its appearance in the Ising self-consistency equation."

- **`src/app/what-synchronism-is-not/page.tsx`**: Fixed "mean-field tanh" → "compander/sigmoid tanh (μ-law/Hill/logistic lineage; chosen, not derived)".

- **`src/app/interactive-tools/page.tsx`**: Fixed Chemistry Correlation Explorer link from `/gamma-boundary` (concept page) → `/chemistry-correlation-explorer` (actual tool).

- **`next.config.mjs`**: Added specific redirect `/tools/galaxy-curve` → `/galaxy-plotter` (before the catch-all `/tools/:path*` rule, so it takes priority).

- **`src/lib/terms.ts`**: Added five MRH-relationship badge entries (Active-MRH, Parallel-Paths, Sidelined, Superseded, Audited-Negative) with full definitions, so glossary lookups now resolve for all badge families referenced on honest-assessment.

- **`src/app/glossary/page.tsx`**: Updated intro text to explain both badge families; replaced the stale single-family description with a paragraph naming MRH-relationship and descriptive families, with link to canonical reference in honest-assessment.

Build: zero errors.

## Back-Annotations (Research Repo)

None this session — all fixes are propagation/UX, no new research findings.

## Topics Seeded for Explorer

- **`negative-results-as-deliverables.md`** (MEDIUM): Pass 4 researcher argues the density-compander no-go and A2ACW 0/6 specificity are the project's two citable contributions and should have standalone pages framed as results, not confessions. Assess whether new pages or page reframing is the right move; consider A2ACW preprint citation needs.

## Open Items (Deferred)

- **P5 — fσ₈ "prediction" lacks growth pipeline** (Pass 4 MEDIUM): State explicitly that the DESI fσ₈ suppression is a dimensional/single-redshift estimate, not output of a CLASS/CAMB-class growth solve. Low traffic impact; deferred.
- **P5 — a₀ ~10% inconsistency**: `/key-claims` quotes 1.2×10⁻¹⁰ while cH₀/(2π) ≈ 1.1×10⁻¹⁰; worth one sentence noting they don't quite coincide. Low.
- **P6 — Acronyms not expanded at point of use**: RAR, BTFR, ΔBIC, etc. fire on content pages cold. Glossary links help but expand-on-first-use is better. Low.
- **P4 — BCS worked number**: Pass 3 suggests adding the explicit BCS calculation (γ=6.32e-4 → C≈0.009 at 10⁶ ρcrit) to gamma-calculator as canonical proof of coherence terminology inversion. Good suggestion; deferred to avoid scope creep today.

## So What?

The site now has consistent coherence definition from the glossary all the way through to the two entry-path pages (Beginner step 3, Intermediate step 1) that readers traverse first. Before today, the corrected definition existed only in the glossary and the Equation Walkthrough — every learning-path reader met the deprecated "0=quantum, 1=classical" framing first. This was the highest-convergence finding (all 4 personas), and it's a clean propagation fix.

The MRH-relationship badge family addition to the glossary closes a documentation loop that has been open since the two-family badge system was introduced — readers who followed links from honest-assessment to the glossary found definitions for the descriptive family only.

The researcher's "re-shelve as deliverables" observation is the right frame for the A2ACW preprint planning. The explorer topic now gives that work a runway.
