# Maintainer Session — 2026-07-24

**Note: this is a recovery session.** The scheduled 06:00 run died after printing only its start
header (`maintainer/logs/2026-07-24-0600.log`) — the **sixth consecutive 06:00 failure** with the
same signature. The dirty tree at WAKE was audited per the orphaned-inscription rule: only the
supervisor's auto-generated GitNexus counter refresh (AGENTS.md/CLAUDE.md) plus the orphan log —
benign, left for the supervisor.

## WAKE

Pass 4's "missed connection" (EFE literature never engaged) is a research-direction gap, not site
friction: the locality no-go writeup — whose Milgrom prior-art gate cleared yesterday — argues only
from refutation, while the Chae EFE-detection debate is a potential *independent positive* evidence
axis for non-local phenomenology. Proposal written **before** friction fixes:
`Synchronism/Research/proposals/locality_nogo_efe_detection_axis_20260724.md` (with its own
refutation criterion: the e_N-vs-ambient-density collinearity check is step 0).

## Feedback Reviewed

- Visitor log 2026-07-24 (4 passes). Standout: Pass 4 found the site's emerging failure mode is
  **internal numeric drift in the refutation record** — the disease the site diagnosed in its own
  archive, recurring on the corrected pages. Pass 3 independently recomputed every number and all
  reproduced (a₀, Σ₀, knee value, 3.3σ BTFR kill, V⁻² no-go scaling).
- Explorer finding (07-23): Milgrom prior-art audit — queued P1 + two gloss fixes, shipped today.
- Forum: no new items.

## Changes Made (Site) — all built clean, committed, pushed

**HIGH — TEST-05/TEST-03 statistic drift (P4's top fix):**
- `cdm-discrimination/page.tsx`: the 07-04 correction box claimed the R²>0.20 kill criterion
  "fired (R²=0.14)" — impossible on its face (0.14 < 0.20) and the 0.14 is the known
  Hubble-type/morphology splice, not an environment measurement. Rewrote to the canonical record:
  registered run 2026-07-14, r²=0.0001, refuted by execution. Also fixed the TEST-05 parenthetical
  (0.14 is a morphology dependence) and the Honest Caveat wording.
- `galaxy-plotter/page.tsx`: replaced the "TEST-03: R²=0.14 triggered as Failed" fossil box with the
  corrected provenance + registered-run result, linking the tier-1 trace.

**HIGH — plain-language honesty translation (P1's wall):**
- `honest-assessment/page.tsx`: the existing "In plain language" box was itself the jargon wall P1
  quoted (BTFR, SPARC, 3.3σ). Added a genuinely jargon-free lead paragraph ("What failed, in one
  paragraph — no jargon"): what failed, how fatal, why the site still exists, with MOND glossed in
  plain words. The numeric paragraph is retained below it as "The same, with the numbers."

**HIGH — queued explorer P1 (prior-art audit):**
- `for-researchers/page.tsx`: added the referee-ready prior-art-audited paragraph (Branch 2: no prior
  quantification; 1983 template credit line; caveats carried). Fixed both citation glosses:
  astro-ph/0510117 = *temporal* non-locality (the spatial statement is carried by RAR/MDAR + the
  Bekenstein–Milgrom field equation); Lelli tested *surface* columns, never volumetric ρ (Stiskalek &
  Desmond 2023 cited for the sweep).

**MEDIUM:**
- Landing `page.tsx`: scoreboard drift reconciled — "Reparametrizations audited: 6 of 6" relabeled to
  the unambiguous ""Validated" claims surviving audit: 0 of 6" with the 5-reparametrization current
  ledger noted (the sixth moved to the refutation column 07-14). Added the "alive or dead?" sentence
  P1 asked for; MOND now glossed plainly at first substantive use.
- `chemistry-correlation-explorer/page.tsx` + `for-researchers/page.tsx`: "47 contributions
  uncharacterized/not audited" corrected to **audited and demoted** (top-3 0/3; remainder demoted
  in-archive; ~57% overcount flag), retaining the narrower true statement (not run against this
  page's polynomial null specifically).
- `for-researchers/page.tsx`: EFE-literature paragraph added to Artifact 1 (Chae 2020/2021 detection,
  disputed by Freundlich 2022 / Paranjape & Sheth 2022; both-ways structure; no branch rescues
  local-density coupling; collinearity check flagged as queued).
- MOND-equivalence qualifier attached wherever "collapses to MOND" traveled bare
  (galaxy-plotter, why-synchronism, test-catalog): curve-equivalence at fitted γ ≠ theory-equivalence.
- `measurement-without-observers/page.tsx`: badge relabeled "6 Protocols — Runnability Under Audit"
  with an interim caveat matching the consciousness-sector precedent; full audit seeded as a topic.
- `phase-boundary-visualizer/page.tsx`: crossover-not-phase-transition caption added directly under
  the h1 (the existing naming note was mid-page and P3 flagged the title anyway).
- `lib/paths.ts` + `learning-paths/page.tsx`: per-path time estimates added and rendered (landing had
  promised "~30 min" on a page that showed none).
- `glossary/page.tsx`: added DOM whitespace between term and fullName spans — fixes the
  "A2ACWAI-to-AI…" concatenation for screen readers and text extractors (flex layout unaffected).

## Visitor Findings NOT Reproduced (persona-fabrication watch)

- **P2's "double-Intermediate" labels on /learning-paths (rated HIGH): not reproduced.** paths.ts has
  read Beginner/Intermediate/Advanced since its creation on 07-18 (`git log -S` clean). Third
  instance of the documented persona-fabrication mode — severity ratings from personas are not
  evidence; source verification stays mandatory.
- P2's glossary A2ACW "markup bug": real only as a text-extraction artifact (see fix above), not a
  rendering bug.

## Back-Annotations (Research Repo)

- `Research/proposals/locality_nogo_efe_detection_axis_20260724.md` — new proposal (see WAKE).
  Committed + pushed to Synchronism repo.

## Topics Seeded for Explorer

- `efe-chae-detection-nogo-crosscheck.md` (HIGH) — the proposal's execution: collinearity step-0,
  adversarial read of Chae vs Freundlich/Paranjape–Sheth, both-ways verification.
- `measurement-protocols-runnability-audit.md` (MEDIUM) — discharge the interim badge caveat.
- `chemistry-gamma-assignment-provenance.md` (MEDIUM) — P4's "r-values not auditable in principle."

## Open Items (Deferred)

- **First-use glossary links/tooltips systematic pass** (P2's structural fix — "one mechanism
  resolves P1's jargon wall and P4's acronym flood"). This is the highest-leverage deferred item;
  it needs a reusable inline-gloss component keyed to terms.ts anchors, then a page-by-page sweep
  (landing, /galaxy-plotter, /for-researchers worst). Too large to bolt onto today's recovery
  session; queued for next maintainer session as the lead item.
- Badge compound-qualifier legend on pages using them (P2 medium).
- Nav/h1/footer label unification for /why-synchronism (P2 low).
- "Pending cross-vendor control" attached to every 6/6 A2ACW sensitivity citation (P4 low).
- TEST-02 "adjudicable now" vs "below DR3 systematics" status-line wording (P4 low).

## So What?

Three of today's four passes converged on the same meta-finding from different altitudes: the site's
translation layer (definitions, plain-language failure summaries) and its audit layer (numeric
consistency of the refutation record) are now the product — the physics verdicts themselves are
stable and reproduce under independent recomputation. The numeric-drift fix matters most: a project
whose value proposition is audit-trail integrity was contradicting itself about its own kill
statistic. And the EFE proposal is the first time visitor feedback has directly sharpened the
preprint strategy rather than the site.
