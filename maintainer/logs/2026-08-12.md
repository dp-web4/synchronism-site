# Maintainer Session — 2026-08-12

## WAKE

Dirty tree at start = supervisor GitNexus count bumps in AGENTS.md/CLAUDE.md (left uncommitted,
supervisor-owned) + this session's own runner log. Not an orphaned inscription.

Standing rule applied: **checked unactioned explorer findings before the visitor log.** The 08-11
covariant-completion finding (explorer ran at 08:00, after yesterday's 06:00 maintainer) had a
4-item unactioned queue — and the visitor's headline complaint today (Pass 4: DE sector
half-shipped) is the *same object* from the outside. The two inputs converged on one work item,
which is the feedback loop working.

**Research-direction check (required before fixes): YES — filed a proposal.** Pass 4 exposed a
registration-design flaw, not just site friction: the TEST-26 draft adjudicates "the sign-locked
locus," an object the archive's own covariant work killed the next day, and the CPL-projection
literature (Shlivko & Steinhardt; Cortês & Liddle; Wolf et al. — the framework's best available
defense) was uncited anywhere. Filed:
`Synchronism/Research/proposals/test26_registration_projection_robustness_20260812.md` (gates on dp).

## Feedback Reviewed

- Visitor log 2026-08-12 (4 personas). Enthusiast: obituary-before-the-patient; no plain "what it
  tried"; MOND unglossed. Tech Writer: status-vocabulary sprawl (20+ terms, legend cites undefined
  labels); three unreconciled refutation counts. Grad student: over-refuting kill statistics
  (ΔBIC +184 vs own ≈33), "+1" regulator never discussed in Equation Anatomy, no cosmology page.
  Researcher: DE sector fragmented (prose-only, /dark-energy 404, TEST-26 absent from catalog,
  tier-1 namespace sentence already false); EFE=0 framing missing SEP/refutation-only + contested
  Chae detection; CPL-projection critique.
- Forum: nothing new since 2026-05-28.
- Explorer: 08-11 covariant-completion finding — 4-item maintainer queue (items 3+4 already
  executed by the Publisher track, Synchronism commit f6de09d3, which also independently re-ran
  the covariant checks).

## Verification pass (before editing, per standing rule)

- Tier-1 "flat namespace TEST-01 … TEST-25" — confirmed at source, confirmed false (TEST-26 exists).
- Test catalog: no TEST-26 anywhere — confirmed.
- Landing: first MOND use unglossed (gloss only appears far below) — confirmed.
- /mond-unification: "already in tension with Chae et al. 2020" still live on the page although
  PREDICTIONS.md withdrew that exact claim 2026-08-05 (not-evaluable, baseline 38–92× the signal) —
  a site-archive drift instance the visitor did NOT report; found while implementing their adjacent
  fix.

## Changes Made (Site)

- `src/app/honest-assessment/page.tsx` — DE block lead rewritten to model-class level (explorer
  item 1): both completions, 0/192 quadrant scan, w=dlnF/dlnx identity, interior-maximum escape
  condition; γ=1/2=ΛCDM flagged substitution-conditional; CPL-projection debate cited both
  directions; provenance updated (the "covariant derivation is open" caveat resolved-by-execution).
- `src/app/top-5-tests/page.tsx` — TEST-26 card → "Class No-Go", verdict rewritten (hardened
  status, class statement, projection-robustness pre-commitment, substitution-conditional
  kill-or-tie); Strategy card route-back paragraph → executed-and-closed + escape condition
  (explorer item 2).
- `src/app/dark-energy/page.tsx` — **NEW PAGE** (closes the 404 both expert personas hit):
  construction, corrected w(z), sign lock, γ=1/2 double-identity, covariant completions A/B,
  escape condition, which-force-law-generates-this-sector section, per-sector ρ_crit disclosure,
  honest bounds, TEST-26 + provenance chain.
- `src/lib/navigation.ts` — /dark-energy registered under Cosmology.
- `src/app/test-catalog/page.tsx` — namespace paragraph extended to TEST-26; TEST-26 card added
  (explicitly outside the 24-test discrimination count).
- `src/app/tier-1-existing/page.tsx` — namespace sentence corrected to TEST-26 with dated
  staleness note.
- `src/app/page.tsx` — hero subtitle rewritten: plain "the bet / what it tried" before the failure
  verdict; MOND glossed at first use; "the honest map of where it fails is the product" surfaced.
- `src/app/mond-unification/page.tsx` — EFE paragraph rewritten: withdrawn Chae-tension claim
  removed; SEP/refutation-only point added; Freundlich + Paranjape & Sheth rebuttals cited (in the
  framework's favor); not-evaluable verdict propagated from the 08-05 ledger execution.

Build: `next build` passes, zero errors; /dark-energy prerendered.

## Back-Annotations (Research Repo)

- `Research/proposals/test26_registration_projection_robustness_20260812.md` — new (WAKE output).
- Items 3+4 of the explorer queue verified already-done by Publisher (Appendix D EdS note at
  manuscripts/Appendix_D…:103; PREDICTIONS.md 2026-08-12 block) — not duplicated.

## Topics Seeded for Explorer

- `fit-the-gamma-family-to-desi-chains.md` — chain-level fit of both w(z;γ) branches vs DESI DR2;
  quantify CPL-projection bias for this family; verify the three projection-paper arXiv IDs.
  (Queue is 70 deep; deliberately seeded only this one.)

## Open Items (Deferred)

- Status-vocabulary freeze (Tech Writer P0) — multi-day editorial; `badge-vocabulary-*` topics
  already queued; needs a single owner decision.
- Kill-statistic hygiene (ΔBIC verdict number, N_eff; TEST-04a ordering) — partially gated on the
  dp count-reconciliation.
- Equation-walkthrough: "+1" regulator step + Ising-disclaimer port.
- Glossary analogy-first rewrite + hover-promise resolution; tools into learning paths;
  galaxy-plotter Cassini-safe ν default + BTFR-normalization disclosure; chemistry
  Null-Class/Failed boundary; γ-calculator badge placement; breadcrumb aria markup.

## So What?

Advances discovery, narrowly but genuinely: the site's one live falsifiable position now exists as
a coherent public object (page + catalog ID + registration-design proposal) instead of prose
fragments, and the registration flaw was caught *before* adoption — the first time the
criterion-object drift class (TEST-03, TEST-04a) has been intercepted prospectively rather than
autopsied. The persona loop also confirmed its ordering mechanism again: the researcher persona
rediscovered yesterday's explorer result from the outside within 24 hours; the fix that prevents
the next rediscovery is shipping the object, not annotating the gap.
