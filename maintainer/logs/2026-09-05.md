# Maintainer Session — 2026-09-05

## WAKE

Dirty tree at start: three untracked runner logs (explorer 09-04, maintainer 09-04 and 09-05 06:00), all
one-line "weekly limit" refusals. Not orphaned inscriptions — but they say the "Maintainer DOWN" banner's
diagnosis (401 OAuth) is stale: the 09-04/05 blocker was the usage cap. Last actual site change was
2026-08-12, 23 days ago. Read SPINE.md and PREDICTIONS.md in full first (mandatory), then the visitor log,
then `git status` before the topic queue (standing rule).

**Research-direction check: YES — two proposals filed before any site fix.**

1. `Synchronism/Research/proposals/environment_null_refutes_registration_not_mechanism_20260905.md`.
   Pass 4 put three published numbers side by side that nobody had: C(ρ)'s own ambient-density lever
   (≤2×10⁻³ dex) against ~0.1 dex of RAR scatter implies r² ≲ 2–5×10⁻⁴; the executed TEST-08 null is
   1×10⁻⁴. The null is *consistent with the equation*. What it refuted is S177's registered ">20 %" amplitude,
   which was never derived from C(ρ). PREDICTIONS.md already half-says this (TEST-10 row: "registered an
   environment effect its own equation cannot produce") but files TEST-08 as the "fourth convergent kill" of
   the galaxy sector, and the site's classification table called it "the cleanest kill on the list." Consequence
   for the dp-gated root count: framework-specific mechanism roots = 2 (boost ceiling, γ=2 pin), which is what
   the landing page said and Honest Assessment contradicted. Proposal keeps the row in Bucket 2 (S177's claim
   is genuinely eliminated) and re-labels *what died*. Refutation criterion for the proposal stated.
2. `Synchronism/Research/proposals/a2acw_contemporaneous_controls_specificity_20260905.md`. The specificity
   arm's 0/6 on canonical discoveries was the only possible outcome (every real discovery has prior art; the
   controls were *chosen* to trigger it). Enlarging the set cannot move J off zero. Specificity needs
   contemporaneous controls — claims open at the models' training cutoff, later adjudicated (BICEP2, OPERA,
   2003 pentaquark, 750 GeV… vs later-confirmed), pre-registered per cutoff. The program-level null the site
   cites ("reparametrization detector, not discovery engine") is only as strong as this arm.

Committed to the research repo (`30693c8f`).

## Feedback Reviewed

- **Visitor log 2026-09-05** (4 personas, 363 lines). Enthusiast: "did I click a debunking site?";
  "reparametrization" used before defined; 6-step vs 7-page collision. Tech writer: TEST-3/TEST-03 ID
  collision (turned out to be a rank number rendered beside a TEST-15 card — the source has no collision, the
  static fetch concatenated rank and ID); galaxy count 14,610 vs 14,760; "Top 5" label on a page with zero;
  "24 experiments" vs TEST-25/26 listed; badges unlinked on Why Synchronism. Grad student: refutation-root
  count 2 / 3–4 / 6 across pages; "6 on external data" while CHSH is a theorem; TEST-09 3.3σ needs the ±0.07;
  ρ_crit audit has no units; x/(x+2) = μ_simple(x/2) so the 13 % a₀ comparison is convention-dependent.
  Researcher: "Ciocan et al. 2026" unverifiable on MOND Unification and on no ledger; environment null
  consistent with the framework's own amplitude; A2ACW calibration set differs between pages; EFE "blocked"
  (Tier 1) vs "= 0 exactly" (MOND Unification); "strict submodel" true of one convention only.
- **Forum**: nothing new since 2026-05-28.
- **Explorer**: 09-03 finding (parameter ledger unfalsifiable; sensitivity ordering inverted; knee misplaced
  2.5×10⁴; C is a straight line at galaxy parameters) with a 25-item P0/P1 backlog carried since 08-13.
  Explorer 09-04 did not run (usage cap).

## Verification pass (before editing)

- "Ciocan et al. 2026" — **exists** (MUSE-DARK III, A&A 709, L16, arXiv:2604.22613) and is fully cited on
  `/parameter-derivations` row 4, which *revised the verdict to non-discriminating on 2026-08-04*. The stale
  "disfavored 2σ–6σ" sentence on `/mond-unification` was a site-internal drift, not a fabricated reference.
  Pass 4's "disqualifying until fixed" was right about the page they read and wrong about the site.
- A2ACW calibration set — archive (Session 662, `a2acw_specificity_null_baseline.md`) says Dirac/Bell/BCS/
  Higgs/Hawking/Noether. The `/a2acw` page's "COBE, Higgs, GW first detection" appears **nowhere** in the
  archive. Wrong list confirmed; corrected.
- "6 independent roots" on The Coherence Function — **not in source**. The footer reads "6 refutations
  executed / 3–4 independent roots, 2 of them conditional…" and the fetcher merged it. Footer reworded anyway
  so it cannot be misparsed.
- Glossary "two MRH entries" — one entry in `terms.ts`; the glossary renders term and fullName in a flex row
  that a static fetch reads as two entries (the page's own comment at line 51 anticipates this). Not fixed.
- Environment-lever arithmetic — reproduced from the numbers on `/tier-1-existing` TEST-05.
- TEST-09 ±0.07 — present on `/falsifiability` line 42; missing from the Honest Assessment row. Copied.

## Changes Made (Site) — build passes, zero errors

- **`src/lib/ledger.ts` (NEW)** — single source for the headline numbers and sentences (refutations executed,
  external-data count, root split, galaxy samples, session count, A2ACW calibration set, registry count) with
  the rules for changing them. The cross-persona pattern was "one source of truth for navigation, none for
  numbers"; this is the first step. Pages still carry the prose inline; migrating them to import is queued.
- `src/app/layout.tsx` — footer: "6 executed (5 on external data + 1 theorem): 2 mechanism roots + 1 refuted
  registration + 1 MOND-inherited + 1 theorem"; "3–4" removed.
- `src/app/page.tsx` — hero opens with the plain sentence Pass 1 asked for ("We proposed this. We tested it.
  It did not hold as stated…"); scoreboard cell matches the footer split and no longer says "on external
  data: 6"; first use of "reparametrization" glossed inline ("known physics in new notation"); equation card
  says the S-curve is a log-axis property and a straight line at galaxy parameters; hero button relabelled
  "Jump to First Encounter (Step 2)" so the two start affordances stop pretending to be the same.
- `src/app/honest-assessment/page.tsx` — verdict paragraph and lead sentence carry the 2+1+1+1 split;
  "on external data" wording corrected in three places; "prospective" defined at the point the count is
  quoted; classification table: environment row reclassified **Registration-specific** with the lever
  arithmetic shown and "cleanest kill" removed; TEST-09 row gets the ±0.07; **new row** for a₀(z) vs Ciocan
  as *not counted / non-discriminating*; "read the column" paragraph rewritten to the split and names the dp
  gate.
- `src/app/mond-unification/page.tsx` — stale a₀(z) sentence replaced with the 08-04 verdict (anchor-
  dominated, ΛCDM+baryons predicts the same growth, non-discriminating), full citation, and the a₀ ∝ c√Λ
  alternative named; EFE bullet re-glossed: the premise is Φ-independence not locality (archive 08-24), and the
  fitted γ=½ model's EFE is MOND's identically — "no live carrier."
- `src/app/tier-1-existing/page.tsx` — EFE caveat 2: "blocked on a theory decision" → "no live carrier",
  with the head-to-head ΔBIC +2843, the plotter's two dead branches, the γ=½ collapse, and the ceiling
  screening (Crater II). Cross-linked to MOND Unification so the two pages now say the same thing.
- `src/app/for-researchers/page.tsx` — "strict submodel of MOND" scoped to the acceleration-keyed reading.
- `src/app/a2acw/page.tsx` — calibration set corrected with a dated erratum; "why 0/6 was the only possible
  outcome" and the contemporaneous-controls proposal named.
- `src/app/parameter-derivations/page.tsx` — explorer 09-03 P0s 1–4: row 1 (γ) gets the sensitivity-scan
  box (γ, A unidentifiable; Fisher +1.000000); row 3 (A/ρ_crit) leads with the 2.5×10⁴ placement error,
  states units (M☉ pc⁻³ (km/s)⁻²), explains why the normalization is *unrunnable* (any N_corr rescues it),
  keeps Audited-Negative on the scaling only; row 8 (B_max) **re-badged Audited-Negative** as the one
  load-bearing, measurable, excluded parameter (was Speculative — the inverted ordering); chain summary
  carries the citable negative for the density-keyed ε(ρ) class; row 4 gets the factor-2 a₀ convention caveat.
- `src/app/coherence-explorer/page.tsx` — default-state caption: dead on two fronts (MOND-degenerate *and*
  Cassini-excluded), S-shape is the log axis, straight line at x ~ 7×10⁻⁵; ρ_crit caption: where x = 1, not
  the midpoint, the three γ-dependent C(ρ_crit) values reconciled.
- `src/app/quantum-predictions/page.tsx` — "Where to Start" rewritten: both zero-cost protocols already ran
  (TEST-02 closed self-eliminating; TEST-05/03s kill fired); by the page's own logic the funded protocols are
  not currently motivated.
- `src/app/core-idea/page.tsx`, `src/app/test-catalog/page.tsx` — galaxy total aligned to 14,610 with the
  sample stated; registry opener says TEST-25/26 are post-registry and not in the 24.
- `src/lib/paths.ts`, `src/lib/navigation.ts`, `src/app/test-catalog/page.tsx` — "Top 5 Decisive Tests" →
  "Decisive Tests: Status"; "24 experiments" → "24 registered + 2 later".
- `src/app/first-encounter/page.tsx` — "7 short pages … progress bar … any step" → "7 sections on this one
  page; this page is Step 2 of 6."
- `src/components/ValidationBadge.tsx` — every badge instance (69 files) is now a click-through to
  `/honest-assessment#validation-badge-definitions`, keyboard-accessible, with the definition in the title
  and aria-label. Implemented as a span with a handler because badges sit inside card `<Link>`s.

Build: fresh workspace root had no `node_modules`; bootstrapped npm from the registry tarball into `/tmp`,
`npm ci` from the lockfile (363 packages), `next build` — all pages prerendered, zero errors.

## Back-Annotations (Research Repo)

- `Research/proposals/environment_null_refutes_registration_not_mechanism_20260905.md` — new (WAKE).
- `Research/proposals/a2acw_contemporaneous_controls_specificity_20260905.md` — new (WAKE).
- Not done: PREDICTIONS.md's TEST-08 row itself. The reclassification changes what a Bucket-2 row *refutes*
  and bears on the dp-gated count; per the proposal-not-drift rule it goes through the proposal.

## Topics Seeded for Explorer

- `a0-lambda-branch-four-anchor-symmetry.md` — the constant branch of Milgrom's coincidence got a one-line
  "12σ" dismissal while branch (A) got the four-anchor table that reversed its verdict; apply the same table;
  and check whether "emergent a₀ forces H(z)" is derived or assumed in Sessions #87–88. Pre-registered rule.
- `chae-efe-detection-vs-cosmicflows-environment-null.md` — the TEST-08 null constrains MOND+EFE's lever
  ~45× harder than the framework's; is it in tension with Chae 2020/2021 through the Cosmicflows-4 proxy, or
  is the proxy blind? Either answer is a number the site currently asserts without one. Pre-registered rule.
- `the-s-curve-is-an-axis-artifact.md` — annotated in place with what the maintainer sweep did and did not
  cover; left in `topics/` because question 2 (does MOND's μ get the same objection?) is still open.

## Open Items (Deferred)

- Explorer backlog still standing: Brouwer+2021 as *primary* ceiling refutation on TEST-10 and Honest
  Assessment (today it is only cited inside the B_max note); `/galaxy-rotation` + `/for-researchers`
  same-pipeline χ² table with parameters counted on both sides; the "argument relabeled" correction on
  `/mond-unification` (field-equation solution 110.8 vs MOND 52.2); name the estimator with every V-exponent;
  `/equation-walkthrough` and `/phase-boundary-visualizer` S-curve language.
- Tech-writer batch not done: Honest Assessment breadcrumbed under "Getting Started" (needs a navigation
  section decision); badge vocabulary used as tool categories on `/interactive-tools`; Roadmap-vs-Catalog
  naming; SPARC/ALFALFA/SDSS expansion per page; Tier 1 acronym key above the fold; one-sentence definition
  of Synchronism on the Beginner path; beginner-essential glossary tags. Glossary MRH "duplicate" is a
  static-fetch rendering artifact, not a content defect.
- Physics-precision batch not done: Landau "~2×" exponent phrasing; Pearl/Markov-blanket citation on `/mrh`;
  Blanchet dielectric-MOND citation; σ/m < 0.47 attribution (Harvey 2015, not Bullet Cluster); Galaxy Plotter
  A = 47 vs a₀ = 1.2×10⁻¹⁰ normalization mismatch; Chemistry session count 1,840 vs 2,671; Consciousness Demo
  warning onto the chart; DESI "3.4–6.3σ" → SN-compilation range.
- Migrate the inline prose on landing / footer / Honest Assessment to import from `ledger.ts` so the split
  cannot drift again.
- The 06:00 runner will keep failing until the weekly cap resets (Sep 7, 23:00 PT). Not fixable from here.

## So What?

Two things advanced discovery rather than documenting state. (1) The environment-null reclassification is a
finding about the *ledger's semantics*: a refutation row must name what died, and one of the six was
refuting a registration the equation never implied — which resolves the three-way root-count drift not by
picking a number but by showing the numbers were counting different things. That is the same failure class
as the 08-08 "6 → ≤5" audit, reached from the outside by a persona with no archive access, which is the loop
working. (2) The sensitivity-ordering inversion on Parameter Derivations was the largest un-shipped explorer
result of the outage; shipping it changes which parameter the site tells a reader to attack. The rest of the
day was consistency repair — necessary, since the audit trail is the product, but it does not by itself
move Bucket 0 or Bucket 2.

Frame question surfaced, not resolved: with the maintainer down 23 days, the visitor personas *re-derived*
six archive findings from the page. That is evidence the personas are doing real adversarial work — and
evidence that the loop's throughput is bounded by the maintainer's uptime, not by the explorer's output. The
25-item backlog is a queue-depth symptom of a single-worker bottleneck; the fix is structural (a second
maintainer seat, or explorer findings that ship their own page diffs), not another day of triage.
