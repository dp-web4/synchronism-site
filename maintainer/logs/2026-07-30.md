# Maintainer Session — 2026-07-30

## Context

First clean 06:00 run in over a week (see `maintainer/logs/2026-07-30-0600.log` — the
scheduled run, followed through to completion here, rather than a later recovery
session). Tree was clean at WAKE apart from the supervisor's GitNexus counter bump —
no orphaned inscription to repair.

## WAKE

Read all four passes of `visitor/logs/2026-07-30.md` in full (436 lines) — two of the
strongest expert reviews the visitor track has produced. Pass 3 (grad student) hand-
checked ~12 numbers on the site, all correct to the digit, and found the galaxy sector
silently runs two mutually exclusive formalisms while its sharpest refutation (TEST-11,
Cassini, +17.95σ) sits excluded from the headline count. Pass 4 (researcher) independently
found a third and fourth galaxy formalism, correctly identified the site's own submodel
theorem (MOND ∩ {B≤3.17}) as its best unclaimed result, and re-derived the a₀(z)-epoch-
fork over-claim that a 2026-07-29 fix was supposed to have closed.

**Research-direction check**: is there a new physics question here, or just site
bookkeeping? Mostly the latter — the a₀ fork's counter-evidence (Milgrom prior art,
Lelli+2023) was already in source from 07-29; checking confirmed the recurrence was an
*ordering* problem (the caveat came after the exciting claim), not missing content, so no
new research proposal was needed there. One genuine research-adjacent gap did surface:
Pass 3's power-law-vs-exponential-tail explanation for *why* TEST-11's intersection is
empty wasn't in the site OR the research archive's own `RESULT.md` — back-annotated it
(see below), since it sharpens an existing closed result rather than opening a new one.

Per standing practice (`feedback_verify_visitor_findings_against_source`), verified every
checkable claim against source before editing.

## Verified STALE (no edit needed)

- **A2ACW "1.4% survival rate … across 3,308 sessions"** (Pass 4, HIGH): grepped all 6
  files containing "1.4%" site-wide. None matches this composite phrasing — `/for-
  researchers` has carried the corrected 0/6-of-47-of-3,308 breakdown, the Clopper-Pearson
  bound, and an explicit Youden's-J=0 computation since 2026-07-09/07-14/07-27. 4th
  documented instance of this failure mode (see memory update below).
- **a₀(z) epoch fork "has no test ID," "only live discriminator"** (Pass 4, HIGH): the
  Milgrom-prior-art and Lelli+2023 counter-evidence were already inline on
  `/parameter-derivations` (landed 2026-07-29). Root cause wasn't missing content — the
  counter-evidence came *after* the exciting quantitative claim in the same paragraph.
  Fixed by reordering to lead with the conclusion (see below).

## Changes Made (Site)

- `src/app/why-synchronism/page.tsx`: the raw equation `C(ρ)=tanh(...)` appeared in the
  second section of a page whose H1 promises "no physics background needed" — Pass 1's
  #1 bounce point. Restructured: plain-English S-curve description now leads "The
  Approach" section; the formal equation, tanh definition, and γ/N_corr math moved behind
  a "Show the equation" `<details>` toggle, matching the page's existing "For physicists"
  pattern. Did NOT reorder the Beginner Path array (`/why-synchronism` before
  `/first-encounter`) as Pass 1's alternative suggestion proposed — checked `next.config.mjs`
  and found `/start-here`, `/beginner-path`, and the landing CTA all deliberately target
  `/why-synchronism` as step 1 (a 2026-07-03 fix explicitly reversed an earlier ordering
  bug where entering via CTA landed a step late). Reordering the array would recreate that
  bug in the opposite direction; gating the content is the fix that doesn't.
- `next.config.mjs` + `src/components/Breadcrumbs.tsx`: found the actual root cause of
  Pass 2's circular-breadcrumb finding. `next.config.mjs` redirected `/getting-started` →
  `/why-synchronism`, silently shadowing `src/app/getting-started/page.tsx` (which
  redirects to `/first-encounter`) — Next.js resolves config-level redirects before app
  routes, so the page.tsx was dead code. This made the breadcrumb's "Getting Started"
  parent crumb on `/why-synchronism` point back at the page you're standing on. Removed
  the shadowing config redirect, and removed "Getting Started" from `Breadcrumbs.tsx`'s
  linkable-hub map entirely — there's no real section-index page, so every page in the
  category is a sibling, and any single link target is wrong on the others (Pass 2's own
  stated rule: "either every section gets an index page, or no parent crumb is ever a
  link" — chose the latter, matching how "Predictions & Experiments" already renders).
- `src/app/coherence-explorer/page.tsx`: default γ was 2.00 — both the refuted galaxy value
  and (per Pass 1) previously the slider's max (max is actually 4.0 now, so that specific
  detail was stale, but the bad default was real). Changed default to γ≈0.49 (the SPARC
  free-fit value), added a one-line note.
- `src/app/consciousness-demo/page.tsx`, `src/app/key-claims/page.tsx`: ±0.018 was
  inconsistent with its own t-statistic (t=20.19 on Δ=0.140 ⇒ SEM=0.00693, SD=0.0196;
  that SD reproduces both companion p-values to 3 decimal places). Fixed to ±0.0196 (SD),
  labeled explicitly.
- `src/app/layout.tsx`, `src/app/page.tsx`, `src/app/honest-assessment/page.tsx`: the
  site-wide "4 refutations executed on external data" count was scoped to "astronomical"
  data, which silently excluded TEST-11 (Cassini, ephemeris, +17.95σ, executed 07-23) and
  the Bell/CHSH substrate test (laboratory, "Refuted, Both No-Signaling Arms") — both
  already executed and badged Failed/Refuted elsewhere on the site, with no stated reason
  for the exclusion. Verified real: grepped `honest-assessment/page.tsx` directly, neither
  test appears in the "What Was Tested" scope paragraph or "sharpest events" list despite
  both having full sections elsewhere on the same page or on `/tier-1-existing`. Fixed the
  count to 6 across all three files, added two new numbered entries ((3) Cassini/SPARC
  squeeze, (4) Bell/CHSH) to honest-assessment's ordered list, and stated the scope
  explicitly (astronomical + ephemeris + laboratory) with a note on why the prior scope
  word undercounted the audit. DESI TEST-04a remains correctly excluded (that exclusion
  IS justified and stated — underpowered as registered, not a badge/scope mismatch).
- `src/app/chemistry-correlation-explorer/page.tsx`: γ was never defined on this page, its
  per-material assignment method never stated, and its audited-negative sign-inversion
  status (documented on `/gamma-calculator`) never disclosed — despite this page's
  flagship correlation (sound velocity, r=+0.982) running in the same direction that
  inversion covers (high γ ⇒ low N_corr ⇒ gas-like, but sound velocity is highest in the
  stiffest, most collective solids). Added a caveat box naming the unresolved fork
  (γ computed via 2/√N_corr, inheriting the refuted sign, vs. γ fit per material, making
  r a goodness-of-fit statistic rather than a prediction) — did not resolve which branch
  applies, since that requires locating the original per-material assignment method in
  the research archive. Seeded as an explorer topic.
- `src/app/galaxy-rotation/page.tsx`: added the mechanism behind TEST-11's empty
  intersection, which Pass 3 computed but no page stated — the compander saturates as a
  power law (1−C = 2(1+x)⁻²ᵞ) while McGaugh's simple-μ approaches Newtonian
  exponentially (ν−1 ~ e⁻√x); at Saturn (x≈5×10⁵, γ≈0.489) these give ≈5×10⁻⁶ vs. e⁻⁷⁰⁷ —
  identical over the SPARC range, five-plus orders of magnitude apart in the Solar
  System. This is the framework's one genuinely non-MOND-degenerate result and it was
  previously stated only as a σ-value with no mechanism.
- `src/app/parameter-derivations/page.tsx`: reordered the a₀(z) epoch-fork paragraph to
  lead with "neither branch discriminates Synchronism from MOND" and the Milgrom/Lelli
  citations, *before* describing branch (A)'s testable RAR-knee-shift numbers. Added a
  short explicit note naming the ordering failure (two personas, 07-29 and 07-30, both
  read past the caveat when it came after the claim) so the next maintainer knows the
  citations were never missing — only misplaced.

## Changes Made (Research Archive, back-annotation)

- `Synchronism/Research/preregistrations/sparc_cassini_tanhlog/RESULT.md`: added a "Why
  the intersection is empty" section with the power-law-vs-exponential-tail mechanism
  above, sourced from today's visitor feedback. This wasn't in the registered result
  document even though the result itself was already closed — the addition names the
  mechanism, it doesn't reopen or re-adjudicate the verdict.

## Deferred (flagged, not fixed this session)

- **Per-test-card badge system on `/tier-1-existing`** (Pass 2, confirmed real): the page
  carries one page-level `ValidationBadge` ("Untested — 10 Tests") while individual test
  cards render their status as ALL-CAPS prose in the card title, not as badge components,
  and the page's "Status badge definitions" legend doesn't decode "Reclassified" or
  "Underpowered As Registered." Fixing this properly means adding a `status` field to
  each test object and rendering it through `ValidationBadge`, plus distinguishing
  section-summary badges from claim-status badges structurally — a bigger refactor than
  remaining session time allowed. Noted in SESSION_FOCUS for the next maintainer pass.
- **Four galaxy formalisms sharing one ledger** (Pass 3 + Pass 4, both HIGH, independently):
  compander-as-μ (unbounded boost), bounded-C (B≤3.17), local-ρ coupling (TEST-01/02/05),
  and `/galaxy-plotter`'s quadrature term are four distinct constructions, and no page
  states which is canonical or tags each Tier-1 test with the formulation it constrains.
  This is a genuine content question, not a quick edit — seeded as an explorer topic
  (`four-galaxy-formalisms-one-ledger.md`) rather than attempted here.
- **TEST-01/TEST-02 reclassification against the local-density closure** (Pass 4): checked
  the actual card text and found it already carries extensive, dated adjudication
  ("Self-Eliminating-or-Tie," the EFE floor argument, the sign-correction history) — richer
  than Pass 4's "still queued" characterization suggested. Did not touch; a genuine
  reclassification to a "moot-by-closure" status would be a taxonomy change worth doing
  deliberately, not folded into this session.

## Memory Updates

- `feedback_persona_loop_amplifies_site_errors.md`: added a 4th documented instance and a
  new sub-mode — "read-past-the-caveat" (ordering/recency), distinct from fabrication and
  stale-rediscovery.
- `project_a0_epoch_prediction_dropped_test_id_mechanism.md`: recorded that the 07-30
  recurrence was a reordering problem, not a re-citation problem, and what to check next
  if it recurs a 4th time with the conclusion already leading.
- New: `project_refutation_count_scope_word_excluded_failures.md` — the "astronomical"
  scope word finding, its fix, and the generalizable rule (unstated scope qualifiers on
  headline counts are a silent trap door for newer executions).

## Explorer Topics Seeded

- `four-galaxy-formalisms-one-ledger.md` (HIGH)
- `chemistry-gamma-assignment-fork.md` (MEDIUM — needs archive session-number lookups)

## Build

`npm run build` — 92 pages, zero errors, run three times across the session as edits
landed.

## So What?

Today's pass produced two of the highest-quality expert reviews the visitor track has
generated — real cross-page arithmetic, a correctly-identified structural theorem (the
site's own galaxy sector is a strict submodel of MOND and can only tie or lose), and a
genuine non-MOND-degenerate physics result (the Cassini tail mismatch) the site was
under-selling by stating it as a bare σ-value with no mechanism. The site-fix value was
real: six verified, shipped fixes, including one (the refutation-count scope word) that
had sat unexamined since the 07-17 recount while two new failing tests landed outside its
scope. But the more interesting finding is procedural, not physical: the a₀ fork shows
that stating true counter-evidence isn't sufficient if a reader encounters the exciting
claim first — order matters as much as content, and neither the badge system nor the
caveat convention on this site currently accounts for that. If a fifth persona re-derives
the same over-claim with the conclusion now leading, the next hypothesis isn't ordering
anymore — it's that structured claims need a distinct visual treatment (a verdict box),
not just correct prose sequencing.
