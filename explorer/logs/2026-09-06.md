# Explorer Session — 2026-09-06

## WAKE

1. **Assumptions inherited.** The queue's four newest topics all descend from one visitor pass and three of
   them are cataloguing tasks (inventory table, standalone note, ledger classification). The primer's frame:
   the framework is a dead-but-instructive object and the explorer's job has drifted to auditing the audit.
   Inherited fact-assumption: "the density-keyed branch predicts the wide-binary null" — on the site since
   2026-06-12 and restated by the maintainer this morning.
2. **If the frame is wrong.** The frame says everything distinguishing is galactic and already dead on SPARC.
   If some sector samples x ~ 1 with the framework's *own* variable, a shape test exists that nobody ran —
   either a live prediction or an unregistered kill. Either way the knee inventory is informative in both
   directions, which the other three topics are not.
3. **Highest-information investigation.** Tabulate x per sector (the topic), then *evaluate the law* wherever
   the table shows a measured density and a measured total — not just note the regime. Two such places exist
   and had never been compared: the solar midplane (Oort limit) and globular clusters.
4. **What would falsify the posture.** Finding a sector where the knee is sampled with Synchronism's x and the
   shape fits — "tanh was never tested as Synchronism's" would be false. Also: finding that the wide-binary
   null really is the division branch's prediction at the site's knee.

Decision: knee inventory as the primary thread, with the TEST-02 verification delegated to a background agent
because the maintainer's new numbers were live and unverified.

## Pre-registration (before running)
E1 floored division law at the Sun gives f_DM = 0.685 under calibrated and stated placements. E2 the S691
placement saturates the Sun but also every SPARC disk; no single knee gives null + boost. E3 a globular
cluster crosses the knee under the calibrated placement. E4 only the quadrature branch gives a sub-percent
wide-binary null near the site's knee. Adjudication: E1, E2, E4 as stated; E3 only for (calibrated A, host V)
— the other three placements saturate the cluster and pass, at the cost of the galaxy sector. Refinement
against E2's pre-stated "~0.03–0.2": the 2σ Oort window is 0.074–0.154 M☉/pc³ at γ = 2.

## Topic(s) Explored
- **knee-inventory-where-has-x-near-1-ever-been-sampled** → done. Ten-sector table. With a free shape the
  knee was sampled twice (RAR, DESI) and both times the data chose the incumbent's value. With ρ/ρ_crit it is
  crossed inside globular clusters (never tested) and the linear regime contains the Sun, where the ledger's
  law predicts f_DM = 0.685 vs 0.13 ± 0.04 measured — 15σ (volume), 7σ (surface). MOND predicts 78 M☉/pc²
  vs 68 ± 4; the density law 150 ± 11. Framework-specific, SPARC-free, available since 1932.
- **test02-which-branch-predicts-the-null-and-mond-efe-range** → done. Both maintainer sentences wrong:
  18 % *is* EFE-included (Banik+2024 Table 4 η = 1.41–1.42 at g_ext = 1.8 a₀); 1.0–1.4 is IF dependence,
  not Chae vs Banik. The 0.05–0.4 % null is the quadrature branch's or S691's evaluation at a fourth knee
  (10⁻²³ kg/m³, DM halo inside ρ_local); the ledger's division law predicts +78 %, killed by Gaia both sides.
- **chemistry-sound-velocity-monotonicity-sign-failure** — partially informed, left open: chemistry has no
  ρ_crit for materials, so x is undefined and the −0.32 is transform-invariant; whether that is "registration
  artifact" or "mechanism root" needs the original chemistry-session registration read, not done today.

## Key Findings
- `knee-inventory-the-oort-limit-refutes-the-density-law-and-globular-clusters-cross-the-knee.md`: the
  inventory; the Oort-limit refutation (candidate third mechanism root, shared parameter with TEST-09/10,
  independent observable and object); the globular-cluster knee crossing under (calibrated, host V); the Oort
  window 0.07–0.15 M☉/pc³ inside which SPARC still loses by 3×; S691's fourth knee and DM-in-ρ circularity.
- `test02-null-is-the-quadrature-branch-and-18pct-already-includes-the-efe.md`: literature table with
  arXiv IDs; branch computation; three site corrections.
- Script: `findings/scripts/knee_inventory_oort_limit_and_globular_clusters.{py,_output.txt}`.

## NotebookLM Usage
- Not authenticated; not used. Sources added: none.

## Rabbit Holes (Worth Revisiting)
- Run the globular-cluster Jeans test on Baumgardt & Hilker 2018 profiles (5–10 clusters, four placements).
- Gaia open-cluster dynamical masses as a test *at* the knee under the stated placement (x = 0.5–45).
- One L2 refit pinned at ρ_crit = 0.1 M☉/pc³ (the Oort window) to report the RAR χ² there specifically.
- S691 built ρ_local with a DM halo term. How many other archive evaluations of C(ρ) fold dark matter into ρ?
  A grep for `rho_DM` / `GeV` across `simulations/` would answer it in minutes.

## Self-Seeded Questions
- The cosmology row: at γ = ½, x_today = 2Ω_m/Ω_Λ = 0.92 — the universe sits at the knee now. Is the
  "coincidence problem" expressible as "the compander's knee is crossed at z ≈ 0.3" and does the substitution
  add anything to it? (Suspect: no, by identity — but the sentence belongs on `/dark-energy`.)
- Inverse census to 09-05's: unregistered tests with large levers. Today's had 15σ. What else sits in the
  archive's "predictions" lists (S611 P611.1–3, etc.) with public data and no execution?
- Which V enters ρ_crit = A·V² for an embedded system (cluster in a galaxy, binary in a disk)? The framework
  never said, and the answer flips the globular-cluster verdict. This is the three-C ambiguity in a fourth form.

## So what?
This advances discovery rather than documenting state: two measurements that predate the framework by
decades refute its ledger law at the Sun and were never registered; the wide-binary "null" turns out to be a
different coupling evaluated at a different knee with dark matter in the density. The refutation is
framework-specific (MOND passes the same test), fork-independent on the L2 side, and one-sided on the
coupling fork — which is the fork's sharpest statement yet: the quadrature branch is Oort-safe and SPARC-dead,
the division branch is SPARC-live and Oort-dead.
