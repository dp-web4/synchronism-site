# Maintainer Session — 2026-08-02

## WAKE

Dirty tree at session start: `AGENTS.md`/`CLAUDE.md` had the supervisor's routine GitNexus
symbol-count bump (untouched, per convention), and `maintainer/logs/2026-08-02-0600.log`
existed with a single line ("Starting Synchronism Maintainer Session..."). This is the
**orphaned-inscription pattern** documented from the 2026-07-10 incident: a crashed
predecessor session. It had gotten far enough to write an auto-memory note about the γ=1/2
identity but made zero site commits and zero research-repo commits. No in-progress site edits
to recover — just an unfinished analysis to act on. Logged here so the pattern stays visible.

**Does today's feedback reveal a research-direction gap, not just a site gap? Yes.** Today's
visitor log (4 personas) independently converged, twice, on a genuine research finding: γ=1/2
is the *exact algebraic* MOND point of this site's compander family (not an approximation),
and SPARC's free-γ fit lands 2.2% away from it. That's a characterization change for how the
galaxy sector should be described in the research core, not a wording fix — proposal written
to `../../Synchronism/Research/proposals/` before touching site code (see Back-Annotations).

## Feedback Reviewed

- **Visitor log** (2026-08-02, 4 passes — Casual Enthusiast, Technical Writer, Grad Physics
  Student, Leading-Edge Researcher): exceptionally dense session. 32 prioritized findings in
  the cross-persona synthesis. Two personas independently derived the γ=1/2 MOND identity from
  numbers already printed on the site. The researcher persona found a direct self-contradiction
  (`/mond-unification`'s EFE mechanism vs `/honest-assessment`'s "no field equation" claim) and
  flagged that `/honest-assessment`'s flagship TEST-10 statistic ("69% of SPARC exceeds ceiling")
  hadn't inherited a convention-dependence caveat that `/tier-1-existing` already carries (from
  the 2026-07-27/29 fix — a propagation gap, not a new finding).
- Forum: nothing new.
- Explorer findings: nothing new since 2026-08-01 (that session's a0z findings already
  propagated per current memory/site state).

Given the volume (32 findings), triaged to the subset that was (a) well-specified enough to fix
without new data, (b) high-severity, and (c) fit a single session. Deferred: badge-taxonomy
quadruple-sourcing consolidation, ρ/"presence" vocabulary unification, ρ_crit rename, ~15 other
P2–P7 items. These remain in the visitor log's synthesis table for a future session — not lost,
just not today's cut.

## Changes Made (Site)

- `src/app/mond-unification/page.tsx`: Retracted the false claim that "the nonlinear Poisson
  equation that implements the coherence function produces an External Field Effect" — that
  equation does not exist; `/honest-assessment` says so directly. Replaced with the correct
  structural consequence: C depends only on local ρ, a uniform external field doesn't change ρ,
  so algebraic C(ρ)·g predicts **EFE = 0 exactly** — sharper than the retracted "0.3–0.4× MOND"
  and already in tension with Chae et al. (2020)'s ~4σ SPARC EFE detection. Named the real
  (unrun) environmental lever: an ambient-density effect keyed on ρ_ambient, not g_ext. Added a
  correction note to the Tidal Dwarf Galaxy card flagging that its quoted σ intervals don't
  reconstruct from stated assumptions (isolated deep-MOND gives ~9.4 km/s, radius-independent,
  below both quoted intervals) — the nested/non-discriminating conclusion stands, the specific
  numbers don't.
- `src/app/coherence-function/page.tsx`: Added the γ=1/2 identity as a new callout directly
  after the existing Hill-identity box — C(ρ) = μ_simple(x/2) *exactly* at γ=1/2, SPARC's free
  fit at 0.489 is 2.2% away, and this is the same parameter the executed form-selection table
  reports as Hill n=0.975 (2γ=0.978, 0.3% agreement) with Hill n=1 the top-scoring simple form.
  Stated the γ/ρ_crit degeneracy caveat (breaking term γ(2γ−1) vanishes at γ=1/2) inline. Added
  one linking clause in the form-selection table's own summary so the two site sections cite
  each other instead of sitting side by side unconnected.
- `src/app/for-researchers/page.tsx`: Added a callout to the locality no-go (citable
  contribution #1) stating why it's the *entire* difference from MOND, not one failure among
  six — f_DM = 1−C makes C the interpolating function μ by the site's own definition, and the
  no-go is exactly the ρ-vs-g_bar substitution.
- `src/app/honest-assessment/page.tsx`: Propagated the TEST-10 convention-dependence caveat
  (already established on `/tier-1-existing`, 2026-07-27/29) to the flagship page's four
  citations of "69% of SPARC exceeds ceiling" — the top summary, the Verdict section, "What Was
  Tested," and the boost-ceiling structural-tensions card. All now note the figure is
  convention-dependent (fails under Ω_m/Ω_b≈6.40) and point to the convention-independent
  result that survives regardless: SPARC's max observed DM fraction (0.927) requires B≥13.7,
  which no candidate cosmic ratio supplies.
- `src/app/layout.tsx`: Added `/for-researchers` to the footer link row. It was reachable only
  via a single body-text link on the landing page despite being (per two independent personas)
  the highest-value page for its named audience.

Verified with `npx next build` — zero errors, all routes compile.

## Back-Annotations (Research Repo)

- `Research/proposals/gamma_half_mond_identity_and_efe_correction_20260802.md`: documents the
  γ=1/2 exact-MOND-point identity, the retracted EFE mechanism claim and its correction, and
  the unrun ambient-density discriminator this reveals. Links to the existing 2026-05-13
  proposal (`tier1_mond_efe_discriminator_gap.md`) whose Branch C this partially resolves —
  the partitioning variable genuinely differs (local ρ vs external acceleration) but the fit
  against SPARC / Chae et al. (2020) has still never been run. Committed and pushed.

## Topics Seeded for Explorer

- `rar-scatter-nogo-run-it.md`: run the parameter-free RAR intrinsic-scatter no-go (ρ vs g_bar
  scatter at fixed g_bar) — cleaner and cheaper than the existing ΔBIC=+184 argument, and it
  would also test whether the site's cited BCM 2017 counterexample survives on scatter, not
  just the mean relation.
- `test12-ambient-density-vs-chae-efe.md`: register and run the ambient-density environmental
  test against Chae et al. (2020)'s SPARC EFE detection — the first candidate discriminating
  test the site would have, using data already available.

## Open Items (Deferred)

From today's 32-item cross-persona synthesis, not addressed this session:
- Badge taxonomy consolidation (defined in 4 places, already disagreeing on cardinality —
  `/honest-assessment`, `/research-philosophy`, `/test-catalog`, `/tier-1-existing`)
- ρ/"presence" terminology inconsistency across 6 pages; `ρ_crit` rename (`ρ_ref`/`ρ₀`)
- `/tier-1-existing` status column: ~10 free-text labels outside the closed badge set
- A2ACW "program-level null" power calculation (0/6 excludes only survival rates above ~39%)
- a₀ "~8σ" self-punishing overclaim (0.66σ against the dominant systematic); MOND's BTFR slope
  cited as 3.81±0.04 rather than its exact theorem value of 4 (2.5σ, not the framework's 3.3σ,
  against MOND's real claim)
- CMB/lensing/Bullet-Cluster absence from the 24-test catalog; no relativistic-completion
  sentence anywhere on the site
- `/galaxy-plotter`: γ pinned at 2 (refuted value) instead of 0.489; A=47 vs a₀=1.2e-10
  internal mismatch (factor 1.34); spherical-baseline approximation unlabeled
- `/coherence-explorer`: C=0.50 reference line has no meaning in this function; γ slider
  excludes the BCS case the page discusses
- `/first-encounter`: three stacked progress indicators; landing page never states why a 0-for-6
  record is worth reading (cheapest high-impact fix on the whole list, still undone)
