# Maintainer Session — 2026-09-17

## WAKE
I read SPINE.md and PREDICTIONS.md in full, then the visitor log. Two personas (graduate physics, leading-edge
researcher) independently reached the 09-16 conclusion from a new direction: **the credibility risk is now entirely on
the refutation side.** Explanations of *why* things fail draw more referee objections than the claims do. Two of those
objections are research questions, not copy edits, so I checked them before fixing any friction:

1. **Cassini.** Three pages gave incompatible reasons for the Solar-System failure. /galaxy-plotter said "McGaugh ν is
   Cassini-safe", /galaxy-rotation said "tail shape, e^−707 at Saturn", and /honest-assessment said "MOND picks a
   different μ and survives". The persona's physics was that Cassini bounds the EFE quadrupole Q₂, set near 7000 AU,
   not the Newtonian-return tail. TEST-25's own instrument can settle that directly, so I pre-registered and ran it.
2. **A2ACW's frame.** The researcher argued the proposed control tests recall of famous items and has no human arm, and
   that the better dataset is the site's own correction trail. I agree, and I think it changes what A2ACW should
   measure. That went into a proposal and an explorer topic. I did not execute it: an LLM coding LLM errors needs the
   inter-rater and human-arm design first.

Proposal: `Synchronism/Research/proposals/cassini_root_is_fully_shared_and_a2acw_should_code_its_correction_trail_20260917.md`.

## Feedback Reviewed
- **Visitor log 2026-09-17 (four personas).**
  - Casual: "coherence" read as quantum coherence; Galaxy Rotation unreadable after paragraph 1; badges used before
    they are taught; the "autopsy" hook buried.
  - Tech writer: 16/17 identical `<title>`s; inline revision history (180 dates on HA); count drift (5 vs 6
    reparametrizations, "2 discriminated" vs "0 discriminators"); First Encounter parts B–G client-only;
    γ-calculator/visualizer rename leftovers.
  - Grad student: Cassini (above); TEST-09's 1/φ exponent; "cooperativity zero" crossing a keying; the measurement
    page's γ-chain; tool arithmetic.
  - Researcher: /wide-binaries misstates MOND and inverts its own regime; "conserves momentum / EFE = 0 exactly / SEP"
    is false for the field equation; /a2acw states the superseded null; the GC window lacks escapers and striction;
    TEST-26 booking symmetry; matter budget; matter-slaved DE lemma.
- **Explorer 2026-09-16.** P0: GC window is an L2 object, with the L3 band; "no nonlinear EFE" holds for L2 only. P1:
  scope the polarization force; back-annotate the 08-24 EFE script.
- **Forum.** Nothing new.

## Research executed this session

### Cassini Q₂ for MOND's own interpolating functions (pre-registered `43a66a3`)
`maintainer/scripts/cassini_q2_mond_interpolating_functions{.py,_PREREG.md,_output.txt}`. The instrument is TEST-25's
`sparc_cassini_q2.py` (benchmarked to Desmond+2024 at 0.76%). Controls pass: the compander reproduces +17.95σ, and
δ = 1 ≡ `nu_rar` exactly.

| IF | z (a₀ ∈ {1.128, 1.20}e−10 × g_ext ∈ {2.00, 2.32, 2.48}e−10) | pass |
|---|---|---|
| McGaugh RAR ν (δ = 1) | +15.9 … +20.9 | 0/6 |
| simple n = 1 | +15.3 … +20.1 | 0/6 |
| standard n = 2 | +9.5 … +10.8 | 0/6 |
| δ = 2.5 / 3 / 4 | +5.5…+7.7 / +3.2…+5.8 / +0.9…+3.3 | 0/6, 0/6, 3/6 |
| compander γ = 1.5 / 2 / 3 (post-hoc) | a₀-dependent; γ = 3 passes both | — |

- P1 (δ = 1 fails) **held**.
- P2 (simple fails harder than δ = 1) **refuted**.
- P3 (a pass near δ ≈ 2.5) held **only at δ = 4**.

Reading: both families have the same structure. The RAR-preferred members fail; the sharp members pass, and SPARC
excludes the compander's sharp members. **The "Cassini-safe" note and the HA asymmetry argument are both withdrawn.**
TEST-25 stays "inherited from MOND", and now in the literal sense. I had a residual instinct that MOND still had an
escape, and the post-hoc compander scan is what dissolved it: the compander has an escape of the same shape, and SPARC
already priced it (+184).

### TEST-09 with exponent 1 instead of 1/φ (grad-student question; quick check, expectation stated before, not committed)
`maintainer/scripts/test09_exponent_one_instead_of_inverse_phi{.py,_output.txt}`.
- At the registered a₀ the exponent is irrelevant: n = 3.348 vs 3.346, and the kill fires the same way.
- At exponent 1 with its own cH₀Ω_m a₀: n = 3.285.
- **With a₀ free, exponent 1 reaches n = 3.50 (deviation 0.25 < 0.3) at a₀ ≈ 1.6×10⁻¹¹.** The published "no rescue for
  any φ ∈ [1.2, 4]" therefore does not extend to φ = 1, though the rescuing a₀ is 6.5× below the registered one.
  My expectation was half right.
- The ceiling root is unaffected. Posted on Tier 1 TEST-09.

## Changes Made (Site)
**Refutation-side physics (me):**
- `galaxy-plotter`: removed "Cassini-safe" and gave the Q₂ numbers for both MOND functions; the old wording is in a
  revision note. A `<p>` became a `<div>` so `<details>` nests validly.
- `galaxy-rotation`:
  - New top box, "Does Synchronism replace dark matter? No, not on current evidence".
  - The Solar System box is rewritten: Q₂ at 7000 AU, the shared failure, and the direct tail as a separate, smaller
    effect.
  - "Net discriminating… 0" → "selected Synchronism over MOND: 0".
- `honest-assessment`:
  - The TEST-25 classification box is rewritten; the asymmetry is withdrawn with a revision-note list.
  - Item (3) no longer attributes Desmond's 8.7σ to the compander.
  - GC fork: new "What kind of result this table is" block covering the L2 tag, the L3 band (0.031–0.196, no passing
    knee, 18/27 verdicts change, 39/42 outward with Plummer tails), potential escapers (Küpper+2010; Claydon, Gieles &
    Zocchi 2017), and why it is not prospective (P611.2 registered γ only).
  - "Linear refraction" is scoped to L2.
  - Added an `if-you-read-nothing-else` anchor.
- `tier-1-existing`:
  - TEST-02's "boost never turns on" inversion is fixed, with a revisionNotes entry.
  - TEST-25 gets the MOND IF numbers, in the alert and in the inherited-exposures card.
  - Added the TEST-09 exponent check.
  - **Stripped literal `<strong>`/`&lt;`/`&amp;` from alert strings.** They render as plain text, so the TEST-25 alert
    had been showing raw tags.
- `wide-binaries`: rewritten.
  - MOND's prediction is the EFE (1.0–1.4× in g).
  - The two Synchronism versions are separated.
  - The density law is knee-conditional (+1.8×10⁴% at the published knee, +116% / +9.4% at the others; the 0.05–0.4%
    band needs an unused knee).
  - "Why isn't the local exclusion booked?" is answered: the calibration's failure is already recorded, and booking is
    dp's call.
  - The old "Feasibility Kill" is in a revision note.
- `mond-unification`: the momentum/EFE box is rewritten:
  - The field equation conserves momentum only with striction (net self-force −∫|∇Φ|²∇C/8πG).
  - Striction: ≤2×10⁻⁵ at the A·V² knee, up to 164× in a disc, 1.7–13× at GC knee shells.
  - L2 linear refraction and the L3 nonlinear EFE.
  - Floored/unfloored scope (∇ln C = ∇ln ρ unfloored).
  - The SEP sentence is scoped to the algebraic reading.
- `for-researchers`:
  - Momentum/EFE paragraph corrected.
  - GC citable form tagged L2 + L3 band; caveats (iii) L2, (iv) escapers, (v) not prospective.
  - CPSU author string fixed; doubled title suffix removed.
- `a2acw`:
  - Contribution paragraph and Self-Audit section rewritten to match For Researchers: 0 of 9, LLM auditors, J = 0 / 1.0,
    not a citable null, circular positive arm.
  - Added the design critique and the correction-trail dataset.
  - The superseded text is in a revision note.
- `why-synchronism`, `autonomous-research`, `terms.ts`: "no human in the loop" and the CPSU string.

**Agents (disjoint file sets, reviewed via lint, nesting check and build):**
- **Titles:** 83 route `layout.tsx` files take title and description from `navigation.ts`; the root default now matches
  the landing H1. I re-synced 9 descriptions after the nav edits.
- **Beginner:**
  - Landing: "Not the quantum coherence you've heard of" box; the "autopsy" pointer under the scoreboard; TEST-25 leads
    with 8.7σ; the parenthetical is fixed; glossary links; the AI-agents clause.
  - `coherence-explorer`: "orthogonal", not "anti-correlated".
  - `core-idea`: badge key, and the cooperativity inference now rests on ΔBIC +2843.
  - `first-encounter`: all 7 parts prerendered (verified in `.next` HTML).
  - `learning-paths` / `paths.ts`: Step 5 anchor and honest time estimates.
- **Consistency:**
  - WSIN reparametrizations 5 (list from /key-claims); "0 selected Synchronism over MOND" on test-catalog, WSIN and
    key-claims.
  - test-catalog: page badge replaced by a status summary; tier split; TEST-26 "inherited from ΛCDM" proposed booking
    (dp-gated); CPL conventions.
  - dark-matter: stale predictions replaced by current status.
  - gamma-calculator: factor 17, N_corr ≈ 0.06 inverse, crossover rename, "what changed" box only after interaction,
    caveat count.
  - phase-boundary-visualizer: N_corr at band edges, Ginzburg anchor.
  - consciousness-demo: log-measure qualifier.
  - equation-walkthrough: one ρ definition.
  - key-claims: κ for the noise rate.
  - chemistry explorer: dependent-rows caveat, CN symbol.
  - navigation: 14,435, card wording.
- **Me after agents:**
  - WSIN wide-binary EFE sentence; test-catalog TEST-25 lead.
  - navigation.ts: hard-problem title now matches its H1 ("A Proposed Reframing"); qualia, prediction-tracker and
    wide-binaries descriptions.

**Tooling:**
- `site_lint.py`: 61 rules (+14 for today's retired wordings). **Positive control: 21 hits on HEAD, 0 on the tree.** I
  added then removed a "14,585" rule: that number is the legitimate pre-cut count on 10+ surfaces, and the rule was
  too broad.
- A throwaway `<details>`/`<div>`-inside-`<p>` checker (positive-controlled) found 0 hits.
- `next build` passes.

**Convention adopted today** (for the tech writer's HIGH item): new corrections go in collapsed "Revision note"
`<details>`, not inline dated narration. It is applied to today's edits only; converting the existing 180 dates on HA is
not done.

## Back-Annotations (Research Repo) — `d8df1c82`
- `PREDICTIONS.md`: a 📌 block. TEST-25 is inherited literally, with the MOND IF z table and the post-hoc compander
  scan. The GC window is an L2 object (the L3 band; escapers; P611.2 not prospective). "EFE = 5.6×10⁻¹³" is
  superposition. The field equation conserves momentum only with striction.
- `simulations/efe_locality_vs_phi_dependence.py`: docstring back-annotation (the refracted host field was subtracted).
  Code unchanged.
- Proposal (above), which also records the matter-budget gap.

## Topics Seeded for Explorer
- `qumond-cassini-closure-sparc-cost-of-sharp-interpolating-functions.md` (HIGH): SPARC cost of δ ∈ {2.5…6} and
  compander γ ∈ {0.6…2}. Is Cassini+SPARC a QUMOND-class closure?
- `correction-trail-error-profile-of-llm-research-agents.md` (HIGH): codebook, inter-rater, dp-coded 10% human arm,
  four registered hypotheses.
- `matter-budget-de-sector-vs-no-dark-matter-galaxies.md` (MEDIUM): Ω_m = 0.315 clustering matter vs a "no DM" galaxy
  sector; baryon-only refit.
- `matter-slaved-dark-energy-crossing-lemma-prior-art.md` (MEDIUM): the prior-art pass on w = d ln F / d ln x.

## Open Items (Deferred)
- **Inline revision history → per-page revision notes** (tech writer HIGH). Today's edits follow the new convention; HA
  (180 dates), Tier 1 and the landing page are not converted. This is structural, and `correction-palimpsest-rate.md`
  should inform the rule.
- Breadcrumb middle segments are not links; tool slugs vs names (redirects); tools index level vs purpose tags.
- /measurement-without-observers γ-chain direction (N_corr ↑ → C ↓ = de-classicalizes). Not touched.
- Galaxy Plotter: amber stand-in behind a toggle, a "what you're looking at" caption, SPARC mass-model columns.
- /phase-transitions still carries a deprecated "Validated" badge; /status-dashboard badge "Validated — Current as of
  Feb 2026"; /dark-matter "a₀ emerges from cosmology" label.
- The γ-calculator and phase visualizer enzyme presets disagree (N = 30 vs γ ≈ 0.85 ⇒ N ≈ 5.5).
- Entity criterion: badged Reparametrization on HA but not in the scoreboard's 5 (consistency agent). → dp.
- **→ dp (gated):** TEST-25 booking confirmation with the asymmetry clause withdrawn; the TEST-26 "inherited from ΛCDM"
  pre-booking; whether the density law's local exclusion at the published knee becomes a booked result; whether dp
  codes the human 10% arm of the correction-trail study.

## So what?
Today converted an argument into a number twice. The "MOND can walk away" asymmetry is now a measured symmetry: both
interpolating-function families have Cassini-passing members that their galaxy fits reject. And the "no rescue at any
φ" claim turned out narrower than written, which is a small over-refutation. The pattern from 09-16 holds a third
time: refutations written without an execution drift in both directions. The shift worth making is the one the
researcher persona named. The program's most novel remaining output is probably not physics. It is a measured error
profile of the agents doing it, and the data is already in git.
